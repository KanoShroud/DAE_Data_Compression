# Smoke 与代码级验证专项历史

> 本文件保存截至 2026-07-27、已被正式运行覆盖的常规 smoke 与代码级验证。
> 它只用于追溯工程链路、历史缺陷和协议变化，不属于默认恢复上下文，
> 不构成性能证据，也不表示其中的临时结果目录仍然存在。
> 当前实验事实以 `../实验结果记录.md` 为准；正式历史事实见
> `实验结果记录_截至2026-07-27_完整历史.md`。

## 1. 后期独立 smoke 记录
### 2026-07-27 新增记录：BRSR Stage 2A.1安全动作门控smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-27 | `NewMethod-Codex` | BRSR Stage 2A.1独立安全动作门控链路smoke | `brsr_stage2_rate_adaptive/run_stage2a1.py --mode smoke` | `运行结果/BRSR/BRSR_STAGE2A1_20260727_114421_smoke` | 冻结正式Stage 2A模型/温度/计划；gate-fit/certificate/development各1个新种子×2轨迹，SNR=`-5/10dB`；SafeGate始终获取一个新增频点，总量29 bit；50次cluster bootstrap | 状态=`SMOKE_ONLY`。冻结源有效、新种子互斥且与Stage 2A隔离、SafeGate无停止、自适应方法精确29 bit、静态时间共享均值在一条cluster量子内一致等6项机械检查全部通过；11项Stage 2A系列pytest通过。小样本未认证任何SNR。 | 证明Stage 2A.1从冻结源重建、三段数据隔离、fit阈值、独立certificate、固定动作回退、29-bit rate matching、重尾诊断到门禁导出的链路可运行。每个SNR只有2条轨迹，认证状态和所有性能统计均无研究意义。 | `run.log`、`gate_decision.json`、`manifest.json`、`frozen_safe_gate_policies.csv`、`stage2a1_trial_results.csv`、`rate_matched_effects*.csv`、`tail_safety_diagnostics.csv`及3张SVG；编译、Ruff和pytest日志 | 高（链路、隔离与预算）；极低（性能） | 不可用于论文性能结论，只能作为实现验证 | 用户在PyCharm运行默认development；正式结果前不得修改预注册阈值、证书规则、种子和主基线 |

### 2026-07-24 新增记录：BRSR Stage 2A 同平均总bit链路 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-24 | `NewMethod-Codex` | BRSR Stage 2A 单动作同平均总bit链路smoke | `brsr_stage2_rate_adaptive/run_stage2a.py --mode smoke` | `运行结果/BRSR/BRSR_STAGE2A_20260724_151830_smoke` | 冻结`Current-Fisher/Linear-Marginal/5×64`；SNR=`-5/10dB`；prior/plan/policy/development为互斥小样本池；stop/acquire=`21/29 bit`；目标=`21/24/27/29 bit`；50次smoke bootstrap | 状态=`SMOKE_ONLY`，耗时约52.8秒。Stage 1证书及冻结源码SHA-256匹配；种子隔离、积分配置、21/29-bit账本、policy-calibration目标码率、development码率Wilson检查、静态时间共享舍入、有限值、完整行数和NLL非劣化共10项机械检查全部通过。编译、Ruff和33项相关测试通过；成功目录无partial文件，生成4张SVG及完整CSV/JSON。 | 只证明Stage 2A从独立数据池、温度校准、静态计划、单动作门控到同实际平均bit配对比较和门禁导出的链路可运行。smoke只有4个独立cluster，所有性能排名、CI、覆盖率和温度边界状态均无研究意义。 | `run.log`、`gate_decision.json`、`manifest.json`、`rate_matched_effects*.csv`、`stage2a_trial_results.csv`、`temperature_calibration.csv`、4张SVG及测试日志 | 高（链路与协议）；极低（性能） | 不可用于论文性能结论，可作为实现验证记录 | 用户在PyCharm直接运行默认development，回读完整120轨迹结果后再裁决Stage 2A |

### 2026-07-24 新增记录：BRSR阶段1认证积分配置 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-24 | `NewMethod-Codex` | BRSR阶段1`5×64`主积分链路smoke | `brsr_route_feasibility/run_stage1.py --mode smoke` | `运行结果/BRSR/BRSR_ROUTE_STAGE1_20260724_133720_smoke` | 主积分=`5×64`，参考=`9×128`；smoke原SNR、种子、样本、候选池、动作、预算和门禁不变 | 状态=`SMOKE_ONLY`，耗时约11.4秒；`quadrature_convergence_pass=true`，主积分最大估计差=`3.77e-9 sample`、风险差=`6.12e-11 sample²`、后验L1=`7.66e-9`；24-bit前向载荷、29-bit自适应总量、种子隔离、Oracle不变量、有限值和0截幅通过。15项产物完整、无partial；阶段1 8项、路线14项测试和项目完整性门禁通过。 | 认证积分配置已正确接入阶段1全链路，未改变资源与统计协议。smoke样本极小，不评价候选池或方法性能，也不更新阶段1路线裁决。 | `run.log`、`gate_decision.json`、`quadrature_convergence.csv`、manifest、方法与动作CSV、两张SVG；编译、Ruff、pytest和项目完整性检查 | 高（配置、链路和不变量）；无性能结论 | 否 | 用户在PyCharm运行默认development，重新计算Oracle、RiskGreedy、配对CI和评分相关性 |

### 2026-07-24 新增记录：BRSR顺序全量积分诊断 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-24 | `NewMethod-Codex` | BRSR顺序全量积分诊断链路smoke | `brsr_route_feasibility/diagnose_quadrature.py --mode smoke` | 源=`运行结果/BRSR/BRSR_ROUTE_STAGE1_20260723_170115_development`；输出=`运行结果/BRSR/BRSR_ROUTE_QUADRATURE_20260724_130434_smoke` | 仅一个15 dB calibration条件；子集候选=`5×16/5×32/7×32`，参考=`7×64`、锚点=`9×128`；原容差不变；按`n_rho`顺序全量验证并在首个通过者停止 | 状态=`SMOKE_ONLY`，约1.0秒；候选顺序正确生成，`5×16`在该单条件通过后停止；`full_calibration_attempts.csv`记录1次尝试，10项产物完整且无partial。四个运行时计算依赖与源结果哈希一致；非运行时`run_stage1.py`编排入口的预期/实际哈希差异已显式记录。编译、Ruff、诊断6项和路线13项测试、项目完整性及SVG渲染通过。 | 新的顺序控制流、首个通过即停止、结果字段、源指纹分类和图表导出可运行。单条件通过不证明任何正式积分网格收敛，阶段1仍保持HOLD。 | `run.log`、`recommendation.json`、`full_calibration_attempts.csv`、subset/full CSV、manifest和两张SVG；静态检查、pytest、项目完整性和视觉回读 | 高（控制流与产物契约）；无研究性能结论 | 否 | 用户在PyCharm运行默认`diagnostic`；回读全部尝试及最终`selected_grid` |

### 2026-07-24 新增记录：统一插值入口 BPSK/TZP-DS smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-24 | `NewMethod-Codex` | BPSK/TZP-DS 代码归并后链路 smoke | `run_bpsk_parameter_sweep.py --smoke --workers 1`（兼容启动器，实际实现位于 `run_interpolation_alignment.py`） | `运行结果/频谱插值对齐/20260724_121702_077301_bpsk_parameter_sweep_smoke` | 信号=`平滑复谱/BPSK-RRC`；$M=32$；SNR=`5 dB`；重叠率=`0.5`；每条件3次；共2条件、6基础试验 | `COMPLETED.json` 为 completed；60/60方法结果行、20条汇总、9条配对记录完整；原网格补全最大时延差与绝对误差差均为0；全部算子门禁通过 | 统一后的参数扫描、TZP-DS 核心算子、分块保存与历史兼容入口可运行。样本极小，不产生或更新任何方法性能结论。 | `run.log`、`COMPLETED.json`、配置、门禁、汇总与配对 CSV；`test_alignment_core` 与 `test_tzp_dense` 共10项测试 | 高（重构调用链与数值不变量）；低（性能） | 否 | 正式结果继续使用冻结目录 `20260721_172703_080303_bpsk_parameter_sweep`；若采用新参数，用户须在 PyCharm 从统一入口重新运行。 |

### 2026-07-23 新增记录：BRSR阶段1数值积分诊断 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-23 | `NewMethod-Codex` | BRSR阶段1连续复增益积分独立诊断 smoke | `brsr_route_feasibility/diagnose_quadrature.py --mode smoke` | 源=`运行结果/BRSR/BRSR_ROUTE_STAGE1_20260723_170115_development`；输出=历史运行标识 `运行结果/BRSR_ROUTE_QUADRATURE_20260723_184238_smoke`（当前目录未保留） | 仅源calibration首个seed/trial的15 dB条件；候选=`5×16/5×32/7×32`，参考=`7×64`，锚点=`9×128`；原数值容差不变；冻结`Current-Fisher`和原SNR-only动作 | 自动状态=`SMOKE_ONLY`，耗时约6.9秒。源代码哈希、候选池、prior功率和冻结计划一致重建；参考在该单条件通过，smoke选择`5×16`且全量链路通过。10项预期产物完整、无partial，两张SVG可解析并通过无头视觉检查；33项相关测试和项目完整性门禁通过。 | 诊断的源结果绑定、节点比较、参考先验验证、最小配置选择、全calibration分支和导出链可运行。单条件结果不能证明`5×16`正式收敛，也不能改变阶段1的`HOLD-NUMERICAL-QUADRATURE`。 | `run.log`、`recommendation.json`、两组scan/summary CSV、分SNR CSV、manifest和两张SVG；编译、Ruff、pytest、项目完整性及产物回读 | 高（诊断链与隔离）；无性能结论 | 否 | 用户在PyCharm运行默认`diagnostic`；只有正式状态=`QUADRATURE_DIAGNOSTIC_PASS`才允许重算阶段1 |

### 2026-07-23 新增记录：BRSR路线可行性阶段1 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-23 | `NewMethod-Codex` | BRSR一次新增频点动作空间阶段1 smoke | `brsr_route_feasibility/run_stage1.py --mode smoke` | 历史运行标识 `运行结果/BRSR_ROUTE_STAGE1_20260723_154109_smoke`（当前目录未保留） | SNR=`-5/10dB`；prior/calibration/evaluation各1个互斥seed，分别4/2/2 trials；四个12频点池；四类似然；所有方法前向载荷24 bit，自适应方法另计5-bit反馈为29 bit，静态/SNR计划为24 bit；连续复增益主积分`5×16`，参考`7×32` | 自动状态=`SMOKE_ONLY`。640条动作、198条方法结果和38组效应数值有限；方法特定反馈账本、候选池规模、种子隔离与Oracle不变量通过，量化截幅0；效应表含cluster标准差与80%功效近似最小可检测效应。积分最大估计偏移=`0.044874 sample`、风险偏移=`0.002435 sample²`、后验L1=`0.095162`，通过登记容差。16项结果文件完整、无partial，2张SVG可解析。 | 独立候选池、似然、一次动作、统计、门禁和导出链路可运行；主门禁固定为calibration候选池上的`Linear-Marginal`，其余组合仅作诊断。本阶段比较同前向载荷的动作选择价值，不是同总bit性能排名。本结果样本极少，任何候选池/似然/方法排序均无研究意义。 | `run.log`、`gate_decision.json`、`manifest.json`、候选池/动作/方法/效应/评分/积分CSV和2张SVG；编译、Ruff、28项BRSR测试、项目完整性门禁及产物回读 | 高（代码链、资源账本和数值不变量）；低（性能） | 否 | 用户在PyCharm运行默认development；正式结果必须重新回读后才能裁决动作空间、似然和评分组件；阶段1若通过，仍需另做同总bit帕累托验证 |

### 2026-07-20 新增记录：BRSR-TDOA 阶段 B 失败机理诊断 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-20 | `NewMethod-Codex` | BRSR-TDOA Stage B 第一阶段失败机理诊断 smoke | `brsr_tdoa/diagnose_stage_b_failures.py --mode smoke` | 历史运行标识 `运行结果/BRSR_STAGE_B_DIAG_20260720_154417_smoke`（当前目录未保留），冻结源=`运行结果/BRSR/BRSR_STAGE_B_20260718_173940_development` | 无噪声模型残差3条轨迹；预注册`-5/15dB`各1条轨迹；正式粗rho网格、`5×16`细rho网格和真值rho上界；同一两动作空间事后穷举；15dB Rollout Q=`2/4`；当前12频点池未量化投影信息/模糊审计 | 状态=`DIAGNOSTIC_ONLY_NO_POLICY_CHANGE`；冻结4个正式源文件哈希完全一致，正式后验重建与源结果偏差不超过`1e-10`。循环相移控制最大NMSE=`8.64e-32`；线性有限窗残差中位数约`0.0020`。输出7个CSV、2个SVG、2个JSON和run.log，无partial。编译、Ruff、18项BRSR测试和主项目完整性门禁通过。 | 诊断链、冻结结果重建、真值上界隔离、动作空间穷举和候选池多尺度审计可运行；smoke样本极少，rho/动作/候选数值不代表正式根因贡献，不改变Stage B的`DEVELOPMENT_NO_GO`，也不允许据此修改算法。 | `run.log`、`diagnostic_summary.json`、`manifest.json`、7个CSV、2个SVG；SHA-256、严格JSON/XML、无头渲染和无partial检查 | 高（链路与隔离）；低（机理统计结论） | 否 | 用户在PyCharm直接运行默认`diagnostic`；正式结果必须按预注册样本与known-failure case分开解释，并据此做路线go/no-go |

### 2026-07-18 新增记录：BRSR-TDOA 阶段 B smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-18 | `NewMethod-Codex` | BRSR-TDOA 结构化 BPSK-RRC 阶段 B 最终 smoke | `brsr_tdoa/run_stage_b.py --mode smoke` | 历史运行标识 `运行结果/BRSR_STAGE_B_20260718_133858_smoke`（当前目录未保留） | `N=1024`、`Fs=40MHz`、BPSK-RRC、2 samples/symbol、滚降0.2、跨度8 symbols；连续TDOA=`[-8,8] samples`；线性分数时延；单径AWGN；12个理论带内正频率候选=`[31,94,102,137,162,182,198,211,224,237,251,270]`；每实数4-bit粗码追加到8-bit；5-bit反馈；最多2次动作；目标24/32/40 bit；prior/calibration/evaluation各1 seed，分别4/2/2 trials；SNR=`-5/10dB`；Rollout Q=`2`并检查`1/2/4` | 自动状态=`SMOKE_ONLY`。生成64条trial、16个方法；自适应最大通信42 bit，与注册硬上限一致；无重复。链路自检：RRC能量误差约`2.22e-16`，循环FFT相移最大误差约`1.60e-15`，线性时延相位/增益误差约`4.10e-6 rad/1.09e-6`，整数3-sample时延恢复为3；嵌套量化、频点唯一性和理论带宽检查通过。3张SVG、CSV、JSON、NPY均可读取。 | 阶段 B 的波形生成、候选设计、4→8 bit协议、精确Greedy、采样Rollout、Best-Static穷举、SNR-only、lambda校准、三池隔离、动作追踪、校准门禁和结果导出链路可运行。smoke仅4个evaluation样本，预算命中、RMSE、覆盖率、方法排序及Q动作变化均无研究意义，不得用于判定路线。 | `run.log`、`stage_b_trials.csv`、`summary_*.csv`、`lambda_calibration.csv`、`static_plans.csv`、`action_summary.csv`、`rollout_convergence.csv`、`chain_self_check.json`、`validation.json`、manifest和3张SVG；15项阶段A/B pytest、编译、Ruff、SVG XML解析、`validate_project_integrity.py`、`git diff --check` | 高（代码链、物理符号、预算和数据隔离）；低（性能） | 否 | 用户在PyCharm直接运行默认`development`；正式分析必须读取开发门禁、同预算强基线、后验校准、协方差失配和Q收敛。development通过后才允许另建locked，失败则停止本路线 |

### 2026-07-17 新增记录：BRSR-TDOA 阶段 A smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-17 | `NewMethod-Codex` | BRSR-TDOA 有限状态贝叶斯逐层精化阶段 A 修正版 smoke | `brsr_tdoa/run_stage_a.py --mode smoke` | 历史运行标识 `运行结果/BRSR_STAGE_A_20260717_181543_smoke`（当前目录未保留） | `N=1024`；17个离散TDOA假设；8个离散未知复增益且相位覆盖完整相位圆；6个候选频点；基础层2个频点；2-bit粗量化/3-bit精化；公开先验预测均方根标准化后固定范围±4；lambda=`0/0.01`；最多2次动作；DP深度2且每个seed/SNR只运行2个诊断样本；SNR=`-5/5dB`；development 1 seed×3 trials，locked 1 seed×5 trials | 自动状态=`SMOKE_ONLY`。128条trial、96条初始动作、16条DP诊断；bit账本恒等、development/locked种子隔离、跨方法同源TDOA/复增益/公开尺度、期望后验风险单调，最大变化=`-4.21e-6`、DP值不劣于一步前瞻、平均截幅率=0；3张SVG合法，5个CSV及3个JSON通过审计。聚类动作风险残差z=`0.362`；Spearman=`-0.072`仅为小样本描述，不再作为理论门禁。 | 对数域精确量化似然消除了高SNR人工概率地板；公开条件尺度消除了跨SNR固定原始范围的截幅/分辨率错配；locked集合不再选择最有利lambda。量化似然30万次蒙特卡洛最大概率误差约`3.65e-4`；基础后验在`-10/0/15 dB`每档1200次的经验MSE/平均后验风险比约`1.04/1.02/1.04`。以上只证明概率和协议机制自洽，不构成方法性能结论。 | `run.log`、trial/action/DP/summary CSV、`validation.json`、`go_no_go.json`、manifest、3张SVG；编译、Ruff、9项测试、非smoke门禁路径、似然/后验蒙特卡洛、SVG XML、CSV/JSON审计、隔离检查和项目完整性门禁 | 高（流程、概率链和理论不变量）；低（性能） | 否 | 用户在 PyCharm 运行默认 `research`；必须回读正式全lambda覆盖率、聚类风险残差、risk-bit图和门禁JSON后再决定是否进入CR16阶段 |

### 2026-07-16 新增记录：PASR-TDOA 独立 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-16 | `NewMethod-Codex` | PASR-TDOA 主动逐层有限比特协议最终复核 smoke | `pasr_tdoa/run_pasr_tdoa.py --mode smoke` | 历史运行标识 `运行结果/PASR_20260716_192922_smoke`（当前目录未保留） | N=16；基础层=`[-1,0]`；最多4个复频点；6-bit I/Q主检验；8-bit尺度；SNR=`-5/5/15dB`；1 development seed×12 trials；1 locked seed×20 trials；所有静态方法扫描4--10 bit；同bit/同风险/高SNR参考由development选择后冻结；连续网格=`1/32 sample` | `overall_status=SMOKE_ONLY`。无噪声最大 TDOA 误差=`1.863e-6 sample`；固定分层与同一频点并集后验差=`2.082e-17`。5568条trial rows、330条action rows、16个方法、3条冻结对手记录；逐行bit账本恒等、方法键无重复、数值有限、预算不越界、development/locked种子隔离、跨方法同源TDOA、manifest文件集合一致、3张SVG合法且无图例遮挡。PASR-Observed pooled RMSE=`2.812 samples`、locked平均/最大bit=`61.2/66`；Always4=`64/64 bit`，Fixed-Nested=`57.8/58 bit`。样本过小，不作为性能判断。 | 代码已消除 locked 选对手泄漏、静态位宽扫描不一致、边界后验伪早停、bootstrap聚类过粗和消融多收反馈位问题；主动后验、量化、反馈、development冻结的静态时分参考、统计与绘图链路可运行。smoke方法排序与CI无研究意义，不能据此判定PASR成败。首次结果审计因PowerShell here-string中文路径转码失败，改用Unicode转义路径后通过，非实验或代码失败。 | `pasr_manifest.json`、`pasr_go_no_go.json`、`pasr_frozen_comparators.csv`、trial/action/static/summary/method CSV、3张SVG；编译/Ruff/9项核心测试/compileall/项目完整性/独立结果与无头渲染审计 | 高（流程与不变量）；低（性能） | 否 | 用户在 PyCharm 运行默认 `research`；正式总体结论只由预注册6-bit主检验决定，4/8-bit仅作敏感性分析；必须使用6个locked seeds和development冻结的同预算参考 |

### 2026-07-16 新增记录：PFRS 连续 TDOA 验证 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-16 | `NewMethod-Codex` | 冻结 PFRS 掩码连续/亚采样 TDOA 验证 smoke | `pfrs_continuous_tdoa_validation.py` | 历史运行标识 `运行结果/PFRS_CONTINUOUS_20260716_150949_continuous_smoke`（当前目录未保留） | source=`PFRS_20260716_141935_confirmation`；SNR=`-10/-5/5/15dB`；TDOA=`[-4,4] samples`；grid=`1/32 sample`；1 development seed × 32 trials；2 locked seeds × 48 trials；8 methods；200 seed bootstrap | development/locked error shape=`[1,4,8,32]/[2,4,8,48]`；格点剖面代价与原标量公式最大差=`4.44e-15`；连续可辨识方法无噪声最大误差=`3.54e-6 samples`；Uniform 连续周期=`3.2 samples` 且被标记不可辨识，其余 gate 方法周期=`16 samples`；最终自动 gate=`HOLD`。11 个输出文件、96 条 by-seed rows、64 条 locked summary rows、NPZ→CSV、SVG XML 和 manifest 均通过独立复算。首次 smoke 因 Uniform 无噪声别名停止，第二次因 smoke 缺少 stress 分组停止；两项均修正后完整重跑，失败目录已删除。 | 连续估计、亚采样细化、固定掩码/哈希、三类 SNR、开发/锁定隔离、统计汇总和自动门禁链路有效。旧可辨识性判据只覆盖整数时延；连续相位必须单独检查 gcd 周期。smoke 方法排名与 HOLD 不作为性能结论。 | manifest/gate、method metadata、by-seed/summary/paired/Fisher CSV、trial NPZ、3 个 SVG、编译/Ruff/项目完整性日志、独立重建脚本和人工渲染 | 高（流程和理论不变量）；低（性能） | 否 | 用户在 PyCharm 运行默认正式 `continuous_validation`；完成后按 locked seeds 结果判定 GO/REDIRECT/HOLD/NO-GO |

### 2026-07-16 新增记录：PFRS 统计确认协议 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-16 | `NewMethod-Codex` | PFRS selection/confirmation 分离 smoke | `pfrs_finite_delay_selection.py` | 历史运行标识 `运行结果/PFRS_20260716_141431_confirmation_smoke`（当前目录未保留） | mode=`confirmation_smoke`；N=12、M=4；2 selection seeds × 30 trials/hypothesis；3 confirmation seeds × 100 trials/hypothesis；200 bootstrap；selection/confirmation seeds 互斥 | 990 selection rows、315 by-h rows、63 seed-SNR rows、15 paired-by-delay rows；成对 error2 shape=`[3,3,5,200]`，NPZ 可精确重建 CSV。PFRS Fisher ratio=`0.9160`；mean gain=`0.00444`，95% CI=`[-0.01205,0.01829]`；robust gain=`-0.00733`，CI=`[-0.0300,0.00833]`；自动状态=`HOLD`。 | 确认协议、统计门禁和结果保存链路有效；小样本按设计没有被误判为 GO。该结果不用于任何方法性能结论。 | gate/manifest、selection/confirmation CSV、paired NPZ、4 个 SVG、NPZ→CSV 重建断言、旧路径回归、项目完整性日志 | 高（流程）；低（性能） | 否 | 用户运行默认正式 `confirmation`，再按独立确认 CI 判定候选 A |

### 2026-07-16 新增记录：PFRS 独立理论门禁 smoke

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-16 | `NewMethod-Codex` | 候选 A：剖面有限时延风险选频（PFRS）smoke | `pfrs_finite_delay_selection.py` | 历史运行标识 `运行结果/PFRS_20260716_131523_smoke`（当前目录未保留） | mode=`smoke`；N=12、M=4；495 masks；SNR=`-8/0/8dB`；120 trials/hypothesis；prior width=6 samples；eta=`0.7/0.8/0.9/0.95/1.0`；primary eta=0.9；center-weighted source；seed=20260716 | 462 个掩码在先验区间内可辨识。PFRS：Fisher ratio=`0.915739`、worst risk=`0.494286`、mean risk=`0.296627`；Zhai-Fisher：`1.0/0.526429/0.316429`；Power：`0.094755/0.446667/0.220992`。局部极限最大偏差=`1.65e-7`；向量化剖面代价与 SciPy 广义特征值最大差=`1.78e-15`。 | 实现和理论内部一致性门禁通过。小样本显示纯 Power 可降低当前设计风险但牺牲约 90.5% 局部 Fisher 信息，PFRS 在 `J/Jmax>=0.9` 条件下优于当前 Zhai-Fisher smoke 点；由于 Monte Carlo 标准误差最高约 `0.0323`，不能据此声称正式性能优势。 | `pfrs_manifest.json`、四个 CSV、三个 SVG、终端日志、固定种子重复性断言、`validate_project_integrity.py` | 高（代码/公式一致性）；低（性能排名） | 否，仅可作为理论流程与机制 smoke | 运行 `research` 模式；随后做多 seed、源谱/增益/先验敏感性和风险排序相关性验证 |

## 2. 早期代码级与 smoke 记录（按时间倒序）

| 日期 | 分支 | 验证名称 | 脚本 | 数据路径 | 关键参数 | 验证结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-09 | `NewMethod-Codex` | V5-B expert-gated compressed-domain TDOA 代码级验证 | `py_compile`；inline dummy posterior experts smoke | 无正式结果目录；仅内存 dummy posterior | 默认 profile 改为 `compressed_tdoa_v5b_fast`；V5-B 融合 `V5A1-Power64` 与 `V5A1-GeoHybrid64`；V5-A.1 experts 使用 bucket-weighted ValMAE restore；dummy posterior 测试 top-K 与 pair weight 接口 | `py_compile` 通过；dummy smoke 返回 finite lag、positive pair weight、top-3 hypotheses 和 expert mixture quality 字段 | V5-B 代码链路可编译，estimator 接口可进入现有 direct TDOA/WLS 评估；该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig9/TDOA SVG 与 CSV 后才能记录正式指标 |
| 2026-07-08 | `NewMethod-Codex` | V5-A compressed-domain TDOA 代码级 smoke | `py_compile`；inline `train_v5a_compressed_tdoa()`；inline `run_urban_method_comparison()` | 无正式结果目录；小样本临时 urban8 数据 | `compressed_tdoa_v5a_fast` 代码路径；10 个 urban snapshot；80 个 LOS pair；V5-A CR16 训练 1 epoch；lag gate=48；20 dB 小样本定位 smoke | `py_compile` 通过；V5-A smoke 训练/验证 loss finite；`ValMAE≈6.133 samples`，`Val<=1≈0.667`；`V5A-CR16` direct estimator 进入 all-pair WLS 后 20 dB valid count 为 6/6；`git diff --check` 仅 Windows CRLF 提示 | V5-A 模型、训练器、direct estimator 和定位评估链路可运行；该记录不代表正式性能，不能用于论文指标结论 | 命令输出和代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig9 和 CSV 后再记录正式指标 |
| 2026-07-04 | `NewMethod-Codex` | Fig8 强基线代码级验证与小样本 smoke | `py_compile`；PyTorch inline smoke | 无正式结果目录；仅合成已知延迟信号与小样本 urban8 临时数据 | `USER_RUN_STRONG_BASELINES=True`；默认 `USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=False`；direct estimators: Direct/Cao/Zhai-CRLB；小样本 urban8 使用 2 个 SNR、4 observation，仅验证流程 | 两套 `py_compile` 通过；Direct/Cao 对 `np.roll(x, 7)` 返回 `-7.0`；简化 phase-superposition 未稳定返回正确延迟，因此默认关闭；小样本 urban8 comparison + Fig8 plotting 输出 `FIG8_SMOKE_OK`；`git diff --check` 仅提示 Windows 行尾转换 | Fig8 强基线代码链路可编译、可构建、可绘图；尚未产生正式性能结论 | 命令输出与本轮代码审查 | 高 | 否 | 需用户在 PyCharm 完整运行 `main.py` 后读取新结果目录，才可记录正式 Fig8 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v3 代码验证与函数级 smoke | `py_compile`、inline Python smoke、`git diff --check` | 函数级小样本；未生成正式结果目录 | 默认 `USER_BASELINE_DFT_MODE="train_band"`；`DFT-train-band` 为训练集平均功率连续频带；`DFT-train-power` 为 top-k 离散频点补充；Salari Hadamard 主图为 Lemma 1 block-1，补充 `Hadamard-block-2`；Fig6 主图高 SNR inset；自动 export 默认 `export_fig6_figures=False` | `py_compile` 通过；inline smoke 成功构建 `DFT-train-band`、`DFT-train-power`、`Hadamard-block-2`，绘图检查到 Fig6 主图含 inset child axis，导出检查到 `fig6_traditional_baselines.csv` 与 `fig6_random_group_summary.csv` 生成且未生成重复 Fig6 SVG；smoke 输出 `SMOKE_OK DFT-train-band contiguous_power_band 1`；`git diff --check` 无空白错误，仅提示 Windows 行尾转换 | Fig6 v3 代码链路可编译并通过函数级验证，已进入待 PyCharm 完整 eval-only 运行状态；该记录不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、`git diff --check` 输出 | 高 | 否 | 下一步需完整运行 `main.py` 生成新结果目录，读取 `run.log`、`plot_data.pkl`、Fig6 SVG 和 `tables/` 后再记录正式 Fig6 v3 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v4 代码验证与函数级 smoke | `py_compile`；PyTorch inline smoke；`git diff --check` | 函数级小样本；未生成正式结果目录 | 默认 `USER_BASELINE_DFT_MODE="fisher_power"`；`DFT-Fisher` 按训练集平均频谱功率 × 归一化频率² 选频，并用 5% 最大功率阈值屏蔽低功率噪声频点；主 Fig6 为 Raw/DAE-CR16/DFT-Fisher/Hadamard/PCA；eval-only 默认不复制模型权重 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py` 通过；PyTorch smoke 输出 `smoke_ok DFT-Fisher 64 [...]`，确认 `DFT-Fisher` 选择 64 个 complex bins，`DFT-train-band/train-power/bandlimited/uniform/random`、Hadamard 变体和 PCA 均可构建并 forward 输出正确形状；Fig6 绘图对象生成成功；`git diff --check` 无空白错误，仅提示 Windows 行尾转换 | v4 代码进入待 PyCharm 完整 eval-only 运行状态；该记录只证明代码链路可运行，不包含正式 RMSE 指标 | 编译输出、inline smoke 输出、`git diff --check` 输出 | 高 | 否 | 下一步需完整运行 `main.py` 生成新结果目录，读取 `run.log`、`plot_data.pkl`、Fig6 SVG 和 `tables/` 后再记录正式 Fig6 v4 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 DFT direct-TDOA 口径修正代码验证 | `py_compile`；PyTorch inline smoke；`git diff --check` | 函数级小样本；未运行完整 eval-only | Fig6 主图的 `DFT` 改为 selected-bin cross-spectrum direct TDOA，来源优先为 `DFT-SCS-lite` partial-Fourier bins；零填充 IDFT 单路波形重构 DFT 仅作补充消融；Fig7 保留 `DFT-Fisher-Direct`、`DFT-train-power-Direct` 等任务导向变体 | 两套 `py_compile` 均通过；PyTorch 小样本 urban8 baseline/direct smoke 输出 `SMOKE_OK DFT-SCS-lite ['DFT', 'DFT-Fisher-Direct'] ...`；`git diff --check` 仅提示 Windows 行尾转换，无空白错误 | 新版 Fig6/Fig7 代码链路进入待 PyCharm 直接运行状态；本记录只验证代码结构和小样本流程，不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、静态检查输出 | 高 | 否 | 下一步需完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig6/Fig7 SVG 和 `tables/` 后再记录正式指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6/Fig7 direct DFT lag-gate 与 zoom 代码验证 | `py_compile`；PyTorch inline smoke；临时目录导出 smoke | 函数级小样本；未生成正式结果目录 | `paper_repro_eval_only` 默认不训练；Direct DFT 支持 `lag_limit_samples`；Fig6 主线 `DFT` direct source 默认 `DFT-train-power`；urban8 自动物理 lag gate；Fig6/Fig7 low/high zoom SVG | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；小样本 method comparison 输出 `SMOKE_OK ['Clean', 'DFT', 'DFT-Fisher-Direct', 'DFT-SCS-lite', 'Geometry', 'Raw'] ...`；临时目录导出输出 `EXPORT_SMOKE_OK 11` | 新代码链路可编译并通过流程验证，已进入待 PyCharm 完整 eval-only 运行状态；该记录不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、导出 smoke 输出 | 高 | 否 | 需完整运行 `main.py` 后读取新结果目录，才能记录新的 Fig6/Fig7 指标 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_v3.1 代码验证与冒烟 | `py_compile`；PyTorch inline smoke；Fig3 plotting smoke | 小样本合成 urban8 数据；无完整结果目录 | `training_protocol=cv_best_epoch_final_train`；`final_epoch_policy=median_cv_best_epoch`；训练 waveform 每 1 epoch 重采样的 smoke；默认代码配置为每 5 epoch 重采样；Fig3 默认不显示 CI 填充 | 两套 `py_compile` 均通过；训练协议冒烟输出 `SMOKE_PROTOCOL cv_best_epoch_final_train median_cv_best_epoch 2 2`；Fig3 绘图冒烟输出 `PLOT_SMOKE [15.0, 4.8] 3` | v3.1 代码可编译，CV 后全训练集 final train、固定计划重采样和 Fig3 版式调整均可运行；尚未验证完整训练效果 | 编译输出、inline smoke 输出、绘图 smoke 输出 | 高 | 否 | 下一步需运行完整 `python main.py` 并读取新结果目录；不得据此写完整实验结论 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_fast_final 小样本冒烟 | `main.py` | 历史运行标识 `运行结果/20260702_140552`（当前目录未保留） | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_fast_final`、`DAE_N_SAMPLES=32`、`DAE_FAST_FINAL_EPOCHS=1`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_NO_SHOW=1` | 三个 CR 均完成 1 epoch final-only 训练、模型保存、UrbanLocalizationSweep、Fig1/Fig2/Fig3 和 pkl 保存；总耗时约 0.4 min | fast_final 代码路径可运行；这是流程冒烟，不可用于结果结论 | 冒烟 run.log 输出 | 高 | 否 | 若要得到可用 fast_final 结果，需使用默认 197 epoch 或指定合理 epoch 后完整运行 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_eval_only 小样本冒烟 | `main.py` | 历史运行标识 `运行结果/20260702_140637`（当前目录未保留） | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_eval_only`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_NO_SHOW=1`；模型源 `运行结果/论文复现与传统基线/20260702_000925_论文复现v3.1` | 成功加载 `model_cr4/8/16.pt` 与源 `plot_data.pkl`，完成小样本 UrbanLocalizationSweep、Fig1/Fig2/Fig3 和 pkl 保存；总耗时约 0.3 min | eval_only 代码路径可运行；适合后续快速重评估/出图，不重新训练 | 冒烟 run.log 输出 | 高 | 否 | 默认完整 eval_only 仍会按配置运行 200 trials；可用环境变量降低试跑成本 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 传统基线代码验证与小样本冒烟 | `main.py`、`export_results.py` | 历史运行标识 `运行结果/20260702_154414`（当前目录未保留） | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_eval_only`、`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1`；模型源 `运行结果/论文复现与传统基线/20260702_000925_论文复现v3.1`；baseline CR=16 | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；小样本运行成功生成 `Fig6_Traditional_Baselines_CR16.svg` 和 `plot_data.pkl`；pkl 中 Fig6 方法顺序为 `Raw/Clean/Geometry/DFT/Hadamard/PCA/DAE-CR16`；baseline 元信息显示 latent real dim=128、DFT complex bins=64、Hadamard rows/channel=64、PCA actual components=31；`export_results.py` 成功导出 `fig6_traditional_baselines.csv/md` 和 Fig6 SVG | Fig6 代码链路可运行，已进入待正式完整 eval-only 运行状态；本记录是小样本流程验证，不可用于论文指标结论 | 编译输出、run.log、plot_data.pkl 字段检查、导出脚本输出 | 高 | 否 | 正式 Fig6 需用完整 `paper_repro_eval_only` 配置运行，PCA 样本数不应使用 32 |
| 2026-07-02 | `NewMethod-Codex` | PyCharm 默认模式修复小样本冒烟 | `main.py` | 历史运行标识 `运行结果/20260702_161432`（当前目录未保留） | 显式移除 `DAE_EXPERIMENT_MODE`；保留小样本覆盖：`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1` | 两套 `py_compile` 通过；运行日志显示 `EXPERIMENT_MODE=paper_repro_eval_only (source=main.py:DEFAULT_EXPERIMENT_MODE)`、`K=0`、`Epochs=0`、`run_training=False`、`model_source_dir=运行结果/论文复现与传统基线/20260702_000925_论文复现v3.1`；成功加载冻结 `model_cr4/8/16.pt` 并生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl` | 证明不依赖外部 PowerShell 环境变量时，`main.py` 默认进入 eval-only，不会重新训练；本记录不是正式 Fig6 结果 | 编译输出与冒烟 run.log | 高 | 否 | 正式运行需移除小样本覆盖，只保留默认 `paper_repro_eval_only` |
| 2026-07-02 | `NewMethod-Codex` | Fig6 baseline variants 默认链路小样本冒烟 | `main.py`、`export_results.py` | 历史运行标识 `运行结果/20260702_195744`（当前目录未保留） | 无 `DAE_EXPERIMENT_MODE`；小样本覆盖：`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1`；默认 `dft_mode=uniform`、`hadamard_mode=salari`、`pca_source=noisy` | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；运行日志显示 `EXPERIMENT_MODE=paper_repro_eval_only`、`run_training=False`；成功生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl`；pkl 方法顺序为 `Raw/Clean/Geometry/DFT/Hadamard/PCA/DAE-CR16`，诊断字段包含 `complex_nmse`、`psd_db_mse`、`gcc_mean_abs_tdoa_error_samples` 等 9 项；首次误用 `export_results.py --result-dir ...` 失败，报错 `FileNotFoundError: --result-dir\plot_data.pkl`，随后用位置参数重跑成功生成 `fig6_method_diagnostics.csv/md` | 新版默认 Fig6 代码链路可运行，PyCharm 直接运行仍不会训练；该记录是小样本流程验证，指标不可用于论文结论 | 编译输出、run.log、plot_data.pkl 字段检查、导出脚本输出和失败重跑记录 | 高 | 否 | 正式结果需移除小样本覆盖，在 PyCharm 直接运行完整 `paper_repro_eval_only` |
| 2026-07-02 | `NewMethod-Codex` | Fig6 diagnostic variants 小样本冒烟 | `main.py` | 历史运行标识 `运行结果/20260702_195918`（当前目录未保留） | 无 `DAE_EXPERIMENT_MODE`；小样本覆盖：`DAE_N_SAMPLES=16`、`DAE_MONTE_CARLO_TRIALS=2`、`DAE_FIG2_DIAGNOSTIC_TRIALS=1`、`DAE_BASELINE_PCA_SAMPLES=16`、`DAE_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=1`、`DAE_NO_SHOW=1` | 运行成功，日志显示 baseline 元信息 `include_diagnostic_variants=True`，方法列表含 `DFT`、`Hadamard`、`DFT-bandlimited`、`DFT-random`、`Hadamard-random`、`PCA`、`DAE-CR16`；生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl` | 证明额外 DFT/Hadamard 变体链路可运行；该记录只验证流程，不用于论文指标结论 | 冒烟 run.log 输出 | 高 | 否 | 若正式运行额外变体，需显式设置 `DAE_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=1` 并使用完整 PCA 样本数 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 baseline v2 代码验证与导出补齐 | `py_compile`、inline smoke、`export_results.py` | 函数级小样本；导出目标 历史运行标识 `运行结果/20260702_203157/tables`（当前目录未保留） | 新版默认主 DFT 为 `DFT-train-power`；补充变体含 `DFT-bandlimited/DFT-uniform/DFT-random*`、`Hadamard-random*/sequency/offset`；随机 seed 数 5；主/补充 Fig6 分离；新增 active PSD、linear PSD NMSE、weighted TDOA error 等诊断 | 两套 `py_compile` 通过；函数级 smoke 成功构建 `DFT-train-power`、`Hadamard-offset-64` 和随机变体，运行两点 SNR 小样本 Fig6 并生成主/补充绘图对象；`export_results.py --result-dir 运行结果/20260702_203157` 成功生成 `Fig6_Traditional_Baselines_CR16.svg`、`Fig6_Supp_Baseline_Ablation_CR16.svg`、`fig6_traditional_baselines.csv/md`、`fig6_method_diagnostics.csv/md` | 新版 Fig6 v2 代码链路可编译并通过函数级验证；既有正式结果目录的 tables 已补齐，但新版 `DFT-train-power` 仍需重新完整 eval-only 才有正式指标 | 编译输出、inline smoke 输出、导出脚本输出 | 高 | 否 | 下一步需在 PyCharm 直接运行 `main.py`，读取新目录后记录正式 Fig6 v2 指标 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v1 代码验证与数据标签自检 | `py_compile`；PyTorch inline smoke；`clean_peak_consistency` | 小样本合成数据；默认 50 固定信道自检，无完整结果目录 | 默认 `EXPERIMENT_MODE=paper_repro`；MSE-only；NLOS=0；LOS label；`multipath_scale=0.1`；训练 SNR -10..20；评估 SNR -10:2:20 | 两套 `py_compile` 通过；小样本端到端输出 `SMOKE_OK 1 paper_repro_v1 2 [-10, 0] 1.0`；默认 50 信道 clean peak 自检：match=1.0、within2=1.0、mean_abs_peak_error=0.0、mean_false_peaks_gt_05≈0.0586 | 代码进入 paper_repro 待完整运行状态；该记录不是完整训练结果 | 编译输出、inline smoke 输出、clean peak 自检输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v2_urban8 代码验证与冒烟 | `py_compile`；PyTorch inline smoke；clean peak/定位轻量检查 | 小样本合成 urban8 数据；无完整结果目录 | 默认 `paper_repro` 已切换为 `paper_repro_v2_urban8`；8 UAV；单目标；建筑物遮挡；oracle LOS；路径损耗；分数时延；per-observation RMS；snapshot GroupKFold；localization RMSE meters | 两套 `py_compile` 通过；小样本端到端输出 `SMOKE_OK paper_repro_v2_urban8 2 localization_m [-10, 0] 1.0`；默认 50 snapshot clean peak 自检 exact=0.5625、within1=1.0、within2=1.0、mean_abs_peak_error=0.4375；R20.1 旧路径输出 `TASK_SMOKE_OK R20.1 1` | 代码进入 `paper_repro_v2_urban8` 待完整运行状态；该记录不是完整训练结果，不能据此判断论文趋势 | 编译输出、inline smoke 输出、轻量自检输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v3_urban8_fair_eval 代码验证与冒烟 | `py_compile`；PyTorch inline smoke | 小样本合成 urban8 数据；无完整结果目录 | v3：固定 snapshot channel；`urban_base_delay=16`；`urban_min_los=5`；`per_observation_noisy_rms`；LOS-only 训练；fixed eval set；all-pair WLS；fixed epoch；final retrain | 两套 `py_compile` 通过；默认 50 snapshot 生成成功，LOS count min/mean/max=5/5.24/7，clean peak within2=1.0；小样本 WLS 中 Geometry oracle 在 -10/0/20 dB 均为 0，Clean 均约 0.16 m；1 epoch CV + final retrain 输出 `TRAIN_OK paper_repro_v3_urban8_fair_eval 1 True`；Fig2 固定 demo 输出相同 true TDOA `[-8, -8]` | v3 代码进入待完整运行状态；该记录不是完整训练结果，不能据此判断论文趋势 | 编译输出、inline smoke 输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v3 训练协议调整冒烟 | `py_compile`；PyTorch inline smoke | 小样本合成 urban8 数据；无完整结果目录 | `training_protocol=cv_fixed200_best_fold`；`early_stopping=False`；`restore_best=True`；`final_retrain=False`；smoke 使用 2-fold × 2 epoch | 两套 `py_compile` 通过；冒烟输出 `PROTOCOL_SMOKE cv_fixed200_best_fold [2, 2] False`，确认每折跑满指定 epoch、无 final retrain | 代码进入更省时的 v3 待运行状态；完整 paper_repro 将是 5 fold × 200 epoch/CR，不再额外全量 200 epoch | 编译输出、inline smoke 输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-06-30 | `NewMethod-Codex` | R20.1 代码级冒烟验证 | `py_compile`；PyTorch inline smoke | 小样本合成数据，无结果目录 | `mse_weight_max/phase_mix` 解耦；`selection_start_epoch=2` smoke；Fig2 `diagnostic_trials=2` | `py_compile` 两套 Python 均通过；CPU 小样本训练、Fig2 diagnostics、Fig3 CI 绘图均通过；输出 `SMOKE_OK 2 2` | R20.1 代码可编译，关键新增字段可运行；尚未验证完整实验效果 | py_compile 与冒烟测试输出 | 高 | 否 | 未运行完整 `python main.py`，不得写完整训练结论 |
| 2026-06-29 | `NewMethod-Codex` | R20 代码级冒烟验证（前序工作） | `python -m py_compile` 与 PyTorch inline smoke | 无；合成随机/小样本 | 固定混合 NMSE；same lag GCC；低 SNR 高 Peak；CR=4/8/16 | 记录中为 `py_compile` 通过；tiny 1 epoch CV 返回新字段；train/evaluate same-window GCC 最大差约 `5.1198735e-05` | R20 代码曾通过基础语法和一致性冒烟测试，但未完整训练 | 当前对话上下文记录 | 中 | 否 | 本轮未重新运行该验证；如需引用需重新执行或查原始输出 |

## 3. 日期合并表中迁出的 smoke 与代码级验证（按时间倒序）

| 日期 | 分支 | 验证名称 | 脚本 | 数据路径 | 关键参数 | 验证结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-14 | `NewMethod-Codex` | 受控重叠频谱对齐 smoke | `standalone_frequency_alignment/run_interpolation_alignment.py` | 历史运行标识 `运行结果/频谱插值对齐/20260714_175153_381904_smoke`（当前目录未保留） | N=64，M=12，L=4；SNR=2/14 dB；重叠=0/0.5/1；2 类信号；每点 4 trials；连续时延步长 0.02 sample | 432 method rows；理论门禁全通过，算子秩=12；理想插值与零填充最大误差约 `6.1e-16`；五种伪补全与 `Intersection-Continuous` 的有效/无效状态和逐试验 TDOA 完全一致；生成缺失频点能量最大约 `2.6e-30` | 在相同原始 DFT 网格上，周期插值没有补出新的有效复频点；该 smoke 只验证理论恒等式和流程，不做方法性能排名 | `operator_diagnostics.csv`、`trial_results.csv`、`paired_comparisons.csv`、`run.log` | 高（流程） | 否 | 已被 `synthetic_full` 覆盖，仅保留为过程回归证据 |
| 2026-07-14 | `NewMethod-Codex` | 双站本地严格 Zhai smoke | 同上 | 历史运行标识 `运行结果/频谱插值对齐/20260714_175510_177406_local_zhai_smoke`（当前目录未保留） | N=64，M=12；A、B 分别使用本地周期图运行严格 0-1 逐点交换；2 个 SNR、2 类信号、每点 4 trials | 两站实际重叠比例约 0.0833 至 0.9167，选频均收敛；当共同有效频点少于 2 个时统一判无效；其余试验中全部伪补全方法与交集连续估计逐试验完全一致 | 双站各自运行 Zhai 确实会产生不同掩码，但周期插值仍不能解决缺失跨站相位观测 | 逐试验 CSV、选频元数据、算子诊断 | 高（流程） | 否 | 仅在另行审批“独立/协调选频策略比较”路线时扩大样本，不再服务于插值伪补全路线 |
| 2026-07-14 | `NewMethod-Codex` | urban8/LOS 项目适配 smoke | 同上 | 历史运行标识 `运行结果/频谱插值对齐/20260714_175605_038956_project_smoke`（当前目录未保留） | N=1024，M=64，L=4；现有 fixed urban8/LOS、per-observation noisy RMS；SNR=0/12 dB；每点 2 trials；lag=±48；双站本地严格 Zhai | 36 method rows；项目 TDOA 符号检查通过；算子秩=64；两站重叠比例约 0.1875 至 0.625；理想插值与零填充误差约机器精度，生成缺失能量最大约 `4.5e-28`；全部伪补全 TDOA 与交集连续估计逐试验一致 | 独立模块能读取现有项目模拟器且不影响主流程；小样本指标不作为 urban8 正式性能结论 | `run.log`、`config.json`、`protocol_manifest.json`、CSV、`plot_data.pkl`、SVG | 高（流程） | 否 | 插值机制已被完整合成实验否定，不再运行 `project_full` |
| 2026-07-11 | `NewMethod-Codex` | Shared64 native-only 公平入口 smoke | `evaluate_topk_fairness.py`；pool-wise export inline smoke | `运行结果/压缩域TDOA_V5/20260711_154042_V5B_Shared64严格CR16/tables/topk_fairness` | mode=`smoke`；训练 pool seed42；-10dB × 2 trials；OriginalTop1-WLS；Raw、DAE16、Power/Geo Shared52、V5B-Shared64、legacy Power/Geo64、DFT、Cao2017、Zhai；不运行 Common Top-K | 最终退出码0；10个方法均 requested=valid=2、failure=0、mean solver calls=1；生成 `FigFair1_Native_Strong_Baselines_Smoke.svg`；pool-wise comparison 临时导出20行。首次 smoke 曾因 native-only 对 Raw 写 Common evidence 触发 `KeyError: Raw`，修复后完整重跑通过。 | P0/P1 代码链路可运行，当前结果只验证加载、预算、方法覆盖、WLS和导出，不用于性能排名。独立 development 三池全量尚未运行。 | 终端日志；smoke CSV/SVG；完整性门禁；临时 pool-wise CSV 断言 | 高（代码链路） | 否 | 默认已切为 `dev_native_multi_pool`；用户在 PyCharm 直接运行 `evaluate_topk_fairness.py`，完成后回读多池 rows/CI/FigFair1 |
| 2026-07-11 | `NewMethod-Codex` | 全项目科学完整性 smoke | `validate_project_integrity.py`；`ruff`；`compileall`；模块导入；V5-A.1 loss inline smoke | 无正式结果目录；不写运行结果 | urban8 8 snapshots；fixed eval `-10/20dB × 4`；已知 7-sample 时移；geometry-oracle WLS；DAE CR4/8/16 单样本；DFT/Hadamard/PCA CR16；缓存 snapshot-pool mismatch | `ruff check *.py`、`compileall`、15 个核心模块导入均通过；完整性脚本全部 PASS：fixed geometry/label、clean waveform direction、正负 TDOA、pair seed 重现与两路差异、GCC/direct 符号、geometry WLS、CR16 预算、DAE latent、缓存隔离；V5-A.1 per-sample loss 与原 batch mean 数值等价。两次单行定位器 guard 命令因 PowerShell/Python 引号构造错误返回 SyntaxError，随后已把同一断言纳入完整性脚本并通过。 | 本轮只证明代码和科学不变量门禁可运行，不产生方法性能结论。当前旧 V5-B 的 88-bin 并集会被识别为超预算，严格 CR16 需新 Budget64 模型。 | 终端检查输出；代码审查；`validate_project_integrity.py` PASS 日志 | 高（代码/协议） | 否 | 修改严格 Budget64 后必须再次运行完整性脚本，再由用户在 PyCharm 做正式训练和评估 |
| 2026-07-10 | `NewMethod-Codex` | 统一 WLS + 公平强基线 Top-K 端到端 smoke | `evaluate_topk_fairness.py --run-mode smoke`；共享 WLS 回归；原生 score 等价断言 | 历史文件模式 `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控/tables/topk_fairness/fair_topk_smoke_*`（当前目录未保留） | `-10dB × 2 trials`；Raw、DAE-CR16、V5-B、DFT-train-power、DFT-Fisher、Cao2017、GeoHybrid-C40F24、Zhai-CRLB、Hadamard、PCA；原生 Top1 + 统一 CommonTop1/Top3；不训练神经网络；PCA 按历史 10000 noisy 样本协议重建一次并缓存 | 退出码 0；10 个方法、37 个方法×求解器组合，共 74 行且全部有效。Direct/Cao2017/Cao2020/Zhai 的 score 与原生 Top1 数值一致；GeoHybrid 的不合法全局 score 调用被阻断。ScreenRefit 不超过 2 次 WLS，GeoBeam 约 21 次；全项目 `compileall` 通过。 | 公平入口已覆盖当前主要强基线，并把原生方法排名与统一 solver 增益分开。2-trial 指标方差极大，不得用于性能排名；正式证据来自 dev_fair，locked 在 solver 决策冻结前不得运行。 | 终端输出；74-row fair CSV；broad leaderboard；native/common alignment；solver attribution；manifest；PCA cache；语义断言 | 高（流程）/低（性能） | 否 | PyCharm 直接运行默认 dev_fair（6 SNR × 30 trials）；分析并冻结唯一 common solver 后再运行 locked_holdout |
| 2026-07-10 | `NewMethod-Codex` | Beam-selected TDOA/复杂度审计 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 10` | 历史文件模式 `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控/tables/v5b_topk_wls_smoke_*.csv`（当前目录未保留） | `-10dB`、20 trials、240 pairs；独立峰 Top3；仅低置信度 pair 参与 Beam；新增 pair decisions、TDOA-only、complexity 和 snapshot-cluster bootstrap | 退出码 0；preflight 240 pairs 全通过。定位 Top1/Beam RMSE=`93.22/70.63m`；TDOA MAE=`17.29/8.60 samples`、RMSE=`23.67/13.92 samples`；snapshot-cluster 定位 RMSE gain=`22.60m`，95% CI=`2.03..51.16m`；TDOA MAE gain=`8.68 samples`，95% CI=`7.29..10.58`；平均每样本约 21 次 WLS。 | 审计链路证明可以同时追踪定位、pairwise TDOA 和计算成本；小样本中三者语义一致。20 trials 方差大，不能作为正式方法性能或论文结果。 | 终端输出；`pair_decisions`/`tdoa_metrics`/`complexity`/`go_no_go` CSV；字段复算断言 | 高（流程）/低（性能） | 否 | PyCharm 直接运行默认 full；再据全 SNR 审计判断 V5-B+ 是否保留 |
| 2026-07-10 | `NewMethod-Codex` | 独立峰 Top-K + 低置信度 BeamWLS 修正 smoke | `diagnose_v5b_topk.py --smoke`；`diagnose_v5b_topk_wls.py --smoke --progress-every 10` | 历史文件模式 `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控/tables/*_smoke.csv`（当前目录未保留） | `-10dB`、20 trials、240 LOS pairs；独立局部峰、逐峰亚采样；Beam 仅处理 `low_confidence_prior` pair；无质心回退 | Top-K：Top1/Top3/Top5 ±1=`15.8%/27.1%/34.2%`，所有样本得到 5 个独立候选，Top1 与 `estimate_lag` 最大差=0。WLS：Top1/Beam RMSE=`93.22/70.63m`，median=`66.10/40.82m`，trimmed=`87.87/60.70m`；两方法均 valid=20、failure=0；配对 RMSE gain=`22.60m`，95% bootstrap CI=`2.06..48.61m`；运行 45.4s，退出码 0。 | 修正后的候选、Beam、failure 和 CSV 统计链路可运行；20 trials 结果方差很大，脚本按设计输出 smoke `NO-GO`，不能作为正式性能证据。 | 终端输出；smoke CSV；人工断言；全项目编译 | 高（流程）/低（性能） | 否 | 先全量重跑 `diagnose_v5b_topk.py`，再运行默认 `low_gate`，按 6 个低 SNR 全量结果判定 |
| 2026-07-10 | `NewMethod-Codex` | 方案A：top-K WLS 主定位器协议对齐 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 5`；全项目 `.py` 编译 | `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控/tables` | 1 SNR(-10dB)、20 trials；`diagnose_v5b_topk_wls.py` 复用 `evaluate._localize_from_tdoa_pairs(standard)`；旧 CSV 暂无 `estimate_lag`，本次 smoke 回退 `top1_lag` | 编译通过。Smoke 输出：`[OK] TDOA preflight passed: checked 240 CSV pairs`；`[OK] All TDOA assertions passed`；Top1-WLS vs V5B-Expert64 max per-SNR RMSE diff=`0.1208 m`；无最终 error。 | 方案A代码路径可运行，且 smoke 下 Top1-WLS 已与主 Fig9 V5B 曲线基本对齐。该记录只证明协议对齐修复和脚本链路可用，不代表正式 Stage 2A 性能。 | 命令输出；`py_compile` 输出；`v5b_topk_wls_smoke_alignment.csv` | 高 | 否 | 需先重新运行 `diagnose_v5b_topk.py` 生成含 `estimate_lag` 的新版 top-K CSV，再全量运行 `diagnose_v5b_topk_wls.py` 做最终 go/no-go |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS geometry 修复 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 5` | `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控/tables` | 1 SNR(-10dB)、20 trials；使用已生成的 `v5b_pair_topk_diagnostics.csv`；修复后 geometry reconstruction=`seeded generator`（旧 CSV 无 `snapshot_id`）；新增 WLS 前 TDOA preflight | Smoke 输出：`[OK] TDOA preflight passed: checked 240 CSV pairs`；`[OK] All TDOA assertions passed`；centroid_fallbacks=2；Top1-WLS RMSE=101.90m，Top3-BeamWLS=111.61m，Top3-RejectBeamWLS=94.84m；Top1-WLS vs V5B-Expert64 max diff=0.1208m；无最终 error。 | 修复有效：WLS 诊断脚本现在能在正式计算前确认 CSV 与 meta 一致，且 smoke 下 Top1-WLS 与原 V5B 曲线对齐。该结果只证明链路修复，不代表 Stage 2A 正式性能。 | 命令输出；读取 `v5b_topk_wls_smoke_summary_by_snr_bucket.csv` 与 `v5b_topk_wls_smoke_alignment.csv`；全入口编译输出 | 高 | 否 | 需要用户全量运行 `diagnose_v5b_topk_wls.py` 覆盖修复前错配的非 smoke CSV |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS smoke 验证 | `diagnose_v5b_topk_wls.py --smoke --progress-every 5` | `运行结果/压缩域TDOA_V5/20260709_120019_V5B专家门控` | 1 SNR(-10dB), 20 trials, beam_max_pairs=12, beam_passes=1, grid_size=3 | Top1-WLS RMSE=103.88m, Top3-BeamWLS=103.13m, Top3-RejectBeamWLS=93.66m; TDOA asserts 0 fail; centroid_fallbacks=1; align max diff=2.10m vs V5B-Expert64; elapsed=60s | Smoke 通过。Top3-RejectBeamWLS 在 -10dB 初步显示优于 Top1-WLS（93.66m vs 103.88m），但 20 trials 不足以做 go/no-go 判定。对齐差 2.10m（≤5m 阈值，20-trial 统计噪声范围内）。 | 命令输出、CSV | 高 | 否 | 需全量运行（200 trials）后判定 go/no-go |
| 2026-07-09 | `NewMethod-Codex` | V5-B+ top-K 诊断包代码级 smoke | `py_compile`；inline dummy expert smoke | 无正式结果目录；仅内存 mock 数据 | `V5BExpertGatedTDOAEstimator.pair_diagnostic()`；mock Power64 fav_lag=5.0、GeoHybrid64 fav_lag=-3.0；true_tdoa=5.5；top_k=5 | Smoke: `pair_diagnostic` 返回 37 个完整字段；top1_lag=5.0 hit1=True；expert weights (0.65, 0.35)；oracle_expert_hit1=True | `pair_diagnostic()` 方法和 `run_v5b_topk_diagnostics()` 函数语法正确、逻辑合理、字段完整。`diagnose_v5b_topk.py` 可编译。该记录只证明代码链路可运行，不包含正式诊断结果。 | 命令输出与代码审查 | 高 | 否 | 需要用户运行 `diagnose_v5b_topk.py` 产生正式结果后，再分析 top-K hit rate 与后续路线 |
| 2026-07-08 | `NewMethod-Codex` | V5-A.1 hard-bin exact likelihood 代码级 smoke | `py_compile`；inline `train_v5a1_hardbin_tdoa()`；inline `run_urban_method_comparison()` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`compressed_tdoa_v5a1_hardbin_fast`；hard-bin 方法先用 `V5A1-Smoke64` uniform 64 bins；10 个 urban snapshot；96 个 LOS pair；1 epoch；batch=24；SNR smoke=`[0,20]`、4 trials | 编译通过。Smoke 输出 `[V5-A.1][V5A1-Smoke64][Epoch 001/1] ValLoss=13.3383 ValMAE=4.839; lowMAE=9.869 highMAE=0.326`；method comparison 输出 `V5A1_SMOKE_OK 4.8387 [4, 4]`，说明 estimator 可进入 all-pair WLS 并生成 valid localization counts。 | V5-A.1 的 hard-bin exact likelihood、posterior diagnostics、direct estimator 和 Fig7/Fig8/Fig9 接线可运行。该记录只证明代码进入待运行状态，不代表正式性能；正式结论需用户完整运行 `main.py` 后读取新时间戳目录。 | 命令输出、inline smoke 输出、编译输出 | 高 | 否 | 需要用户在 PyCharm 运行完整 `main.py`；回读 `run.log`、`plot_data.pkl`、`tables/*.csv` 和 Fig7/Fig8/Fig9 后补正式结果 |
| 2026-07-08 | `NewMethod-Codex` | `FreqDAE-v4-min` 代码级 smoke 与 CR16 快速诊断 | `py_compile`；inline `NestedTaskSufficientDAE` forward smoke；inline CR16 2 epoch 训练 smoke；inline task-head 快速诊断 | 无正式结果目录；临时小样本 synthetic/urban pair 数据 | 默认 profile=`freq_task_v4_min_fast`；active model=`NestedTaskSufficientDAE`；loss=`freq_task_v4_min_pair`；嵌套 CR masks=`(4,8,16)`；快速训练 smoke 使用 64 samples、2 epochs；task-head 诊断使用 32 pair eval | 编译通过；`git diff --check` 无空白错误，仅 CRLF 提示。Forward smoke 输出 waveform `(3,2,1024)`、latent `(3,512)`、task logits `(2,1024)`。CR16 快速训练 smoke 输出 `V4_CR16_QUICK_OK ... 2 ...`，loss finite。CR16 task-head 快速诊断输出 CR4/8/16 mean_abs 约 `5.415/6.450/6.839 samples`，within1 约 `0.156/0.062/0.031`，mean_logvar 约 `2.727/1.403/0.723`。 | V4-min 的模型、loss、profile、训练/验证接线和 task-head 诊断链路可运行；该记录只证明代码进入待运行状态，不代表正式性能。GeoHybrid 当前冻结为 baseline/消融，不再继续调参。 | 命令输出、inline smoke 输出、CR16 快速诊断输出 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py` 生成新结果目录后，再读取 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3/Fig9 和 tables 判断 V4 是否有效 |
| 2026-07-08 | `NewMethod-Codex` | GeoHybrid v2 代码级 smoke 与 sanity | `py_compile`；inline 已知时移 sanity；inline `build_traditional_baselines` + `run_urban_method_comparison` smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`geohybrid_v2_eval`；`GeoHybridDFTTDOAEstimator` 使用 coarse=`DFT-train-power` bins、fine=`DFT-GeoAmbi` bins；CR=16；physical lag gate=48 samples；smoke SNR=`[-10,10]`、4 trials；对照 `GeoAmbi-DFT-AML` | 编译通过；`git diff --check` 无空白错误，仅 CRLF 提示；已知时移 sanity：`np.roll(x, 7)` 返回 `7.0`。小样本 smoke 输出 `GEOHYBRID_SMOKE_OK 64 64 ['Raw', 'Clean', 'Geometry', 'GeoHybrid-DFT', 'GeoHybrid-DFT-PHAT-gated', 'GeoAmbi-DFT-AML']`。smoke 中 `GeoHybrid-DFT` 诊断字段包含 `coarse_coherence/fine_coherence/coarse_fine_deviation/consistency/fine_beta/pair_weight` 等；小样本下 10 dB GeoHybrid 优于 GeoAmbi v1，但 -10 dB 仍较差。 | GeoHybrid v2 的 estimator、direct TDOA、uncertainty diagnostics、all-pair WLS、图表/导出/重绘接线可运行。该 smoke 只证明代码链路与诊断字段正确，不代表正式性能；低 SNR 是否改善需完整 `main.py` 运行后判断。 | 命令输出、inline smoke 输出、编译与 `git diff --check` 输出 | 高 | 否 | 用户需在 PyCharm 运行 `main.py`，读取新结果目录的 `run.log`、Fig8/Fig8_supp、`tables/fig8*_baselines.csv`、`tables/fig8*_method_diagnostics.csv` 和 worst localization details 后记录正式结论 |
| 2026-07-07 | `NewMethod-Codex` | GeoAmbi-DFT Direct 阶段 A 代码级 smoke | `py_compile`；inline `build_traditional_baselines` + `run_urban_method_comparison`；选频平衡检查 | 无正式结果目录；临时小样本 urban8 数据 | 默认 profile 已切到 `geoambi_direct_eval`；正式 PyCharm 运行将使用 `paper_repro_eval_only`、不训练；smoke 使用 `SignalSimulator(scenario_mode=urban8, urban_base_delay=16, urban_min_los=5)`、CR=16、`n_pca_samples=64/256`、`include_strong_variants=True`、physical lag gate=48 samples、direct methods=`GeoAmbi-DFT-Direct/AML` | 编译通过。`git diff --check` 无空白错误，仅 Windows CRLF 提示。Smoke 输出 `GEOAMBI_SMOKE_OK 64 ['Raw', 'Clean', 'Geometry', 'GeoAmbi-DFT-Direct', 'GeoAmbi-DFT-AML']`；小样本 0 dB 下 Raw≈0.72 m、Clean≈0.14 m、Geometry≈0、GeoAmbi-DFT-Direct≈21.26 m、GeoAmbi-DFT-AML≈12.06 m。选频平衡检查：64 个复频点中负频 32、正频 32、DC 0，bins 范围约 168..851。 | 阶段 A 的 GeoAmbi 频点选择、direct estimator 构造、WLS 评估、图表/重绘/导出/go-no-go 接线可运行。小样本数值只用于流程检查，不能作为正式性能结论；当前主视图优先采用 `GeoAmbi-DFT-AML`。 | 命令输出、inline smoke 输出、选频检查输出 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录的 Fig7/Fig8/Fig8_supp、`plot_data.pkl`、`tables/fig8*_baselines.csv` 和 alias diagnostics 后记录正式结论；若要纳入创新 Fig9，再切回创新 profile 并使用新 cache |
| 2026-07-07 | `NewMethod-Codex` | go/no-go 诊断脚本代码级验证 | `py_compile`；`diagnose_go_no_go.py` smoke；`git diff --check` | 历史运行标识 `运行结果/go_no_go_smoke`（当前目录未保留） | 新增 `diagnose_go_no_go.py`；smoke 使用 SNR=`[-10,20]`、4 observation/SNR、4 sanity observation；加载 `20260707_143945` v3 权重与 `plot_data.pkl` | 编译通过；新增脚本无空白错误。第一次 smoke 暴露旧 `DAE` 构造函数不支持 `signal_len`，已修复为 `DAE(cr=...)`；第二次 smoke 暴露非数值诊断字段汇总问题，已修复为跳过字符串字段；最终 smoke 成功导出 baseline、diagnostic、sanity、decision 表、SVG 和 pkl。 | 诊断脚本链路可运行。Smoke 只验证流程，不用于性能结论。 | 命令输出与 smoke 目录 | 高 | 否 | 无 |
| 2026-07-07 | `NewMethod-Codex` | FreqDAE v3 nested supernet 代码级验证 | `py_compile`；inline `NestedFrequencyPairwiseDAE` forward/task-head smoke；inline urban8 `train_with_cv` 1 epoch smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`freq_task_v3_nested_fast`；模型=`NestedFrequencyPairwiseDAE`；loss=`freq_task_nested_pair`；smoke 使用 CR4 supernet、nested CR masks `(4,8,16)`、24 LOS-pair samples、1 epoch、batch=8；loss 权重 smoke：corr=0.05、xphase=0.02、peak=0.01、task_head=0.01 | 编译通过。Forward smoke：CR4/8/16 active mask 下 waveform 输出均为 `(2,2,1024)`，task head logits 为 `(2,1024)`。训练 smoke 成功输出 `V3_SMOKE_OK torch.Size([2, 2, 1024]) torch.Size([2, 1024]) freq_task_nested_pair 1`。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | v3 最小结构、nested latent mask、pairwise task head、urban pair 数据链路和 final-only 训练链路均可运行。该记录只证明代码链路，不代表 v3 性能有效。 | 命令输出、编译与 smoke 输出 | 高 | 否 | 需要用户在 PyCharm 完整运行默认 `main.py`，读取新结果目录后才能判断 v3 是否改善时频域重构、GCC 峰宽和 CR 区分度 |
| 2026-07-07 | `NewMethod-Codex` | 静态 baseline cache 与 reconstruction-only 诊断代码验证 | `py_compile`；inline `experiment_cache` smoke；`diagnose_reconstruction_only.py`；`git diff --check` | 缓存源：`运行结果/FreqDAE/20260706_230643_FreqDAE_v2成对频域`；诊断输出：历史运行标识 `运行结果/diag_recon_only_20260707_134404`（当前目录未保留） | `main.py` 新增创新模式静态 baseline cache，默认复用 `20260706_230643` 的 Fig6-Fig8 和 `baseline_all_results`；diagnostic 使用 `FrequencyPairwiseDAE-ReconOnly`，CR4/16，1024 LOS-pair samples，12 epochs，batch=128，`corr_weight=0`、`pair_phase_weight=0`、`lambda_peak=0`、`spectral_blend=0.20` | 编译通过。缓存 smoke：`20260706_230643` 含 `fig6_results/fig7_results/fig8_results/fig8_supp_results/baseline_all_results/traditional_baseline_meta`，`baseline_all_results` 方法数=25，merge smoke 成功。Reconstruction-only 诊断：CR4 train/val PairFreqNMSE≈0.93703/0.96484，CR16≈0.99289/0.97043，总耗时≈0.8 min。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | 缓存机制可用，后续创新模式可避免重复重算不变的 Fig6-Fig8。Reconstruction-only 小诊断显示，去掉 pairwise task loss 后重构仍未快速下降到有效区间，说明 v2/v2.1 问题不只是 pairwise loss 冲突，单 waveform 频域重构结构/目标本身也偏弱。 | 命令输出、缓存读取 smoke、`diagnostic_summary.csv`、编译与 `git diff --check` 输出 | 高 | 否 | 该诊断是小样本/短训练，只用于机制判断；若用于论文，需要补正式 reconstruction-only 消融、多 seed 或更长 epoch。下一轮完整创新运行应检查日志 `[BaselineCache]` 是否成功加载 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE v2.1 loss 修复代码级验证 | `py_compile`；inline sanity；inline urban8 `train_with_cv` 1 epoch smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 默认仍为 `freq_task_v2_200_final`；新增 `soft_peak_sharpness_weight=0.25`；smoke 使用 `FrequencyPairwiseDAE` CR16、`freq_task_pair`、12 个 LOS pair、2-fold、1 epoch；修正后的 `frequency_task_reconstruction_loss` | `py_compile` 全套入口通过。sanity 确认零输出的 time/complex NMSE≈1.0，cross-spectrum phase loss 与 sharpness peak loss finite。小样本训练 smoke 输出 `V2_1_SMOKE_OK freq_task_pair 0.25 2`。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | v2.1 代码链路可运行。该修复只说明损失定义和训练入口正确，尚不代表正式性能改善。由于 NMSE 定义被修正，后续 Fig1 PairFreqNMSE 数值相对 `20260706_190513` 上升是预期现象。 | 编译输出、inline sanity/smoke 输出、`git diff --check` | 高 | 否 | 需要用户完整运行 `main.py` 后读取新结果目录，确认 high-SNR TDOA floor、peak width、complex NMSE 和 CR 区分度是否改善 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE v2 pair-aware 创新链路代码级验证 | `py_compile`；inline urban8 `train_with_cv` 1 epoch smoke；inline `run_urban_method_comparison`；`export_method_outlier_details` smoke | 无正式结果目录；临时小样本 urban8 数据；临时导出目录 | 默认代码已切到 `freq_task_v2_200_final`；smoke 使用 `FrequencyPairwiseDAE` CR16、`loss_mode=freq_task_pair`、12 个 LOS pair、1 epoch、4 个 urban localization trials；定位器 `all_pair_wls`；lag gate=48 samples | `py_compile` 全套入口通过。训练 smoke 输出 `TRAIN_SMOKE freq_task_pair FrequencyPairwiseDAE 1`。小样本 method comparison 可运行并输出 `FreqDAE-v2-CR16`；导出 smoke 显示 worst localization CSV/MD 成功生成，字段包含 `pair_uav_indices`、`pair_true_tdoa_samples`、`pair_est_tdoa_samples`、`pair_tdoa_error_samples`、`pair_weight`、`pair_sidelobe_ratio`。`git diff --check` 无空白错误，仅 CRLF 提示。 | FreqDAE v2 的数据生成、训练损失、模型前向、urban localization 评估、worst TDOA-定位对应诊断和导出链路均可运行。该记录只证明代码链路，不包含正式性能结论。 | 编译输出、inline smoke 输出、导出 smoke 输出、`git diff --check` | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录后才能记录正式 Fig1-Fig9 和 RMSE 指标 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE eval-only + Fig10 localizer ablation 代码级验证 | `py_compile`；inline urban8 `run_urban_method_comparison` smoke；inline FreqDAE 权重加载；inline Fig10 plot/export smoke | 模型源 `运行结果/FreqDAE/20260706_103701_FreqDAE_v1频率选择`；小样本 smoke 无正式结果目录 | 默认模式改为 `freq_task_eval_only`；FreqDAE 权重源 `20260706_103701`；Chen DAE 参考源 `20260702_000925`；新增定位器 `all_pair_wls_grid/pruned/grid_pruned`；Fig10 high-SNR localizer ablation 默认 SNR `[10,12,14,16,18,20]` | `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...` 通过；`git diff --check` 无空白错误，仅 CRLF 提示；urban8 smoke：`all_pair_wls` 下 Raw≈0.29/0.23 m、DFT-smoke≈1.96/1.27 m，`all_pair_wls_grid_pruned` 下 Raw≈0.28/0.27 m、DFT-smoke≈2.11/0.90 m；增强字段如 `geometry_gdop`、`candidate_count` 有值；FreqDAE CR4/8/16 权重均可加载并 forward 输出 `(2,2,1024)`；Fig10 临时 SVG 与 `fig10_localizer_baselines.csv/md` 导出成功 | 代码已进入 PyCharm 直接运行 `main.py` 的 eval-only 待运行状态；本轮不产生正式性能曲线，只证明新 eval-only 源路径、鲁棒定位器、增强 worst diagnostics、Fig10 绘图/导出链路可运行 | 编译输出、函数级 smoke 输出、权重加载输出、临时导出输出 | 高 | 否 | 需要用户运行完整 `main.py` 生成正式 Fig10 与增强 tables，再读取 `run.log`、`plot_data.pkl`、`Fig10_Localizer_Robustness_Ablation.svg` 和 `tables/fig10_localizer_*` |
| 2026-07-06 | `NewMethod-Codex` | A+B 图表/离群诊断增强代码验证与 `20260705_202516` 离线重绘 | `py_compile`；inline `run_urban_method_comparison` smoke；inline top-k worst 导出 smoke；`export_results.py --no-fig6-svg`；`replot.py` | 历史运行标识 `运行结果/20260705_202516`（当前目录未保留）；小样本 smoke 无正式结果目录 | Fig2 图例保持 `DAE`；Cao2017 改青色；新增 Fig9 focused 视图；method-comparison 新增 p90/p95/max/valid/failure/outlier 统计和未来 top-k worst localization details | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK [0] [6.130430977112897] 4`；导出 smoke 输出 `EXPORT_WORST_OK 1395`；旧结果目录新增/覆盖 `Fig9_Focused_DAE_CR_Comparison.svg`、`Fig9_Focused_DAE_CR_Comparison_SNR_Zooms.svg`、`tables/fig9_focus_baselines.csv/md`、`tables/fig9_focus_method_diagnostics.csv/md`；`fig9_focus_baselines.csv` 表头去重正常 | 本轮没有产生新的训练或评估性能；只证明图表 focused 视图、增强导出字段、未来 top-k 离群诊断链路可运行。旧 `plot_data.pkl` 缺少逐样本定位明细，因此不能补生成 `*_worst_localization_errors.csv` | 命令输出、旧结果目录导出文件、代码编译与 smoke | 高 | 否 | 若要获得正式离群样本表，需要重新运行 `main.py` 或 eval-only 生成带 `outlier_details` 的新 `plot_data.pkl` |
| 2026-07-05 | `NewMethod-Codex` | FreqDAE 200 epoch 公平复核入口与 Fig9 全 CR 对照代码级验证 | `py_compile`；inline `FrequencySelectiveDAE` forward；inline Fig9/zoom plotting smoke | 无正式结果目录；仅内存随机张量与虚拟 method_data | 新默认 `USER_EXPERIMENT_MODE="freq_task_200_final"`；Fig9 方法集合含 Raw、冻结 Chen-DAE CR4/8/16、FreqDAE CR4/8/16、DFT/DFT-Fisher/Cao/Zhai/Hadamard/PCA；绘图样式按方法类别分色 | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK torch.Size([2, 2, 1024]) 13`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | 代码进入待 PyCharm 运行状态；可以生成包含新旧 DAE 全 CR 对照的 Fig9，并使用 200 epoch 训练 FreqDAE。该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录确认 200 epoch FreqDAE 性能、Fig9 可读性和同 CR 对比结论 |
| 2026-07-05 | `NewMethod-Codex` | 频域任务感知创新模式代码级验证 | `py_compile`；inline `FrequencySelectiveDAE` 训练/评估 smoke；Fig9 绘图/导出 smoke | 无正式结果目录；小样本临时 urban8 数据与临时 SVG/CSV 目录 | 新模式 `freq_task_fast_final`；新模型 `FrequencySelectiveDAE`；loss=`freq_task_mse`；smoke 用 CR=16、16 个 waveform、1 epoch、2 个 urban trials；Fig9 临时数据只验证绘图/导出 | 两套 `py_compile` 通过；训练/评估 smoke 输出 `FREQ_SMOKE_OK FrequencySelectiveDAE freq_task_mse ['Raw', 'Clean', 'Geometry', 'DAE-CR16', 'FreqDAE-CR16']`；Fig9 导出 smoke 输出 `FIG9_EXPORT_SMOKE_OK`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | 新创新链路可编译、可训练、可进入同一 urban8/WLS 定位评估，并可生成 Fig9 表格/图；该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录确认 Fig9/Fig3/Fig2 正式趋势 |
| 2026-07-05 | `NewMethod-Codex` | Fig3 physical lag gate 统一代码级验证 | `py_compile`；inline `UrbanLocalizationExperiment` smoke | 无正式结果目录；小样本临时 urban8 数据 | `tdoa_lag_limit_samples=24`；2 个 SNR 点；4 trials；无 DAE 模型，仅验证 Geometry/Raw/Clean 路径和配置保存 | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK 24.0 2 [...]`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | Fig3 的 `UrbanLocalizationExperiment` 已能接收并保存 lag gate，后续正式运行会与 Fig6-Fig8 使用同一 physical TDOA 搜索窗口；该记录不是正式性能指标 | 命令输出与代码审查 | 高 | 否 | 需要完整运行 `main.py`，读取新目录确认 `mc_results` 的 `localization_config.tdoa_lag_limit_samples≈48`，并比较 Fig3 CR16 与 Fig8 DAE-CR16 是否对齐 |
| 2026-07-05 | `NewMethod-Codex` | Fig8 主/补充轨道增强代码级验证 | `py_compile`；inline estimator sanity；小样本 urban8 Fig8 smoke；`analyze_results.py` | 旧结果分析输入：`运行结果/论文复现与传统基线/20260704_195946_强任务基线评估`；小样本 smoke 无正式结果目录 | 新增 `Cao2020SegmentedFCEstimator`；`ZhaiPhaseSuperpositionEstimator(weight_mode="sqrt_power")`；Fig8 主图正式强基线，Fig8 supplement 保存 Cao2020/Zhai phase 诊断候选；`analyze_results.py` 基于 pandas 分 band 汇总 | 两套 `py_compile` 通过；四个 direct estimator 对 `np.roll(x, 7)` 均返回 `-7.0`；小样本 urban8 smoke 输出 `SMOKE_OK`；`analyze_results.py` 成功读取 `20260704_195946/plot_data.pkl` 并生成 `tables/analysis_*`；`update_dft_fisher_direct.py` 当前根目录不存在 | 新代码进入待 PyCharm 正式运行状态；本轮只验证代码链路和分析脚本，不产生新的正式 Fig8 性能结论 | 命令输出、旧结果目录 pkl 读取、生成的 analysis tables | 高 | 否 | 需要用户运行完整 `main.py`，读取新目录的 `run.log`、`plot_data.pkl`、Fig8/Fig8 supplement SVG 与 tables 后，才能记录正式指标 |
| 2026-07-04 | `NewMethod-Codex` | Cao2020-HighFC 重做代码级验证与小样本 smoke | `py_compile`；PyTorch inline clean urban8 direct TDOA smoke；代码路径检查 | 无正式结果目录；仅小样本临时数据 | 新 `DFT-Cao2020-CRB`：单侧连续高频 FC 窗；Fisher/CRB 延迟信息 + 保留功率 + 高频偏好 + 物理 lag 窗旁瓣惩罚；baseline CR=16 | 两套 `py_compile` 通过；`git diff --check` 无空白错误；小样本 clean urban8 中旧 two-edge high-frequency 约 mean abs 4.08 samples、median 3.58、within1 0.23，新 `DFT-Cao2020-CRB` 约 mean abs 1.07 samples、median 0.14、within1 0.80；Zhai-CRLB 同一 smoke 约 mean abs 0.21、within1 0.98 | 新 Cao2020 代理相对旧最高频硬选明显改善，但仍弱于 Zhai-CRLB，符合“Cao high-FC 是更稳的高频/CRB baseline，而非理论最优选频”的定位；该记录不是正式 Fig8 结果 | 命令输出与小样本 smoke 输出 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录的 `tables/fig8_baselines.csv` 和 `tables/fig8_method_diagnostics.csv` 后记录正式指标 |
