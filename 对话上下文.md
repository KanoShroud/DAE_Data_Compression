# 对话上下文

## 当前状态（2026-07-09 19:00 +08:00）

| 字段 | 内容 |
|---|---|
| 项目 | `DAE Data Compression`：多 UAV 协作定位中的压缩数据/TDOA 任务保持 |
| 当前分支 | `NewMethod-Codex` |
| 当前默认入口 | PyCharm 直接运行 `main.py` |
| 当前默认 profile | `compressed_tdoa_v5b_fast` |
| 当前默认模式 | `compressed_tdoa_v5b_fast`，加载冻结 Chen DAE 近似复现基线，训练 V5-A.1 hard-bin experts，并新增 V5-B 非 oracle expert-gated posterior mixture |
| 当前主目标 | V5-B+ top-K 诊断已重新生成 pair-level CSV；`diagnose_v5b_topk_wls.py` 的 geometry 重建错配已修复并通过 smoke，下一步全量重跑 Stage 2A。 |

## 当前研究判断

- `Raw` 是未压缩全数据参考，不再作为创新方法必须超越的硬目标；创新目标是优于当前压缩基线、Chen-style DAE-CR16 和更强 task-aware 压缩方法，并给出清晰的同预算解释。
- `paper_repro_v3.1` 近似复现基线已冻结，关键参考目录为 `运行结果/20260702_000925`。后续创新对比应复用同一 urban8 数据生成、fixed eval set、all-pair WLS、physical lag gate 等评估口径。
- GeoAmbi/GeoHybrid 已冻结为 baseline/消融：公平 CR16 预算下没有形成足够强的主创新性能，不再继续调参。
- FreqDAE v1-v4 的 waveform decoder 路线未通过 go/no-go：多轮结果显示容易产生假窄带/宽峰模板，CR 区分度不足。V4-min 结果 `运行结果/20260708_163957` 表明 task head 本身也没有强到足以支撑该路线。
- 新主线 V5-A 不再要求重构 waveform，而是学习 CR16 预算下的压缩域 TDOA likelihood，并通过同一 all-pair WLS 定位链路评估。
- V5-A.0 正式结果 `运行结果/20260708_205930` 未通过 go/no-go：训练 CE/ValLoss 显著下降，但 ValMAE 基本停在约 `6.1 samples`，低 SNR mean/median/trimmed RMSE 均明显弱于 DAE-CR16 和主要压缩基线。高 SNR 典型样本可工作，但低 SNR 出现成片 confidently-wrong TDOA 峰。
- V5-A.1 正式结果 `运行结果/20260708_235611` 表明：hard-bin exact likelihood 修复了 V5-A.0 的主要物理失配，`V5A1-GeoHybrid64/Power64` 全 SNR mean RMSE 约 `24.98/26.48 m`，显著优于 V5-A.0 的 `40.69 m`，也优于 DFT、Cao2017、GeoHybrid-B64、Zhai、Hadamard/PCA 等多数压缩基线；但仍弱于冻结 Chen DAE-CR16 的 `21.38 m`，低 SNR 尤其弱于 DAE-CR16。
- 2026-07-09 已新增 TDOA-first 评估/导出轨道：`evaluate.py` 从既有 `method_diagnostics` 构造 `tdoa_results`，`main.py` 保存 TDOA MAE/within-1 SVG，`export_results.py` 输出 `tdoa_method_tdoa_metrics.csv`，`replot.py` 可对旧 pkl 补图。它用于回答“方法的 TDOA 是否准”，避免定位 WLS/几何条件把估计器优劣混淆。
- 对 `运行结果/20260708_235611` 的 TDOA-first 完整分析结论：TDOA MAE 与 localization mean 的相关性很高，全/低/中/高 SNR Pearson 约 `0.925/0.952/0.956/0.784`。低 SNR(≤0 dB) 下 V5A1-Power64/GeoHybrid64 的 TDOA MAE 约 `6.89/7.77 samples`，仍弱于 DAE-CR16 的约 `4.28 samples`，因此低 SNR 主要是 TDOA 错峰问题；高 SNR(≥8 dB) 下 V5A1-GeoHybrid64 约 `0.147 samples`，优于 DAE-CR16 约 `0.171 samples`，此时 localization mean 的残差更可能来自 WLS/几何离群放大。DAE-CR16 首次达到 MAE≤1 sample 的 SNR 为 `-4 dB`，V5A1-Power64/GeoHybrid64 为 `-2 dB`。
- 2026-07-09 已实现 V5-B：不新增 waveform decoder，不做大型神经网络去噪训练；先训练/恢复 V5-A.1 hard-bin experts，再用非 oracle posterior quality gate 融合 Power64 和 GeoHybrid64。V5-B 的研究定位是轻量、可解释、压缩域 TDOA estimator，而不是传统 Chen DAE 的替代式重构网络。
- 评估口径更新：V5-B 与 DAE 的低 SNR 对比应放在复杂度、训练成本、泛化性、TDOA 精度、高 SNR 地板和定位稳定性共同框架下判断。由于 DAE 经过重型去噪训练，V5-B 低 SNR 未必必须直接超过 DAE；更关键的是是否优于未训练/弱训练压缩基线，并形成清楚的 Pareto 优势。
- V5-B 正式结果（`运行结果/20260709_120019`）核心结论：
  - expert gating 机制有效：V5B all SNR mean=23.92 m，优于 Power64(26.50) 和 GeoHybrid64(25.12)；
  - 高 SNR（≥4 dB）V5B 持续超越 DAE-CR16：4 dB 时 V5B 7.84 m vs DAE 9.51 m；
  - 低 SNR TDOA MAE 仍有 ~60% 差距（V5B 6.88 vs DAE 4.28 samples），但显著优于 DFT/Cao/Zhai/GeoHybrid 等压缩基线；
  - ValLoss↓ ≠ ValMAE↓ 训练瓶颈仍在（20 epoch ValLoss 下降但 ValMAE 基本不变）；
- 已提出 A/B/C 三条路线供用户决策。
- 阶段 1（V5-B+ top-K 诊断包）已实现：`compressed_tdoa.py` 新增 `pair_diagnostic()` 和 `run_v5b_topk_diagnostics()`；新增独立脚本 `diagnose_v5b_topk.py`。代码已通过编译和冒烟测试，进入待运行状态。运行 `diagnose_v5b_topk.py` 即可对 `运行结果/20260709_120019` 做 eval-only pair-level 诊断，输出 4 个 CSV 和终端汇总。
- low-SNR top-K 命中率（2026-07-09 重新运行 `diagnose_v5b_topk.py`）：top1=59.3%，top3=71.9%（+12.6pp），top5=77.4%（+18.1pp），top5±2=81.1%，oracle expert=65.2%。top-K 有信息可挖掘，但 oracle expert 相对 V5B top1 的收益仅约 5.9pp，继续调 expert gate 的空间有限。
- 阶段 2A（top-K WLS go/no-go）脚本 `diagnose_v5b_topk_wls.py` 已修复：脚本优先按 top-K CSV 中的 `snapshot_id` 重建 geometry；旧 CSV 无 `snapshot_id` 时退回到与 `diagnose_v5b_topk.py` 一致的 `snapshot_indices=None, seed=seed`。新增 WLS 前 TDOA preflight，避免再次全量结束后才发现错配。`compressed_tdoa.py` 后续生成的 top-K CSV 会写入 `snapshot_id`。smoke 结果：preflight checked=240，TDOA assertions=0，Top1-WLS 与原 V5B-Expert64 对齐 max diff=0.1208m。修复前生成的非 smoke `v5b_topk_wls_*.csv` 仍视为无效，需全量重跑覆盖。

## 当前代码状态

| 文件 | 当前作用 |
|---|---|
| `main.py` | PyCharm 一键入口；默认 profile 已切到 `compressed_tdoa_v5b_fast`；V5-B 走 direct-only 输出，训练 V5-A.1 experts 后加入 `V5B-Expert64` |
| `compressed_tdoa.py` | 新增 V5-A、V5-A.1 与 V5-B；V5-B 使用 Power64/GeoHybrid64 posterior quality 非 oracle 门控融合，并保留 top-K posterior diagnostics |
| `experiment_profiles.py` | 新增 `compressed_tdoa_v5a_fast`、`compressed_tdoa_v5a1_hardbin_fast`、`compressed_tdoa_v5b_fast` profile |
| `evaluate.py` | 新增 `V5A-CR16`、`V5A1-Uniform64/Power64/Cao64/GeoHybrid64`、`V5B-Expert64` 绘图样式；V5 系列通过 direct estimator 接口进入 Fig7/Fig8/Fig9 |
| `signal_gen.py` | 继续提供 urban8 fixed snapshot/pair 数据；V5-A 训练使用 LOS pair 计划和 snapshot-level val split |
| `task_baselines.py` | 继续提供 DFT/Cao/Zhai/GeoHybrid direct baseline 接口，V5-A 复用同一评估 API |
| `运行结果/20260702_000925` | 冻结 Chen-style DAE 近似复现基线，V5-A 默认从此目录加载 `model_cr*.pt` 和 `plot_data.pkl` |
| `export_results.py` / `replot.py` | 支持从既有 `plot_data.pkl` 导出/重绘 TDOA-only 指标；无需重新训练即可补 `tdoa_method_tdoa_metrics.csv` 与 TDOA SVG |

## V5-A 已实现内容

- 新增 `LearnableCompressedTDOALikelihood`：
  - 每个 UAV waveform 经 FFT 后由可学习频域 filterbank 投影为 64 个复数特征，严格对应 CR16 的 128 real scalars；
  - pairwise score 使用物理 steering 形式：`Re sum_k alpha_k z_i,k conj(z_j,k) exp(j 2*pi*f_k*tau)`；
  - 输出物理 lag window 内的 soft TDOA posterior；
  - 带 filter entropy/overlap 正则，避免 filterbank 退化成无解释的 dense 黑箱。
- 新增训练器 `train_v5a_compressed_tdoa()`：
  - 使用 urban8 LOS UAV-pair 数据；
  - train/val 按 snapshot group 切分，避免同一 snapshot 泄漏；
  - loss 由 soft TDOA CE、ambiguity mass、peak width、posterior uncertainty 和 filterbank regularization 组成；
  - 保存 `model_v5a_cr16.pt` 与 `v5a_training` 历史到 `plot_data.pkl`。
- 新增 direct estimator `LearnedCompressedTDOAEstimator`：
  - 与现有 DFT/Cao/Zhai/GeoHybrid 一样实现 `estimate_pair()`；
  - 进入 `run_urban_method_comparison()` 后共享 all-pair WLS、physical lag gate、fixed eval set。

## 最新正式结果：V5-A.1（`运行结果/20260708_235611`）

- 运行配置：profile=`compressed_tdoa_v5a1_hardbin_fast`，direct-only 输出，冻结 Chen DAE 源目录 `运行结果/20260702_000925`；V5-A.1 训练 `V5A1-Uniform64/Power64/Cao64/GeoHybrid64`，每条 6000 LOS pairs、20 epoch、CR16 预算 64 complex bins。
- 训练层结论：`Power64/GeoHybrid64` 的 ValLoss 下降，但 ValMAE 没有同步下降，甚至略升。`Power64` ValMAE `2.483 -> 2.557 samples`，`GeoHybrid64` `2.543 -> 2.566 samples`；说明继续加 epoch 不是主解，保存准则也不应只看 ValLoss。
- 定位层结论（Fig9 focused，全 SNR mean RMSE）：Raw≈`15.72 m`，DAE-CR16≈`21.38 m`，V5A1-GeoHybrid64≈`24.98 m`，V5A1-Power64≈`26.48 m`，Cao2017≈`29.39 m`，GeoHybrid-B64-C40F24≈`31.84 m`，DFT≈`32.17 m`，PCA≈`34.84 m`，Zhai≈`37.21 m`，Hadamard≈`38.83 m`，DFT-Fisher≈`40.67 m`，V5A1-Cao64≈`40.70 m`。
- 低 SNR(≤0 dB)：DAE-CR16≈`41.00 m`，V5A1-GeoHybrid64≈`53.00 m`，V5A1-Power64≈`53.12 m`，Cao2017≈`61.25 m`，DFT≈`65.27 m`，GeoHybrid-B64≈`67.00 m`。V5-A.1 低 SNR 已显著优于 V5-A.0 (`90.39 m`) 和多数压缩基线，但尚未超过 DAE-CR16。
- 高 SNR(≥8 dB)：V5A1-GeoHybrid64≈`7.85 m`，接近 Zhai≈`7.65 m`、Hadamard≈`7.48 m`、DFT-Fisher≈`7.93 m`，并优于 DAE-CR16≈`9.64 m`。高 SNR direct TDOA MAE：GeoHybrid64≈`0.147 samples`，Power64≈`0.257`，Uniform64≈`0.232`，Zhai≈`0.067`。
- 机制结论：频点集合主导性能。`Power64` 低 SNR 最稳，`GeoHybrid64` 中高 SNR 最均衡，`Cao64` one-sided high-frequency 窗在当前合成 urban8 下不稳。posterior calibration 有一定 SNR 区分能力，但低 SNR 仍不能充分抑制错峰 pair。
- 代码/协议注意：V5-A.1 bin sets 与后续 Fig baseline bin sets 都来自相同 selector 类，但当前是分两次 `build_traditional_baselines()` 生成，seed 不完全一致；后续若做论文级公平消融，应缓存并复用同一 selected-bin meta，避免“频点差异”和“likelihood/校准层差异”混在一起。

## V5-A.1 已实现内容

- 新增 `HardBinCompressedTDOALikelihood`：
  - 每个 waveform 只抽取固定 selected FFT bins，严格维持 64 个复数压缩特征/128 real scalars 的 CR16 预算；
  - pairwise score 使用精确形式 `Re sum_k alpha_k X_i[f_k] conj(X_j[f_k]) exp(j2π f_k tau)`，不再使用 V5-A.0 的 soft filterbank 和 effective-frequency 近似；
  - 支持 `uniform`、`power`、`cao`、`geohybrid` 四类 hard-bin 频点集合，分别输出 `V5A1-Uniform64/Power64/Cao64/GeoHybrid64`。
- 新增 `train_v5a1_hardbin_tdoa()` 与 `prepare_v5a1_hardbin_dataset()`：
  - 所有 V5-A.1 频点集合共享同一 explicit-SNR LOS pair 数据集；
  - train/val 继续按 snapshot group 切分；
  - loss 包含 soft-label CE、point smooth L1、ambiguity、width 和 posterior uncertainty 项；
  - 训练日志额外记录 low/mid/high SNR bucket 的 MAE、within-1、ambiguity、width 和 entropy。
- 新增 `LearnedHardBinCompressedTDOAEstimator`：
  - 输出 argmax TDOA，同时根据 posterior entropy、top1/top2 margin 和 variance 给出 calibrated pair weight；
  - 进入与 DAE、DFT、Cao、Zhai、GeoHybrid 完全相同的 all-pair WLS 评估链路；
  - `plot_data.pkl` 会保存 `v5a1_training`、`v5a1_bin_meta` 和 `v5a1_weight_mode`。

## 最新正式结果：V5-A.0（`运行结果/20260708_205930`）

- 运行配置：profile=`compressed_tdoa_v5a_fast`，V5-A CR16 direct likelihood，64 个复数压缩特征，40 epoch，10000 LOS pair，lag gate=48 samples；冻结 Chen DAE 源目录为 `运行结果/20260702_000925`。
- 训练目标表现：`TrainLoss 11.2865 -> 3.1138`，`ValLoss 10.6283 -> 3.0750`，`ValCE 10.5161 -> 3.0304`。
- 点估计未同步改善：`ValMAE 6.0813 -> 6.2070 samples`，`Val<=1` 约 `0.75` 基本不变；`ValAmb 0.2387 -> 0.3633`、`ValWidth 0.0773 -> 0.1170` 反而变差。
- Fig9 focused mean RMSE 全 SNR：Raw≈`15.72 m`，DAE-CR16≈`21.38 m`，Cao2017≈`29.39 m`，GeoHybrid-B64-C40F24≈`31.84 m`，DFT≈`32.17 m`，Zhai≈`37.21 m`，DFT-Fisher≈`40.67 m`，V5A-CR16≈`40.69 m`。
- 低 SNR(≤0 dB) mean RMSE：Raw≈`30.04 m`，DAE-CR16≈`41.00 m`，Cao2017≈`61.25 m`，DFT≈`65.27 m`，GeoHybrid≈`67.00 m`，Zhai≈`84.16 m`，V5A-CR16≈`90.39 m`，DFT-Fisher≈`91.81 m`。
- 高 SNR(≥8 dB) mean RMSE：Raw≈`6.70 m`，Zhai≈`7.65 m`，DFT-Fisher≈`7.93 m`，Cao2017≈`9.36 m`，V5A-CR16≈`9.53 m`，DAE-CR16≈`9.64 m`。V5A 高 SNR median/trimmed 分别约 `0.719/1.115 m`，典型样本可用，但 mean 受几何/WLS 离群放大。
- 诊断结论：V5-A.0 不是代码链路失败，而是目标函数与鲁棒点估计错位。低 SNR direct TDOA mean/median 误差约 `16.90/13.37 samples`，远弱于 Raw GCC 的约 `2.47/0.27 samples`；当前 likelihood 在低 SNR 会自信地选错峰。

## 下一步建议

1. 用户在 PyCharm 全量运行 `diagnose_v5b_topk_wls.py`，无需参数，不要加 `--strict-align`。
2. 回读 `v5b_topk_wls_baselines.csv`、`v5b_topk_wls_summary_by_snr_bucket.csv`、`v5b_topk_wls_alignment.csv` 与终端最后日志。
3. 若 Top3-BeamWLS 通过 go/no-go，补图表：low/mid/high RMSE、outlier>20/50、Top1 vs Top3-BeamWLS per-SNR 曲线、复杂度说明。

## 需要用户确认

- 请确认是否将 Top3-BeamWLS 作为 V5-B+ 的主增强结果保留。
- 请确认下一轮是否进入“论文收尾/消融与复杂度表”路线，还是继续做更深入的 low-SNR posterior 修复。
