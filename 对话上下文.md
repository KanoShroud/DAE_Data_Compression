# 对话上下文

## 2026-07-08 最新上下文补充：过时运行结果目录已清理

- 用户明确要求：`历史记录_6.13-6.30` 是已归档目录，不要动；`diagnose_dae.py`、`diagnose_reconstruction_only.py` 已删除；清理“明确过度过时目录”，保留所有当前代码引用目录。
- 已执行清理：删除 `运行结果/` 下 17 个过时目录，包括早期 paper_repro、Fig6/Fig7/DFT-Fisher、FreqDAE v1/v2 中间结果和 reconstruction-only 诊断目录。
- 已保留：`20260702_000925`、`20260704_195946`、`20260706_103701`、`20260706_230643`、`20260707_143945`、`20260707_193226`、`20260708_111614`、`go_no_go_20260707_183216`、`历史记录_6.13-6.30`。
- 当前 Python 代码直接引用目录均未删除：`main.py` 引用 `20260702_000925`、`20260706_103701`、`20260706_230643`；`diagnose_go_no_go.py` 默认引用 `20260707_143945`；`analyze_results.py` 默认引用 `20260704_195946`。
- 注意：部分历史 Markdown 仍会引用已删除目录作为讨论记录，这是历史记录语义，不代表当前本地仍存在对应实验文件。

## 2026-07-08 最新上下文补充：GeoHybrid v2 假设检验版已实现，待 PyCharm 完整运行

### 本轮执行内容
- 用户批准执行 `GeoHybrid v2`。本轮按“假设检验方案”实现，不把它直接包装成最终创新方法。
- `task_baselines.py` 新增 `GeoHybridDFTTDOAEstimator`：
  1. coarse stage 使用高功率/高相干 DFT bins，在完整 physical lag window 搜索；
  2. fine stage 使用 `DFT-GeoAmbi` bins，只在 coarse 峰附近局部 refinement；
  3. fusion 使用 reliability-controlled score blending，不是简单 if-else fallback；
  4. pair weight 将 coherence、peak margin、coarse-fine consistency、sidelobe ambiguity 传播到 all-pair WLS。
- `main.py` 默认 profile 改为 `geohybrid_v2_eval`。PyCharm 直接运行 `main.py` 将进入 `paper_repro_eval_only`，不训练网络，加载冻结 Chen-style DAE，并重跑含 `GeoHybrid-DFT` 的 Fig6-Fig8/Fig8_supp。
- `GeoAmbi-DFT-*` v1 保留为 fine-only/PHAT 消融；Fig8 主图优先展示 `GeoHybrid-DFT`，Fig8 supplement 同时保留 `GeoHybrid-DFT-PHAT-gated` 和 GeoAmbi v1。
- `experiment_profiles.py` 新增 `geohybrid_v2_eval`，保留 `geoambi_direct_eval` 以复查 v1。
- `evaluate.py`、`replot.py`、`export_results.py`、`diagnose_go_no_go.py` 已同步图例、方法顺序、摘要说明和诊断 estimator 构造。

### 验证状态
- 全套 `py_compile` 通过。
- `git diff --check` 通过，仅 Windows CRLF 提示。
- 已知时移 sanity：`GeoHybridDFTTDOAEstimator` 对 `np.roll(x, 7)` 返回 `7.0`。
- 小样本 urban8 smoke 成功：输出 `GEOHYBRID_SMOKE_OK 64 64 ['Raw', 'Clean', 'Geometry', 'GeoHybrid-DFT', 'GeoHybrid-DFT-PHAT-gated', 'GeoAmbi-DFT-AML']`。
- smoke 已确认 `GeoHybrid-DFT` 诊断字段可导出，包括 coarse/fine coherence、coarse/fine margin、coarse-fine deviation、consistency、fine beta 和 pair weight。

### 当前待验证问题
- 本轮 smoke 不是正式性能结果。小样本中 high SNR 有改善迹象，但低 SNR 仍较差，必须完整运行后才能判断 v2 是否真正降低 GeoAmbi v1 的低 SNR outlier。
- 正式运行后重点检查：
  1. `GeoHybrid-DFT` 低 SNR mean/outlier 是否显著低于 `GeoAmbi-DFT-AML`；
  2. high SNR TDOA MAE 是否保持 `~0.05 samples` 量级；
  3. high SNR mean localization 是否仍被 sample `26/130/16/168` 一类坏几何样本拖高；
  4. `hybrid_mean_coarse_fine_deviation_samples`、`hybrid_mean_fine_beta`、`hybrid_mean_pair_weight` 是否随 SNR 呈合理趋势；
  5. gated PHAT 是否只在高置信/high SNR 条件下有价值。

### 下一步
1. 用户在 PyCharm 运行 `main.py`。
2. 日志应显示：`experiment_profile=geohybrid_v2_eval`、`EXPERIMENT_MODE=paper_repro_eval_only`、`run_training=False`、`geohybrid=True`。
3. 回读新结果目录：`run.log`、`plot_data.pkl`、Fig8/Fig8_supp、`tables/fig8*_baselines.csv`、`tables/fig8*_method_diagnostics.csv`、`tables/fig8*_worst_localization_errors.csv`。

## 2026-07-07 最新上下文补充：`20260707_193226` GeoAmbi-DFT Direct 完整结果已审查

### 结果路径与配置
- 最新结果目录：`运行结果/20260707_193226`。
- 运行模式：profile=`geoambi_direct_eval`，`EXPERIMENT_MODE=paper_repro_eval_only`，`run_training=False`。
- 该运行加载冻结 Chen-style DAE：`运行结果/20260702_000925`，不训练神经网络，只重算含 GeoAmbi 的 Fig6-Fig8/Fig8_supp 强基线。
- 评估链路：urban8 fixed eval set、all-pair WLS、physical lag gate=48 samples、baseline CR=16。

### 关键结果
- 用户观察成立：`GeoAmbi-DFT-AML` 在低 SNR 排名靠后，高 SNR mean RMSE 也没有超过 Raw/DAE/Zhai/Hadamard 等强基线。
- 但需要分开看 TDOA 与定位 mean：
  - 低 SNR(≤0 dB)：GeoAmbi Direct/AML/PHAT mean RMSE 约 `85.94/78.17/103.31 m`，outlier>20m 约 `75.2%/67.9%/91.0%`，属于真实失败。
  - 高 SNR(≥10 dB)：GeoAmbi Direct/AML 的 pairwise direct TDOA 很准，TDOA MAE 约 `0.048/0.047 samples`，within-1=1.0；但 localization mean RMSE 仍约 `11.33/11.32 m`，主要被固定坏样本和 WLS/几何放大拖高。
  - GeoAmbi 静态 alias 目标生效：64 个频点负/正频各 32，physical-lag inside sidelobe 约 `0.188`，明显低于 DFT-train-power 的约 `0.752`。
- 高 SNR worst localization 反复集中在 sample `26/130/16/168` 等固定样本；这些样本常有极大的 geometry condition/GDOP、boundary hit，且 GeoAmbi TDOA 误差只有约 `0.05-0.13 samples`，却可被定位解放大到约 `78-80 m`。

### 新判断
- GeoAmbi v1 不是代码接线错误，而是目标函数只解决了 deterministic partial-Fourier ambiguity，没有解决 stochastic low-SNR phase/noise variance。
- 纯高频/稀疏/几何消歧选频在低 SNR 会牺牲每个频点的有效 SNR 和 cross-spectrum coherence；PHAT 变体更差，说明低 SNR 不能丢弃幅度/相干度信息。
- 高 SNR mean RMSE 差不能简单解读为 GeoAmbi TDOA 差；更准确地说，是少量坏几何样本中微小 TDOA 偏差被 WLS 放大。论文或创新评估必须同时报告 mean、median/trimmed、outlier rate、pair TDOA MAE 和 geometry/GDOP 分层。

### 下一步
1. 不建议继续只调 GeoAmbi v1 的静态选频权重。
2. 推荐进入 `Hybrid Geo/coherence-aware DFT Direct`：先用高功率/高相干 bins 做 coarse TDOA，再用 GeoAmbi bins 在 coarse 附近做 fine refinement；若 GeoAmbi 质量差或与 coarse 不一致，则回退或加权融合。
3. 同步改进 direct estimator 的 pair quality/uncertainty 权重，使 all-pair WLS 能下调旁瓣高、coherence 低、coarse-fine 不一致的 pair。
4. 后续结果分析必须区分：低 SNR 随机相位失败、high-GDOP 几何放大、定位器边界解、以及真正的选频/估计器缺陷。

## 2026-07-07 最新上下文补充：阶段 A Geo-Ambiguity-Aware DFT Direct 已实现

### 本轮执行内容
- 用户要求执行下一步主线：`Geo-Ambiguity-Aware DFT Direct`。本轮没有继续修改 FreqDAE/v3 decoder，也没有训练神经网络。
- `baselines.py` 新增 `select_geo_ambiguity_dft_bins()` 与内部 `_geo_ambiguity_bins()`。该选择器在训练 noisy waveform 平均频谱上计算 Fisher/CRLB 动机分数，同时加入：
  1. physical TDOA lag window 内的最大旁瓣惩罚；
  2. 基于 urban8 snapshot LOS pair TDOA 分布和 TDOA Jacobian 敏感度的 geometry-weighted ambiguity 惩罚；
  3. 频点聚簇惩罚；
  4. 正负频率 balance 惩罚；
  5. sparse uniform scaffold，避免候选频点塌缩到窄频簇。
- `build_traditional_baselines(..., include_strong_variants=True)` 会生成 `DFT-GeoAmbi`，并在 `traditional_baseline_meta` 中保存 `dft_geoambi_role` 和 `dft_geoambi_selected_bins`。
- `main.py` 新增 direct estimator：
  - `GeoAmbi-DFT-Direct`：selected-bin cross-spectrum direct TDOA；
  - `GeoAmbi-DFT-AML`：Cao2017 风格 AML 相位似然评分，作为 Fig8/Fig9 主视图中的 GeoAmbi 主曲线；
  - `GeoAmbi-DFT-PHAT`：PHAT 变体，放在 Fig8 supplement。
- 为避免默认创新 profile 复用旧 Fig6-Fig8 静态 baseline cache 而漏掉新方法，`main.py` 新增 cache freshness 检查：当 `RUN_STRONG_BASELINES=True` 且缓存的 `baseline_all_results` 中没有 `GeoAmbi-DFT-AML` 时，自动将 cache 判定为 stale 并重算 baseline。
- `experiment_profiles.py` 新增 `geoambi_direct_eval`，并已设为 `main.py` 默认 profile。该 profile 使用 `paper_repro_eval_only`，不训练神经网络，加载冻结 `运行结果/20260702_000925` 的 Chen-style DAE，重跑含 GeoAmbi 的 Fig6/Fig7/Fig8/Fig8_supp。
- `evaluate.py`、`replot.py`、`export_results.py`、`diagnose_go_no_go.py` 已同步新增样式、重绘顺序、summary 说明和 go/no-go estimator 构造。

### 验证状态
- 全套 `py_compile` 通过。
- `git diff --check -- baselines.py main.py evaluate.py replot.py diagnose_go_no_go.py export_results.py` 通过，仅 Windows CRLF 提示。
- 小样本 urban8 smoke 成功：构建 `DFT-GeoAmbi`，CR16 选出 64 个复频点，并运行 `GeoAmbi-DFT-Direct` 与 `GeoAmbi-DFT-AML` 的 direct TDOA + all-pair WLS 评估。
- 选频平衡检查：64 个频点中负频 32、正频 32、DC 0，说明当前实现没有退化为单侧频谱或 DC/低频偏置。

### 当前待验证问题
- 本轮 smoke 只验证链路，不代表正式性能。小样本下 `GeoAmbi-DFT-AML` 优于 `GeoAmbi-DFT-Direct`，因此当前主视图优先看 AML。
- 完整运行后需要重点检查：
  1. GeoAmbi 是否降低 physical lag window 内 alias sidelobe；
  2. 高 SNR 是否优于 Zhai-CRLB、Cao2017、DFT-Fisher 和 Raw；
  3. 低 SNR 是否因高频/稀疏频点而退化；
  4. 若定位 mean 仍有离群，应结合 pair TDOA worst diagnostics 区分是 TDOA 错误还是 WLS/几何放大。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`。日志应显示 `experiment_profile=geoambi_direct_eval`、`EXPERIMENT_MODE=paper_repro_eval_only`、`run_training=False`。
2. 回读新结果目录的 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig8_supp、`tables/fig8*_baselines.csv` 和 method diagnostics。
3. 若 GeoAmbi 表现稳定，可将其作为创新频域选择路线的第一条强基线；若表现不稳定，优先调选频目标中的 geometry ambiguity/cluster/lag sidelobe 权重，并分析 selected bins 的 alias diagnostics。若需要把 GeoAmbi 放入创新 Fig9，再切回创新 profile 并用新 cache 或关闭旧 cache。

## 2026-07-07 最新上下文补充：go/no-go 诊断包已执行，当前 v3 task-head direct 不足以支撑继续该路线

### 本轮执行内容
- 用户批准“先执行 go/no-go 诊断包”。已新增 `diagnose_go_no_go.py`，该脚本不训练模型，直接加载 `运行结果/20260707_143945` 的 v3 权重和 `plot_data.pkl` 配置。
- 诊断包内容：
  1. 把 `NestedFrequencyPairwiseDAE` 的 pairwise task head 包装成 direct TDOA estimator，直接输出 pair TDOA，再进入现有 all-pair WLS。
  2. 与 Raw、Chen DAE、Hadamard、DFT、Cao2017、Cao2020、Zhai-CRLB、Zhai phase-superposition 在同一固定 urban8/WLS/physical lag gate 链路下比较。
  3. 执行 fake-signal sanity：phase randomized、shuffle second observation、zero input、alternate geometry seed。
  4. 导出 `go_no_go_baselines.csv`、`go_no_go_method_diagnostics.csv`、`go_no_go_sanity.csv`、`go_no_go_decision.csv/md`、`Fig_GoNoGo_TaskHead_Direct.svg` 和 `go_no_go_data.pkl`。

### 验证与结果路径
- 编译通过：新增脚本与主入口相关 `.py` 均通过 `py_compile`。
- 小样本 smoke 通过，输出 `运行结果/go_no_go_smoke`。
- 中等规模正式诊断输出目录：`运行结果/go_no_go_20260707_183216`。
- 运行规模：SNR=`[-10,0,10,20]`，每个 SNR 40 个 urban observation，sanity 使用 40 个 observation；不涉及训练，总耗时约 9.4 min。

### 关键结果
- Task-head direct 相比 v3 waveform decoder 没有形成决定性优势：高 SNR mean RMSE 约 `20.2 m`，v3 waveform CR16 约 `21.0 m`；高 SNR TDOA MAE 约 `0.422 samples`，v3 waveform 约 `0.466 samples`。
- Task-head direct 明显弱于高 SNR 传统强基线：本次 high-SNR mean RMSE 中 Hadamard 约 `6.68 m`，Zhai-CRLB 约 `6.89 m`，Raw 约 `8.06 m`，而 TaskHead-CR4/8/16 均约 `20.2 m`。
- CR 仍无区分度：TaskHead-CR4/8/16 全 SNR mean RMSE 约 `22.70/22.70/22.73 m`，高 SNR均约 `20.18/20.19/20.22 m`。
- Sanity 结果显示 task head 不是纯几何/标签先验：CR16 normal TDOA MAE≈`0.484 samples`；phase randomized≈`0.891`，shuffle second observation≈`5.92`，zero input≈`4.42`，alternate geometry seed≈`0.567`。说明 task head 对信号一致性敏感，也具备一定几何泛化，但性能上限不足。
- 决策表：`G1_task_head_beats_v3_waveform=FAIL`，`G2_task_head_competes_with_classical_direct=WARN`，`G3_cr_extra_dimensions_used=FAIL`，fake-signal sanity 大多 PASS，latent quantization 尚未执行。

### 新判断
- v3 task head 学到了一些真实信号/TDOA 信息，但它不是当前设置下的强 task-direct estimator。
- 当前 nested latent 的额外维度没有被使用，CR4/8/16 的 task-head direct 结果几乎完全重合。
- 不建议继续沿“当前 v3 + 直接加训/微调 loss/马上做 v4 decoder”推进。更合理的研究路线是转向 ambiguity/geometry-aware frequency selection 或 hybrid direct 方法；若继续神经网络，应先重新定义 task-direct 架构，而非继续 waveform decoder。

### 下一步
1. 以 `go_no_go_20260707_183216` 为依据，暂停当前 v3/v4 waveform 救火路线。
2. 优先设计强基线启发的 hybrid/frequency-selection 创新：利用 Zhai/Cao/DFT direct 的相位/FIM/ambiguity 思路，再加入几何感知、物理 lag 旁瓣控制或学习型频点/权重选择。
3. 若继续学习型方法，先做 task-direct architecture，而不是 waveform reconstruction：例如输入 pair 或 observation，多输出 TDOA distribution/uncertainty，和 WLS/DPD 直接耦合。
4. 只有当新的 task-direct 方法通过 high-SNR 强基线竞争和 CR 区分门槛后，再做 latent quantization 与多 geometry/multipath 泛化。

## 2026-07-07 最新上下文补充：`20260707_143945` FreqDAE v3 fast 结果已审查，当前 v3 路线需分叉

### 结果路径与配置
- 最新结果目录：`运行结果/20260707_143945`。
- 配置：`EXPERIMENT_MODE=freq_task_v3_fast_final`，profile=`freq_task_v3_nested_fast`，active model=`NestedFrequencyPairwiseDAE`，loss=`freq_task_nested_pair`，CR4 共享 supernet 训练 60 epoch，CR8/CR16 复用同一权重并切换 active mask。
- 静态 baseline cache 已生效：Fig6-Fig8 从 `运行结果/20260706_230643` 复用；本轮只重新训练/评估 v3 与 Fig9 focused 创新对比。

### 关键结果
- 训练没有学到有效 waveform reconstruction：60 epoch 结束时 Train/Val PairFreqNMSE 约 `1.0009/1.0009`；Fig2 诊断中 FreqDAE-v3 三个 CR 的 complex NMSE 约 `1.001-1.003`，mag NMSE 约 `0.973-0.981`，PSD dB MSE 约 `665-826`。
- Fig2 的时频域崩坏与用户观察一致：模型输出仍是低信息量/窄带假信号，而不是 clean waveform。
- Fig9 focused 显示 v3 低 SNR 有鲁棒性但高 SNR 有误差地板：低 SNR(≤0 dB) mean RMSE 约 `24 m`，优于 Raw 的约 `30 m` 和 Chen DAE-CR16 的约 `41 m`；高 SNR(≥10 dB) mean RMSE 约 `20 m`，明显弱于 Raw 的约 `6.55 m` 和 Chen DAE-CR4 的约 `8.0 m`。
- v3 三个 CR 几乎完全重合：全 SNR mean RMSE 分别约 `CR4=21.920 m`、`CR8=22.008 m`、`CR16=22.042 m`，说明嵌套额外维度没有被有效利用。
- GCC 诊断显示 v3 学到的是“宽峰模板”：高 SNR TDOA mean abs error 约 `0.413 samples`，而 Raw/Chen DAE-CR4 约 `0.024/0.032 samples`；v3 peak width 约 `9.8 samples`，Raw/Chen DAE 约 `2.9-3.0 samples`；sidelobe ratio 约 `0.74`，Raw/Chen DAE-CR4 约 `0.236/0.158`。

### 新判断
- 当前 v3 最小结构证明了“嵌套 latent + task head”代码链路可运行，但没有证明其作为 waveform reconstruction 创新路线有效。
- 根本问题不是训练轮数不足，而是结构和目标仍允许模型走捷径：task head 参与训练但最终评估仍丢弃 task head；共享 decoder + mask 轮换会鼓励所有有用 TDOA 信息塞进最小 CR16 前缀，额外 CR 维度缺少显式残差职责。
- 当前创新路线应分叉，而不是继续直接加训 v3：一条是 waveform-comparable 路线，需要 residual nested decoder 与多 CR 同 batch 约束；另一条是 task-direct 路线，直接评估 task head 的 TDOA/定位能力，并与 Zhai/Cao/DFT direct 类强基线同轨比较。

### 下一步
1. 不建议直接把当前 v3 跑到 200 epoch；先做 task-head direct TDOA 诊断，判断 latent task head 是否学到了定位信息而 decoder 没学到。
2. 若 task head 有效，开启 `task_direct` 创新轨道；若 task head 也无效，停止当前 v3 结构。
3. 若坚持 waveform reconstruction 评估链路，进入 v4：CR16 base decoder + CR8 residual decoder + CR4 residual decoder，并在同一 batch 同时计算 CR16/8/4 输出损失和 CR 单调改进约束。

## 2026-07-07 最新上下文补充：FreqDAE v3 最小可运行结构已实现

### 本轮执行内容
- 用户批准进入 v3。已完成“profile 配置整理 + v3 最小结构实现 + 编译/小样本 smoke”，未运行完整训练。
- 新增 `experiment_profiles.py`；该历史条目执行完成时默认 `USER_EXPERIMENT_PROFILE="freq_task_v3_nested_fast"`。当前默认 profile 已由最新条目更新为 `geoambi_direct_eval`。
- 新增 `NestedFrequencyPairwiseDAE`：共享最大 CR4 latent，CR16/CR8/CR4 使用同一 latent 的嵌套前缀 mask；模型仍输出 waveform，保持现有 Fig2/Fig3/Fig9 的 waveform→GCC→WLS 评估链路。
- v3 训练新增 pairwise task head：从两路 latent prefix 构造 pair feature，预测 soft TDOA lag distribution；训练 loss 为 reconstruction + GCC shape + soft peak + cross-spectrum phase + task-head TDOA。
- `main.py` 的 v3 训练只训练一次共享 supernet，之后为 CR4/8/16 三个模型实例加载同一权重并设置不同 active mask。因此 v3 的三条 CR 曲线来自同一个嵌套模型，而不是三个互不相干模型。

### 验证状态
- 全套 `py_compile` 通过。
- v3 forward smoke：CR4/8/16 active mask 下输出均为 `(2,2,1024)`；task head logits 为 `(2,1024)`。
- 小样本 urban8 pair training smoke 通过：`V3_SMOKE_OK ... freq_task_nested_pair 1`。
- `git diff --check` 无空白错误，仅 Windows CRLF 提示。

### 当前默认运行
- PyCharm 直接运行 `main.py` 会进入：
  - `EXPERIMENT_MODE=freq_task_v3_fast_final`
  - `ACTIVE_MODEL_NAME=NestedFrequencyPairwiseDAE`
  - `LOSS_MODE=freq_task_nested_pair`
  - `MAX_EPOCHS=60`
  - `REUSE_STATIC_BASELINE_CACHE=True`
- 预期日志应出现 `[BaselineCache] Loaded static Fig6-Fig8 cache`；若缓存不可用，会自动回退完整 baseline 重算。

### 当前局限
- v3 当前是最小可运行版本。训练 batch 轮换 CR masks，但验证曲线暂以 CR4 mask 为参考，尚未实现三 mask 验证平均。
- task head 目前只参与训练，不参与最终定位推理；最终评估仍严格使用 waveform reconstruction + GCC + WLS，保证与 Chen DAE 和传统 baseline 同链路比较。
- 尚未得到正式性能结果，不能判断 v3 是否真正解决 CR 无区分度和窄带假信号。

### 下一步
1. 用户运行 `main.py` 得到 v3 fast 结果。
2. 回读 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3/Fig9 focused、`tables/fig9_focus_baselines.csv`、`tables/fig2_diagnostics.csv`。
3. 若 Fig2 重构仍崩坏或 Fig9 无 CR 区分度，优先检查 task-head loss 是否过弱、nested mask 训练是否需要三 mask 同 batch 平均、以及 reconstruction head 是否需要独立 low/high-band 分支。

## 2026-07-07 最新上下文补充：静态 baseline 缓存与 reconstruction-only 诊断已执行

### 本轮执行内容
- 用户批准执行 v2.1 后续方案，并询问当前大量开关变量是否应拆分成不同 py 文件。
- 已新增 `experiment_cache.py`：用于从既有结果目录读取 `plot_data.pkl` 中的 Fig6-Fig8、`baseline_all_results` 和传统 baseline 元信息，并与新一轮创新模型的 Fig9/Fig9 focused 结果合并。
- `main.py` 新增静态 baseline 缓存配置：`USER_REUSE_STATIC_BASELINE_CACHE=True`，默认缓存源为 `运行结果/20260706_230643`。当 `EXPERIMENT_MODE` 属于创新模式时，默认复用缓存的 Fig6-Fig8，只重新评估当前创新模型与冻结 Chen-DAE 的 Fig9/Fig9 focused；缓存不可用时自动回退完整 baseline 重算。
- 已新增 `diagnose_reconstruction_only.py`：在 urban LOS-pair 数据链路上关闭 pairwise `corr/xphase/peak` 项，仅保留 reconstruction loss，用于判断 v2/v2.1 的重构崩坏是否主要由 pairwise loss 造成。

### 轻量验证与诊断
- 编译通过：`main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py experiment_cache.py diagnose_reconstruction_only.py analyze_results.py`。
- 缓存读取 smoke 通过：`运行结果/20260706_230643` 的缓存包含 `fig6_results`、`fig7_results`、`fig8_results`、`fig8_supp_results`、`baseline_all_results`、`traditional_baseline_meta`；`baseline_all_results` 含 25 个方法，合并函数可运行。
- 已运行 reconstruction-only 诊断，结果目录：`运行结果/diag_recon_only_20260707_134404`。12 epoch 小样本下：CR4 train/val PairFreqNMSE≈0.937/0.965，CR16≈0.993/0.970。
- 诊断含义：去掉 pairwise GCC/phase/peak 后，重构也没有快速明显改善；因此 v2/v2.1 的失败不能简单归因于 pairwise loss 梯度冲突，单 waveform 频域重构结构本身也可能不足。

### 代码结构判断
- 当前 `main.py` 顶部开关变量已经偏多，长期会影响可读性和误运行风险。
- 但当前不建议马上把不同路线拆成多套完整主脚本，因为训练、评估、绘图、导出链路很长，硬拆容易复制逻辑并导致 paper_repro、innovation、eval_only 之间口径漂移。
- 当前推荐分阶段拆分：
  1. 保留 `main.py` 作为 PyCharm 一键主入口。
  2. 已先外提缓存工具和诊断脚本，减少主流程重复耗时。
  3. 下一步把 `USER_*` 开关整理为 profile/dataclass，例如 `paper_repro_eval_only`、`freq_task_v2_200_final`、未来 `freq_task_v3_nested`。
  4. 待 v3 稳定后，再考虑新增 `run_paper_repro.py`、`run_innovation_v3.py`、`run_eval_only.py` 这类很薄的入口脚本，它们只选择 profile，不复制训练/评估逻辑。

### 下一步
1. 若继续运行当前创新模式，PyCharm 直接运行 `main.py`，重点看日志中是否出现 `[BaselineCache] Loaded static Fig6-Fig8 cache`；预期 Fig6-Fig8 不重算，耗时显著下降。
2. 若进入 v3，先整理 profile，再实现 nested latent supernet + waveform reconstruction head + pairwise TDOA/GCC task head。
3. `diagnose_reconstruction_only.py` 可作为后续 v3 前后的快速对照脚本：如果 v3 reconstruction-only 仍无法下降，说明问题在数据/目标；如果能下降但 full task 不行，说明任务分支冲突仍需处理。

## 2026-07-07 最新上下文补充：`20260706_230643` FreqDAE v2.1 正式结果已审查，确认应转向结构级 v3

### 结果路径与配置
- 最新结果目录：`运行结果/20260706_230643`。
- 配置：`EXPERIMENT_MODE=freq_task_v2_200_final`，active model=`FrequencyPairwiseDAE`，loss=`freq_task_pair`，CR4/8/16 均 final-only 200 epoch。
- v2.1 修复已生效：`freq_task_v2_peak_sharpness_weight=0.25`；PairFreqNMSE 由 v2 的约 0.600 回到约 1.000，说明此前 NMSE 低估已被修正。

### 关键结果
- 训练 loss：三种 CR 在前 20-30 epoch 快速下降，之后极慢；最终 train/val PairFreqNMSE 基本都约 1.000，ValTotal 约 1.03。
- Fig2 诊断：时频域仍崩坏，complex NMSE≈1.0，mag NMSE≈0.96-0.99，PSD dB MSE 很高，模型仍输出窄带/低信息量假信号。
- v2.1 相比 v2 的正向变化：GCC 峰宽从约 14 samples 降到约 9 samples，sidelobe ratio 从约 0.88-0.89 降到约 0.73-0.78，高 SNR TDOA error 从约 0.33-0.47 samples 降到约 0.25-0.28 samples。
- 但定位结论没有根本改变：Fig9 focused 全 SNR mean RMSE 约为 `FreqDAE-v2-CR4=20.759 m`、`CR8=18.791 m`、`CR16=19.790 m`，无稳定 CR 阶梯；高 SNR 仍明显弱于 Raw 和 Chen DAE-CR4/8。
- Fig6-Fig8 与 `20260706_190513` 完全一致，相关 CSV 公共字段最大差为 0。这些图使用冻结 Chen DAE 和传统/强基线，不依赖本轮 v2.1 模型，后续创新迭代无必要重复计算。

### 新判断
- 当前 `FrequencyPairwiseDAE` 结构上仍是单 waveform 输入/输出的 `FrequencySelectiveDAE` 变体，pair-aware 只发生在训练 loss，不是实际双输入/双分支/嵌套 latent 架构。
- v2.1 证明“修 bug + 调 loss”只能改善峰宽和局部 TDOA floor，不能恢复波形，也不能让额外 CR 维度自然发挥作用。
- clean pair 自检确认 TDOA 标签符号正确：clean GCC peak 与几何 tdoa 的 mean abs error 约 0.25 sample、within-1=1.0，因此当前失败不是标签符号错误。

### 下一步建议
1. 停止继续完整训练 v2.2/v2.x 的 loss 微调；v2.1 可作为失败消融和机制证据。
2. 先做一个轻量诊断：同一 pair 数据上跑 reconstruction-only/无 pair loss 小样本，判断重构崩坏来自模型/数据还是 pairwise loss 梯度冲突。
3. 进入 v3：nested latent supernet（CR4 包含 CR8 包含 CR16）+ 双分支结构（waveform reconstruction head + pairwise TDOA/GCC task head），让额外维度和任务分支在结构上被使用，而不是等待 loss 自发分配。
4. 工程上新增 innovation-run baseline cache/skip：创新模型变化时只重算 Fig1-Fig3/Fig9 和必要 diagnostics，Fig6-Fig8 直接复用或拷贝缓存结果，避免每次浪费约 1 小时。

## 2026-07-06 最新上下文补充：FreqDAE v2.1 修复已执行，代码进入待运行状态

### 本轮执行内容
- 用户批准执行 `20260706_190513` 结果审查后的 v2.1 修复，并询问这些问题是否影响其他方法。
- 已修复 `train.py` 中 FreqDAE 训练损失的三个关键问题：训练 NMSE 与诊断 NMSE 定义不一致、cross-spectrum phase reliability 近似恒为 1、soft peak 不惩罚宽峰。
- 已在 `main.py` 新增 `USER_FREQ_TASK_V2_PEAK_SHARPNESS_WEIGHT=0.25`，并写入 `plot_data.pkl` 的配置区。

### 影响范围
- `frequency_task_reconstruction_loss()` 的 NMSE 修正会影响 `freq_task_mse` v1 和 `freq_task_pair` v2/v2.1，因为两者都调用该函数。
- cross-spectrum reliability 修正只影响 `freq_task_pair` v2/v2.1。
- soft peak sharpness 只影响 `freq_task_pair` v2/v2.1，且只有 `lambda_peak>0` 时生效。
- `paper_repro`/Chen-style DAE 复现轨道、R20.1 旧任务损失、传统 DFT/Hadamard/PCA baseline、Zhai/Cao direct TDOA baseline 不走这些训练损失，因此不受本轮代码修复影响；但若重新运行创新模式，v1/v2 结果会因 NMSE 定义修正而变化。

### 验证状态
- 已通过 `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`。
- 已通过 sanity：零输出对应的 time/complex NMSE 约为 1.0；cross-spectrum phase loss 与 sharpness peak loss finite。
- 已通过小样本 urban8 `FrequencyPairwiseDAE` CR16、`freq_task_pair`、2-fold、1 epoch smoke，输出 `V2_1_SMOKE_OK freq_task_pair 0.25 2`。
- `git diff --check` 无空白错误，仅有 Windows CRLF 提示。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`。当前默认仍是 `USER_EXPERIMENT_MODE="freq_task_v2_200_final"`，但已变为 v2.1 loss 定义。
2. 回读新结果目录，重点对比 `20260706_190513`：Fig1 PairFreqNMSE 数值应因定义修正上升；Fig2 complex NMSE、peak width、sidelobe ratio；Fig9 focused high-SNR TDOA floor 和 CR 区分度。
3. 如果 v2.1 仍保持低 SNR 好但高 SNR 有误差地板，应停止继续微调 loss，进入 v3：nested latent + 双分支结构。

## 2026-07-06 最新上下文补充：`20260706_190513` FreqDAE v2 正式结果已审查，v2 暴露宽峰模板捷径

### 结果路径与配置
- 最新结果目录：`运行结果/20260706_190513`。
- 配置：`EXPERIMENT_MODE=freq_task_v2_200_final`，active model=`FrequencyPairwiseDAE`，loss=`freq_task_pair`，CR4/8/16 均 final-only 200 epoch。
- 评估仍使用 urban8 fixed eval set、physical lag gate=48 samples、`all_pair_wls`，并加载冻结 Chen-style DAE 参考 `运行结果/20260702_000925`。
- 本次总耗时约 195.7 min；CR4/8/16 final train/val loss 均约 0.600，三种 CR 几乎无训练损失区分。

### 关键结果
- Fig9 focused 全 SNR mean RMSE：Raw≈15.720 m，DAE-CR4≈13.878 m，DAE-CR8≈17.027 m，DAE-CR16≈21.380 m；FreqDAE-v2-CR4≈20.352 m，FreqDAE-v2-CR8≈19.913 m，FreqDAE-v2-CR16≈22.474 m。
- 低 SNR(≤0 dB)：FreqDAE-v2 明显优于 Raw 和 DAE-CR16，CR4/8/16 分别约 24.828/25.708/25.858 m；Raw≈30.038 m，DAE-CR16≈40.998 m。
- 中高 SNR 后 v2 明显落后：高 SNR(≥10 dB) v2 CR4/8/16 分别约 15.867/13.439/18.717 m，而 Raw≈6.550 m，DAE-CR4≈7.996 m。
- Fig2/诊断显示 v2 的重构基本失败：complex NMSE≈1.0，mag NMSE≈0.96-0.99，PSD MSE 很高；但 GCC/TDOA 低 SNR 稳定，伪峰少。
- v2 的 GCC 主峰很宽：peak width 约 14 samples，而 Raw/Clean 高 SNR 约 2.9 samples；这解释了低 SNR 抗噪好但高 SNR 不随 SNR 明显改善。

### 新发现的代码/原理风险
- `frequency_task_reconstruction_loss` 的 time NMSE 归一化存在尺度不一致：`time_mse` 对 I/Q 两通道求均值，而 `sig_power` 使用两通道功率和；若输出接近 0，训练 NMSE 约 0.5，而诊断 complex NMSE 约 1.0。这会让训练日志低估重构崩坏。
- `pairwise_cross_spectrum_phase_loss` 先归一化互谱再取幅值作为 reliability，导致 reliability 近似恒为 1，丢失了原本应使用的 clean 互谱幅度可靠性。
- soft peak loss 只鼓励真值附近能量，不惩罚峰宽和局部旁瓣；因此网络容易学出“宽而稳定”的 TDOA 模板。
- 三个 CR 无区分度的根因不是特征量必然无效，而是当前任务目标可由 CR16 表示的低维宽峰模板完成，额外 latent 维度没有被结构或损失强制用于高 SNR 精细定时/重构。

### 下一步建议
1. 先做 v2.1 小修而不是直接大改：修正 reconstruction NMSE 归一化、修复 cross-spectrum reliability、增加峰宽/局部 margin 约束，并把 loss 分量日志与 Fig2 诊断统一。
2. 用短训练或 eval smoke 检查高 SNR TDOA 误差地板是否下降；如果仍无 CR 区分，再进入 v3 架构。
3. v3 应采用结构级方案：nested latent（CR4 包含 CR8 包含 CR16）+ 双分支（重构分支与 TDOA/GCC 分支）+ 多 CR 联合训练，避免继续只在 loss 上反复打补丁。

## 2026-07-06 最新上下文补充：FreqDAE v2 已实现，代码进入待 PyCharm 运行状态

### 本轮执行内容
- 用户批准进入 FreqDAE v2 设计，并要求移除不合适定位器建议、减少结果目录臃肿，同时在诊断数据中保存 TDOA 与对应定位结果。
- 已实现 `FrequencyPairwiseDAE`，作为 FreqDAE v2 模型。推理接口仍是单 waveform 输入/输出，保证能进入与 Chen DAE 相同的 waveform reconstruction + GCC + WLS 评估链路。
- v2 训练不再只用单信号频域重构，而是新增 urban LOS pair-aware 训练集：同一 observation 内随机抽 LOS UAV pair，TDOA 标签使用几何距离差的浮点采样值，和 all-pair WLS 定位评估一致。
- 新增 `freq_task_pair` 损失：任务感知重构 + GCC 形状一致 + float-TDOA soft peak + cross-spectrum phase consistency。所有 CR 使用同一组损失权重，避免靠人为权重制造 CR 排序。

### 关键代码状态
- `main.py` 默认 `USER_EXPERIMENT_MODE="freq_task_v2_200_final"`；PyCharm 直接运行会训练 `FrequencyPairwiseDAE` CR4/8/16 各 200 epoch，然后加载冻结 Chen-style DAE 参考 `运行结果/20260702_000925`，输出 Fig1-Fig9 与 tables。
- v2 曲线标签为 `FreqDAE-v2-CR4/8/16`，与历史 v1 的 `FreqDAE-CR4/8/16` 区分。
- `USER_RUN_LOCALIZER_ABLATION=False`，不再默认生成 Fig10；`USER_LOCALIZER_ABLATION_ESTIMATORS` 仅保留 `all_pair_wls`。
- `evaluate.py` 对 `all_pair_wls_grid/pruned/grid_pruned` 显式报错，防止把已判定不适合的定位器误用作主评估链路。
- worst localization 导出新增 pair 级 TDOA 明细字段：`pair_uav_indices`、`pair_true_tdoa_samples`、`pair_est_tdoa_samples`、`pair_tdoa_error_samples`、`pair_weight`、`pair_sidelobe_ratio`。每条 worst row 同时保留 source/estimated 坐标、WLS residual、GDOP 等定位信息。

### 验证状态
- 已通过 `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`。
- 已通过小样本 smoke：`FrequencyPairwiseDAE` CR16 使用 `freq_task_pair` 训练 1 epoch 可运行；小样本 method comparison 可输出 `FreqDAE-v2-CR16`；worst localization CSV/MD 可导出新增 pair TDOA 字段。
- `git diff --check` 无空白错误，仅有 Windows CRLF 提示。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`，确认日志显示 `EXPERIMENT_MODE=freq_task_v2_200_final`、`active_model=FrequencyPairwiseDAE`、`loss_mode=freq_task_pair`。
2. 回读新结果目录的 `run.log`、`plot_data.pkl`、Fig1-Fig3、Fig9 focused、`tables/fig9_focus_baselines.csv`、`tables/fig9_focus_worst_localization_errors.csv`。
3. 判断 v2 是否解决 v1 的核心问题：同 CR 下是否超过 Chen DAE，CR4/8 是否不再明显弱于 v1/Chen，worst 表中的 pairwise TDOA bias 是否下降。
4. 如果 v2 仍不优，应优先分析 `pair_tdoa_error_samples` 与 `estimated_x/y` 的对应关系，再决定是否调损失权重或改结构。

## 2026-07-06 最新上下文补充：`20260706_155312` eval-only 与 Fig10 定位器消融已审查

### 结果路径与配置
- 最新结果目录：`运行结果/20260706_155312`。
- 配置：`EXPERIMENT_MODE=freq_task_eval_only`，`run_training=False`，active model=`FrequencySelectiveDAE`。
- FreqDAE 权重源：`运行结果/20260706_103701`；冻结 Chen-style DAE 参考源：`运行结果/20260702_000925`。
- 本次没有重新训练，主要价值是完整生成 Fig10 high-SNR localizer ablation 与增强 worst localization tables。
- 运行耗时约 6993.9 s，即 116.6 min。虽然是 eval-only，但 Fig6-Fig9 完整评估加 Fig10 四种定位器消融仍然很耗时。

### Fig9 主性能结论
- `fig9_focus_baselines.csv` 与 `20260706_103701` 的公共数值几乎完全一致，说明 eval-only 正确复用了既有模型，未引入新训练差异。
- 全 SNR mean RMSE：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。
- 低 SNR(≤0 dB)：DAE-CR4≈23.295 m 最好，Raw≈30.038 m，FreqDAE-CR4≈30.330 m，FreqDAE-CR8≈36.087 m，FreqDAE-CR16≈37.292 m，DAE-CR16≈40.998 m。
- 高 SNR(≥10 dB)：Raw≈6.550 m，DAE-CR4≈7.996 m，FreqDAE-CR4≈9.020 m，DAE-CR8≈9.158 m，FreqDAE-CR16≈9.611 m，DAE-CR16≈9.661 m，FreqDAE-CR8≈9.794 m。
- 结论不变：FreqDAE v1 未能超过 Chen-style DAE-CR4/CR8；只在 CR16 上相对 DAE-CR16 略有改善。

### Fig10 定位器消融结论
- Grid 多起点与普通 WLS 的 mean/median 几乎一致，说明 high-SNR 离群不是简单的 WLS 初值局部最优问题。
- Pruned/Grid+Pruned 对 DAE/FreqDAE 的 high-SNR mean 有小幅改善，例如 DAE-CR4 约 7.996→7.752 m、FreqDAE-CR4 约 9.020→8.626 m、FreqDAE-CR16 约 9.611→9.118 m。
- 但 pruning 会让 Raw 和 Zhai-CRLB-Decimation 变差，例如 Raw 约 6.550→6.903 m，Zhai 约 7.694→7.907 m；同时 median 普遍变差。因此 pruning 不能作为默认主定位器替换，只适合作为诊断或补充消融。
- High-SNR median 很低但 mean 被少数固定坏样本支配：Raw median≈0.109 m，DAE-CR4≈0.201 m，FreqDAE-CR4≈0.527 m，FreqDAE-CR16≈0.999 m。

### worst diagnostics 新发现
- top worst 样本高度集中：sample 26、168、130、16 出现次数最多，且多个样本 source 坐标相同或相近，说明 fixed eval set 中存在固定几何/多解困难场景。
- Raw 在高 SNR 的坏样本 pairwise TDOA error 很小（约 0.02-0.03 sample），但 GDOP 可达几十到上百，说明是几何放大小误差。
- Zhai-CRLB-Decimation 在 sample 168/16 可出现边界解，pairwise TDOA error 仍只有约 0.05 sample，但 GDOP 可达 1700+，说明高 SNR mean 回升主要是几何/WLS 多解放大，而非 direct TDOA 本身变差。
- DAE/FreqDAE 的坏样本有两类：DAE-CR4 更像小 TDOA bias 被几何放大；FreqDAE-CR4/CR16 则存在更明显的方法诱导系统偏差，pairwise TDOA error 可达约 0.09-0.24 sample，residual 更大。

### 下一步建议
1. 不建议把 `all_pair_wls_pruned` 或 `grid_pruned` 替换为主评估定位器；主链路仍保持 `all_pair_wls`，Fig10 作为补充诊断。
2. 默认运行建议关闭 Fig10 或改为按需开关，否则 eval-only 仍需约 2 小时；如用户批准，可把 `USER_RUN_LOCALIZER_ABLATION=False` 设为默认。
3. 创新方案不要继续微调 FreqDAE v1 的频域损失；应转向 FreqDAE v2：pairwise cross-spectrum/GCC task branch、嵌套 latent、以及显式控制系统性 TDOA bias。
4. 论文结果报告中需要区分 mean、median、p95 和 worst case；mean 是主指标，但高 SNR mean 的解释必须结合 fixed geometry outlier diagnostics。

## 2026-07-06 最新上下文补充：建议方案前4步已实现，代码进入 FreqDAE eval-only 待运行状态

### 本轮执行结论
- 用户询问能否一步到位执行建议方案前4步。结论：不能直接把主 Fig3/Fig6-Fig9 的默认定位器替换成新鲁棒定位器，否则会破坏与历史 v3.1、Fig6-Fig9、FreqDAE v1 结果的可比性。
- 已执行可行且严谨的版本：保留主链路默认 `all_pair_wls` 不变，新增补充诊断轨道 Fig10，用于检查 high-SNR mean RMSE 离群是否由 WLS 多解/几何病态主导。

### 关键代码状态
- `main.py` 默认 `USER_EXPERIMENT_MODE="freq_task_eval_only"`，PyCharm 直接点击 `main.py` 默认加载 `运行结果/20260706_103701/model_cr*.pt`，不再重新训练 FreqDAE 三个 CR。
- `USER_FREQ_TASK_MODEL_SOURCE_DIR` 指向 `运行结果/20260706_103701`；冻结 Chen-style DAE 参考仍由 `USER_REPRO_REFERENCE_MODEL_DIR=运行结果/20260702_000925` 单独控制，避免创新模型源和复现参考源混用。
- 新增 `USER_RUN_LOCALIZER_ABLATION=True`，默认在 high SNR `[10,12,14,16,18,20] dB` 上生成 Fig10 定位器鲁棒性补充图。
- Fig10 默认比较定位器：`all_pair_wls`、`all_pair_wls_grid`、`all_pair_wls_pruned`、`all_pair_wls_grid_pruned`；默认比较方法：Raw、DAE-CR4、DAE-CR16、FreqDAE-CR4、FreqDAE-CR16、Zhai-CRLB-Decimation。
- `evaluate.py` 的 worst localization detail 已补充 LOS UAV 坐标、used UAV、WLS residual、normalized cost、几何条件数、GDOP、是否命中边界、多起点候选数、次优候选和 pruning 信息。
- `export_results.py` 和 `replot.py` 已支持 `fig10_localizer_results` 与增强 worst localization CSV/MD 字段。

### 验证状态
- 已通过 `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`。
- 已通过函数级 urban8 smoke：`all_pair_wls` 与 `all_pair_wls_grid_pruned` 均可运行，Raw/Clean/Geometry/DirectDFT 都能输出 RMSE 和增强诊断字段。
- 已验证 `运行结果/20260706_103701/model_cr4.pt/model_cr8.pt/model_cr16.pt` 可由当前 `FrequencySelectiveDAE` 正常加载并 forward。
- 已通过 Fig10 `方法|定位器` 图例绘图与表格导出 smoke。
- `git diff --check` 未发现空白错误，仅有 Windows CRLF 转换提示。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`，应进入 `freq_task_eval_only`，不训练，只重评估并输出 Fig1-Fig10 与 tables。
2. 回读新结果目录中的 `run.log`、`plot_data.pkl`、`Fig10_Localizer_Robustness_Ablation.svg`、`tables/fig10_localizer_baselines.csv`、`tables/fig10_localizer_worst_localization_errors.csv`。
3. 若 Fig10 显示 `grid/pruned` 显著降低 high-SNR mean 离群，而 median/trimmed 几乎不变，则主问题是定位器/几何异常值；后续应决定是否把鲁棒定位器作为补充报告或正式替代链路。
4. 若 Fig10 不能改善离群，则说明 FreqDAE/强基线本身在固定坏几何样本上存在系统性 TDOA 偏差，后续创新应转向 pairwise cross-spectrum/task branch，而不是继续微调 FreqDAE v1。

## 2026-07-06 最新上下文补充：`20260706_103701` 完整重跑与 worst diagnostics 结论

### 结果路径与配置
- 最新结果目录：`运行结果/20260706_103701`。
- 配置：`EXPERIMENT_MODE=freq_task_200_final`，active model=`FrequencySelectiveDAE`，loss=`freq_task_mse`，训练协议 `fixed_epoch_final_only`，CR4/8/16 均训练 200 epoch。
- 冻结 Chen-style DAE 参考模型仍来自 `运行结果/20260702_000925`；baseline CR=16；physical TDOA lag gate=48 samples；Monte Carlo trials=200。
- 总耗时约 13141.6 s，即 219.0 min。本次不是 eval-only，而是重新完整训练了 FreqDAE 三个 CR。

### 与 `20260705_202516` 的关系
- `fig9_focus_baselines.csv` 的公共数值与 `20260705_202516` 完全一致；当前 seed/配置下训练和评估具有确定性。
- 本次新增价值不是性能变化，而是代码增强后正式生成了 `fig6/7/8/9*_worst_localization_errors.csv/md`，可做逐样本离群诊断。

### Fig9 focused 核心结果
- 全 SNR mean RMSE：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。
- 低 SNR(≤0 dB)：Raw≈30.038、DAE-CR4≈23.295、DAE-CR8≈30.157、DAE-CR16≈40.998、FreqDAE-CR4≈30.330、FreqDAE-CR8≈36.087、FreqDAE-CR16≈37.292 m。
- 高 SNR(≥10 dB)：Raw≈6.550、DAE-CR4≈7.996、DAE-CR8≈9.158、DAE-CR16≈9.661、FreqDAE-CR4≈9.020、FreqDAE-CR8≈9.794、FreqDAE-CR16≈9.611 m。
- 结论不变：FreqDAE v1 只在 CR16 相对 Chen DAE-CR16 有小幅改善；CR4/CR8 均弱于 Chen DAE，对“更多维度转化为更好定位/重构”的创新目标支持不足。

### worst diagnostics 关键发现
- 高 SNR 下 median 与 p95 往往很低，但 mean 被少量固定坏样本主导。例如 Raw high-SNR median≈0.109 m、p95≈1.012 m，但 mean≈6.550 m、max≈50.075 m。
- 重复离群样本主要是 sample index 16、26、130、168。多个样本 source 坐标相同或相近，说明 fixed eval set 中存在固定几何/定位歧义场景。
- Zhai-CRLB-Decimation 高 SNR direct TDOA 诊断很好：mean abs TDOA error≈0.063 sample、within-1≈0.999，但 sample 168 在 10-20 dB 仍反复约 78 m 定位误差，且 pair mean TDOA error 只有约 0.055 sample。这说明 Zhai 高 SNR mean 回升主要是 WLS/几何歧义或小系统误差被几何放大，不是 direct TDOA 随 SNR 变差。
- FreqDAE-CR8 的 sample 130 在高 SNR 多次约 78-79 m；FreqDAE-CR16 在 sample 130 最高约 91.9 m。说明 FreqDAE v1 还会引入固定几何下的系统性定位偏差。

### 下一步建议
1. 新增 `freq_task_eval_only` 或 `innovation_eval_only`，区分 FreqDAE 模型源目录与冻结 Chen-DAE 参考目录，避免为了重新诊断再次训练 219 min。
2. 在 worst diagnostics 中补充 UAV 坐标、几何条件数/GDOP、WLS residual map 或多初值候选；当前表已有 source/estimate、LOS 数、WLS cost、pairwise TDOA error，但还不足以完全解释坏几何。
3. FreqDAE v1 不建议继续只调 epoch 或 spectral_blend。下一步应设计 FreqDAE v2：nested latent + pairwise cross-spectrum/GCC task branch + 显式任务分支/不确定性权重。

## 2026-07-06 最新上下文补充：A+B 图表/诊断增强已执行

### 本轮执行内容
- 用户批准执行 A+B，并特别要求 Fig2 图例保持 `DAE`。本轮没有修改 Fig2 图例，只保留此前标题区分 active model 的做法。
- `evaluate.py`：`Cao2017-DFT-AML` 从蓝色系改为青色系，减少与 Chen DAE 蓝色曲线混淆；method-comparison 统计新增 `outlier_gt20_rate/outlier_gt50_rate`；未来 Fig6-Fig9 评估会保存每个方法、每个 SNR 的 top-k worst localization details，包括 source/estimated 坐标、LOS 数、WLS cost、pairwise TDOA error 和 sidelobe ratio。
- `main.py`：新增 `fig9_focus_results`，输出 `Fig9_Focused_DAE_CR_Comparison.svg` 与 `Fig9_Focused_DAE_CR_Comparison_SNR_Zooms.svg`，只保留 Raw、Chen DAE CR4/8/16、FreqDAE CR4/8/16，便于直接比较新旧模型全 CR。
- `export_results.py`、`replot.py`：支持旧 `plot_data.pkl` 自动从 `fig9_results` 过滤生成 focused Fig9；baseline CSV/MD 增加 p90/p95/max/valid/failure/outlier 字段；若 pkl 内有 `outlier_details`，会导出 `*_worst_localization_errors.csv/md`。

### 已处理旧结果
- 已对 `运行结果/20260705_202516` 执行离线导出与重绘，不重新训练、不重新评估。
- 新增/覆盖文件包括：
  - `Fig9_Focused_DAE_CR_Comparison.svg`
  - `Fig9_Focused_DAE_CR_Comparison_SNR_Zooms.svg`
  - `tables/fig9_focus_baselines.csv/md`
  - `tables/fig9_focus_method_diagnostics.csv/md`
- 旧 `20260705_202516/plot_data.pkl` 没有逐样本定位明细，因此本次不能 retroactively 生成 top-k worst localization table；下一次运行 `main.py` 后会自动保存。

### 验证状态
- 两套 `py_compile` 通过。
- 小样本 method-comparison smoke 通过：确认新增 outlier/top-k 路径可运行。
- top-k worst 导出 smoke 通过：`EXPORT_WORST_OK 1395`，确认未来含 `outlier_details` 的新 pkl 可写出 `*_worst_localization_errors.csv/md`。
- `export_results.py "运行结果/20260705_202516" --no-fig6-svg` 与 `replot.py "运行结果/20260705_202516"` 已成功执行。
- `git diff --check` 无空白错误，仅有 Windows 行尾提示。

### 下一步
1. 若要定位 Zhai-CRLB 高 SNR mean 回升的具体离群样本，需要重新运行 `main.py` 或 eval-only，读取新生成的 `tables/*_worst_localization_errors.csv`。
2. FreqDAE v1 已基本定性：CR16 有低维收益但 CR4/CR8 不如 Chen DAE。下一步应转向 FreqDAE v2，而不是继续只调 v1 训练轮数。
3. 暂未新增 innovation eval-only 模式；如后续需要，应单独设计清楚“FreqDAE 模型源目录”和“冻结 Chen 参考目录”两个路径，避免误加载。

## 2026-07-06 最新上下文补充：`20260705_202516` FreqDAE 200 epoch 复核结论

### 结果路径与配置
- 已读取最新结果目录：`运行结果/20260705_202516`，包括 `run.log`、`plot_data.pkl`、Fig1-Fig9 和 `tables/*.csv`。
- 配置：`EXPERIMENT_MODE=freq_task_200_final`，active model=`FrequencySelectiveDAE`，loss=`freq_task_mse`，CR4/8/16 均 fixed final-only 200 epochs。
- 冻结 Chen-style DAE 参考模型来自 `运行结果/20260702_000925`，Fig9 已包含 `DAE-CR4/8/16` 与 `FreqDAE-CR4/8/16`。

### 关键结果
- 训练最终 ValTotal：FreqDAE CR4≈0.29455、CR8≈0.32483、CR16≈0.39019。重构损失按 CR 正确排序，且 200 epoch 相比 120 epoch 继续下降。
- Fig9 全 SNR mean RMSE 均值：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。
- 同 CR 对比：FreqDAE-CR4 比 DAE-CR4 平均差约 +3.13 m；FreqDAE-CR8 比 DAE-CR8 差约 +2.53 m；FreqDAE-CR16 比 DAE-CR16 好约 -1.33 m。
- 低 SNR(≤0 dB)：DAE-CR4 最好，Raw/DAE-CR8/FreqDAE-CR4 接近；FreqDAE-CR8/16 虽优于冻结 DAE-CR16，但均不如冻结 DAE-CR8，更不如 DAE-CR4。
- 高 SNR(≥10 dB)：Raw 最好；DAE-CR4 次优；FreqDAE-CR16 和 DAE-CR16 接近，FreqDAE-CR8 反而略差。median/trimmed 指标大体仍按 CR 排序，mean 的局部反转主要受少量定位离群点影响。

### 方法判断
- FreqDAE v1 不是最终创新主线。它证明“频域任务感知”方向有一定低维 CR16 收益，但 soft pre-emphasis + Fisher-weighted spectral NMSE 没能让额外维度转化为更强定位或重构优势。
- 继续单纯延长训练意义不大；200 epoch 已经说明主要瓶颈在结构/目标设计，而不是训练轮数。
- 下一步应转向 FreqDAE v2：嵌套 latent、显式 pairwise cross-spectrum/GCC task branch、SNR/CR adaptive loss 和频域 mask 正则，而不是继续调 v1 的 `spectral_blend`。

### 新发现问题
- Fig9 中 `Cao2017-DFT-AML` 颜色仍与 Chen DAE 蓝色系接近，需改为独立的青绿/青色系。
- Fig2 多 CR 线条图例仍用 `DAE CR=*`，在 innovation mode 下应改为 `FreqDAE CR=*`。
- Zhai-CRLB 的 direct TDOA 误差随 SNR 单调下降，但 localization mean RMSE 高 SNR 回升；需新增 top-k worst/outlier-rate/WLS residual/GDOP 诊断，判断是 partial-Fourier 固定离群、WLS 几何病态，还是少数 pair 错峰。

### 下一步待审批
1. 小改图表语义与颜色：Cao2017 换色、Fig2 根据 active model 改 legend、Fig9 增加 “DAE-only vs FreqDAE-only” focused view 或 inset。
2. 增加 outlier diagnostics：保存每个方法每个 SNR 的 p90/p95/max、outlier rate、WLS residual、top-k worst sample 信息。
3. 新增 innovation eval-only 模式：可直接加载某个 FreqDAE 结果目录模型和冻结 Chen-DAE，不重新训练，只重跑评估/重绘/诊断。
4. 开始 FreqDAE v2 设计实现，优先采用嵌套 latent + pairwise cross-spectrum/GCC task branch。

## 2026-07-05 最新上下文补充：FreqDAE 200 epoch 公平复核入口已就绪

### 本轮执行内容
- 用户批准上一轮建议，并要求增强所有 Fig 可读性、Fig9 补齐冻结 DAE 的所有 CR。
- `main.py` 新增 `freq_task_200_final`，并把默认 `USER_EXPERIMENT_MODE` 切到该模式；PyCharm 直接运行 `main.py` 将训练 `FrequencySelectiveDAE` CR4/8/16 各 200 epoch。
- `freq_task_fast_final` 仍保留为 120 epoch 快速筛选模式；如需回到快速筛选，只需在 `main.py` 顶部把 `USER_EXPERIMENT_MODE` 改回 `"freq_task_fast_final"`。
- Fig9 现在会加载冻结 Chen-style `DAE-CR4/8/16`，并与 `FreqDAE-CR4/8/16`、Raw、DFT/Hadamard/PCA/Cao/Zhai 强基线同图比较，解决旧版只有冻结 `DAE-CR16`、无法逐 CR 对照的问题。
- `train.py` 训练日志标题改为真实模型名，例如 `Training FrequencySelectiveDAE (CR=4)`，避免把创新模型误读为冻结 DAE。
- 图表样式已调整：Chen DAE 为蓝色系，FreqDAE 为红橙色系，DFT 为紫色系，Hadamard 为橙棕色系，PCA 为绿色系；整体降低线宽、缩小 marker、提高曲线类别区分度。Fig2 创新模式标题会带 `FrequencySelectiveDAE`。

### 验证状态
- 两套 `py_compile` 均通过。
- PyTorch smoke 通过：`FrequencySelectiveDAE` forward 输出形状正确，Fig9 强基线主图与 zoom 可同时绘制 13 条方法曲线。
- `git diff --check` 无空白错误，仅有 Windows 行尾提示。

### 待运行事项
- 尚未运行完整 `freq_task_200_final`，没有新的正式 RMSE。
- 预计耗时高于 `20260705_165958` 的 136.9 min；按 120→200 epoch 线性估计，三 CR 训练与评估总耗时可能约 3.5-4 小时。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`。
2. 回读新目录的 `run.log`、`plot_data.pkl`、Fig1-Fig3、Fig9、`tables/fig9_baselines.csv` 和 `tables/fig9_method_diagnostics.csv`。
3. 判断 200 epoch 后 FreqDAE 是否保持低 SNR 优势、是否改善 CR16 高 SNR 不稳定、是否形成比冻结 Chen-DAE 更清晰的 CR 阶梯。

## 2026-07-05 最新上下文补充：`20260705_165958` FreqDAE v1 正式结果已审查

### 结果路径与配置
- 已读取最新结果目录：`运行结果/20260705_165958`，包括 `run.log`、`plot_data.pkl`、Fig1-Fig9、`tables/fig9_baselines.csv` 和 `tables/fig9_method_diagnostics.csv`。
- 配置：`EXPERIMENT_MODE=freq_task_fast_final`，active model=`FrequencySelectiveDAE`，loss=`freq_task_mse`，CR4/8/16 均为 full-data final-only 120 epochs；冻结 Chen-style DAE 参考模型来自 `运行结果/20260702_000925`。
- 评估协议：urban8 fixed eval set、all-pair WLS、physical lag gate=48 samples。Fig1-Fig3 展示的是本轮主动训练的 FreqDAE CR4/8/16；Fig6-Fig8 仍展示冻结 Chen-DAE CR16 与传统/强基线；Fig9 才是创新模型与冻结复现基线的同图比较。

### 关键结果
- 训练 loss：CR4/8/16 最终 ValTotal 约为 0.319/0.347/0.406；CR8 和 CR16 到 120 epoch 仍有下降趋势，CR4 末期轻微波动。
- Fig9 全 SNR mean RMSE 均值：Raw≈15.720 m，冻结 `DAE-CR16`≈21.380 m，`FreqDAE-CR4`≈18.049 m，`FreqDAE-CR8`≈18.757 m，`FreqDAE-CR16`≈21.566 m。
- 低 SNR(≤0 dB)：`FreqDAE-CR4/8/16` 均优于冻结 `DAE-CR16`，其中 CR4 最好；说明频域任务感知 v1 的低 SNR 定位方向有效。
- 高 SNR(≥10 dB)：`FreqDAE-CR4/8` 略优于冻结 `DAE-CR16`，但 `FreqDAE-CR16` 明显不稳；Raw 仍是当前合成/LOS/WLS 协议下最强参考。
- Fig8 与 `20260705_140948` 的公共方法数值完全一致，原因是 Fig8 不包含新 FreqDAE，仍使用冻结复现模型和未变的强基线。

### 新发现问题
- 图表语义容易误导：日志标题和 Fig1-Fig3 仍容易让人以为“DAE”是冻结 Chen-DAE，实际是当前 active model `FrequencySelectiveDAE`。
- FreqDAE v1 的高 SNR 重构质量弱于 paper_repro v3.1；频域 soft pre-emphasis + 固定 Fisher spectral NMSE 不是最终创新结构，只是低风险首版。
- 120 epoch 与 Chen-DAE 复现基线的 200 epoch/CV 训练协议不完全公平。当前结果只能作为 fast-screening，不能作为最终论文对比。

### 下一步建议
1. 先修图表/日志语义：Fig1-Fig3 和日志明确标注 `FreqDAE`，Fig6-Fig8 明确标注冻结 `Chen-DAE`。
2. 做公平训练复核：至少新增 `freq_task_200_final`，让 FreqDAE 用 200 epoch final-only 跑一版；若仍有潜力，再做与 paper_repro 对齐的 CV+best-epoch 协议。
3. 进入 FreqDAE v2：加入嵌套 latent、显式 cross-spectrum/GCC task branch 或 SNR-adaptive spectral blend，解决 CR16 高 SNR 不稳与 CR4/8 曲线区分度不足。

## 2026-07-05 最新上下文补充：频域任务感知创新模式 v1 已接入

### 当前代码状态
- 当前分支：`NewMethod-Codex`。
- 默认运行方式已按用户最新要求切到：`main.py` 顶部 `USER_EXPERIMENT_MODE="freq_task_fast_final"`。
- `paper_repro`、`paper_repro_fast_final`、`paper_repro_eval_only` 分支逻辑未删除、未改训练目标；如需回到复现基线，只需把 `USER_EXPERIMENT_MODE` 改回 `"paper_repro_eval_only"` 或 `"paper_repro"`。
- 新增创新模型 `FrequencySelectiveDAE`：保留原 `DAE` backbone 和相同 CR latent 维度定义，在 backbone 前加入可学习频域 soft pre-emphasis；高频先验来自 RMS bandwidth/Fisher delay-information 动机，但不硬选频点，也不改变最终 GCC/WLS 评估链路。
- 新增创新训练目标 `freq_task_mse`：`time-domain NMSE` 保持波形重构，`Fisher-weighted spectral NMSE` 加强对高 RMS 带宽/TDOA 敏感频率成分的保护；默认 `spectral_blend=0.30`、`spectral_power=2.0`。
- 新增默认创新训练协议：`freq_task_fast_final`，三种 CR 都是 full-data final-only 训练；默认 `USER_FREQ_TASK_EPOCHS=120`；不重训 Chen 复现模型；训练创新 `FreqDAE-CR4/8/16` 后，加载冻结 `运行结果/20260702_000925/model_cr*.pt` 作为 Chen-DAE 参考。
- 新增结果输出：Fig9 `Frequency-Task DAE vs Reproduction and Strong Baselines`，比较 `Raw`、冻结 `DAE-CR16`、`FreqDAE-CR4/8/16`、DFT/Hadamard/PCA/Cao/Zhai 等方法；`plot_data.pkl` 保存 `fig9_results`，`tables/` 导出 `fig9_baselines.csv/md` 和 `fig9_method_diagnostics.csv/md`。

### 已完成验证
- 两套 `py_compile` 通过。
- 小样本创新训练/评估 smoke 通过，输出 `FREQ_SMOKE_OK FrequencySelectiveDAE freq_task_mse [...]`。
- Fig9 绘图与导出 smoke 通过，输出 `FIG9_EXPORT_SMOKE_OK`。
- `git diff --check` 无空白错误，仅提示 Windows 行尾转换。

### 当前未解决问题
- 尚未完整运行 `main.py`，因此没有正式创新性能结论。
- `FrequencySelectiveDAE` 是创新 v1 骨架，是否能形成 `CR4 > CR8 > CR16`、是否低 SNR 优于 Raw/Chen-DAE，需要正式结果判断。
- 默认 120 epoch 是为了比 v3.1 的 CV+final 大幅降耗；如结果显示未收敛，可再提高 epoch 或增加更强的 task branch。

### 下一步
1. 用户在 PyCharm 直接运行 `main.py`。
2. 读取新结果目录的 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3、Fig6-Fig9、`tables/fig9_baselines.csv` 和 `tables/fig9_method_diagnostics.csv`。
3. 判断是否进入 v2 创新：例如嵌套 latent 共享模型、显式 cross-spectrum/GCC task branch、SNR-adaptive spectral blend 或 WLS uncertainty head。

## 2026-07-05 最新上下文补充：Fig3 与 Fig6-Fig8 lag gate 协议已统一

### 本轮执行内容
- 用户批准执行上一轮方案，但要求创新方案只先给设计、不急于实现。本轮只修改评估协议，不实现创新模型。
- `evaluate.py`：`UrbanLocalizationExperiment` 新增 `tdoa_lag_limit_samples` 参数，并在 Geometry/Raw/Clean/DAE 的 `_localization_errors_from_batch()` 调用中统一传入；`localization_config` 也保存该字段。
- `main.py`：构造 Fig3/Monte Carlo urban localization 实验时传入 `method_tdoa_lag_limit`，与 Fig6-Fig8 method comparison 的 physical lag gate 保持一致。

### 验证状态
- 两套编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py analyze_results.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- 小样本 urban8 smoke 通过：`UrbanLocalizationExperiment(..., tdoa_lag_limit_samples=24)` 能运行，返回配置中 `tdoa_lag_limit_samples=24.0`。
- `git diff --check` 无空白错误，仅有 Windows 行尾提示。

### 当前待运行状态
- PyCharm 直接运行 `main.py` 会重新生成口径统一后的 Fig3/Fig6/Fig7/Fig8；默认仍为 `paper_repro_eval_only`，不训练，加载冻结 v3.1 模型。
- 预期变化：Fig3 的 Raw/DAE/Clean 定位结果会接近 Fig6-Fig8 中对应 waveform 方法的结果；`plot_data.pkl` 中 `mc_results[0]['localization_config']['tdoa_lag_limit_samples']` 应记录约 48 samples。

### 创新方案状态
- 代码未进入创新方案实现。下一步先给用户完整设计方案供审批，建议以“频域任务感知双分支压缩/重构”作为主线，并继续沿用当前统一后的 urban8 + fixed eval set + all-pair WLS + physical lag gate 公平协议。

## 2026-07-05 最新上下文补充：`20260705_140948` Fig8/Fig8 supplement 结果审查

### 结果路径与配置
- 已读取最新结果目录：`运行结果/20260705_140948`，包括 `run.log`、`plot_data.pkl`、Fig6-Fig8 SVG、`tables/*.csv`。
- 配置：`paper_repro_eval_only`，冻结模型源 `运行结果/20260702_000925`，`baseline_cr=16`，`dft_mode=scs_lite`，`dft_direct_source=DFT-train-power`，physical lag gate=48 samples，`strong_diagnostic_supplement=True`。
- 本轮已运行 `analyze_results.py`，在该目录 `tables/` 下生成 `analysis_*` pandas 汇总表。

### 关键结果
- Fig3/DAE 近似复现基线与 `20260702_000925` 完全一致：平均 mean RMSE 为 Geometry≈0、Clean≈5.105、CR4≈17.839、Raw≈19.996、CR8≈21.172、CR16≈27.757 m。
- Fig6/Fig7 主结果与 `20260704_195946` 基本一致，说明本轮改动没有影响 DFT/Hadamard/PCA/DAE 主链路。
- Fig8 主图已按新口径精简，不再包含 `Cao2020-HighFC` 和 `Zhai-Phase-Superposition`。Fig8 主图全 SNR mean RMSE 均值：Raw≈15.720、DAE-CR16≈21.380、Cao2017-DFT-AML≈29.387、DFT≈32.162、PCA≈34.834、Zhai-CRLB-Decimation≈37.209、Hadamard≈38.783、DFT-Fisher-Direct≈40.662 m。
- Fig8 supplement 中，`Cao2020-HighFC` 从 `20260704_195946` 的全 SNR mean≈43.099 m 小幅改善到≈42.717 m；median/trimmed 均改善，高 SNR mean 从≈16.244 m 变差到≈18.380 m，但高 SNR median/trimmed 反而改善，说明 mean 被少数离群定位失败影响。
- `Zhai-Phase-Superposition` 从全 SNR mean≈51.584 m 改善到≈48.462 m；低/中 SNR 改善明显，高 SNR mean 略差，但 median/trimmed 改善。它仍显著弱于 `Zhai-CRLB-Decimation`，不适合进入 Fig8 主图。

### 新发现的问题
- Fig3 与 Fig6-Fig8 的 Raw/DAE 数值口径不完全一致：Fig6-Fig8 的 method comparison 使用 physical TDOA lag gate=48 samples，而 Fig3 的 `UrbanLocalizationExperiment` 当前没有传入该 gate。例：Fig3 中 CR16 平均 mean≈27.757 m，但 Fig8 中 `DAE-CR16`≈21.380 m。这是后续需要优先统一的评估协议问题。
- Cao2020 segmented FC 虽改善了典型误差和 direct TDOA 诊断，但 high-FC 单侧频带仍有较高 sidelobe，且对低 SNR 噪声敏感；当前仍只能作为 supplement proxy。

### 待审批下一步
1. 优先修正 Fig3 与 Fig6-Fig8 的 lag gate 一致性，让主复现图和基线对比图使用同一 TDOA 搜索协议。
2. 之后重跑 `paper_repro_eval_only`，确认 Fig3 Raw/DAE 与 Fig8 DAE-CR16 口径一致。
3. 冻结当前 Fig8 主图口径，把 `Cao2020-HighFC` 和 `Zhai-Phase-Superposition` 保留在 supplement；不建议继续把它们强行调进主图。
4. 若用户确认进入创新方案，下一步应基于统一评估协议设计创新 DAE/频域任务感知模型，而不是继续微调 proxy baseline。

## 2026-07-05 最新上下文补充：Fig8 主/补充轨道清理、Cao/Zhai 候选增强与 pandas 分析脚本

### 本轮已执行修改
- 用户批准执行 A1-A3 + C1-C2 + B1-B2。本轮已修改代码，不运行完整训练或完整 eval-only。
- `task_baselines.py` 新增 `Cao2020SegmentedFCEstimator`：使用 Cao2020-style high-FC selected bins，按频率段分别计算 delay-alignment likelihood，再做归一化 incoherent 累加，避免旧单窗 high-FC proxy 的强 alias 风险。
- `ZhaiPhaseSuperpositionEstimator` 从纯 unit phasor 改为默认 `sqrt_power` 加权的 delay-compensated phase superposition，保留相位叠加思想，同时不完全丢弃幅度/相干度信息。
- `main.py` 将 Fig8 主图精简为正式强基线集合：`Raw`、`DAE-CR16`、`DFT`、`DFT-Fisher-Direct`、`Cao2017-DFT-AML`、`Zhai-CRLB-Decimation`、Hadamard、PCA；`Cao2020-HighFC` 与 `Zhai-Phase-Superposition` 继续计算，但进入新增 `Fig8_Supp_Strong_Baseline_Diagnostics_CR16` 补充图。
- `export_results.py`、`replot.py` 已支持 `fig8_supp_results` 的导出和重绘。
- 新增 `analyze_results.py`：基于 pandas 读取 `plot_data.pkl`，按 all/low/mid/high SNR 自动导出 RMSE 排名和数值诊断表到 `tables/analysis_*.csv/md`。
- `update_dft_fisher_direct.py` 当前根目录不存在；无需删除。历史文档中仅作为过去一次性脚本记录保留。

### 已完成验证
- 两套编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py analyze_results.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- 已知整数时移 sanity：`DirectDFTTDOAEstimator`、`Cao2017DFTAMLEstimator`、`Cao2020SegmentedFCEstimator`、`ZhaiPhaseSuperpositionEstimator` 对 `np.roll(x, 7)` 均返回 `-7.0`。
- 小样本 urban8 Fig8 smoke 通过，确认 Fig8 主图和 Fig8 supplement 的方法过滤、绘图、zoom 逻辑可运行。
- 已运行 `analyze_results.py` 读取 `运行结果/20260704_195946` 并生成 `tables/analysis_*` 汇总表；该动作只分析旧结果，不产生新正式实验指标。
- `git diff --check` 无空白错误，仅提示 Windows 行尾转换。

### 当前待运行状态
- PyCharm 直接运行 `main.py` 仍默认 `paper_repro_eval_only`，加载冻结 v3.1 模型 `运行结果/20260702_000925`，不训练。
- 新结果目录预期新增/更新：
  - `Fig8_Strong_TaskAware_Baselines_CR16.svg`
  - `Fig8_Strong_TaskAware_Baselines_CR16_SNR_Zooms.svg`
  - `Fig8_Supp_Strong_Baseline_Diagnostics_CR16.svg`
  - `Fig8_Supp_Strong_Baseline_Diagnostics_CR16_SNR_Zooms.svg`
  - `tables/fig8_*` 与 `tables/fig8_supp_*`
- 下一步：用户运行 `main.py` 后回读新目录，重点比较新 `Cao2020-HighFC` segmented estimator 与新 `Zhai-Phase-Superposition` weighted estimator 是否相对 `20260704_195946` 改善。

## 2026-07-05 最新上下文补充：`20260704_195946` Fig8 结果审查

### 结果路径与配置
- 已读取最新结果目录：`运行结果/20260704_195946`。
- 配置：`paper_repro_eval_only`，模型源 `运行结果/20260702_000925`，`baseline_cr=16`，`dft_mode=scs_lite`，`dft_direct_source=DFT-train-power`，`physical_lag_gate=True`，lag limit 约 48 samples，`strong_baselines=True`，`phase_superposition=True`。
- 结果证据：`run.log`、`plot_data.pkl`、`tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv` 与 Fig8 SVG。

### Fig8 关键结论
- 全 SNR mean RMSE 均值排序：Raw≈15.720 m < DAE-CR16≈21.380 m < Cao2017-DFT-AML≈29.387 m < DFT≈32.162 m < PCA≈34.834 m < Zhai-CRLB-Decimation≈37.209 m < Hadamard≈38.783 m < DFT-Fisher-Direct≈40.662 m < Cao2020-HighFC≈43.099 m < Zhai-Phase-Superposition≈51.584 m。
- Cao2020 重做有效但不够强：相对旧 `20260704_163145`，全 SNR mean RMSE 从约 92.340 m 降到约 43.099 m，高 SNR 从约 76.841 m 降到约 16.244 m；但 high-FC 单侧连续窗的 direct sidelobe ratio 仍约 0.96，导致离群定位误差仍大。
- `Zhai-Phase-Superposition` 表现最差：高 SNR direct mean abs TDOA error 约 0.668 samples，within-1 约 0.751，显著弱于同组选频的 `Zhai-CRLB-Decimation` 高 SNR direct mean abs 约 0.067 samples、within-1 约 0.998。当前相位叠加曲线不宜作为正式强基线。
- 高 SNR 下 `Zhai-CRLB-Decimation`、`DFT-Fisher-Direct`、Hadamard 优于或接近 DAE-CR16；低 SNR 下 Raw 和 DAE 更稳，频域/压缩域 direct 方法普遍被噪声和 partial-Fourier 旁瓣击穿。

### 待审批方案方向
1. 精简 Fig8 主图：保留稳定且可解释的强基线，把 `Cao2020-HighFC` 和 `Zhai-Phase-Superposition` 标注为 proxy/消融或移出主图。
2. 如继续修 Cao2020，下一步应实现更接近原文的 incoherent segmented averaging，而不是继续调单窗 high-FC。
3. `update_dft_fisher_direct.py` 当前只是一次性增量脚本，建议删除或移动到 legacy/tool 脚本目录，避免用户误以为它是常规入口。

## 2026-07-04 最新上下文补充：Cao2020-HighFC 已重做，代码待 PyCharm 运行

### 本轮修改结论
- 用户要求“重做 Cao2020，然后让我执行 main”。本轮已按 Cao 2020 的 CRB/partial Fourier coefficient 动机重做当前 Fig8 中的 `Cao2020-HighFC` 代理实现。
- 旧实现直接选最高 `|f|` 的两端 FFT bins，容易形成两端拼接的梳状 partial-Fourier steering response，导致高 SNR direct TDOA 仍有强混叠。
- 新实现不再硬选两端最高频，而是在训练集平均频谱上扫描单侧连续高频 Fourier coefficient 窗，按 delay Fisher/CRB 信息、保留功率、高频偏好和物理 lag 窗内旁瓣惩罚共同打分，选择更稳定的 `DFT-Cao2020-CRB` bins。
- `main.py` 中 Fig8 的 `Cao2020-HighFC` 现在优先复用 `DFT-Cao2020-CRB.selected_bins` 构造 `Cao2017DFTAMLEstimator`；只有缺少该 strong variant 时才回退旧的 `high_frequency_dft_bins()`。

### 已完成验证
- 两套编译均通过：
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`
- 小样本 clean urban8 对比显示，新 Cao2020 选频相对旧两端最高频硬选明显降低 direct TDOA 误差：旧 two-edge high-frequency 约 mean abs 4.08 samples、within1 约 0.23；新 `DFT-Cao2020-CRB` 约 mean abs 1.07 samples、within1 约 0.80。该 smoke 只用于验证修正方向，不是正式论文指标。
- `git diff --check` 无空白错误，仅提示 Windows 行尾转换。

### 下一步
1. 用户直接在 PyCharm 运行 `main.py`，无需改命令行环境变量。
2. 新结果目录中重点检查 `Fig8_Strong_TaskAware_Baselines_CR16.svg`、`Fig8_Strong_TaskAware_Baselines_CR16_SNR_Zooms.svg`、`tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv` 和 `plot_data.pkl`。
3. 重点复核 `Cao2020-HighFC` 的高 SNR direct mean abs TDOA error 是否明显低于 `运行结果/20260704_163145` 中约 6.410 samples 的旧值，并比较其与 `Zhai-CRLB-Decimation` 的差距。

## 2026-07-04 最新上下文补充：`20260704_163145` 结果审查、phase-superposition 修正与 zoom 图合并

### 最新结果审查结论
- 已读取最新结果目录：`运行结果/20260704_163145`，包括 `run.log`、`plot_data.pkl`、`tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv` 以及 Fig6/Fig7/Fig8 SVG。
- 本次运行配置：`paper_repro_eval_only`，冻结模型源为 `运行结果/20260702_000925`，`baseline_cr=16`，`dft_direct_source=DFT-train-power`，物理 lag gate 开启且约为 48 samples，`strong_include_phase_superposition=False`。
- Fig8 mean RMSE 全 SNR 均值：Raw 约 15.720 m，DAE-CR16 约 21.380 m，DFT 约 32.162 m，DFT-Fisher-Direct 约 40.662 m，Cao2017-DFT-AML 约 29.387 m，Cao2020-HighFC 约 92.340 m，Zhai-CRLB-Decimation 约 37.209 m，Hadamard 约 38.783 m，PCA 约 34.834 m。
- Cao2020-HighFC 高 SNR 仍很差不是定位 WLS 的主因，而是 direct TDOA 已经失败：高 SNR direct mean abs TDOA error 约 6.410 samples，within-1 rate 约 0.155，物理 lag 内 alias/sidelobe 很强。当前实现只是“高频边缘 partial Fourier + AML”的粗代理，不等价于 Cao 2020 的完整 incoherent/CRB 方案，应继续校准或在图中标注为 proxy。
- Zhai-CRLB-Decimation 在部分高 SNR 点优于 Raw 是可信现象，不一定是 bug：高 SNR direct mean abs TDOA error 约 0.067 samples，within-1 rate 约 0.998，说明 CRLB/FIM 选频在高 SNR 可形成比 noisy Raw GCC 更尖锐的 direct TDOA；但低 SNR 平均很差，所以它是强但偏科的高 SNR baseline。

### 本轮代码状态
- `task_baselines.py`：已把 `ZhaiPhaseSuperpositionEstimator` 从旧的“按观测相位先分组再用组中心频率估计”改为“候选时延补偿后的 selected-bin unit phasor 相干叠加”。新实现默认使用 Zhai-CRLB 选出的同一组 DFT bins，目标是比较“幅度加权 AML”与“相位叠加”的差异。
- `main.py`：`USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=True`，PyCharm 直接运行 `main.py` 会把 `Zhai-Phase-Superposition` 纳入新的 Fig8；旧结果 `20260704_163145` 的 pkl 没有该曲线，需下一次 eval-only 重新运行后才会有正式指标。
- `evaluate.py`、`main.py`、`export_results.py`、`replot.py`：Fig6/Fig7/Fig8 的低 SNR 与高 SNR zoom 不再分别生成两个 SVG，而是统一生成一张 `*_SNR_Zooms.svg`，内部为 1x2 两个子图。
- 已对历史目录 `运行结果/20260703_191104` 和 `运行结果/20260704_163145` 执行离线重绘，仅重绘已有 pkl 数据，不重新评估；旧的 `*_LowSNR_Zoom.svg` 和 `*_HighSNR_Zoom.svg` 已在这两个目录中删除，替换为合并版 `*_SNR_Zooms.svg`。

### 验证状态
- 已通过默认 Python 和 PyTorch Conda 环境的 `py_compile`。
- 新 phase-superposition 已通过已知整数时移 sanity：对 `np.roll(x, 7)`，Direct DFT 与新的 phase-superposition 均返回 `-7.0`，符号与现有 GCC/TDOA 评估链一致。
- 已用 PyTorch Conda 解释器运行 `replot.py` 重绘 `20260703_191104` 和 `20260704_163145` 的合并 zoom 图；没有重新运行完整 `main.py`，因此没有新的正式 phase-superposition RMSE。

### 下一步建议
1. 在 PyCharm 直接运行 `main.py`，生成新的 `paper_repro_eval_only` 结果目录，使 Fig8 正式包含 `Zhai-Phase-Superposition`。
2. 回读新目录的 `run.log`、`plot_data.pkl`、`tables/fig8_*` 和 Fig8 SVG，重点比较 `Zhai-CRLB-Decimation` 与 `Zhai-Phase-Superposition`：如果相位叠加低 SNR 更稳，可作为后续创新强基线；如果仍只在高 SNR 好，应在论文中定位为高 SNR/task-aware baseline。
3. Cao2020-HighFC 下一步不建议继续用“最高频硬选”代表原文，应回到 Cao 2020 的 CRB/incoherent processing 公式，至少实现带混叠惩罚和噪声稳健加权的可解释版本，再决定是否保留在主 Fig8。

## 2026-07-04 最新上下文补充：Fig8 强基线轨道已接入，待正式 eval-only 运行

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式仍为 `main.py` 顶部普通变量控制，`USER_EXPERIMENT_MODE="paper_repro_eval_only"`，`ALLOW_ENV_OVERRIDES=False`。
- 本轮按用户批准执行代码修改：不把新的强基线继续塞进原 Fig7，而是新增独立 `Fig8_Strong_TaskAware_Baselines_CR16` 路线。Fig7 保留任务感知代表方法概览；Fig8 用于后续“创新方案 vs Chen DAE vs 更新强基线”的公平对比。

### 本轮核心修改

- `task_baselines.py`
  - 新增 `Cao2017DFTAMLEstimator`：用 selected DFT bins 的互谱相位似然/AML 风格目标直接估计 pairwise TDOA。
  - 新增 `ZhaiPhaseSuperpositionEstimator`：保留为 pair-adaptive cross-spectrum phase-superposition 诊断类，但默认不进入正式 Fig8，因为简化 group-center 近似未通过已知延迟 sanity check，严格版需继续对照 Zhai 第三章推导。
  - 抽取 direct estimator 质量统计与诊断 helper，保持 Direct/Cao/Zhai 接口一致。
- `baselines.py`
  - 新增 `select_zhai_crlb_dft_bins()` 与 `_zhai_crlb_bins()`：Zhai-style CRLB/FIM greedy 选频代理，目标是最大化 delay Fisher 信息对应的加权频谱二阶中心矩，同时惩罚物理 lag 窗内的 partial-Fourier 旁瓣。
  - `build_traditional_baselines()` 新增 `include_strong_variants`，默认强基线运行时构建 `DFT-Zhai-CRLB` 供 direct estimator 使用。
- `main.py`
  - 新增普通配置变量 `USER_RUN_STRONG_BASELINES=True` 与 `USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=False`。
  - 默认 Fig8 strong baseline 包含：`Raw`、`DAE-CR16`、`DFT`、`DFT-Fisher-Direct`、`Cao2017-DFT-AML`、`Cao2020-HighFC`、`Zhai-CRLB-Decimation`、`Hadamard`、`PCA`。
  - 正式运行后 `plot_data.pkl` 会新增 `fig8_results`，根目录会新增 Fig8 主图与 low/high SNR zoom SVG，`tables/` 会导出 `fig8_baselines.csv/md` 与 `fig8_method_diagnostics.csv/md`。
- `evaluate.py`、`export_results.py`、`replot.py`
  - 支持 `plot_kind="strong"`、Fig8 样式、Fig8 导出和从旧 pkl 重绘。

### 已完成验证

- 两套编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py update_dft_fisher_direct.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- direct estimator 已知延迟 sanity：`DirectDFTTDOAEstimator` 和 `Cao2017DFTAMLEstimator` 对 `np.roll(x, 7)` 返回 `-7.0`；简化 `ZhaiPhaseSuperpositionEstimator` 未稳定返回正确延迟，因此默认不纳入 Fig8。
- 小样本 urban8 smoke：`build_traditional_baselines(include_strong_variants=True)`、`run_urban_method_comparison()`、`filter_method_comparison_data()` 和 `plot_method_comparison(plot_kind="strong")` 均可运行。该 smoke 仅验证流程，不产生论文可用指标。
- `git diff --check` 仅提示 Windows 行尾转换，无空白错误。

### 当前判断与下一步

- 新强基线应独立成 Fig8 路线，而不是继续塞进 Fig7。原因是 Fig7 已经承担 task-aware 概览和诊断作用，强基线集合会越来越多，单图过载会牺牲可读性。
- Fig8 当前默认强基线仍属于“第一版可运行实现”：Cao/Zhai 方法均在同一 fixed eval set、LOS mask、物理 lag gate 和 all-pair WLS 下评估，但还不是对每篇原文所有工程细节的完全复刻。
- 下一步：用户在 PyCharm 直接运行 `main.py`，读取新结果目录中的 `run.log`、`plot_data.pkl`、`Fig8_Strong_TaskAware_Baselines_CR16*.svg` 和 `tables/fig8_*`，再判断哪些强基线值得作为创新方案正式对比对象。

## 2026-07-03 最新上下文补充：Balanced DFT-Fisher 已实现并增量更新 `20260703_191104`

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式仍为 `main.py` 顶部普通变量控制，`USER_EXPERIMENT_MODE="paper_repro_eval_only"`，`ALLOW_ENV_OVERRIDES=False`。
- 本轮已按用户批准执行代码修改、单方法增量评估和重绘；没有重训 DAE，也没有完整重跑所有传统 baseline。

### 本轮代码修改

- `baselines.py`
  - 新增 `_balanced_fisher_bins()`：以训练集平均频谱功率乘频率平方作为 Fisher/FIM 候选分数，但在 greedy 选频时显式惩罚物理 TDOA lag 窗内的最大旁瓣与 RMS 旁瓣。
  - 新增 `select_balanced_fisher_dft_bins()`，供增量脚本直接选频，避免为了 DFT-Fisher 单独更新而重新拟合 PCA。
  - `build_traditional_baselines()` 新增 `dft_lag_limit_samples`，所有 `fisher_power` / `DFT-Fisher` 变体默认使用 balanced Fisher/FIM 选频。
- `main.py`
  - 构建传统 baseline 时把当前 method comparison 使用的物理 TDOA lag gate 传入 DFT-Fisher 选频器。
- `evaluate.py`
  - `DFT-Fisher-Direct` 图例改为 `Balanced Fisher direct`。
- `export_results.py`
  - summary 中明确 Fig7 的 `DFT-Fisher-Direct` 是 balanced Fisher/FIM + physical-lag sidelobe control。
- `update_dft_fisher_direct.py`
  - 新增脚本，可在 PyCharm 中直接修改顶部 `DEFAULT_RESULT_DIR` 运行，也可命令行传入结果目录。
  - 功能：读取旧 `plot_data.pkl` 配置，重建 simulator，重算 `DFT-Fisher-Direct`，只替换该方法在 `baseline_all_results` / `fig7_results` 中的数据，更新 pkl、tables 和 SVG。

### 已执行验证与结果

- 编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py update_dft_fisher_direct.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- 小样本 alias smoke：
  - naive Fisher 物理 lag 内最大旁瓣约 `0.907`。
  - balanced Fisher 物理 lag 内最大旁瓣约 `0.483`。
- 已增量更新 `运行结果/20260703_191104`：
  - `DFT-Fisher-Direct` mean RMSE 平均值：`53.8813 m -> 40.6645 m`。
  - 完整选频 alias 诊断：`selected_bin_min=232`、`selected_bin_max=792`、`max_sidelobe_inside_physical_lag=0.456`、`max_sidelobe_outside_physical_lag=0.274`。
  - Fig7 当前均值：`Raw≈15.72 m`、`DAE-CR16≈21.38 m`、`DFT≈32.16 m`、`DFT-SCS-lite≈40.68 m`、`DFT-Fisher-Direct≈40.66 m`、`Hadamard≈38.78 m`、`PCA≈34.83 m`。
  - `DFT-Fisher-Direct` 低 SNR(≤0 dB)平均仍约 `91.79 m`，高 SNR(≥8 dB)平均约 `7.94 m`。
- SVG 文本检查确认：
  - Fig7 和 zoom 图显示 `Balanced Fisher direct`。
  - 不再出现旧 `Naive Fisher direct`。
  - 不再出现重复 `DFT-train-power-Direct`。

### 当前判断

- Balanced/FIM-greedy 修复了 naive `power*f^2` 选频产生的强物理 lag 内旁瓣，证明“高频对 TDOA 有贡献”这个动机并未错，错在硬选边缘高频导致 partial Fourier steering response 旁瓣过强。
- 但 `DFT-Fisher-Direct` 仍不是成熟强基线：它在高 SNR 可接近 Hadamard/Raw，但低 SNR 仍明显崩坏，说明下一步需要处理低 SNR 下的频点加权、PHAT/非 PHAT、稳健峰值选择或多窗融合。
- 当前结果目录 `运行结果/20260703_191104` 已可作为“balanced Fisher 修复后的 Fig7 版本”继续分析；如需完全干净的新目录，可再用 PyCharm 直接运行 `main.py` 生成新时间戳目录。

### 下一步建议

1. 若继续 DFT task-aware 基线，优先做低 SNR 稳健化：比较 direct cross-spectrum 的 PHAT、power weighting、SNR-aware bin weighting、多组 balanced bins voting。
2. 若进入创新方案，保留当前 Chen-style Fig6 与 task-aware Fig7 双轨，创新方法必须复用同一 `urban8 + fixed eval set + all-pair WLS + physical lag gate` 链路。
3. 不建议再把零填充 IDFT 伪重构作为 DFT task-aware 主路径；它可作为 reconstruction ablation，但不是更合理的 TDOA direct 路径。

## 2026-07-03 最新上下文补充：Fig6/Fig7 精简与 `20260703_191104` SVG 重绘完成

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式仍为 `main.py` 顶部普通变量控制，`USER_EXPERIMENT_MODE="paper_repro_eval_only"`，`ALLOW_ENV_OVERRIDES=False`。
- 本轮已按用户审批执行代码修改和重绘，不重新训练、不重新评估。

### 本轮代码修改

- `baselines.py`
  - `build_traditional_baselines()` 新增 `include_legacy_variants=False` 参数。
  - 默认不再构建 `DFT-random-*`、`Hadamard-random-*`、`Hadamard-sequency` 这类已验证无论文依据或正式图价值有限的方法。
  - 仍保留 `DFT-Fisher`、`DFT-train-band`、`DFT-train-power`、`DFT-bandlimited`、`Hadamard-block-2` 等有诊断价值的方法。
- `main.py`
  - 默认 `USER_BASELINE_INCLUDE_LEGACY_VARIANTS=False`、`USER_BASELINE_RANDOM_VARIANT_SEEDS=0`。
  - 如果 Fig6 主线 `DFT` 已经使用某个 selected-bin source，则 Fig7 不再重复生成同 source 的 `*-Direct` 曲线；当前默认因此移除重复的 `DFT-train-power-Direct`。
  - Fig6 supplement order 精简为：`Raw / DAE-CR16 / DFT / DFT-SCS-lite / DFT-train-power / DFT-bandlimited / Hadamard / Hadamard-block-2 / PCA`。
  - Fig7 order 精简为：`Raw / DAE-CR16 / DFT / DFT-SCS-lite / DFT-Fisher-Direct / Hadamard / PCA`。
  - Fig6/Fig7 zoom order 继承对应完整图 order，保证 zoom 图只裁剪 SNR，不改变曲线集合。
- `evaluate.py`
  - `DFT` 图例会根据 `chen_fig6_dft_direct_source` 显示为 `DFT direct (train-power bins)`。
  - `DFT-Fisher-Direct` 图例改为 `Naive Fisher direct`，保留在 Fig7 中但明确其当前是待改进的 naive 方法。
- `replot.py`
  - 对旧 `plot_data.pkl` 应用当前推荐 Fig6/Fig7 方法集合，方便重绘旧结果。
  - 新增 `DAE_REPLOT_NO_SHOW=1`，支持无窗口自动重绘。

### 已执行验证与重绘

- 编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- 小样本 smoke 通过：
  - 默认不构建 random/sequency legacy 变体；
  - Fig6 zoom order 等于 Fig6 supplement order；
  - Fig7 zoom order 等于 Fig7 task-aware order；
  - Fig7 不含 `DFT-train-power-Direct`，但保留 `DFT-Fisher-Direct`。
- 已运行重绘命令并覆盖 `运行结果/20260703_191104` 的 SVG：
  - `Fig6_Chen_Traditional_Baselines_CR16.svg`
  - `Fig6_Supp_Chen_Baseline_Ablation_CR16.svg`
  - `Fig6_Supp_Chen_Baseline_Ablation_CR16_LowSNR_Zoom.svg`
  - `Fig6_Supp_Chen_Baseline_Ablation_CR16_HighSNR_Zoom.svg`
  - `Fig7_TaskAware_Baselines_CR16.svg`
  - `Fig7_TaskAware_Baselines_CR16_LowSNR_Zoom.svg`
  - `Fig7_TaskAware_Baselines_CR16_HighSNR_Zoom.svg`
  - 以及 replot 默认覆盖的 Fig1/Fig2/Fig3。
- SVG 文本检查确认：
  - Fig7 已无 `DFT-train-power-Direct`；
  - Fig7 和 zoom 图保留 `Naive Fisher direct`；
  - Fig6 supplement 已无 random/sequency/Fisher waveform 默认曲线。

### 下一步

1. 用户检查 `运行结果/20260703_191104` 中重绘后的 SVG。
2. 若图表组织认可，下一步建议实现更强的 balanced/FIM-greedy DFT 选频，目标是降低物理 lag 内窗函数旁瓣，而不是继续使用 naive `power*f²` 高通硬选。
3. 或者进入创新方法轨道，将创新模型迁移到同一 `urban8 + fixed eval set + all-pair WLS + lag gate` 公平对比链路。

## 2026-07-03 最新上下文补充：`20260703_191104` Fig6/Fig7 结果审查

### 结果路径与状态

- 最新结果目录：`运行结果/20260703_191104`。
- 已读取：`run.log`、`plot_data.pkl`、`tables/fig6_traditional_baselines.csv`、`tables/fig7_baselines.csv`、`tables/fig6_method_diagnostics.csv`、`tables/fig7_method_diagnostics.csv`。
- 本轮未修改 Python 代码，只做结果审查并提出下一步方案；方案需用户审批后执行。

### 关键结果

- 当前配置：
  - `paper_repro_eval_only`
  - `baseline_cr=16`
  - `dft_mode=scs_lite`
  - `dft_direct_source=DFT-train-power`
  - `physical_lag_gate=True`
  - `tdoa_lag_limit_samples=48`
  - `task_aware_baselines=True`
- Fig6/Fig7 mean RMSE 平均值：
  - `Raw`≈15.72 m
  - `DAE-CR16`≈21.38 m
  - `DFT` direct≈32.16 m
  - `DFT-train-power` waveform recon≈32.22 m
  - `DFT-bandlimited`≈31.65 m
  - `DFT-train-band`≈35.73 m
  - `Hadamard`≈38.78 m
  - `PCA`≈34.83 m
  - `DFT-Fisher` waveform recon≈52.95 m
  - `DFT-Fisher-Direct`≈53.88 m
- `DFT` 与 `DFT-train-power-Direct` 逐 SNR 完全重合，`maxabsdiff=0.0`。

### 问题诊断

- Fig7 中 `DFT` 与 `DFT-train-power-Direct` 是重复曲线。当前主线 `DFT` direct 就是用 `DFT-train-power` selected bins 构造的 direct estimator，因此 Fig7 不应再重复画 `DFT-train-power-Direct`。
- Fig6 zoom 曲线比完整 supplement 少，是因为 `supplement_zoom_method_order` 包含 `DFT-train-power-Direct` 和 `DFT-Fisher-Direct`，但 Fig6 视图已经把 direct 方法过滤掉，只剩 `DFT`。绘图函数遇到不存在的方法会跳过，所以 zoom 少线。
- Fig7 zoom 曲线比完整 Fig7 少，是因为 `taskaware_zoom_method_order` 与 `taskaware_method_order` 不一致，zoom order 少了 `DFT-Fisher` 等曲线。
- Fig6 supplement 方法过多，包含已验证无论文依据或诊断价值有限的方法：
  - `Hadamard-random*` 与 `Hadamard-sequency` 不应继续默认进入正式图；
  - `DFT-random*` 可只作为离线/诊断表，不必默认画；
  - DFT 变体需要按 Chen-style、工程诊断、task-aware 三层分轨。
- `DFT-Fisher` 明显差的根因不是“高频信息理论错”，而是当前实现选中两段高频边缘 bins `[287..318]` 与 `[706..737]`，在物理 lag 内形成强旁瓣：
  - alias diagnostics：`max_sidelobe_inside_physical_lag≈0.950`
  - 0/10/20 dB 下 `gcc_mean_sidelobe_ratio≈0.979/0.978/0.978`
  - 0/10/20 dB 下 `gcc_within_1_sample_rate≈0.220/0.383/0.405`
  - direct 版本同样差，说明问题在选频窗本身，而不是 IDFT 重构链路独有问题。

### 下一步待审批方案

1. 删除 Fig7 重复曲线：不再生成或不再绘制 `DFT-train-power-Direct`，保留 `DFT` 并在配置/图例中标明 source 为 `DFT-train-power`。
2. 修复 zoom order：Fig6 zoom 默认使用精简后的 `supplement_method_order`；Fig7 zoom 默认使用 `taskaware_method_order`，保证 zoom 图与完整图曲线集合一致。
3. 精简 Fig6 supplement：
   - 主图保留 `Raw / DAE-CR16 / DFT / Hadamard / PCA`；
   - supplement 建议保留 `Raw / DAE-CR16 / DFT / DFT-SCS-lite / DFT-train-power / DFT-bandlimited / Hadamard / Hadamard-block-2 / PCA`；
   - 默认移除 `DFT-Fisher`、`DFT-random*`、`Hadamard-random*`、`Hadamard-sequency`。
4. Fig7 task-aware 建议保留 `Raw / DAE-CR16 / DFT / DFT-SCS-lite / DFT-Fisher-Direct / Hadamard / PCA`，并把 `DFT-Fisher` waveform recon 移出 Fig7 或改入独立失败消融图。
5. 后续若继续做更强 DFT baseline，应实现 balanced/FIM-greedy 选频，约束物理 lag 内窗函数旁瓣，而不是继续使用 naive `power*f^2` 硬选边缘高频。

## 2026-07-03 最新上下文补充：Fig6/Fig7 direct DFT lag-gate 与 zoom 改造已完成

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式：`main.py` 顶部 `USER_EXPERIMENT_MODE="paper_repro_eval_only"`，`ALLOW_ENV_OVERRIDES=False`；PyCharm 直接运行默认不训练，加载冻结 v3.1 模型 `运行结果/20260702_000925`。
- 本轮已执行用户批准的 `20260703_164540` 后续修正方案，代码已进入待正式 eval-only 运行状态。

### 核心修改

- `task_baselines.py`
  - `DirectDFTTDOAEstimator.estimate_pair()` 新增 `lag_limit_samples`，只在物理可行 lag 范围内找峰。
  - 新增 `alias_diagnostics()`，记录 selected bins 间隔、强 alias 旁瓣、物理 lag 内/外最大旁瓣。
  - diagnostics 同步记录 `direct_lag_limit_samples`。
- `evaluate.py`
  - Raw/Clean/Geometry/DAE/waveform baseline 和 direct estimator 共用同一 `tdoa_lag_limit_samples`。
  - waveform 诊断的 GCC peak、false peaks、sidelobe 统计也基于同一 lag gate。
  - `plot_method_comparison()` 支持 `snr_min/snr_max/method_order/title_suffix`，用于 Fig6/Fig7 low-SNR/high-SNR zoom。
  - DFT direct、DFT recon、DFT-Fisher direct、DFT train-power direct 使用不同颜色、marker、线型，提高曲线区分度。
- `main.py`
  - 新增顶部普通变量：`USER_BASELINE_DFT_DIRECT_SOURCE="DFT-train-power"`、`USER_USE_PHYSICAL_TDOA_LAG_GATE=True`、`USER_TDOA_LAG_LIMIT_SAMPLES=None`、`USER_TDOA_LAG_MARGIN_SAMPLES=4`、`USER_EXPORT_METHOD_ZOOM_FIGURES=True`。
  - `TDOA_LAG_LIMIT_SAMPLES=None` 时按 urban8 区域对角线和采样距离自动估计物理 lag gate；当前默认约 48 samples。
  - Fig6 主线 `DFT` direct 默认使用非 oracle 的 `DFT-train-power` selected bins；`DFT-SCS-lite` 均匀梳状选频保留在补充图中作为 recon/aliasing 消融。
  - 正式运行会额外保存：
    - `Fig6_Supp_Chen_Baseline_Ablation_CR16_LowSNR_Zoom.svg`
    - `Fig6_Supp_Chen_Baseline_Ablation_CR16_HighSNR_Zoom.svg`
    - `Fig7_TaskAware_Baselines_CR16_LowSNR_Zoom.svg`
    - `Fig7_TaskAware_Baselines_CR16_HighSNR_Zoom.svg`
- `export_results.py` 与 `replot.py`
  - 同步支持 Fig6/Fig7 zoom 图导出和重绘；默认 `tables/` 仍不重复保存 Fig6/Fig7 SVG，除非显式开启图导出。

### 验证结果

- 两套编译通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`
  - `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`
- `git diff --check` 仅提示 Windows 行尾转换，无空白错误。
- PyTorch 小样本 urban8 smoke 通过：Direct DFT、物理 lag gate、method comparison 和 zoom plotting 均可运行。
- `export_results.py` 临时目录导出 smoke 通过，输出 `EXPORT_SMOKE_OK 11`，确认主图、补充图、low/high zoom SVG 生成链路可运行。

### 下一步

1. 用户在 PyCharm 直接运行 `main.py`，生成新的完整 `paper_repro_eval_only` 结果目录。
2. 读取新目录中的 `run.log`、`plot_data.pkl`、Fig6/Fig7 主图与 zoom SVG、`tables/fig6_*`、`tables/fig7_*`。
3. 重点判断：
   - `DFT` direct 是否因物理 lag gate 和 `DFT-train-power` source 恢复合理；
   - `DFT-SCS-lite` 是否仍表现为 uniform-comb aliasing 消融；
   - Fig6/Fig7 low/high zoom 是否解决曲线过密与样式区分问题。
4. 若 direct DFT 仍不稳定，再实现 Cao/Zhai/FIM-greedy 或 balanced-Fisher 选频，而不是回到零填充 IDFT 单路重构路径。

## 2026-07-03 最新上下文补充：`20260703_164540` 结果审查

### 结果路径与状态

- 最新结果目录：`运行结果/20260703_164540`。
- 已读取：`run.log`、`plot_data.pkl`、`tables/fig6_traditional_baselines.csv`、`tables/fig7_baselines.csv`、`tables/fig6_method_diagnostics.csv`。
- 本轮未修改训练/评估代码，只做结果审查和下一步方案设计；代码修改需等待用户审批。

### 关键数值

- Fig6/Fig7 主线 `DFT` 当前是 selected-bin cross-spectrum direct-TDOA，来源为 `DFT-SCS-lite` uniform partial-Fourier bins。
- Fig6 mean RMSE 平均值：
  - `Raw`≈19.996 m
  - `DAE-CR16`≈27.757 m
  - `DFT-SCS-lite` waveform recon≈47.791 m
  - `DFT` direct≈103.632 m
  - `DFT-train-power-Direct`≈39.770 m
  - `DFT-Fisher-Direct`≈58.841 m
- `DFT` direct 诊断：`direct_mean_abs_tdoa_error_samples`≈122.567、`direct_within_1_sample_rate`≈0.213、`direct_mean_sidelobe_ratio`≈0.996。

### 根因判断

- 不是频域互谱 direct-TDOA 理论错，也不是符号反了。小样本 clean 检查中，全频 direct DFT 与现有 `gcc_standard` 一致，mean TDOA error≈0.26 samples。
- 真正问题是 `DFT-SCS-lite` 采用近均匀梳状选频 `[0,16,32,49,...,1023]`。该选频窗自身在 lag `±63/±126/±189/±252` samples 产生强周期假峰，最大旁瓣接近主峰：`±63` 处约 0.989。
- 当前 direct estimator 在全 lag 范围 `[-512,511]` 搜索，未利用 urban8 几何给出的物理可行 TDOA 范围。假峰经常压过真实峰，导致 WLS 定位崩坏。
- 加入物理 lag limit≈48 samples 的小样本检查显示，`DFT-SCS-lite` direct clean mean TDOA error 可从约 50 samples 降到约 0.26 samples。

### 下一步候选方案

1. 增加统一物理可行 lag gate，建议同时用于 waveform GCC 和 direct estimator，保持 Raw/DAE/DFT/Hadamard/PCA 公平。
2. Chen Fig6 主线 `DFT` 的 direct source 建议从 `DFT-SCS-lite` 改为 `DFT-train-power`，或保留 `DFT-SCS-lite` 但必须启用 lag gate。`DFT-train-power` 是非 oracle、训练集功率选频，当前 direct 平均约 39.77 m，比 `DFT-SCS-lite` direct 稳定得多。
3. `DFT-SCS-lite direct` 保留为补充诊断并标注为 uniform-comb aliasing ablation，不建议作为主图结论。
4. Fig6 supplement 和 Fig7 需要额外 low-SNR/high-SNR zoom SVG；同时调整 DFT direct/recon/Fisher 的颜色、marker、linestyle，避免仅靠颜色区分。


## 2026-07-03 最新上下文补充：Chen Fig6 DFT 主线改为频域互谱 direct-TDOA

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式：`main.py` 顶部 `USER_EXPERIMENT_MODE="paper_repro_eval_only"`，`ALLOW_ENV_OVERRIDES=False`；PyCharm 直接点击 `main.py` 默认不训练，加载冻结 v3.1 模型 `运行结果/20260702_000925`。
- Chen Fig6 主图当前口径：
  - `Raw`
  - `DAE-CR16`
  - `DFT`：使用 `DFT-SCS-lite` 的 partial-Fourier bins，通过 `DirectDFTTDOAEstimator` 对两路 selected-bin cross-spectrum `X_i(f) conj(X_j(f))` 直接估计 TDOA，再进入同一 all-pair WLS 定位器。
  - `Hadamard`：Salari Lemma 1 连续行块 + `H_M^T H_M` 投影伪重构 + GCC/WLS。
  - `PCA`：noisy waveform 拟合线性压缩重构 + GCC/WLS。
- Fig6 补充图保留 `DFT recon`、`DFT Fisher recon`、`DFT-train-band`、`DFT-train-power`、`DFT-bandlimited`、`DFT-random*` 等消融。零填充 IDFT 单路波形重构不再作为 Chen Fig6 的 DFT 主线。
- Fig7 task-aware 轨道继续保存 direct DFT/Fisher/top-power 等任务导向变体，后续用于创新方案公平对比。

### 关键理论判断

- 若 DFT 来源文献没有明确要求“零填充 IDFT 重构单路 waveform”，则默认采用频域 TDOA 估计更合理。
- `IFFT{X_i(f) conj(X_j(f))}` 得到的是互相关函数，这是互相关定理/维纳-辛钦类结论在互谱上的应用；它与“对每一路压缩频谱零填充 IDFT 成伪波形，然后再 GCC”不是同一件事。
- 当前保留零填充 IDFT DFT 路径仅用于补充消融，用来说明重构链路会引入额外波形误差；若后续论文版面有限，可以将该路径从主文排除。

### 本轮修改

- `main.py`：新增 Fig6 主图 direct `DFT`，来源优先为 `DFT-SCS-lite` selected bins；`DFT-Fisher-Direct`、`DFT-train-power-Direct` 仅在 task-aware 轨道中保留。
- `task_baselines.py`：补充 direct DFT 的互谱/互相关定理说明。
- `evaluate.py`：修正文档说明和图例，区分 `DFT`、`DFT recon`、`DFT Fisher recon`。
- `baselines.py`、`export_results.py`：更新 DFT 主图/补充消融的元信息和 summary 解释。

### 当前验证

- `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py` 通过。
- `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...` 通过。
- PyTorch 小样本 urban8 baseline/direct smoke 通过，输出 `SMOKE_OK DFT-SCS-lite ['DFT', 'DFT-Fisher-Direct'] ...`。
- `git diff --check` 仅提示 Windows 行尾转换，无空白错误。

### 下一步

1. 在 PyCharm 直接运行 `main.py`，生成新的完整 eval-only 结果目录。
2. 审查新目录中的 `run.log`、`plot_data.pkl`、`Fig6_Chen_Traditional_Baselines_CR16.svg`、`Fig7_TaskAware_Baselines_CR16.svg` 和 `tables/`。
3. 若 direct DFT 仍不稳定，下一步实现更严格的 Cao/Zhai/FIM-greedy 或 balanced Fisher 选频，而不是回到零填充 IDFT 重构路径。

## 2026-07-03 最新上下文补充：DFT-Fisher 结果审查与 Fig6/Fig7 分轨

### 当前代码状态

- 当前分支：`NewMethod-Codex`。
- 默认运行方式：`main.py` 顶部普通变量控制，`ALLOW_ENV_OVERRIDES=False`，PyCharm 直接点击 `main.py` 默认进入 `paper_repro_eval_only`，不重新训练。
- 默认模型源：`运行结果/20260702_000925` 的 v3.1 冻结模型。
- 当前默认 baseline：
  - Fig6 严格复现轨道：`Raw`、`DAE-CR16`、`DFT-SCS-lite`、`Hadamard`、`PCA`。
  - Fig7 task-aware 轨道：在同一 fixed eval set、同一 LOS mask、同一 all-pair WLS 定位器下，额外比较 `DFT-SCS-lite-Direct`、`DFT-Fisher-Direct`、`DFT-train-power-Direct` 等 direct-TDOA 方法。
  - Hadamard 只保留 Salari Lemma 1 连续行块 + `H_M^T H_M` 投影伪重构 + GCC/WLS 这一路径，不再区分所谓 Hadamard direct/projection 两条主方法。
- 新增/修改关键文件：
  - `task_baselines.py`：新增 `DirectDFTTDOAEstimator`，用于从选定 DFT bins 直接估计 pairwise TDOA，再进入同一 WLS 定位链路。
  - `evaluate.py`：`run_urban_method_comparison` 支持 waveform-output 模型与 direct estimator 同批评估；新增 `filter_method_comparison_data` 生成 Fig6/Fig7 视图。
  - `baselines.py`：新增 `DFT-SCS-lite` 命名和元信息；`DFT-Fisher` 明确为 task-aware/diagnostic ablation，不再默认占据 Chen-style Fig6 主图。
  - `main.py`：默认 `USER_BASELINE_DFT_MODE="scs_lite"`；新增 `USER_RUN_TASK_AWARE_BASELINES=True`；保存 `fig6_results`、`fig7_results`、`baseline_all_results`。
  - `export_results.py`/`replot.py`：支持 Fig7 task-aware 表格、SVG 导出与重绘。

### `运行结果/20260703_134722` 审查结论

- 该结果是 `USER_BASELINE_DFT_MODE="fisher_power"` 时 DFT-Fisher 进入 Fig6 主图的正式 eval-only 结果。
- 读取证据：`run.log`、`plot_data.pkl`、`tables/fig6_method_diagnostics.csv`。
- 关键数值：
  - DFT-Fisher 全 SNR mean RMSE 平均约 `57.36 m`，低 SNR<=0 平均约 `95.62 m`，高 SNR>=10 平均约 `32.03 m`，20 dB 仍约 `31.53 m`。
  - 对比：DAE-CR16 全 SNR mean RMSE 约 `27.76 m`，Hadamard 约 `42.17 m`，DFT-train-power 约 `36.79 m`，DFT-bandlimited 约 `35.48 m`，Raw 约 `20.00 m`。
  - DFT-Fisher 选择 bins 为 `[287..318]` 与 `[706..737]` 两段高频边缘，而不是中心能量主带。
  - 高 SNR 诊断：DFT-Fisher `gcc_weighted_abs_tdoa_error_samples≈1.689`、`within_1_sample_rate≈0.397`、`sidelobe_ratio≈0.978`、`false_peaks_gt_05≈15.04`，说明不是噪声主导，而是选频/重构导致互相关结构退化。
- 结论：
  - “高频分量对 TDOA 更有贡献”不能被简单实现为 `平均功率 × 频率平方` 的硬选高频再零填充 IDFT 伪重构。
  - DFT-Fisher 作为重构链路主图方法不合格；它应移出 Chen-style Fig6，保留为 task-aware 消融或 direct-TDOA 轨道候选。
  - Chen-style Fig6 应回到更传统、非任务感知的 `DFT-SCS-lite/partial-Fourier`，创新论文对比则另设 Fig7/后续图与更强 baseline 公平比较。

### 当前验证

- 已通过：
  - `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`
  - `git diff --check`，仅有 Windows 行尾转换提示。
  - 小样本 urban8 baseline + direct estimator + Fig6/Fig7 plotting smoke。
  - 全频 direct DFT 简单移位信号 sanity check：direct lag 与现有 GCC lag 一致，符号未反。
  - Fig7 导出 smoke：生成 `fig7_baselines.csv/md`、`fig7_method_diagnostics.csv/md` 和 `Fig7_Test.svg`。

### 下一步

1. 直接在 PyCharm 点击运行 `main.py`，生成新的完整 eval-only 结果目录。
2. 审查新目录中的 `run.log`、`plot_data.pkl`、`Fig6_Chen_Traditional_Baselines_CR16.svg`、`Fig7_TaskAware_Baselines_CR16.svg`、`tables/fig6_*`、`tables/fig7_*`。
3. 若 Fig7 direct DFT 仍不稳定，下一步再实现更严格的 Zhai/FIM-greedy 或 balanced Fisher selector，而不是继续把 DFT-Fisher 放入 Chen-style 主图。

## 1. 当前最新状态

| 字段 | 内容 |
|---|---|
| 当前代码版本 | `paper_repro_v3_1_epoch_final_train` 已冻结为近似复现基线；`paper_repro_eval_only` 为 `main.py` 顶部 `USER_EXPERIMENT_MODE` 默认值，PyCharm 直接点击 `main.py` 默认不训练；常用配置已集中到 `USER_*` 普通变量；Chen Fig.6 传统基线已升级为 v4：主图默认使用综述中 RMS 带宽/Fisher 信息动机的 `DFT-Fisher`（训练集平均频谱功率 × 归一化频率² 选频，低功率噪声频点屏蔽）、Salari-Hadamard block-1、PCA、DAE-CR16，并在同一 Fig6 主图内加入高 SNR zoom inset；补充图输出 `DFT-Fisher/DFT-train-band/DFT-train-power/DFT-bandlimited/DFT-uniform/DFT-random*` 和 `Hadamard-random*/sequency/block-2` 消融，并保存每方法 NMSE/PSD/GCC 诊断 |
| 当前实验状态 | R20.1 完整结果 `运行结果/20260630_120041` 已审查；`paper_repro_v1` 完整结果 `运行结果/20260701_122523` 已审查；`paper_repro_v2_urban8` 完整结果 `运行结果/20260701_150204` 已审查；`paper_repro_v3_urban8_fair_eval` 完整结果 `运行结果/20260701_185455` 已审查；`paper_repro_v3_1_epoch_final_train` 完整结果 `运行结果/20260702_000925` 已审查；正式 Fig6 eval-only 结果 `运行结果/20260702_162659` 已审查；baseline variants 小样本流程验证 `运行结果/20260702_195744` 和 `运行结果/20260702_195918` 已通过；正式 baseline variants 结果 `运行结果/20260702_203157` 已审查；Fig6 baseline v2 正式结果 `运行结果/20260702_220605` 已审查；Fig6 baseline v3 正式结果 `运行结果/20260703_110113` 已审查；Fig6 baseline v4 代码已通过编译和函数级 smoke，尚未完整 eval-only 运行 |
| 当前主要结论 | v3.1 是目前最接近论文趋势的复现基线；Fig6 baseline v3 中 DAE-CR16 仍是综合 mean RMSE 最好的压缩方法之一，平均约 27.76 m，优于 `DFT-train-band` 约 37.61 m、`DFT-train-power` 约 36.79 m、Hadamard 约 42.17 m、PCA 约 45.99 m；但 Raw 平均约 20.00 m、高 SNR 约 6.54 m，仍因 synthetic LOS/oracle 评估过强而优于所有压缩方法。`DFT-train-band` 低 SNR 略优于 top-k 但中高 SNR 更差，说明“连续高功率频带”不等价于 TDOA 最优频带；Hadamard block-1 明显优于 block-2/sequency/random；PCA 保谱但不保 TDOA |
| 当前主要风险 | v3.1/Fig6 v4 仍不是严格 Chen 2025 复现：没有 Wireless InSite、MLP LOS 检测和 DNN-DAE/CNN-DAE 学习型对比；Raw 在高 SNR 接近 Clean，DAE 不可能稳定超过 Raw；Hadamard 行选择极敏感，随机 Hadamard 高 SNR 崩坏；完整 Fig6 开启 5 个随机 seed 和全量诊断后耗时较长；`DFT-Fisher` 代码已通过冒烟但尚未完整 eval-only 验证正式数值 |
| 下一步方向 | 在 PyCharm 直接运行 `main.py`，生成 Fig6 v4 正式 eval-only 结果。新结果预期：根目录保存 Fig6 主图/补充图，`tables/` 默认不再重复保存 Fig6 SVG，只保存 CSV/MD 和 Fig3 论文图；eval_only 默认不复制 `model_cr*.pt` 到新结果目录以瘦身；重点审查 `DFT-Fisher` 与 `DFT-train-band/top-k/known-band/uniform/random` 的 mean/median/trimmed 差异、高 SNR inset 可读性，以及 `fig6_random_group_summary.csv/md` 是否便于解释随机变体稳定性。如后续与创新方法公平对比，需迁移创新方法到同一 `urban8 + all-pair WLS + fixed eval set` 协议 |

## 2. 项目目标

- 总目标：研究基于深度去噪自编码器（DAE）的多 UAV 协作定位数据压缩。
- 当前阶段目标：在 BPSK 复信号、Saleh-Valenzuela 多径信道、CR=4/8/16 条件下，平衡时频域恢复与 GCC/TDOA 精度。
- 论文/实验关注点：压缩表示是否能服务 TDOA/GCC 定位任务，而不是只追求波形 MSE 最小。
- 基准论文：Chen 等 2025 IEEE TVT，`Collaborative Localization Using Multiple UAVs: A Deep Denoising Autoencoder-Based Data Compression Approach`。

## 3. 核心代码结构

| 文件 | 作用 |
|---|---|
| `main.py` | 实验入口、全局配置、CR 列表、LOSS_CONFIG、训练/评估/绘图与结果保存 |
| `train.py` | K-fold 训练、`paper_mse` 论文复现分支、R20.1 GCC/Peak 任务分支、早停、验证分量记录 |
| `model.py` | 复数 DAE 网络结构 |
| `signal_gen.py` | BPSK/RRC/S-V 信道；`sv_pair` 与 `urban8` 两种场景；8 UAV 城市几何、建筑物遮挡、分数时延、路径损耗、归一化、训练与评估数据生成 |
| `evaluate.py` | Pairwise TDOA RMSE；8 UAV 定位 RMSE；GCC/亚采样 TDOA；clean peak 标签一致性自检；Fig2/Fig3 绘图数据 |
| `replot.py` | 从 `plot_data.pkl` 重绘 Fig1/Fig2/Fig3 |
| `export_results.py` | 从冻结 `plot_data.pkl` 导出论文表格、正文 mean RMSE 图、补充 median/trimmed 图和 summary |
| `baselines.py` | Chen Fig.6 传统压缩基线：DFT 固定频域投影、Hadamard `H_M^T H_M` 投影重构、PCA noisy waveform 训练与线性重构 |
| `diagnose_dae.py` | 历史诊断脚本，默认结果目录较旧，使用前需确认路径 |
| `代码修改记录/修改记录.md` | 历史详细修改记录 |
| `Obsidian Vault/文献总结&创新方案/研究总结_文献综述_创新方案.md` | 文献综述、创新方案和研究叙事 |

## 4. 方法演化脉络

| 阶段 | 核心变化 | 结论 |
|---|---|---|
| 基础复现 | 复现并修正 ComplexConv、ComplexLinear、ComplexBatchNorm、ResidualBlock；引入 RRC 成形、固定信道池、Saleh-Valenzuela 多径模型 | 建立可训练的复数 DAE 与信号生成流程 |
| GCC/TDOA 修正 | NLOS 标签改为物理最强径；评估由 GCC-PHAT 转为标准 GCC；统一训练/验证/评估的 GCC 幅度定义 | 目标更接近定位任务，但重建质量与 TDOA 之间出现冲突 |
| R13-R19 | 尝试 CR 自适应 MSE/NMSE、包络 NMSE、alpha-blend、grace period、纯包络等 | 过强复数 MSE 与 Corr 梯度冲突；纯包络可稳定 GCC 但导致时频域崩坏和 CR 区分度不足 |
| R20 | 固定比例混合 NMSE + same lag GCC/Peak + 验证分量记录 | R20 完整结果已审查：TDOA 明显优于 Raw，但 CR 区分度不足，时频域恢复仍崩坏 |
| R20.1 | 将 `epsilon_mse` 拆成 `mse_weight_max` 与 `phase_mix`；best-state 从 warmup 后选择；补充 Fig2/Monte Carlo 诊断字段 | 代码修改与基础验证已完成，待完整训练验证真实效果 |
| paper_repro_v1 | 默认 `EXPERIMENT_MODE="paper_repro"`；关闭 Corr/Peak/FI，使用论文式实/虚 MSE；5-fold、10000 样本、200 epoch、SNR -10:2:20；LOS 标签、NLOS=0、`multipath_scale=0.1` | 完整结果 `20260701_122523` 已审查：MSE 重构排序合理，但 TDOA/Raw/CR 趋势与论文定位结果不一致 |
| paper_repro_v2_urban8 | 默认 `paper_repro` 切换为 8 UAV 城市近似复现：单目标、建筑物遮挡、oracle LOS、路径损耗、分数时延、snapshot-level GroupKFold、per-observation RMS、亚采样 TDOA、非线性最小二乘定位 RMSE | 完整结果 `20260701_150204` 已审查：重构质量改善，但 Fig3 mean 受定位异常值主导，Raw 高 SNR 接近 Clean，DAE 未体现论文式低 SNR 优势 |
| paper_repro_v3_urban8_fair_eval | 修正 v2 协议问题：snapshot 固定完整信道；base delay 缩短且 TDOA 允许正负；evaluation set 固定同源；all-pair WLS + multi-start；geometry oracle；noisy RMS 归一化；LOS-only 训练；paper CV 每折固定 200 epoch；恢复各折 best checkpoint 并选择 best-fold 模型 | 完整结果 `20260701_185455` 已审查：geometry oracle 约 0，Clean mean 固定约 5.10 m、median 约 0.083 m；CR 排序在 14/16 个 SNR 下基本正确，但 Raw 在大多数 SNR 下仍优于 DAE，MSE-only 复现轨道尚不能得到论文趋势 |
| paper_repro_v3_1_epoch_final_train | 用户批准 A1-A3+B1-B2：训练协议改为 `cv_best_epoch_final_train`；CV 每折仍固定 200 epoch，但最终模型由全训练集按 CV best epoch 中位数重训得到；urban8 训练集新增固定 snapshot/UAV 计划，每 5 epoch 重采样基带、SNR 与噪声；Fig3 收窄横向尺寸并默认关闭 CI 浅色填充；图窗标题与 Fig1/Fig2/Fig3 文件名一致 | 代码已通过两套 `py_compile`、2-fold×2 epoch final retrain 冒烟和 Fig3 绘图冒烟；完整训练待运行 |
| paper_repro_v3_1_epoch_final_train 完整结果 | 完整结果目录 `20260702_000925`：5-fold 均固定 200 epoch，final retrain 使用各 CR 的 CV best epoch 中位数（本次均为 197）；训练 waveform 重采样后 val 不再早期上升；Fig3 mean 平均 Raw≈20.00 m、CR4≈17.84 m、CR8≈21.17 m、CR16≈27.76 m；median/trimmed 排序严格 CR4<CR8<CR16 | 当前结果已明显优于 v3，建议作为当前 paper_repro 基线；不建议继续在同一合成 oracle-LOS 设置下强行追求论文完全一致曲线 |
| v3.1 后处理与快速模式 | `export_results.py` 从冻结结果目录导出 `tables/` 下的 CSV/Markdown 表格、正文 mean RMSE 图和补充 median/trimmed 图；`paper_repro_eval_only` 从冻结模型重跑评估/出图；`paper_repro_fast_final` 跳过 CV，按 `{4:197,8:197,16:197}` 直接全训练集 final train | 代码已通过编译；`export_results.py` 已成功导出 `20260702_000925/tables`；fast_final/eval_only 均通过小样本冒烟 |
| Fig6 传统基线 | 新增 DFT/Hadamard/PCA 线性压缩-重构 baseline，并与 Raw、Clean、Geometry、DAE-CR16 在同一 urban8/WLS 链路下比较；Hadamard 主图采用 Salari Lemma 1 的连续行块 block-1，执行 `H_M^T H_M` 投影重构后统一 GCC/WLS；DFT 主图默认改为 `DFT-Fisher`：依据 Fowler/Baroughi/综述中 TDOA 精度依赖 RMS 带宽、Fisher 信息按频率平方加权的结论，用训练集平均频谱功率 × 归一化频率² 选择有效高频频点，同时用功率阈值避免选纯噪声；`DFT-train-band`、`DFT-train-power`、`DFT-bandlimited` 保留为补充消融 | v3 正式结果 `20260703_110113` 已审查；v4 代码已通过 `py_compile`、函数级 smoke、Fig6 inset 绘图 smoke 和 `git diff --check`。新版本尚需完整 eval-only 运行，验证 `DFT-Fisher` 正式数值和高 SNR inset 可读性 |

实验指标、路径、证据来源和可信度详见 `实验结果记录.md`，本文件只保留综合判断。

## 5. 当前未解决问题

- `paper_repro_v1` 已完整运行，但只在 MSE/PSD 恢复上呈现 CR4 > CR8 > CR16；TDOA RMSE 中高 SNR Raw 为 0 或近 0，压缩模型无法优于 Raw。
- R20.1 已显示任务驱动 loss 会让 TDOA 结果优于 Raw 但 CR 无区分度，不能再作为论文复现基线。
- 当前 clean peak 自检在 `multipath_scale=0.1` 下通过，但这使 Raw 在 0 dB 以上过强；该设定有利于标签一致性，却不利于复现论文中压缩数据低 SNR/复杂场景优势。
- `paper_repro_v3_urban8_fair_eval` 已完整运行；结论是评估链路基本可信，但仍未复现论文中 DAE 压缩数据优于 Raw 的定位趋势。
- v3 Fig1 显示明显过拟合：5-fold 均跑满 200 epoch，但 best epoch 约 CR4=33-64、CR8=17-19、CR16=13-15；当前保存的是各折 best checkpoint，最终模型不是最后 epoch，但训练协议耗时且不够公平。
- v3 Fig3 中 Raw 高 SNR 接近 Clean；根本原因更可能是当前 oracle LOS、合成 LOS-dominant 信道、低多径难度和 all-pair WLS 使 Raw baseline 过强，而不是定位求解符号或固定评估集错误。
- v3.1 已修正 best-fold 模型不够公平的问题；完整结果显示 CR4 低 SNR 优于 Raw，CR8 部分低 SNR 优于 Raw，CR16 未稳定优于 Raw，符合容量受限的物理预期。
- 当前 R20.1 创新轨道仍使用 `sv_pair`、pairwise TDOA、不同训练 SNR/归一化/损失，与 v3.1 paper_repro 的 `urban8`、localization RMSE、oracle LOS、WLS 不公平可比；后续若比较创新方法，需迁移到同一 urban8 数据生成与评估协议。
- v3 仍未实现论文中的 MLP LOS 检测，仍使用 oracle LOS mask。
- Chen 论文中的 DFT/Hadamard/PCA 传统对比基线已实现；DNN-DAE/CNN-DAE 学习型对比基线仍未实现。
- 已读取 `参考文献` 目录下 Chen 2025 与其引用 [20]-[22]：Chen Fig.6 传统对比为 DFT-based、Hadamard-based、PCA-based，固定 CR=16；但 Chen 未给出保留系数选择、PCA 训练集、Hadamard 行选择等实现细节。当前实现已在代码中明确工程约定：DFT 主图采用 RMS-bandwidth/Fisher 动机的 `DFT-Fisher`，Hadamard 采用 Salari 连续行块 `H_M^T H_M` 投影重构，PCA 用 noisy waveform 拟合。
- v2 当前使用 oracle LOS mask，尚未实现论文中的 LOS 检测网络与 UAV 选择误差传播。
- 当前城市信道为合成近似模型，仍缺少 Wireless InSite 射线追踪数据中的真实反射、绕射、散射和建筑材料损耗。
- 根目录暂无 `requirements.txt`、`environment.yml`、`pyproject.toml`，环境复现信息不足。
- 多个 Python 文件中文注释在 PowerShell 默认输出中可能显示乱码；文件编码一致性需后续单独确认。

## 6. 重要注意事项

- 不要把当前版本号、实验结果、当前分支或下一步任务写入 `AGENTS.md`。
- 当前默认 `python main.py` / PyCharm 直接点击运行 `paper_repro_eval_only`，加载冻结 v3.1 模型并输出 Fig1/Fig2/Fig3/Fig6，不重新训练；常用模式通过 `main.py` 顶部 `USER_*` 普通变量修改，默认不读取外部环境变量；若要执行完整 paper_repro 训练，把 `USER_EXPERIMENT_MODE` 改为 `"paper_repro"`；若要切回 R20.1，改为 `"r20_1"`。
- 完整训练耗时较长，运行 `python main.py` 前需用户确认。
- 涉及损失函数时必须同步检查训练集与验证集目标是否一致。
- 涉及 GCC/TDOA 时必须检查训练、验证、评估使用的 lag window 和相关定义是否一致。
- 涉及 Fig1/Fig2/Fig3 时必须检查图例、坐标轴、曲线含义和 pkl 字段是否一致。
- 不编造实验结果；只有实际读取过日志、pkl、svg 或实际运行过命令，才能写具体指标。

## 7. paper_repro_v3_1_epoch_final_train 结果判断标准

`paper_repro_v3_1_epoch_final_train` 已进入待完整运行状态，后续优先检查：

1. 运行日志开头的 `[PaperRepro Check]`：分数时延下 exact 不必为 1，重点看 within1/within2。
2. Fig1：paper_repro 的每个 CV fold 应固定跑满 200 epoch，不再 50 epoch 内早停；pkl 中 `training_protocol` 应为 `cv_best_epoch_final_train`，各 CR 应记录 `final_epoch_policy=median_cv_best_epoch` 和 `final_epochs`。
3. Fig2：同一 demo trial 跨 SNR，只改变噪声强度；时域开头不应再有约 2 us 的长静默。
4. Fig3：mean RMSE 仍是主图，同时检查 median/trimmed RMSE、Geometry oracle、Clean oracle、failure rate、p90/p95/max。
5. pkl：检查 `diagnostics_version=paper_repro_v3_1_epoch_final_train`、`fixed_eval_set=True`、`localization_estimator=all_pair_wls`、`normalization_mode=per_observation_noisy_rms`、`resample_train_each_epoch=True`、`resample_interval=5`。
6. 若 v3 的 Geometry oracle 不接近 0 或 Clean mean 仍大幅波动，优先修定位求解；若 Clean 稳定但 DAE 仍不优于 Raw，再讨论数据复杂度或创新 loss/架构。
