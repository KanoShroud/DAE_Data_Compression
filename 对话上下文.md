# 对话上下文

## 当前状态（2026-07-08 19:30 +08:00）

| 字段 | 内容 |
|---|---|
| 项目 | `DAE Data Compression`：多 UAV 协作定位中的压缩数据/TDOA 任务保持 |
| 当前分支 | `NewMethod-Codex` |
| 当前默认入口 | PyCharm 直接运行 `main.py` |
| 当前默认 profile | `compressed_tdoa_v5a_fast` |
| 当前默认模式 | `compressed_tdoa_v5a_fast`，加载冻结 Chen DAE 近似复现基线，同时训练 V5-A CR16 direct TDOA likelihood |
| 当前主目标 | 从 waveform decoder 路线切换到 V5-A：CR16-only learnable compressed-domain TDOA likelihood，优先验证同预算压缩域直接 TDOA/定位性能 |

## 当前研究判断

- `Raw` 是未压缩全数据参考，不再作为创新方法必须超越的硬目标；创新目标是优于当前压缩基线、Chen-style DAE-CR16 和更强 task-aware 压缩方法，并给出清晰的同预算解释。
- `paper_repro_v3.1` 近似复现基线已冻结，关键参考目录为 `运行结果/20260702_000925`。后续创新对比应复用同一 urban8 数据生成、fixed eval set、all-pair WLS、physical lag gate 等评估口径。
- GeoAmbi/GeoHybrid 已冻结为 baseline/消融：公平 CR16 预算下没有形成足够强的主创新性能，不再继续调参。
- FreqDAE v1-v4 的 waveform decoder 路线未通过 go/no-go：多轮结果显示容易产生假窄带/宽峰模板，CR 区分度不足。V4-min 结果 `运行结果/20260708_163957` 表明 task head 本身也没有强到足以支撑该路线。
- 新主线 V5-A 不再要求重构 waveform，而是学习 CR16 预算下的压缩域 TDOA likelihood，并通过同一 all-pair WLS 定位链路评估。

## 当前代码状态

| 文件 | 当前作用 |
|---|---|
| `main.py` | PyCharm 一键入口；默认 profile 已切到 `compressed_tdoa_v5a_fast`；V5-A 走 direct-only 输出，避免重复生成不变核心图 |
| `compressed_tdoa.py` | 新增 V5-A 模型、训练器与 `LearnedCompressedTDOAEstimator`；64 个复数压缩特征，输出 lag posterior |
| `experiment_profiles.py` | 新增 `compressed_tdoa_v5a_fast` profile |
| `evaluate.py` | 新增 `V5A-CR16` 绘图样式；V5-A 通过 direct estimator 接口进入 Fig7/Fig8/Fig9 |
| `signal_gen.py` | 继续提供 urban8 fixed snapshot/pair 数据；V5-A 训练使用 LOS pair 计划和 snapshot-level val split |
| `task_baselines.py` | 继续提供 DFT/Cao/Zhai/GeoHybrid direct baseline 接口，V5-A 复用同一评估 API |
| `运行结果/20260702_000925` | 冻结 Chen-style DAE 近似复现基线，V5-A 默认从此目录加载 `model_cr*.pt` 和 `plot_data.pkl` |

## V5-A 已实现内容

- 新增 `LearnableCompressedTDOALikelihood`：
  - 每个 UAV waveform 经 FFT 后由可学习频域 filterbank 投影为 64 个复数特征，严格对应 CR16 的 128 real scalars；
  - pairwise score 使用物理 steering 形式：`Re sum_k alpha_k z_i,k conj(z_j,k) exp(j 2*pi*f_k*tau)`；
  - 输出物理 lag window 内的 soft TDOA posterior；
  - 带 filter entropy/overlap 正则，避免 filterbank 退化成无解释的 dense 黑箱。
- 新增训练器 `train_v5a_compressed_tdoa()`：
  - 使用 urban8 LOS UAV-pair 数据；
  - train/val 按 snapshot group 切分，避免同一 snapshot 泄漏；
  - loss 由 soft TDOA CE、ambiguity mass、peak width、posterior uncertainty 和 filterbank regularization 组成；
  - 保存 `model_v5a_cr16.pt` 与 `v5a_training` 历史到 `plot_data.pkl`。
- 新增 direct estimator `LearnedCompressedTDOAEstimator`：
  - 与现有 DFT/Cao/Zhai/GeoHybrid 一样实现 `estimate_pair()`；
  - 进入 `run_urban_method_comparison()` 后共享 all-pair WLS、physical lag gate、fixed eval set。

## 本轮验证

- 编译通过：
  - `main.py compressed_tdoa.py experiment_profiles.py evaluate.py task_baselines.py train.py signal_gen.py model.py baselines.py export_results.py replot.py`
- 小样本 smoke 通过：
  - 10 个 urban snapshot，80 个 LOS pair，V5-A 训练 1 epoch；
  - 训练/验证 loss finite，`ValMAE≈6.133 samples`，`Val<=1≈0.667`；
  - `run_urban_method_comparison()` 中 `V5A-CR16` direct estimator 可进入 all-pair WLS，20 dB 小样本 valid count 为 6/6；
  - 该 smoke 只证明代码链路可运行，不代表正式性能。
- `git diff --check` 通过，仅有 Windows CRLF 提示。

## 下一步建议

1. 用户在 PyCharm 直接运行 `main.py`，确认日志中：
   - `experiment_profile=compressed_tdoa_v5a_fast`
   - `EXPERIMENT_MODE=compressed_tdoa_v5a_fast`
   - `[V5-A] Training CR16 learnable compressed-domain TDOA likelihood`
2. 运行完成后回读新目录：
   - `run.log`
   - `plot_data.pkl`
   - `Fig7_TaskAware_Baselines_CR16.svg`
   - `Fig8_Strong_TaskAware_Baselines_CR16.svg`
   - `Fig9_V5A_Compressed_TDOA_CR16.svg`
   - `tables/fig8*.csv`、`tables/fig9_v5a*.csv`
3. Go/no-go 判断重点：
   - pairwise TDOA high-SNR MAE 是否明显低于 DAE-CR16/GeoHybrid；
   - low-SNR mean/trimmed/outlier 是否优于压缩基线；
   - posterior sidelobe/ambiguity 是否随 SNR 改善；
   - V5-A 是否能在不重构 waveform 的情况下形成可解释的 CR16 压缩域 TDOA 似然。

## 需要用户确认

- 完整运行后是否接受 V5-A 作为主创新候选，还是只作为 direct-TDOA 消融。
- 如果 V5-A CR16 成功，再审批是否进入 V5-B：uncertainty-aware WLS / CR4-CR8-CR16 nested compressed-domain extension。
