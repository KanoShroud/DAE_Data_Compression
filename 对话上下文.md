# 对话上下文

> 当前状态摘要更新至 2026-08-13。完整历史保存在 `文档归档/`。
> 当前唯一任务及审批状态见 `当前任务.md`；完整路线历史见 `研究总结.md`。

## 1. 当前状态

| 字段 | 内容 |
|---|---|
| 项目目标 | 在相同通信预算下，提高压缩数据的 TDOA 参数估计与多 UAV 定位精度 |
| 当前主线 | RAPTQ-TDOA完整`RelativePhase`最终有限码率可行性周期：串联确认连续信息headroom与同24 bit任务量化收益 |
| 当前阶段 | 两坐标`SparseAmpPhase`正式冻结为`HOLD_COMPUTABILITY_36_CONTEXT`；独立`RelativePhase`有限码率入口已完成实现和smoke，当前=`MODIFICATION_COMPLETE_PENDING_PYCHARM_RUN`，尚无正式性能结果 |
| 配置裁决 | Stage 2A.1=`NO-GO-CONFIG-STAGE2A1` |
| 协议裁决 | IID Block=`NO-GO-PROTOCOL-IID-BLOCK` |
| 可观测性裁决 | `HOLD_PROXY_MISMATCH` |
| 共享状态裁决 | `NO_GO_ROUTE_PLANNING_NONCONVERGENCE`；正式精度复验止于planning收敛门禁 |
| 路线裁决 | BRSR共享状态路线终止；SITQ单一全局对称阈值继续冻结；RAPTQ理论商表示成立，但两坐标`SparseAmpPhase`在完整36-context上仍有局部数值HOLD且中高SNR信息损失明显，`SparsePhase`亦被积分精度阻塞；RAPTQ路线本身尚未通过信息与码率门禁 |
| BRSR转折区裁决 | planning动作可辨识性通过，但同总bit主效应和S2相对S1异质性归因未通过；不进入ObservablePolicy或locked |
| 当前禁止事项 | 不把smoke当性能证据；不再修补两坐标稀疏表示；不依据本轮结果调整频点、位宽、门槛、积分预算或阈值网格；不进入联合选频、真实波形、神经网络或locked |

## 1.1 RAPTQ-TDOA Stage 0当前实现

1. 独立目录为`raptq_tdoa/`，不导入、不修改DAE、BRSR、SITQ和主评估链路；PyCharm入口为`raptq_tdoa/run_stage0.py`。
2. 匹配模型沿用SITQ冻结的1024点DFT几何、40 MHz、6个频点、RRC功率形状、TDOA网格和增益幅度，但将8点离散增益相位替换为连续均匀先验，并通过修正Bessel函数解析边缘化。
3. `Stage 0-T`验证三项理论边界：任意公共相位旋转不改变TDOA后验；`radius + yy^H/||y||²`与完整复观测只差公共相位并后验等价；有限窗口非酉时延下公共相位不变性仍成立，但径向量可能随时延变化，不能无条件删除。
4. `Stage 0-I`在未量化连续域比较完整复观测、无损商表示、仅投影、完整相对相位、完整相对幅度、稀疏相位和稀疏幅相。后五类有损统计量使用复极坐标Jacobian和scrambled Sobol QMC边缘化未观测坐标。
5. 开发配置为2个seed×3条轨迹×6个SNR，共36个配对context；主QMC为`2^10`点×4个scramble，前12个context使用独立`2^12`参考诊断数值误差。该规模用于理论与信息审计，不是最终方法性能评估。
6. 正式目录为`运行结果/RAPTQ/RAPTQ_STAGE0_20260805_105037_development`。36个context和252行结果完整；公共相位不变性、无损商表示和有限窗口边界证书全部通过。
7. `FullComplex`与`Radius+Projector`后验TV最大约`1.27e-14`；`ProjectorOnly`的高预算参考TV最大约`8e-6`，数值稳定。`RelativePhase`最大TV约`0.0136`，初步可读但统计样本不足以证明等价。
8. `RelativePower`、`SparsePhase`和`SparseAmpPhase`在高SNR的QMC误差明显增大；全局最大TV=`0.4157`，最大风险差=`7.7878 sample²`。正式门禁为`HOLD_NUMERICAL_INTEGRATION`，高SNR定量排序无效。
9. 初步信息结构符合物理预期：相对幅度风险约等于TDOA先验方差`22 sample²`，几乎不含时延信息；完整相对相位最接近完整复观测；只保留2个相位或2个幅相坐标会在中高SNR损失信息。但后两项需数值收敛和更大独立样本确认。
10. 当前不进入Stage 0-R。`core.py`已从QMC中解析删除公共相位，并将`RelativePower`实现为精确TDOA先验负对照，消除了正式结果中最明显的无意义高维积分误差。
11. `RelativePhase`新增`defensive_observed`数值提案，以模型预测功率与观测幅度的等权混合覆盖尖锐高SNR似然，并保留完整重要性权重；该提案只用于数值收敛参考，不作为部署侧信息或方法性能收益。
12. 新入口`raptq_tdoa/diagnose_stage0_numerical.py`固定读取且哈希保护原正式目录，只重建源manifest规定的模型和种子；正式配置比较`2^8/2^10/2^12/2^14`与独立`2^15×8`参考，共12个预注册context。
13. 正式数值证书目录为`运行结果/RAPTQ/RAPTQ_STAGE0_NUMERICAL_20260805_115458_development`；12个context完整，源目录未变，解析负对照误差为0。
14. `2^14`与独立`2^15×8`参考在全部SNR上的表示风险排序一致，四个目标表示从`2^12`到`2^14`的最大风险误差均下降；因此积分没有发散，定性排序已稳定。
15. `ProjectorOnly`和`RelativePhase-Defensive`最大风险误差分别为`1.55e-5/0.00231 sample²`，通过单网格平方门槛；`SparseAmpPhase/SparsePhase`为`0.08674/0.39641`，正式阻塞仅来自稀疏表示。
16. 防御性提案将完整相对相位的最大参考风险差从模型提案的`0.04526`降至`0.00231 sample²`，证明高SNR积分效率可以通过严格重要性校正改善；但该离线提案不构成部署信息。
17. 独立入口`raptq_tdoa/diagnose_sparse_integrator.py`使用无冗余Dirichlet stick-breaking、模型proposal和固定等权防御性混合proposal，正式目录为`运行结果/RAPTQ/RAPTQ_SPARSE_INTEGRATOR_20260813_102100_development`。
18. 正式机械检查、低维独立参考、新旧坐标低SNR一致性和历史源哈希保护全部通过。`SparseAmpPhase`在7个开发压力context和原12-context复核中的最大双proposal风险差分别为`0.05483`和`0.01568 sample²`，正式认证为`CERTIFIED`。
19. `SparsePhase`在开发压力context的最大风险差为`0.12030 sample²`，超过`delta_guard=0.0625 sample²`，按预注册止损为`HOLD_COMPUTABILITY_DEVELOPMENT`，未进入12-context复核。
20. 高SNR下`SparseAmpPhase`仍有重要性权重集中：12-context复核最高预算的最小启发式ESS约`1.17`，最大权重约`0.924`。同一seed 4302、10 dB context的防御性proposal在开发/复核两套QMC种子下相差`0.07055 sample²`，但模型proposal仅差`3.8e-5`，且两阶段内部风险门禁均通过；当前作为局部可复现性警告继续报告，不追溯推翻自动认证。
21. 本轮只认证数值可计算性，且7个压力context与12-context复核重叠，不是独立统计确认。`CondMI-Matched24`、全新trajectory和Stage 0-R继续阻塞。
22. 已新增`raptq_tdoa/recompute_stage0_sparse_amp_phase.py`，只对原36-context重算`SparseAmpPhase`。每个context固定使用双proposal、双独立QMC seed family和`2^16×8`预算，四分量在似然层等权合并；逐context四分量风险跨度必须不超过`delta_guard`。
23. 正式纠正性目录为`运行结果/RAPTQ/RAPTQ_STAGE0_SPARSE_CORRECTION_20260813_150041_development`。36个context和144行分量结果完整，源哈希与历史FullComplex对齐通过。
24. 34/36个context通过四分量风险跨度门禁；seed 4302 trial 0/2的10 dB跨度=`0.09126/0.08228 sample²`，超过`delta_guard`，正式=`HOLD_COMPUTABILITY_36_CONTEXT`。两个context的TV和估计跨度都很小，问题主要位于后验尾部风险的QMC稳定性。
25. 纠正后的描述性风险超额为：全SNR=`2.2951 sample²`，低SNR=`0.0204`，5/10 dB=`6.7693`，15 dB=`0.1711`。只有6条历史trajectory且数值门禁未全通过，不能把CI当独立确认；但中高SNR稀疏信息损失与历史结构一致。
26. 当前两坐标`SparseAmpPhase`不再是可直接进入24 bit设计的主候选。RAPTQ公共相位商空间和已数值稳定的完整`RelativePhase`分支未被否定；下一步必须在冻结当前配置后另行审批。
27. 已批准并独立实现`run_relative_phase_rate.py`。`CondMI-Matched24`与`RAPTQ-RelativePhase24`使用相同6频点、连续公共相位模型和24 payload bit；后者固定量化5个相对相位，不重构伪波形。
28. 正式协议采用串联止损：阈值/数值门禁失败则停止；连续相位相对CondMI的headroom未超过预注册裕量则不执行最终24-bit RAPTQ性能阶段。design/evaluation分别使用`7301/7302`与`8301--8304`，不得依据结果回调配置。
29. 新旧27项RAPTQ测试、编译、Ruff和项目完整性检查通过；smoke仅证明链路，临时结果已清理。当前等待用户通过PyCharm执行正式development。

## 1.2 SITQ-TDOA Stage A 当前实现

1. 独立目录为`sitq_tdoa/`，不依赖DAE、BRSR和主评估链路。
2. A站选定频点作为融合端侧信息；B站只发送6个固定复频点的2-bit I/Q量化码，总载荷严格为24 bit，无索引、反馈或停止开销。
3. 生成端与解码端使用同一有限TDOA先验、RRC形状源频谱和复增益先验；解码器对未知复增益精确边缘化，并用高斯矩形积分计算量化码似然。
4. 三个同预算方法只改变量化阈值的离线设计目标：观测量化MSE、条件后验熵、TDOA后验贝叶斯风险。正式评价统一使用后验均值估计器。
5. 阈值在design seeds上冻结，evaluation seeds完全互斥；若最优阈值位于搜索边界则只允许`HOLD`。正式主效应使用轨迹聚类配对MSE改善及95% CI，不设置事后百分比收益门槛。
6. 正式目录为`运行结果/SITQ/SITQ_STAGE_A_20260803_132419_development`。机械、公平性、阈值边界和后验校准均正常。
7. SITQ总体RMSE=`3.5122 sample`，略差于CondMI=`3.4951`；配对MSE改善=`-0.1194 sample²`，95% CI=`[-0.3423,0.0529]`，3个evaluation seed均为负。负收益主要集中在0/5 dB及少数重尾样本。
8. Stage A.1正式目录为`运行结果/SITQ/SITQ_STAGE_A1_20260803_140306_diagnostic`。原固定阈值的design风险改善为`0.0711 sample²`，CI跨0；六折交叉冻结改善仅`0.0138 sample²`，95% CI=`[-0.0797,0.1147]`，两个design seed方向相反。
9. 四个交叉冻结fold中Bayes-risk与CondMI选择了同一阈值；48个未见轨迹中32个收益为0、8个为正、8个为负。当前证据说明两个目标在这一单阈值自由度下大多等价，不能支持使用全新evaluation确认。
10. 正式裁决为`HOLD_SELECTION_UNCERTAIN`。当前单一全局对称阈值组件继续冻结；若后续重启SITQ，必须提出逐频点阈值、有限bit分配或向量量化等新的理论自由度，并使用新的development协议预注册验证。

## 2. Oracle 审计后的关键认识

1. `Oracle-OneAcquire`、`Oracle-B24` 和 `OracleAction-B29` 使用真实 TDOA 及全部动作结果，属于 `ClairvoyantOutcomeOracle`。它们只能给出不可部署的事后下界，不能证明动作价值可由动作前信息学习。
2. 历史负面结果仍成立：Stage 2A 可部署策略、Stage 2A.1 SafeGate 和 BlockPredicted 均未超过相应同 bit 强基线。
3. 历史 `BlockOracle` 把没有共享物理状态的独立样本事后拼块，再用真值选择共同动作；该优势不能证明真实块级共享协议可行，协议改判为 `NO-GO-PROTOCOL-IID-BLOCK`。
4. 独立单包可观测性证书已经说明当前动作前代理不足；若要保留主动选频思想，必须先证明慢变共享状态提供非事后、可重复的条件动作价值。

## 3. 当前实现

| 路径 | 作用 |
|---|---|
| `raptq_tdoa/run_stage0.py` | 当前主线PyCharm入口；执行Stage 0-T精确证书和Stage 0-I连续信息审计 |
| `raptq_tdoa/core.py` | 连续相位Bayesian模型、解析干扰相位边缘化、商表示及QMC诱导似然 |
| `raptq_tdoa/test_stage0.py` | 检查任意相位不变性、无损商表示、QMC后验、有限窗口边界和连续相位生成 |
| `raptq_tdoa/recompute_stage0_sparse_amp_phase.py` | 当前PyCharm正式入口；执行原36-context的`SparseAmpPhase`双proposal、双seed-family纠正性信息重算 |
| `brsr_observability_certificate/run_certificate.py` | PyCharm 正式 development 入口；生成独立 train/calibration/test 数据并完成证书 |
| `brsr_observability_certificate/features.py` | 只从基础层量化码、当前后验、公开 SNR/尺度和动作前预测风险构造白名单特征 |
| `brsr_observability_certificate/certificate.py` | 预注册 Ridge/HGB、校准集选模、配对 cluster CI、MDE、动作排序诊断和门禁 |
| `brsr_observability_certificate/audit_oracles.py` | 扫描源码与当前长期文档，登记历史 Oracle 的信息边界 |
| `brsr_observability_certificate/test_certificate.py` | 检查真值不泄漏、Oracle 不进入门禁、IID Block 状态和 29-bit 公平性 |
| `brsr_stage2_rate_adaptive/` | 冻结 Stage 2A/2A.1 实现；本轮未修改 |
| `brsr_state_oracle_certificate/run_certificate.py` | 新主线的PyCharm入口；构造S0/S1/S2并执行StateOracle同bit证书 |
| `brsr_state_oracle_certificate/certificate.py` | 共享状态生成、快速冻结配置评价、策略冻结、配对统计和路线门禁 |
| `brsr_state_oracle_certificate/test_certificate.py` | 检查planning/evaluation隔离、S0负对照、预算、快速评价一致性和门禁边界 |
| `brsr_s1_precision_replication/run_replication.py` | 当前PyCharm入口；先做独立planning收敛校准，再在48个新状态上执行S1 L16确认性复验 |
| `brsr_s1_precision_replication/replication.py` | planning等价统计、独立check交叉拟合、确认性配对cluster CI和一次性路线裁决 |
| `brsr_s1_precision_replication/test_replication.py` | 检查收敛选择、功效样本量、同bit审计、流隔离和PASS/NO-GO边界 |
| `brsr_transition_regime/run_experiment.py` | 新路线PyCharm入口；执行转折区、平坦负对照和块相干陷波StateOracle证书 |
| `brsr_transition_regime/experiment.py` | 同bit预算、planning奇偶交叉拟合、配对效应、异质性归因和门禁纯函数 |
| `brsr_transition_regime/test_experiment.py` | 检查预算、交叉拟合、差分归因和门禁边界 |

新证书继续冻结 `Current-Fisher`、`Linear-Marginal` 和 `5x64`。S1/S2只在块内共享TDOA、复增益和预注册频谱状态，planning与evaluation的符号和噪声互斥。StateOracle只用planning期望损失冻结每个状态的B24动作，不读取evaluation结果；5 bit动作反馈按`L=4/8/16`摊销，静态基线以与结果无关的hash在B24/B32之间时间共享到相同平均总bit。

## 4. 正式可观测性证书结论

- 目录：`运行结果/BRSR/BRSR_OBSERVABILITY_20260730_153403_development`。
- 数据：train/calibration/test=`4×30/2×30/3×40`互斥轨迹，6个SNR；test共720样本、120独立轨迹，每样本10个动作。
- 所有方法平均总量均为29 bit，源码哈希、种子隔离、特征白名单和Oracle信息边界均通过。
- calibration选择HGB，但test中HGB/Ridge RMSE=`3.4508/3.4411 sample`，差异很小；两者都弱于BestStatic/SNR-only=`3.2834/3.3159`。
- HGB相对两基线的配对MSE改善=`-1.1275/-0.9132 sample²`，95% CI=`[-1.7765,-0.5558]/[-1.5017,-0.3281]`；3个seed全部为负，不是统计功效不足。
- HGB动作损失$R^2=0.1615$、样本内Spearman=`0.1425`、事后top-1一致率=`27.78%`，均弱于RiskGreedy的`0.2271/0.1914/35.14%`。
- 低SNR的动作预测几乎无效；中高SNR能识别部分坏动作，但少量错误选择造成重尾损失。当前代理目标不能把弱排序信号转化为策略收益。
- ClairvoyantOutcomeOracle RMSE=`3.0577 sample`只作不可部署下界，不参与裁决。

## 5. StateOracle 正式证书

- 新目录：`brsr_state_oracle_certificate/`；没有修改既有 BRSR 主线代码。
- S0 强制令 StateOracle 退化为 SNR-only，防止独立样本真值泄漏。
- S2 增加预注册公共频谱陷波，并加入已知陷波类别的 `ContextStatic` 强基线，避免把公开上下文优势错误归给主动策略。
- 正式目录：`运行结果/BRSR/BRSR_STATE_ORACLE_20260731_170154_development`。3个state seed、每seed 4个状态、每状态32个planning包和16个evaluation包，SNR为`-10/0/5/15 dB`。
- 10项机械检查全部通过；23,808条评价记录无缺失、非有限值或重复，量化截幅率为0，通信预算符合预注册口径。
- S1 StateOracle RMSE=`2.6957 sample`，相对B24基线约有`2.0%`点改善。`L=16`相对两类同bit基线的MSE改善约`0.305/0.312 sample²`，但95% CI下界为`-0.075/-0.031`，12个state cluster不足以闭合证据；3个seed均为正。
- S2 StateOracle RMSE=`2.7601 sample`，相对SNR-only和ContextStatic同bit L16基线点改善约`5.3%/6.2%`，但CI大幅跨0且seed方向不一致。一个`true_tau=7.303 sample`的边界状态贡献了主要正收益，不能解释为稳定状态余量。
- planning前后半包选出完全相同动作的比例仅S1=`14.6%`、S2=`16.7%`；半包动作价值排序的平均Spearman仅约`0.18/0.19`，说明32个planning包下动作冻结仍受重尾误差影响。
- 主图存在图例与横坐标区域拥挤的问题，但不影响CSV和门禁结论。
- 源码冻结检查发现历史哈希只因 Git 的 LF 与 Windows 工作树 CRLF 不同；新入口同时核对原始哈希和换行归一化哈希，语义内容必须与历史冻结版本一致。

## 6. S1确认性复验

- 新目录：`brsr_s1_precision_replication/`；旧StateOracle和BRSR源码保持不变，并由历史正式结果哈希锁定。
- planning calibration和effect confirmation使用互斥状态种子。前者比较`32/64/128`包与`256`包参考，并通过独立`128`包check流验证候选和参考动作。
- planning主等价界固定为历史最小S1 L16 MSE点效应的10%；5%和20%只作预注册敏感性，不改变主裁决。
- 正式确认使用48个独立状态，超过基于历史效应和cluster SD推导的41状态最低近似需求；只确认S1和`L=16`，不再并行筛选S2或其他块长。
- smoke目录：`运行结果/BRSR/BRSR_S1_PRECISION_20260731_175641_smoke`；20项机械检查通过，仅证明工程链路。
- 正式目录：`运行结果/BRSR/BRSR_S1_PRECISION_20260731_182000_development`。13项正式机械检查全部通过，源码证书、种子隔离、公开尺度和量化截幅检查均正常。
- 256包参考通过独立check交叉拟合充分性检查；32/64/128包候选均未达到等价门禁。对应平均regret为`0.22236/0.19637/0.01325 sample²`，95% CI上界为`0.53295/0.37207/0.09234`，而主等价界为`0.03046 sample²`。
- 128包虽然总体均值已低于等价界，但一个seed均值为`0.13708 sample²`，且CI上界仍超界；在5%、10%和20%历史效应容差下均未通过。该结果表示条件动作规划缺少跨状态、跨seed的可重复性。
- 自动裁决=`NO_GO_ROUTE_PLANNING_NONCONVERGENCE`。脚本按预注册协议没有进入48状态确认阶段，因此没有确认性主效应、Fig2或ObservablePolicy推进依据。

## 7. 已冻结的转折区路线

1. 新路线把主效应限定在`0/5 dB`模糊度转折区；`-10/15 dB`保留为控制边界，不能事后删除。
2. S2用三类块相干公共频谱陷波产生可重复异质性；`ContextStatic`获得相同类别信息，避免把公开上下文优势归给主动动作。
3. StateOracle只用planning数据，先以奇偶包交叉拟合证明状态动作可辨识，再在独立evaluation上比较同总bit主效应。
4. 正式目录为`运行结果/BRSR/BRSR_TRANSITION_20260802_175247_development`；14项机械检查通过，排除了预算、信息泄漏、截幅和源码漂移等基础问题。
5. planning奇偶交叉拟合通过，但独立evaluation的同总bit主效应CI跨0；24个状态仅11个收益为正，均值主要由少数状态贡献。
6. S2减S1的异质性归因CI跨0且seed方向不一致，不能主张公共陷波是额外收益来源。当前配置冻结，不进入ObservablePolicy或locked。
7. 5 dB单点显示正效应，只能作为后续全新假设线索；不得在当前数据上删除0 dB或重新定义转折区后宣称通过。

## 8. 当前下一步

1. 在PyCharm直接运行`raptq_tdoa/run_relative_phase_rate.py`，不要修改顶部development配置。
2. 若阈值、数值或连续headroom门禁止损，则回读现有产物并裁决，不重新调阈值网格、QMC预算、位宽或频点。
3. 只有`GO_COMPONENT_RELATIVE_PHASE24`才审批真实BPSK-RRC与信道失配验证；否则结束当前RAPTQ有限码率路线。
