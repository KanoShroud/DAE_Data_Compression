# 项目上下文摘要

**生成日期**: 2026-06-24
**目的**: 新对话快速恢复——阅读此文档约10分钟即可掌握全部关键信息

---

## 一、项目核心信息

| 项目 | 内容 |
|------|------|
| 项目 | 深度去噪自编码器(DAE)用于多UAV协作定位数据压缩 |
| 基准论文 | Chen等, "Collaborative Localization Using Multiple UAVs: A Deep Denoising Autoencoder-Based Data Compression Approach", IEEE TVT 2025 |
| 核心问题 | 多UAV通过DAE压缩BPSK信号(1024采样点, CR=4/8/16), 传输到中心UAV做TDOA估计 |
| 代码 | `F:\PythonWorkspace\DAE Data Compression\` |
| 当前分支 | `NewMethod_Deepseek` |
| 运行环境 | 康达环境 `PyTorch`, 路径 `D:\Software\anaconda3\envs\PyTorch\python.exe` |
| 运行命令 | `python main.py` |

## 二、损失函数理论体系

### 2.1 核心公式（所有CR统一，仅ε不同）

$$L = 1.0 \cdot L_{\text{Corr}} + \lambda_{\text{peak}} \cdot L_{\text{Peak}} + \varepsilon \cdot \text{NMSE}$$

其中所有分量**无量纲**（NMSE = MSE / signal_power，L_Corr已除以峰值，L_Peak在[0,1]）。

### 2.2 容量分配理论定权

| CR | 潜空间 M | Corr最低占用 | 额外可分配 | $\varepsilon = \frac{M-128}{M}$ | $\lambda_{\text{peak}}$ |
|----|---------|-------------|-----------|-------------------------------|------------------------|
| 4 | 512 | 128 (25%) | 384 (75%) | **0.75** | **0.15** |
| 8 | 256 | 128 (50%) | 128 (50%) | **0.50** | **0.20** |
| 16 | 128 | 128 (100%) | 0 (0%) | **0.03** | **0.25** |

**理论依据**：
1. **Corr=1.0恒定**：瓶颈独立性——R12实验验证128维足够保留GCC结构
2. **$\varepsilon = (M-128)/M$**：率失真理论——额外维度以指数速率降低MSE，权重正比于容量比例
3. **$\lambda_{\text{peak}}$ 反比于M**：容量越受限，直接优化TDOA峰值的损失越重要

### 2.3 所有CR的损失公式

```
CR=4: L = 1.0·Corr + 0.15·Peak + 0.75·NMSE  → 兼顾重建+TDOA
CR=8: L = 1.0·Corr + 0.20·Peak + 0.50·NMSE  → 优先TDOA
CR=16: L = 1.0·Corr + 0.25·Peak + 0.03·NMSE → 纯TDOA
```

### 2.4 与旧方案的关键区别

| 维度 | R12（GCC中心，ε=0.03统一） | R14（理论版本） |
|------|-------------------------|---------------|
| MSE量纲 | 有量纲（~13），与Corr差4500× | 无量纲（NMSE~1），三同量级 |
| 权重依据 | 经验调参（11轮实验找到ε=0.03） | 容量分配理论（ε=(M-128)/M） |
| CR区分 | 无（排序7/21） | 预期恢复（ε=0.75/0.50/0.03） |
| CR4重建 | 崩溃（NMSE≈1） | 预期改善（ε=0.75强重建驱动力） |
| 代码路径 | 双路径（MSE-primary/GCC-centric） | 统一公式 |

## 三、全部Bug修复记录

### 2026-06-24 理论重构（5项修复）

| 严重度 | 修复 | 文件 | 影响 |
|--------|------|------|------|
| 高 | **MSE归一化(NMSE)**：除以信号功率使损失无量纲 | `train.py:323-327` | 消除4500×量级差，使理论定权成为可能 |
| 高 | **验证损失与训练目标对齐**：val也计算corr+peak+ε·NMSE | `train.py:410-471` | 早停反映真实目标 |
| 中 | **统一GCC公式**：移除双路径判断 | `train.py:377-386` | 简化代码，消除经验分支 |
| 中 | **λ跟踪误导**：adaptive_lambda不参与loss，改为SNR显示 | `train.py:344-350`, `main.py:206-228` | Fig1 Row3不再误导 |
| 低 | **Fig2多CR叠加**：新增generate_snr_data_all+plot_snr_comparison_multi | `evaluate.py:189-263,382-463` | 三CR时频域/GCC叠加对比 |

### 2026-06-23 全代码审计（16项）

| 严重度 | Bug | 文件 | 影响 |
|--------|-----|------|------|
| 高 | **NLOS分支完全失效**——LOS径固定1.0>"NLOS"径最大0.9 | `signal_gen.py:148-153` | 所有历史实验信道=LOS+弱多径 |
| 高 | **GCC定义不一致**——gcc_torch返回cc.real vs peak_loss使用|cc| | `train.py:112vs145` | GCC效率偏低 |
| 中 | **RNG可复现性受损**——apply_channel_and_noise未save/restore RNG | `signal_gen.py:186-238` | 跨批次可复现性 |
| 中 | **RRC群延迟**——16采样点未补偿(不影响TDOA) | `signal_gen.py` | 绝对延迟偏差 |
| 中 | **Rayleigh裁剪**——uniform(0.01,1.0)限制深衰落 | `signal_gen.py:142` | 衰落幅度受限 |
| 低 | CR硬编码、PHAT复数、xlim索引、死变量avg_l等 | 多个文件 | 健壮性 |
| 低 | h[main_delay]=改为+= | `signal_gen.py:122` | |

## 四、实验时间线

| 日期 | 运行 | 阶段 | 关键事件 |
|------|------|------|----------|
| 05/23-26 | — | Deepseek R1-4 | ComplexBN/Linear, RRC, K-fold CV, 早停 |
| 05/27-29 | — | Mimo R5-10 | 固定信道, GCC标准, Median RMSE |
| 06/02-10 | — | Mimo R11-17 | 信道演进→S-V, GCC相关性损失, 20/21 |
| 06/13 | R1 | MSE基线 | 首次完整运行 |
| 06/15 | R4 | MSE最优 | ε≈1, CR4=19/21, CR16=0/21 |
| 06/17 | R9 | GCC中心(ε=0.01) | 范式转换：全CR=21/21, 信号退化 |
| 06/22 | R11 | GCC中心(ε=0.03) | 选定最优ε，全CR=21/21 |
| 06/23 | — | 全代码审计 | 16项Bug修复(NLOS/GCC/RNG等) |
| 06/23 | R12 | Bug修复验证 | 全CR=21/21, CR排序=7/21, NLOS信道 |
| 06/24 | R13 | CR自适应ε测试 | CR_EPSILON字典失败, CR4<CR8/16 |
| 06/24 | — | 理论重构 | NMSE归一化+统一公式+容量分配定权 |
| 06/24 | **R14** | **待运行** | **理论版本: L = Corr+Peak+ε·NMSE** |

## 五、代码库结构

| 文件 | 功能 | 关键位置 |
|------|------|----------|
| `main.py` | 入口+配置+LOSS_CONFIG+Fig1/2/3绘制 | LOSS_CONFIG L41-45 |
| `model.py` | DAE架构(ComplexConv1d/Linear/BN, ResidualBlock) | DAE类L164 |
| `train.py` | 训练循环(NMSE归一化+GCC损失+峰值损失+自适应peak) | train_one_fold L265 |
| `signal_gen.py` | 信号+信道生成(S-V模型, 固定池) | SignalSimulator L50 |
| `evaluate.py` | MonteCarlo实验+GCC评估+绘图+多CR SNR叠加 | MonteCarloExperiment L44 |
| `replot.py` | plot_data.pkl离线重绘 | |
| `代码修改记录/修改记录.md` | 完整修改历史 | §3.10 理论重构 |
| `Obsidian Vault/.../研究总结_文献综述_创新方案.md` | 文献综述+创新方案+研究总结 | |

## 六、实验设计

### 参数配置（R14）

| 参数 | 值 |
|------|-----|
| N_SAMPLES | 50000, BATCH_SIZE=128, K_FOLDS=3 |
| PATIENCE=10, MAX_EPOCHS=150, LR=0.0005 |
| 损失公式 | `L = 1.0·Corr + λp·Peak + ε·NMSE`（各CR不同） |
| CR=4: ε=0.75, λp=0.15 |
| CR=8: ε=0.50, λp=0.20 |
| CR=16: ε=0.03, λp=0.25 |
| 信道 | Saleh-Valenzuela, 50固定快照, 20%真实NLOS |
| SNR | 训练[-10,10]dB, 评估[-10,10]dB, MC=1000 trials |
| 架构 | 3层Conv→ResBlock×2→ComplexLinear→对称解码器 |

### 信号生成

- BPSK, 20MHz带宽, 40MHz采样率, 1024采样点
- RRC脉冲成形(beta=0.2, span=8, sps=2)
- 配对数据(X1, X2同源同噪声不同信道)→GCC训练

## 七、关键发现

| # | 发现 | 证据 |
|---|------|------|
| 1 | **TDOA精度跃升一个数量级** | R4→R12: CR4@-10dB从171→16.9(-89%) |
| 2 | **CR16瓶颈被消除（瓶颈独立性）** | 128维在GCC主导下足够保留GCC结构 |
| 3 | **CR排序消失是正面发现** | GCC结构所需信息量<128维 |
| 4 | **信号退化验证Fowler论断** | MSE≠TDOA的首次深度学习验证 |
| 5 | **MSE未归一化导致权重无意义** | MSE=13 vs Corr=0.003, 差4500× → NMSE归一化后差55× |
| 6 | **ε=0.03恰好补偿自然量级差** | ε≈E[Corr]/E[NMSE]=0.02 → 不是偶然调参 |

## 八、下一步方向

| 优先级 | 方向 | 内容 |
|--------|------|------|
| **P0** | **运行R14验证** | python main.py，验证容量分配理论 |
| P1 | 方案7：频带选择性压缩 | STFT频段重要性→频段注意力→加权损失 |
| P2 | 论文撰写 | 整理R4+R12+R14结果 |
| P3 | N接收器扩展 | 从2→N路信号 |
| P4 | 基线方法 | DFT/Hadamard/PCA/极值编码对比 |

## 九、关键目录

| 路径 | 内容 |
|------|------|
| `运行结果/20260624_125100` | R13（失败，CR4<CR8/16） |
| `运行结果/20260623_184731` | R12（全CR=21/21, CR排序7/21） |
| `运行结果/20260622_113516` | R11（ε=0.03, 预修复最优） |
| `运行结果/20260617_185231` | R9（范式转换: GCC中心） |
| `运行结果/20260615_185915` | R4（MSE基线） |
