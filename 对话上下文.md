# 对话上下文

> 当前状态摘要更新至 2026-07-29。完整历史保存在
> `文档归档/对话上下文_截至2026-07-27_完整历史.md`。

## 1. 当前状态

| 字段 | 内容 |
|---|---|
| 项目目标 | 在相同通信预算下，提高压缩数据的 TDOA 参数估计与多 UAV 定位精度 |
| 当前主线 | BRSR 新块级可观测评分或协议模型的理论设计 |
| 当前阶段 | 方案待提出、待用户审批；尚未批准修改研究代码或运行新实验 |
| 最新配置裁决 | Stage 2A.1=`NO-GO-CONFIG-STAGE2A1` |
| 最新协议裁决 | 块级共享反馈证书=`UPPER-BOUND-ONLY` |
| 路线裁决 | BRSR=`HOLD`；Stage 2B 和 locked 继续阻塞 |
| 当前禁止事项 | 不在原 development 上调阈值或评分，不复用该集合验证新方案，不把 Oracle 上界写成可部署收益 |

## 2. 最新关键证据

- Stage 2A.1 正式目录：`运行结果/BRSR/BRSR_STAGE2A1_20260727_115123_development`。
- Stage 2A.1 SafeGate 总体 RMSE=`3.8719 sample`；同 29-bit BestStatic/SNR-only=`3.7131/3.8002 sample`。相对两者的配对 MSE 改善置信区间均为负或不支持正收益。
- OracleAction RMSE=`3.4082 sample`，相对两类同 bit 静态基线仍有显著正余量，因此当前失败属于评分与门控配置，不是动作空间已经被否定。
- 协议证书目录：`运行结果/BRSR/BRSR_STAGE2A1_PROTOCOL_20260727_161528_diagnostic`。
- BlockPredicted 在 `L=1/2/4/8/40` 均未稳定超过两类同 bit 静态前沿；BlockOracle 在 `L=1/2/4/8` 通过，`L=40` 未通过。
- 当前“块内平均预测风险最小”不能兑现 Oracle 余量；逐样本不同动作却摊薄反馈 bit 只是不具备部署含义的乐观上界。

详细参数、指标和证据来源见 `实验结果记录.md` 及其历史归档。

## 3. 当前代码与入口

| 路径 | 作用 |
|---|---|
| `brsr_stage2_rate_adaptive/stage2a1.py` | Stage 2A.1 安全动作门控核心 |
| `brsr_stage2_rate_adaptive/run_stage2a1.py` | Stage 2A.1 smoke/development 入口 |
| `brsr_stage2_rate_adaptive/diagnose_stage2a_failures.py` | 冻结 Stage 2A 失败机理只读诊断 |
| `brsr_stage2_rate_adaptive/diagnose_stage2a1_protocol.py` | Stage 2A.1 反馈与块级协议只读证书 |
| `brsr_route_feasibility/` | BRSR Stage 1 路线可行性证书 |
| `brsr_tdoa/` | BRSR Stage A、Stage B v1/v2 历史实现 |
| `main.py` | 论文复现、DAE、传统基线和 V5 等既有主工程入口；不是当前 BRSR 下一步默认入口 |

当前没有需要重复运行的正式入口。新方案获批前只允许只读分析和理论设计。

## 4. 路线状态

| 路线 | 当前用途 | 状态 |
|---|---|---|
| BRSR | 当前研究主线 | `HOLD`，等待理论上不同的新评分或协议 |
| PASR-TDOA v1 | 主动逐层协议失败机理与消融 | 配置 `NO-GO-CONFIG` |
| PFRS 固定掩码 | Fisher/全局错峰联合设计证据 | 当前配置失败；冻结有限空间为限定范围内路线停止 |
| V5B-Shared64 / Top-K | 严格 CR16 与共同后端消融 | `HOLD` |
| GeoHybrid / DFT / Cao / Zhai | 任务型强基线 | 冻结，除非出现新研究假设 |
| R20/R20.1、FreqDAE v1-v4 | 波形重构失败机制 | 冻结 |
| 双站异构插值补全 | 否定性机制证据 | 冻结 |

精确门禁状态和适用边界以 `研究门禁注册表.md` 为准。

## 5. 当前未解决问题

1. 如何仅用可部署观测构造与真实动作收益稳定相关、且能控制重尾风险的块级评分。
2. 如何让多个观测共用反馈动作，同时保持真实缓存、延迟和前向载荷语义。
3. 如何在设计阶段解释新评分为何可能逼近 BlockOracle，而不是继续组合现有弱风险特征。
4. 如何在全新 development 数据上预注册强基线、效应量、置信区间、功效和门槛敏感性。

## 6. 下一步

- 先提出理论方案，不修改代码。
- 方案必须列明可观测量、动作语义、块长、反馈、缓存、延迟和总 bit。
- 用户批准后才实现，并使用与现有 Stage 2A/2A.1 数据互斥的新 development 集合。
- 只有可观测策略通过同 bit 强基线及统计、稳健性门禁，才允许讨论 Stage 2B 或新的 locked 设计。

当前唯一任务及审批状态见 `当前任务.md`；完整路线历史见 `研究总结.md`。
