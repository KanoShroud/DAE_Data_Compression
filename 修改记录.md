# 修改记录

本文件是根目录的短版长期修改记录，用于新对话快速恢复近期工作。历史详细记录保留在 `代码修改记录/修改记录.md`，不得删除或覆盖。

本文件只记录已经执行的代码或文档修改。未执行的方案、讨论和待审批事项不写入本文件，应写入 `当前任务.md`。

历史记录按当时状态保留，可能与后续记录中的最新状态不同；判断当前状态时以 `对话上下文.md` 和 `当前任务.md` 为准。

本文件记录有项目意义的代码或文档修改。轻微排版、错别字、仅更新任务状态、仅追加本文件记录本身，不单独产生新的修改记录；更新本文件是记录流程的一部分，不递归记录。

## 记录模板

每条记录包含：

- 日期时间
- 分支名
- 本轮目标
- 修改/生成文件
- 核心变化
- 验证方式
- 验证结果
- 是否产生实验结果；若是，见 `实验结果记录.md`
- 未解决问题
- 下一轮建议

---

## 2026-06-29 18:52:57 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 建立根目录长期上下文维护文档，并固定后续每轮文档更新规则 |
| 修改/生成文件 | `AGENTS.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | 新增 Codex 项目规则；重写短版对话上下文；建立当前任务记录；建立根目录短版修改记录；建立实验结果记录模板和历史摘要 |
| 验证方式 | 检查 Git 状态、当前分支、最近 5 条 commit、根目录结构；读取所有 Python 文件、`.gitignore`、既有上下文、历史修改记录、Obsidian 研究总结 |
| 验证结果 | 文档已生成/更新；本轮未运行训练、评估或语法检查；未修改训练代码 |
| 是否产生实验结果 | 否 |
| 未解决问题 | R20 尚未完整运行；CR 排序、伪峰、时频域恢复效果待验证；环境依赖文件缺失；终端中文注释显示乱码问题待确认 |
| 下一轮建议 | 用户确认后运行 R20；读取新 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3；再决定是否进入频域选择/频带注意力方案 |

## 2026-06-30 11:31:41 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 A1-A7，完成 R20 审查后的 R20.1 代码修改，并使代码进入待完整运行状态 |
| 修改/生成文件 | `main.py`、`train.py`、`evaluate.py`、`replot.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心改动 | 拆分 `mse_weight_max` 与 `phase_mix`；best-state 默认从 warmup 后开始选择；保存 mag/complex NMSE 分量；Monte Carlo 保存 trial squared error、RMSE 95% CI、clean peak diagnostics；Fig2 pkl 保存多样本重建/GCC/PSD 诊断；replot 输出 R20.1 配置 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；CPU 小样本 inline smoke |
| 验证结果 | 编译通过；冒烟通过，覆盖 `train_with_cv` 新字段、`generate_snr_data_all(..., diagnostic_trials=2)` 与 Fig3 CI 绘图；未运行完整训练 |
| 是否产生实验结果 | 是；见 `实验结果记录.md` 中 R20 审查与 R20.1 冒烟验证记录 |
| 风险或未解决问题 | R20.1 尚未验证真实效果；R20 已显示 CR 区分度不足和时频域恢复差，loss-level 修复未必能达成 CR4 > CR8 > CR16；多径/NLOS 下 clean GCC peak 与物理 TDOA 的一致性需用新诊断字段复核 |
| 下一轮建议 | 完整运行 `python main.py` 生成 R20.1 结果；读取 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3；重点检查 `fold_best_epoch`、mag/complex NMSE、Fig2 diagnostics、RMSE CI 和 clean peak diagnostics |

## 2026-06-30 文档治理

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 按用户提供的文档治理方案，清理 5 个根目录 md 文档的职责边界 |
| 修改/生成文件 | `AGENTS.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | `AGENTS.md` 去动态化；`当前任务.md` 单任务化；`对话上下文.md` 精简为研究状态摘要并修正 R20/R20.1 表述；`修改记录.md` 明确只记录已执行修改；`实验结果记录.md` 移除非实验项并补充证据来源、是否可用于论文、复核需求字段 |
| 验证方式 | 检查 Git 状态、当前分支和最近 5 条 commit；读取 5 个根目录 md 文档；修改后复读关键文档内容 |
| 验证结果 | 本轮仅修改文档；未修改 `.py` 代码；未运行训练、评估、`py_compile` 或冒烟测试 |
| 是否产生实验结果 | 否 |
| 未解决问题 | R20.1 完整训练仍待用户确认后执行；工作树仍包含前序未提交代码/文档修改 |
| 下一轮建议 | 用户审阅文档治理结果；若继续实验流程，优先决定是否启动完整 R20.1 训练或读取新的实验结果 |

## 2026-06-30 文档治理轻量精修

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 按用户提供的轻量精修方案，完善文档治理规则与 R20.1 结果判断标准 |
| 修改/生成文件 | `AGENTS.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | `AGENTS.md` 将项目路径改为本地参考路径，放宽并完善 py_compile 规则，补充非递归记录原则；`对话上下文.md` 新增 R20.1 结果判断标准；`实验结果记录.md` 补充论文可用结果的复现要求；`修改记录.md` 补充历史记录解释与非递归原则；`当前任务.md` 切换为文档治理轻量精修待审阅 |
| 验证方式 | 检查 Git 状态、当前分支和最近 5 条 commit；读取 5 个根目录 md 文档；修改后用 `rg` 检查关键规则 |
| 验证结果 | 本轮仅修改文档；未修改 `.py` 代码；未运行训练、评估、`py_compile` 或冒烟测试 |
| 是否产生实验结果 | 否 |
| 未解决问题 | R20.1 完整训练仍待用户确认后执行；本轮未切换到实验执行任务 |
| 下一轮建议 | 用户审阅轻量精修结果；审阅通过后可将 `当前任务.md` 切换为“实验流程待继续 / R20.1 完整训练待确认” |

## 2026-07-01 12:21:20 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 建立 Chen 2025 论文复现基线轨道 `paper_repro_v1`，使当前代码进入待完整运行状态 |
| 修改/生成文件 | `main.py`、`train.py`、`signal_gen.py`、`evaluate.py`、`replot.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | `main.py` 新增 `EXPERIMENT_MODE`，默认 `paper_repro`，并保留 `$env:DAE_EXPERIMENT_MODE='r20_1'` 切回 R20.1；`train.py` 新增 `paper_mse` 分支，关闭 Corr/Peak/FI、warmup 和 grace period，使用论文式实/虚 MSE；`signal_gen.py` 新增 LOS 标签、NLOS 概率、训练 SNR 范围和多径强度控制；`evaluate.py` 支持可配置 SNR/Monte Carlo 次数并新增 clean peak 标签一致性自检；`replot.py` 补充新配置打印 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py diagnose_dae.py`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；PyTorch 小样本 inline smoke；默认 50 固定信道 clean peak 自检 |
| 验证结果 | 两套 `py_compile` 均通过；小样本 `paper_mse` 训练、Monte Carlo 与 Fig2 诊断链路通过，输出 `SMOKE_OK 1 paper_repro_v1 2 [-10, 0] 1.0`；默认 50 信道在 `multipath_scale=0.1` 下 clean peak 自检为 match=1.0、within2=1.0、mean_abs_peak_error=0.0 |
| 是否产生实验结果 | 是；仅限代码验证和数据标签自检，见 `实验结果记录.md`；未运行完整训练 |
| 风险或未解决问题 | `paper_repro_v1` 仍是合成 LOS-dominant 基线，不是 Wireless InSite；当前完整评价仍是 TDOA RMSE 代理，不是论文多 UAV 几何定位误差；完整 `python main.py` 尚未运行 |
| 下一轮建议 | 用户确认后运行默认 `python main.py`；读取新结果目录的 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3；先判断 MSE 频谱复现与 CR 排序，再决定是否实现几何定位或继续创新方案 |

## 2026-07-01 13:33:45 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 审查 `paper_repro_v1` 完整结果 `运行结果/20260701_122523`，更新项目上下文与实验事实 |
| 修改/生成文件 | `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | 记录 `paper_repro_v1` 完整结果：MSE/PSD 重构呈 CR4>CR8>CR16 的合理趋势，但 TDOA RMSE 与论文定位趋势不一致；明确当前差异主要来自合成 LOS-dominant 数据、TDOA 代理指标和缺少多 UAV 几何定位 |
| 验证方式 | 读取 `run.log`、`plot_data.pkl`、SVG 对应数据；运行轻量 Python 分析脚本；加载 `model_cr4/8/16.pt` 做按 SNR 重构 MSE 诊断 |
| 验证结果 | CV best ValMSE 均值约 CR4=1.108、CR8=1.302、CR16=1.455；Monte Carlo 平均 RMSE 约 Raw=15.21、CR4=25.69、CR8=25.73、CR16=29.19；Raw 在 0 dB 以上接近 0，压缩模型无法优于 Raw |
| 是否产生实验结果 | 是；见 `实验结果记录.md` |
| 风险或未解决问题 | 本轮未修改训练代码；当前结果仍非严格论文复现，因为缺少 Wireless InSite、多 UAV 几何定位、论文真实预处理/归一化细节 |
| 下一轮建议 | 若继续复现，优先实现或近似多 UAV 几何定位和数据/归一化审查；若进入创新，应以当前 MSE baseline 作为重构基线而非 TDOA 结论基线 |

## 2026-07-01 14:43:25 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 A+B+C 三阶段修改，建立 `paper_repro_v2_urban8` 论文近似复现轨道 |
| 修改/生成文件 | `main.py`、`train.py`、`signal_gen.py`、`evaluate.py`、`replot.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | `signal_gen.py` 新增 `urban8` 场景：单目标、8 UAV、固定建筑物遮挡、LOS mask、路径损耗、分数时延、多径与 per-observation RMS；`train.py` 新增 snapshot-level GroupKFold；`evaluate.py` 新增亚采样 TDOA、8 UAV 非线性最小二乘定位 RMSE、Clean oracle；`main.py` 默认 `paper_repro` 切换为 `paper_repro_v2_urban8` 并保留 R20.1 环境变量切换；`replot.py` 补充新配置打印 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py diagnose_dae.py`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；PyTorch 小样本 urban8 训练/评估/Fig2 冒烟；默认 urban8 clean peak/定位轻量检查；R20.1 旧路径冒烟；`git diff --check` |
| 验证结果 | 两套 `py_compile` 均通过；urban8 冒烟输出 `SMOKE_OK paper_repro_v2_urban8 2 localization_m [-10, 0] 1.0`；默认 50 snapshot clean peak 自检 exact=0.5625、within1=1.0、within2=1.0，exact 降低来自分数时延；R20.1 旧路径输出 `TASK_SMOKE_OK R20.1 1`；`git diff --check` 仅提示既有/编码相关行尾警告 |
| 是否产生实验结果 | 是；仅限代码验证和小样本冒烟，见 `实验结果记录.md`；未运行完整训练 |
| 风险或未解决问题 | v2 仍是合成城市近似，不是 Wireless InSite；当前 LOS 选择为 oracle mask，尚未实现论文 MLP LOS 检测；DFT/Hadamard/PCA/DNN-DAE/CNN-DAE 对比基线未实现；完整 `python main.py` 尚未运行 |
| 下一轮建议 | 用户确认后运行默认 `python main.py`；读取新结果目录并重点检查 localization RMSE、Clean oracle、Raw/CR 趋势、95% CI、Fig2 重构阶梯和 pkl 配置一致性 |

## 历史索引

| 日期/阶段 | 位置 | 摘要 |
|---|---|---|
| 2026-05-23 至 2026-06-23 | `代码修改记录/修改记录.md` | 基础复现、ComplexBN/Linear、RRC、固定信道、GCC、早停、可视化、R1-R12 结果 |
| 2026-06-24 至 2026-06-27 | `代码修改记录/修改记录.md` | R13-R19 损失函数诊断：NMSE、包络 NMSE、alpha-blend、纯包络、伪峰和 CR 区分度问题 |
| 2026-06-29 | `代码修改记录/修改记录.md`、`对话上下文.md` | R20 代码进入待运行：固定混合 NMSE、same lag GCC/Peak、val 分量记录、目标一致性修复 |
