# 修改记录

> 本文件已于 2026-07-08 按用户要求瘦身：保留近期关键修改的详细记录，较早且与当前路线关系较弱的记录压缩为历史摘要。详细实验事实以 `实验结果记录.md` 为准。

## 2026-07-23 研究门禁与路线裁决长期规则

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-23 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 将用户确认的门禁治理准则固化为项目级长期规则，并建立可追溯的统一门禁注册表，避免把人为阈值误作理论结论。 |
| 修改/生成文件 | 新增 `研究门禁注册表.md`；修改 `AGENTS.md`、`修改记录.md`；另向 Codex 持久记忆补充规则说明。未修改 Python、实验配置或运行结果。 |
| 核心改动 | 注册表统一定义门禁类型、状态等级、通用规则和敏感性审计流程；登记 PFRS、PASR、BRSR Stage A/B/v2 的原始门禁、来源、证据、当前分级裁决和外推边界；缺少正式门禁的路线明确标为待回溯审计。`AGENTS.md`中的原则压缩为四条，并新增注册表的文档职责和更新触发规则。 |
| 验证方式 | UTF-8回读新增与修改章节，检查Markdown标题、表格列数、Gate ID唯一性、内部链接和`git diff --check`。 |
| 验证结果 | UTF-8编码、Markdown标题/表格、42个Gate ID唯一性、注册表与BRSR代码阈值对照均通过；`git diff --check`无空白错误，仅有既有Windows行尾提示。本轮未运行实验。 |

## 2026-07-22 BRSR Stage B v2 连续复增益剖面门禁

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-22 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 按三步方案先隔离验证连续复增益处理；固定候选频点、通信预算和策略，只有步骤1通过才允许进入候选池及主动策略实验。 |
| 修改/生成文件 | 新增 `brsr_tdoa/diagnose_stage_b_v2_profile.py`、`brsr_tdoa/test_stage_b_v2_profile.py`；更新 `当前任务.md`、`对话上下文.md`、`实验结果记录.md`。未修改 Stage B v1 核心、候选池、`main.py` 或历史结果。 |
| 核心改动 | 独立实现理想相移量化剖面、融合端完整A观测的线性有限窗量化剖面、无量化剖面和真值复增益辅助诊断；复增益在`[0.7,0.9]`和完整相位圆上由冻结网格初始化后做4轮确定性局部精化。固定BestStatic/SNR-only T32计划，使用独立种子和精确4-bit I/Q区间概率；新增门禁、轨迹聚类bootstrap、数据完整性检查、partial保存、run.log和SVG。 |
| 验证方式 | 项目解释器`py_compile`、Ruff F/E9；新增3项测试；全量BRSR 21项pytest；`validate_project_integrity.py`；正式360观测运行；JSON/CSV/SVG、重复行、有限值、partial清理和`run.log`审计；`git diff --check`。 |
| 验证结果 | 正式目录=`运行结果/BRSR_STAGE_B_V2_STEP1_20260722_192558_research`，状态=`STEP1_NO_GO`。主方法总体RMSE=`3.5587 sample`，当前方法=`3.3999`；配对MSE增加95% CI=`[0.1470,2.0677] sample²`，最坏逐SNR退化`10.32%`，高SNR严重错峰率由`3.33%`升至`4.17%`。按门禁未执行步骤2和步骤3。 |

## 2026-07-22 BPSK 报告审计修订与稳健性复核

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-22 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行已批准的 A+B：收紧 BPSK 扩展报告的理论表述与信息预算口径，并只读冻结的150,000次正式试验，补充搜索边界、条件异质性和估计器实现的审计。 |
| 修改/生成文件 | 修改 `standalone_frequency_alignment/plot_bpsk_parameter_sweep.py`、README、`Obsidian Vault/插值频谱补全与TDOA_20260722.md` 及四份项目记录；重导出图11、4份新增 CSV 和既有汇报图。未运行新的 Monte Carlo，未修改 `main.py`、BRSR、Shared64、V5-A.1 或冻结 trial。 |
| 核心改动 | 正式主实验的受控随机掩码与仅用于非对称对照的 Zhai 选频明确分离；补充时域补零密集互谱的跨频率双线性展开、秩等于 $M$ 的完备条件、Fisher 信息的可逆变换边界、噪声/SNR精确定义和复系数数量预算。报告将“真实联合上界”“伪补全”“门禁”分别规范为“联合集合真实频谱对照”“原网格确定性补全”“数值一致性检验”。新增表14—15和图11，给出边界分层、400条件等权效应和40条件定义式重放。 |
| 验证方式 | 重读500个条件 chunk；项目解释器 `py_compile`、Ruff F/E9；全量重绘11张图；公式98项连续编号、Markdown公式块、11个图片引用和24个相对证据链接检查；`git diff --check`。 |
| 验证结果 | 120,000个正重叠试验中 TZP-DS 的试验等权 MAE 改善为`-0.2915 sample`，重抽样95%区间`[-0.3178,-0.2671]`；去除搜索边界1 sample内试验后仍为`-0.2674`，95%区间`[-0.2928,-0.2419]`。400个条件中316个更差、84个更好；40个正式条件中快速估计与定义式直接求和的最大差为真实交集`3.55e-15 sample`、TZP-DS为0。结论改为“固定8倍补零和等权密集匹配器下无总体收益且存在损失”，不再误写为所有条件均恶化。 |

## 2026-07-22 BPSK 扩展报告证据表补全与术语澄清

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-22 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 补齐时域补零结果中“按频点预算、按信号结构”的数值证据，并消除“密集频谱非零值”“TDOA 得分”等容易被误解的表述。 |
| 修改/生成文件 | 修改 `plot_bpsk_parameter_sweep.py`、README、BPSK 扩展独立汇报、`实验结果记录.md` 和本记录；从既有正式目录重新导出两份分层 CSV 及报告 SVG。未重新运行 Monte Carlo，未修改 `main.py`、BRSR、Shared64、V5-A.1 或既有正式 trial。 |
| 核心改动 | 绘图器新增按单站频点数与信号结构汇总的时域补零/真实交集 MAE、RMSE 与离群率表；报告新增交集配对 MAE 表、完整分层误差表，并将补零算子门禁、分层误差和密集频谱质量分别编号为表11—13。全文统一使用“派生密集点”指代由已传输频点经固定变换计算出的非零数值，明确其不等于新测量或已恢复的缺失频点。 |
| 验证方式 | 回读正式 CSV 并逐项复算表中预算/信号分层值；项目解释器编译、Ruff F/E9、全量重绘及正式门禁回读；检查10张 SVG、Markdown 图表引用、95个独立公式与连续编号、`git diff --check`。 |
| 验证结果 | 表12的4个预算行和5个信号行均与正式结果 CSV 一致；表10的120,000个正重叠配对表明确给出时域补零 MAE 改善`-0.291`、95%区间`[-0.317,-0.267] sample`。本轮只是证据展示和措辞修订，既有“时域补零没有恢复缺失频点且会降低当前 TDOA 性能”的正式结论不变。 |

## 2026-07-21 时域补零密集频谱正式对照

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-21 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在既有 BPSK 扩展实验中保留全部9种方法、500个条件和评价协议，新增固定8倍“时域补零密集频谱”对照，直接检验频率栅格加密能否恢复未选频点并提高 TDOA。 |
| 修改/生成文件 | 新增 `standalone_frequency_alignment/tzp_dense.py` 与 `test_tzp_dense.py`；修改 `run_bpsk_parameter_sweep.py`、`plot_bpsk_parameter_sweep.py`、README、BPSK扩展独立汇报和4份项目记录；生成独立正式目录 `运行结果/频谱插值对齐/20260721_172703_080303_bpsk_parameter_sweep` 及图9、图10。未修改 `main.py`、BRSR、Shared64、V5-A.1 或既有正式结果。 |
| 核心改动 | 对每站稀疏频谱执行正交 IFFT、时域末尾补零至4096点、幅度校正 FFT，并用 ZoomFFT 在统一0.01 sample网格独立估计 TDOA；新增原频点一致性、原缺失能量、算子秩、Gram矩阵和直接DTFT一致性门禁，以及密集频谱 NMSE、相位RMSE、复相关、数值非零率和零重叠行为。原五种伪补全仍只在门禁通过后复用交集估计。 |
| 验证方式 | 项目解释器编译；Ruff F/E9；10项单元测试；10方法 smoke；500条件正式运行；绘图 `--validate-only`；150万行完整性、24项算子门禁、配对bootstrap、10张SVG/PNG解析与哈希、Markdown公式/编号/链接检查；`git diff --check`。 |
| 验证结果 | 500/500条件、150,000基础 trial、1,500,000方法行、5,000汇总行和9条配对结果完整。时域补零算子秩在四种 $M$ 下均等于 $M$，原缺失能量不超过`2.26e-29`。正重叠时真实交集与补零密集频谱 MAE/RMSE=`2.6089/7.9421`与`2.9004/8.3158 sample`，平均 MAE 改善95%区间=`[-0.3166,-0.2672]`；零重叠补零 MAE/RMSE=`17.5453/22.9652 sample`。 |
| 路线影响 | 频率栅格加密没有增加独立观测，且等权使用相关密集点会稳定恶化 TDOA。该独立路线继续冻结；BRSR 当前主任务及其他创新路线状态不变。 |

## 2026-07-21 BPSK 扩展报告 MAE/RMSE 与等价性重构

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-21 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 将 BPSK 扩展报告由过程式实验记录重构为学术论文式论证；按插值输出真实统计有效共同频点率，前置并细化等价性判定，修正热图色阶，补充非对称选频与真实联合上界的量化图表。 |
| 修改/生成文件 | 修改 `standalone_frequency_alignment/plot_bpsk_parameter_sweep.py`、`standalone_frequency_alignment/README.md` 和 `Obsidian Vault/导师汇报_插值频谱补全与TDOA_BPSK扩展_20260720.md`；基于原正式结果重绘8张 SVG 和8张300 dpi PNG，新增独立重构、等价性判定和信息结构分 SNR 数据表。未重跑150,000次 Monte Carlo，未改动旧结果或其他创新路线。 |
| 核心改动 | 有效共同频点率改为对插值后复频谱应用统一幅度阈值，再统计双站同时有效频点数除以 $M$；精确重放500个正式条件首试验，独立执行五种重构并复算400个可估计 TDOA。报告明确区分运行前20项算子门禁、运行后独立复核与120,000条复用结果的一致性检查。热图改为高值深色；新增非对称选频、真实交集和真实联合上界的分 SNR MAE/RMSE 图表；正文加入摘要并按引言、模型、理论、方法、结果、讨论和结论组织。 |
| 验证方式 | 正式结果门禁回读；首试验随机流与正式 trial 精确对齐；项目解释器 `py_compile`；Ruff F/E9；全量重绘；8张 PNG/SVG 视觉审查；独立审计表、bootstrap、SVG XML、PNG、manifest、Markdown 公式、图表编号、本地引用和旧结果哈希检查。 |
| 路线影响 | 仅增强已完成的插值否定性证据展示；新增 RMSE 显示低 SNR/小 $M$ 的错峰长尾，但不改变“五种伪补全与真实交集逐 trial 相同”的结论。BRSR、Shared64、V5-A.1 等路线未修改。 |

## 2026-07-20 插值伪补全 BPSK 扩展全量验证与独立报告

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 不覆盖原 `synthetic_full` 代码、结果和报告，修正旧图1的 A3 图例与 IQ 波形展示；再按 `N=512`、四种 $M$、五种 SNR、每条件300次和 `[-25,25] sample` 时延范围新增 Urban8 同参数 BPSK-RRC 扩展验证、正式绘图和独立报告。 |
| 修改/生成文件 | 修改 `plot_interpolation_mechanism.py` 与 README；新增 `run_bpsk_parameter_sweep.py`、`plot_bpsk_parameter_sweep.py` 和 BPSK 扩展报告；生成新正式目录及独立 assets；同步更新 `对话上下文.md`、`当前任务.md`、`实验结果记录.md`。原报告、原结果和其他创新路线未覆盖。 |
| 核心改动 | 旧图 A3 图例拆分到左右上角，第二列改为无噪声 I、含噪完整 I/Q、稀疏 I/Q 原始点和8倍插值。新运行器实现500条件分块、原子保存、断点续跑、旧结果 SHA-256 冻结、Urban8 同参数 BPSK-RRC，以及与直接矩阵等价的批量 FFT 时延估计；20项原网格门禁通过后复用五种伪补全与交集的确定性相同估计。新绘图器生成5张 SVG/PNG 和7份报告表。 |
| 验证方式 | 项目解释器 `py_compile`；本机 Ruff F/E9；`test_alignment_core.py` 7项测试；新 smoke；500条件全量运行；1,350,000行完整性、20项算子门禁、配对差异、FFT估计器、CSV/JSON/SVG/PNG、Markdown公式/表格/图片/链接和 Git 差异检查。 |
| 验证结果 | 500/500条件、150,000基础 trial、1,350,000方法行全部完成；120,000个可估计配对 trial 中五种伪补全相对真实交集的点估计、绝对误差、平均改善、95%区间和最大单 trial 差异均为0；最大生成缺失能量`3.21e-29`，最大频谱差`9.19e-16`。 |
| 路线影响 | BPSK-RRC 与四类机制频谱均否定纯插值恢复和 TDOA 增益，进一步支持路线冻结。BRSR、Shared64、V5-A.1 等主线状态未改变。 |

## 2026-07-20 插值汇报理论与图表修正

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 回答四类合成频谱的典型性、随机种子、分数时延和四种插值数学模型，并修复导师汇报图中频谱尺度、频谱类型覆盖、九方法展示和子图图例缺失。 |
| 修改/生成文件 | 修改 `standalone_frequency_alignment/plot_interpolation_mechanism.py`、`standalone_frequency_alignment/README.md`、`Obsidian Vault/导师汇报_插值频谱补全与TDOA_20260720.md`、`实验结果记录.md` 和本记录；正式生成 `figures/advisor_report` 下3张SVG、3张PNG和图表清单，并复制到 Obsidian assets。未修改原始逐试验结果、主流程或其他创新路线。 |
| 核心改动 | 图1改为4行×4列，分别覆盖平滑复谱、平滑幅度随机相位、宽带随机复谱和多频带随机相位；第1列同时显示无噪声包络和完整noisy频谱；完整插值频谱使用独立线性纵轴，缺失频点使用独立对数纵轴；A3图例改为不透明白底。图2上图合并标注五种完全重合的伪补全得分，下图逐行列出表2全部九种方法的点估计。图3四个子图均补充图例。文档新增四类频谱的精确生成式、典型性、正式随机流与机制子种子、频域相位斜坡分数时延，以及 Ideal/FIR/Linear/Cubic 的数学模型和原网格映射。 |
| 验证方式 | 项目解释器 `py_compile`；本机 Ruff F/E9；`--validate-only`；三张图内存绘制和临时目录视觉预览；正式绘图；SVG XML、PNG和UTF-8 JSON读取检查；Markdown 公式、表格、标题和图片引用检查；`git diff --check`。 |
| 验证结果 | 编译和 Ruff 通过；60,000个正式基础试验与48,000个有效配对门禁保持不变。四类机制样本均为10个共同频点，五种伪补全相对真实交集的最大得分差均为0，最大生成缺失能量不超过`7.76e-30`。正式输出3张SVG、3张300dpi PNG和1个图表清单，结果目录与Obsidian副本均通过读取检查。 |
| 风险或未解决问题 | 图1信息量较大，适合文档横向整页展示；PPT中建议按频谱类型或列拆分讲解。 |

## 2026-07-20 插值伪补全独立汇报与绘图入口

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 将进展总结中的“双站异构选频与插值伪补全”提取并扩展为初读者可理解的独立导师汇报，同时新增只读取既有正式结果的独立绘图入口；正式绘图由用户在 PyCharm 中运行。 |
| 修改/生成文件 | 新增 `standalone_frequency_alignment/plot_interpolation_mechanism.py`、`Obsidian Vault/导师汇报_插值频谱补全与TDOA_20260720.md`；更新 `standalone_frequency_alignment/README.md` 和本记录。未修改 `main.py`、现有插值算法、正式结果、`进展总结_20260715.md` 或其他创新路线文件。 |
| 核心改动 | 绘图入口从既有 `20260714_184753_411401_synthetic_full` 读取540,000行方法结果，强制校验五种伪补全与真实交集逐试验等价；另用固定种子20260720生成非事后筛选的机制样本，计划输出波形/频谱、连续时延得分和总体TDOA性能三组SVG与300 dpi PNG，并保存图表清单。独立汇报补充双站模型、Zhai CRLB、线性算子秩、Fisher信息边界、实验参数、指标公式、配对统计、结果解释、限制和运行索引。 |
| 验证方式 | 新脚本 `py_compile`；因项目解释器未安装 Ruff 模块，使用本机已有 Ruff 可执行文件执行 F/E9 检查；`--validate-only` 读取正式结果并校验配对等价和固定机制样本；三张图执行内存绘制 smoke、不写入正式结果；Markdown 展示公式、表格、标题和图片路径检查；`git diff --check`。 |
| 验证结果 | Ruff F/E9、编译和三张图内存绘制均通过。`--validate-only` 确认60,000个基础试验、540,000行方法结果、48,000个有效配对，五种方法的最大时延差、最大绝对误差差和配对统计绝对值均为0；固定机制样本重叠率0.5、10个共同频点、最大时延得分差为0、最大生成缺失能量约`7.10e-30`。正式图片未运行。 |
| 风险或未解决问题 | 项目 PyTorch 环境仍未安装 `ruff` 模块；独立汇报中的三张新增图片将在用户正式运行绘图脚本后出现。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `standalone_frequency_alignment/plot_interpolation_mechanism.py`，回读 `figures/advisor_report/advisor_figure_manifest.json`、三张SVG和PNG后再进行最终视觉审查。 |

## 2026-07-20 Weiss 2026 联合极值压缩与检测全文复核

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 阅读 Zotero 新导入论文 *Joint extremum compression and detection of a time-delayed signal for distributed sensing* 全文，补充极值编码的检测理论、适用边界及对当前创新路线的启发。 |
| 修改/生成文件 | `Obsidian Vault/文献综述_20260714.md`、`修改记录.md`。未修改 Python、配置、模型、运行结果、`当前任务.md` 或 `实验结果记录.md`。 |
| 核心改动 | 新增连续时间未知时延检测模型、最大索引检测（MID）、非渐近虚警/漏检界和通信一致性结论；明确其任务是共同源检测而非 TDOA MSE，且 `N=2^k` 同时增加消息长度与观测时长。补充 FI 基线为极低码率检测改写、多径检测成功不保证直达径 TDOA 正确等公平性边界。新增“极值检测门控 + 风险驱动 TDOA 精化”的远期两阶段启发，并明确该启发不解决当前 BRSR 的候选池和剖面似然问题。参考文献扩展至 `[83]`。 |
| 验证方式 | Zotero `complete` 模式读取正式条目和 PDF 全文；UTF-8 回读；检查标题、公式定界符、代码围栏、Markdown 表格、参考文献连续性、正文引用闭合、Zotero key/DOI 和 `git diff --check`。 |
| 验证结果 | 综述展示公式定界符与代码围栏均成对，Markdown 表格列数一致，无重复标题或失效本地链接；参考文献连续扩展为 `[1]`--`[83]`，正文无未定义引用或未使用条目；新条目的 DOI `10.48550/arXiv.2607.10874` 与 Zotero key `79D8A9M9` 已核对。`git diff --check` 无空白错误，仅报告工作树既有文件的 LF/CRLF 提示。 |
| 风险或未解决问题 | 该条目截至本轮是 2026-07-12 发布的 arXiv v1 预印本，尚非正式期刊版本。两阶段检测--估计结构仅为项目启发，未经实现或实验验证，不能表述为已完成创新。 |
| 下一轮建议 | 当前 BRSR 主线仍先验证连续复增益解析剖面似然和新候选池；若未来场景包含信号缺失、错误源关联或机会感知，再独立审批极值检测门控与 TDOA 精化的联合风险模型。 |

## 2026-07-20 BRSR-TDOA 阶段 B 第一阶段失败机理诊断

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在冻结 Stage B v1 算法、候选池、通信协议和既有 development 结果的前提下，实现模型一致性、复增益离散化、停止/动作上界、Rollout 稳定性和候选池离线审计，避免直接在同一 evaluation 集上调参。 |
| 修改/生成文件 | 新增 `brsr_tdoa/diagnose_stage_b_failures.py`、`brsr_tdoa/test_stage_b_failures.py`；更新 `brsr_tdoa/README.md`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`；最终 smoke 为 `运行结果/BRSR_STAGE_B_DIAG_20260720_154417_smoke`。未修改 `brsr_core.py`、`brsr_stage_b.py`、`brsr_waveform.py`、`run_stage_b.py` 或原 development 结果。 |
| 核心改动 | 新入口按原 manifest、代码哈希、候选元数据和 evaluation seeds 重建冻结实验。无噪声诊断比较线性有限窗与循环相移控制；固定已传输码比较正式粗复增益网格、细网格和真值复增益上界；在同一两动作空间中穷举事后最优测量状态，分离停止剩余信息与动作排序差距；在预注册少量样本上比较 Rollout 的 Q 敏感性；候选池只计算未知复增益投影后的局部信息代理、`1/2/4 sample` 多尺度模糊和逐频点留一贡献，不重选频。 |
| 原理与公平性处理 | 细复增益网格和真值复增益共用正式运行已经产生的量化码、公共尺度和频点层级，避免把重新量化收益混入 nuisance 诊断。真值复增益和事后动作仅标记为不可部署上界；候选审计明确为未量化信息代理，不冒充精确 Schur-Fisher 或性能结果。正式 Q16 重建须与冻结 Rollout-T32 逐样本对齐；任一冻结源码哈希、结果行或动作层级不一致会直接报错。 |
| 验证方式 | 全项目 Python 编译；根目录及 `brsr_tdoa/*.py` Ruff F/E9；阶段 A/B/诊断共18项 pytest；主项目 `validate_project_integrity.py`；冻结4个 Stage B 源文件 SHA-256 对齐；端到端 smoke；严格 JSON、CSV 行数、无 partial、SVG XML和无头渲染检查。 |
| 验证结果 | 所有编译、Ruff、18项测试和项目科学完整性门禁通过；冻结4文件哈希完全一致。smoke 生成7个CSV、2个SVG、summary/manifest/run.log，无 partial；正式后验重建与冻结结果最大偏差不超过`1e-10`。循环控制NMSE约`8.64e-32`，证明FFT符号链正确；其他 smoke 数值样本极少，仅证明诊断链可运行。 |
| 风险或未解决问题 | smoke 仅3条无噪声轨迹和2个SNR下的预注册样本，不能判定四类根因的实际贡献。细网格仍不是连续解析边缘化；候选池审计未包含量化后KL/Chernoff距离。正式 diagnostic 的 Rollout Q64 会增加运行时间，但只覆盖预注册少量样本。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `brsr_tdoa/diagnose_stage_b_failures.py`，默认 `PYCHARM_RUN_MODE="diagnostic"`。回读 `diagnostic_summary.json`、7个CSV和2个SVG后，再决定修正似然、重做候选池公平消融，或冻结BRSR路线；正式诊断完成前不修改Stage B算法。 |

## 2026-07-18 BRSR-TDOA 阶段 B 结构化波形开发门禁

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-18 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的阶段 B：在不影响主项目和已冻结阶段 A 的前提下，以完整1024点 BPSK-RRC、连续 TDOA/复增益和4→8 bit逐层量化建立可直接运行的 development 公平门禁。 |
| 修改/生成文件 | 新增 `brsr_tdoa/brsr_waveform.py`、`brsr_tdoa/brsr_stage_b.py`、`brsr_tdoa/run_stage_b.py`、`brsr_tdoa/test_brsr_stage_b.py`；更新 `brsr_tdoa/__init__.py`、`brsr_tdoa/README.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。最终 smoke 为 `运行结果/BRSR_STAGE_B_20260718_133858_smoke`。未修改 `main.py`、训练器、主评估器或既有基线 Python 文件。 |
| 核心改动 | 实现40 MHz、BPSK-RRC、保护区线性分数时延和跨SNR同源噪声；按RRC理论带宽和`功率×角频率平方`信息密度冻结12个正频率候选，development clean A只估计功率/协方差。量化协议为每实数4-bit粗码追加到8-bit；基础载荷、精化载荷、动作码和停止码逐项核算。实现精确一步Greedy、后验预测采样两步Rollout、24/32/40 bit通信代价校准、4920个合法配置的Best-Static穷举、SNR-only开放环路调度、prior/calibration/evaluation三池隔离、样本级动作追踪、Rollout Q敏感性、后验校准、轨迹聚类门禁和CSV/JSON/SVG/manifest导出。 |
| 原理与公平性处理 | A站位于融合端且不计通信、B站缓存全精度候选FFT的假设显式写入manifest；静态/SNR-only计划预共享且不收动作码，自适应方法计入反馈与停止开销。完整协方差仅作失配诊断，不用未经验证的高维截断高斯近似冒充精确量化后验。完整4→8位两步Exact-DP不作虚假标注；精确递归只由小模型Track M测试验证，Track S明确使用Monte Carlo Rollout。 |
| 验证方式 | 全部 `brsr_tdoa/*.py` 编译；`ruff check`；`test_brsr_core.py` 与 `test_brsr_stage_b.py` 共15项pytest；Stage B端到端smoke；链路自检；CSV字段/唯一性与bit上限检查；3张SVG XML解析；根目录Ruff；`validate_project_integrity.py`；`git diff --check`。 |
| 验证结果 | 编译、Ruff、15项测试和主项目科学完整性门禁全部通过。最终smoke生成64条trial、16个方法，最大自适应通信42 bit、无重复；RRC单位能量误差约`2.22e-16`，循环FFT相移最大误差约`1.60e-15`，线性分数时延相位误差约`4.10e-6 rad`，整数3-sample时延恢复正确，嵌套量化和候选带宽检查通过；3张SVG可解析。状态严格为`SMOKE_ONLY`。 |
| 风险或未解决问题 | Stage B 使用逐频点复高斯工作后验，而真实BPSK-RRC频点可能相关；必须依赖完整协方差、90%覆盖率、风险/MSE比与截幅共同判断。smoke样本极少，预算校准和RMSE排序不能用于结论。当前24/32/40 bit为B站逐观测预算，不是CR16；尚未涉及多径、NLOS、urban8或多UAV总预算。 |
| 下一轮建议 | 用户在PyCharm直接运行 `brsr_tdoa/run_stage_b.py` 默认development。回读 `run.log`、`development_gate.json`、`summary_*.csv`、`lambda_calibration.csv`、`static_plans.csv`、`rollout_convergence.csv`、协方差/动作诊断和3张SVG；development未通过则按停止规则冻结路线，通过后再独立审批全新locked。 |

## 2026-07-17 BRSR-TDOA 阶段 A 独立实现

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-17 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行并复审已批准的阶段 A：在不影响既有主项目、PASR 和 PFRS 的前提下，实现严格概率模型、嵌套量化精确似然、实际 bit 账本和小规模精确贝尔曼基准，并在正式运行前排除数值、尺度和 locked 选择偏差。 |
| 修改/生成文件 | 新增并复审 `brsr_tdoa/__init__.py`、`brsr_tdoa/brsr_core.py`、`brsr_tdoa/run_stage_a.py`、`brsr_tdoa/test_brsr_core.py`、`brsr_tdoa/README.md`；当前最终 smoke 为 `运行结果/BRSR_STAGE_A_20260717_181543_smoke`；更新 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。未修改任何既有 `.py` 文件。 |
| 核心改动 | 将 FBRSA 理论候选落实为 BRSR 阶段 A。采用随机公共频谱、有限 TDOA/复增益先验，复增益相位均匀覆盖完整相位圆；给定 A 端观测后解析边缘化公共频谱。B 端先使用仅依赖公开 SNR 和先验二阶矩的确定性尺度标准化，再对固定范围嵌套 I/Q 量化码计算稳定对数域高斯区间似然。实现获取频点和追加 bit 动作、基础/精化载荷及动作/停止码账本、一步前瞻、有限深度精确贝尔曼基准、固定层基线、后验/动作校准、风险-bit图、自动机制门禁和独立 manifest。门禁不再从 locked 集合挑选最佳lambda；所有预注册贪心lambda均须通过覆盖率要求。Spearman仅保留为描述，动作机制改用按独立轨迹聚类的条件期望风险残差检验。 |
| 验证方式 | 涉及文件 `py_compile`；根目录及 `brsr_tdoa/*.py` 的 `ruff check --select F,E9`；9项 `pytest`；端到端 smoke；非 smoke 门禁路径临时目录测试；高信噪比极端尾部与子码概率归一化测试；30万次量化似然蒙特卡洛核对；每档1200次基础后验风险校准核对；CSV重复/字段、严格JSON和SVG XML审计；隔离检查；`validate_project_integrity.py`；`git diff --check`。 |
| 验证结果 | 编译、Ruff、9项测试和主项目全部科学完整性门禁通过。最终 smoke 保存128条trial、96条动作、16条DP诊断；bit账本恒等、development/locked隔离、跨方法同源TDOA/复增益/公开尺度、期望风险单调、DP值不劣于一步前瞻、量化0截幅及3张SVG合法。量化似然蒙特卡洛最大概率误差约`3.65e-4`；基础后验在`-10/0/15 dB`的经验MSE/平均后验风险比约`1.04/1.02/1.04`。 |
| 风险或未解决问题 | smoke 和轻量蒙特卡洛只验证机制，RMSE和策略排名不能用于论文性能判断。阶段 A 使用模型匹配的离散状态、已知SNR条件、A端本地完整观测和公开先验尺度；尚未验证连续/失配复增益、CR16、urban8、多UAV总预算、Ji/Greiff强自适应基线或定位性能。 |
| 下一轮建议 | 用户在 PyCharm 直接运行默认 `research`；回读正式结果后按所有预注册策略的覆盖率、聚类条件期望风险残差、风险单调性、DP值关系和截幅率判定是否批准CR16等比特阶段。 |

## 2026-07-17 15 篇文献复核与 FBRSA-TDOA 创新边界更新

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-17 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 将最新 15 篇高度相关论文的全文复核结论写入文献综述，校正已有文献表述，补齐缺失文献，并把 PASR v1 的否定性结果、创新重合风险和最终候选 FBRSA-TDOA 同步到研究进展。 |
| 修改/生成文件 | 修改 `Obsidian Vault/文献综述_20260714.md`、`Obsidian Vault/进展总结_20260715.md`、`对话上下文.md`、`当前任务.md` 和本记录。未修改 Python、配置、模型、运行结果或 `实验结果记录.md`。 |
| 核心改动 | 综述原有 13 篇内容完成校对，新增 Greiff 2018 的 WWB 顺序主动感知和 Ji 2008 的贝叶斯自适应压缩感知；Gu 2017 的 Zotero 链接改为有效主条目。新增 15 篇统一创新重合矩阵，明确 Greiff、Ji、Graff 和 Zhai 的组合已覆盖后验主动测量、不确定度追加、ZZB 资源配置和非对称无源频域抽取。将宽泛逐层选频的创新风险定为高，并把候选收窄为“完整有限比特账本 + 终端 TDOA 风险驱动的频点/量化/停止联合决策”。进展总结补入 PASR v1 正式 `NO_GO`、oracle 差距、可主张/需弱化/不可主张边界及分阶段 go/no-go。 |
| 验证方式 | UTF-8 回读；核对 15 篇 DOI、卷期页码和 Zotero 主条目；检查公式定界符、代码围栏、标题唯一性、Markdown 表格列数、本地链接、参考文献连续性、正文引用闭合和过时状态表述；执行 `git diff --check`。 |
| 验证结果 | `文献综述_20260714.md` 的 44 个展示公式定界符、2 个代码围栏及全部表格通过；参考文献连续扩展至 `[1]`--`[82]`，正文无未定义引用，15 篇重点文献 Zotero key 全部匹配。`进展总结_20260715.md` 的 124 个展示公式定界符、4 个代码围栏、表格和本地链接全部通过；两文档无重复标题、无失效本地链接，`git diff --check` 无空白错误。 |
| 风险或未解决问题 | 本轮只更新文献与研究边界，没有证明 FBRSA-TDOA 有效。下一阶段仍须在严格贝叶斯边缘化与确定性剖面决策风险之间冻结口径，并在全新 development/locked seeds 上与 Ji/Greiff 风格自适应强基线比较。 |
| 下一轮建议 | 先审批 FBRSA-TDOA 的理论口径、有限比特消息格式、oracle 一步风险诊断和强基线协议；理论与 oracle 门禁未通过前不新增实验代码。 |

## 2026-07-16 PASR-TDOA 独立主动逐层编码实现

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 18:46 +08:00 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在不影响现有项目代码的前提下，实现已批准的“剖面后验驱动主动逐层精化 TDOA 编码”，补齐有限比特、反馈开销、静态同预算强基线、消融和自动 go/no-go。 |
| 修改/生成文件 | 新增 `pasr_tdoa/__init__.py`、`pasr_tdoa/pasr_core.py`、`pasr_tdoa/run_pasr_tdoa.py`、`pasr_tdoa/test_pasr_core.py`、`pasr_tdoa/README.md`；生成最终复核 smoke `运行结果/PASR_20260716_192922_smoke`；更新 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`、`研究总结.md`。未修改任何既有 `.py` 文件。 |
| 核心改动 | 实现连续剖面后验、独立模态提取、每 bit 预期剖面判别增益、主动/随机/固定/oracle 请求、4/6/8 bit subtractive-dither I/Q 量化、8-bit尺度、动作/停止反馈账本和机制消融。公平性复核后，所有静态方法统一扫描4--10 bit；同平均 bit 时分组合、同风险组合和高 SNR 参考仅在 development 上选择并冻结；6-bit 预注册为主检验；locked 只用于最终评估。补充边界后验禁止早停、`(seed,trial)` 成对聚类 bootstrap、逐行预算/键/种子/同源数据硬门禁和冻结对手 CSV。`Exhaustive-Proxy` 明确表示全掩码枚举后按理论代理选择，不宣称经验最优。 |
| 验证方式 | 涉及文件 `py_compile`；全项目 `ruff check *.py pasr_tdoa pfrs_frequency_mask standalone_frequency_alignment --select F,E9`；9项核心测试逐函数执行；最终 smoke；CSV 唯一性/数值有限性/bit恒等式/预算上限/development-locked隔离/method metadata/冻结对手/manifest/SVG XML 与无头渲染审计；`validate_project_integrity.py`；`compileall`；`git diff --check`。 |
| 验证结果 | 编译、Ruff、9项测试、全项目科学完整性门禁全部通过。最终 smoke 无噪声最大误差=`1.863e-6 sample`；固定分层与同一频点并集后验差=`2.082e-17`；5568条逐试验记录无重复、330条动作记录、bit四分量逐行严格相等，16个方法元数据与结果一致，3个冻结对手均标记 development 选择，3张SVG合法且图例不遮挡。`PASR-Always4` 为 `64/64 bit`，`Fixed-Nested` 为平均/最大 `57.8/58 bit`，确认不再多收协议不需要的决策/动作反馈。首次独立审计命令因 PowerShell here-string 将中文路径转成 `????` 而失败，改用 Unicode 转义路径后同一审计通过，非代码错误。PyTorch环境未安装 `pytest`，故使用同一解释器逐项调用测试函数，全部通过。 |
| 风险或未解决问题 | smoke 只验证流程，不提供性能结论。静态池包含理论代理选出的掩码和已发表/冻结掩码，不是全掩码经验风险最优；减性抖动种子和静态时分表假设预共享。正式模型仍是 N=16 的固定确定性源谱/复增益/等噪声非对称双站模型；主动方法必须计入反馈并与 development 冻结的跨位宽静态参考比较。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `pasr_tdoa/run_pasr_tdoa.py`；完成后优先读取 go/no-go、summary、action trace、trial rows 和三张图，未通过门禁则不进入 nuisance 或 urban8。 |

## 2026-07-16 IEEE 风格研究文档重构

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 18:01 +08:00 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 按已批准目录，将文献综述和研究进展重构为中文 IEEE Transactions 风格，修复结构混杂、结论状态滞后、Zhai 基线归因不清和证据边界不足等问题。 |
| 修改/生成文件 | 修改 `Obsidian Vault/文献综述_20260714.md`、`Obsidian Vault/进展总结_20260715.md` 和本记录；未修改代码、配置、模型、运行结果及实验记录。 |
| 核心改动 | 综述改为“引言—模型与预算—理论—技术谱系—统一比较—研究缺口—项目启示—结论—证据附录”；进展报告改为“问题与边界—协议—基线—方法演进—两项机制实验—综合讨论—候选假设—局限与结论”。保留公式、指标、运行路径和引用编号；将 Shared64、插值和 PFRS 明确归为冻结或否定性证据，将逐层/可变率有限比特统计编码标记为待验证候选；拆分 Zhai 原始 Fisher 准则与项目旁瓣增强/Cao 后端实现。 |
| 验证方式 | UTF-8 回读；检查标题层级、展示公式定界符、数学环境裸 `<`、Markdown 表格列数、图题/表题连续性、正文引用与参考文献闭合、本地图片和运行路径。 |
| 验证结果 | 两文档展示公式定界符均成对，数学环境无裸 `<`；综述 80 条参考文献全部被正文使用，进展报告 22 条选列文献全部被引用；进展报告图1至图12、表1至表19连续唯一，Markdown 表格列数一致；两文档全部本地链接存在。 |
| 风险或未解决问题 | 本轮不产生新文献、实验或性能结论。[10] DOI 与 [31] 发表类型/日期仍标记为待核验；PFRS 饱和结论明确限定为有限掩码集合和稠密网格数值证据。 |

## 2026-07-16 PFRS 与异构选频路线研究总结更新

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 16:48 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 将固定掩码 PFRS 从有限时延筛选、连续 TDOA 验证到全掩码饱和证书的完整研究链补入长期总结，并在根目录研究总结中补齐双站异构选频与插值伪补全路线。 |
| 修改/生成文件 | 修改 `Obsidian Vault/进展总结_20260715.md`、`研究总结.md` 和本记录；未修改 Python、配置、模型或运行结果。 |
| 核心改动 | 进展报告新增 PFRS 数学模型、创新边界、独立确认、连续性能 `NO_GO`、`FIXED_MASK_SATURATED` 证书和逐层编码转向；更新摘要、当前阶段、贡献边界、停止规则及证据索引。根目录研究总结新增“双站异构选频与插值伪补全”和“PFRS”两条完整路线卡片，并将当前创新状态更新为固定掩码路线已冻结。 |
| 验证方式 | UTF-8 回读；核对标题层级、表18/表19交叉引用、数学块定界符、关键结果路径和运行指标；检查 Markdown 表格列数、相对链接、重复标题和 `git diff --check`。 |
| 验证结果 | 两份总结均可按 UTF-8 完整回读；进展报告表1至表19连续唯一，附录旧表号引用清零，58个数学块和代码围栏均成对，未使用 `\boldsymbol` 或可能触发 Markdown 列表/表格误解析的公式行；两份文档相对链接零失效、标题零重复、行尾空白为零。PFRS 的 `HOLD/NO_GO/FIXED_MASK_SATURATED` 状态及核心指标已与三个正式 JSON/CSV 结果复核；`git diff --check` 无空白错误，仅有既存 LF/CRLF 提示。 |
| 风险或未解决问题 | 本轮只整理已落盘证据，不产生新实验结论。PFRS 饱和证书仅适用于冻结的固定掩码有限空间，双站伪补全结论仅否定无额外先验的确定性补全。 |
| 下一轮建议 | 先审批逐层/可变率有限比特统计编码的数学模型、预算和 go/no-go；理论通过前不新增实现。 |

## 2026-07-16 PFRS 固定掩码可行性证书

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 16:20 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行正式连续 TDOA `NO_GO` 后的第一优先级检查：在不继续调参的前提下，证明当前固定 `N=16、M=4、eta=0.9` 掩码空间是否还存在同等局部信息、更低连续旁峰的替代方案。 |
| 修改/生成文件 | 新增 `pfrs_fixed_mask_feasibility.py`；生成 smoke 目录 `运行结果/PFRS_FIXED_MASK_20260716_160937_smoke` 和正式目录 `运行结果/PFRS_FIXED_MASK_20260716_161036_certificate`；更新 `当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。未修改任何现有 Python 文件。 |
| 核心改动 | 对全部 1820 个四频点掩码计算连续可辨识周期、精确无噪声剖面相干函数、主瓣边界、最强非主瓣峰、旁峰积分能量、Schur 补 Fisher 比和二维 Pareto 前沿；新增解析剖面代价与既有 `profiled_cost()` 的数值等价验证，并自动输出 `FIXED_MASK_SATURATED/ALTERNATIVE_MASK_EXISTS` 证书。 |
| 验证方式 | smoke=`1/512 sample`、正式=`1/4096 sample` 两级网格；正式/烟雾排序和旁峰收敛对照；CSV/JSON/manifest/源哈希/1820 掩码唯一性审计；两张 SVG XML 与无头浏览器渲染检查；涉及脚本编译、全项目 `ruff check *.py --select F,E9` 和 `validate_project_integrity.py`。 |
| 验证结果 | 正式/烟雾 PFRS 旁峰差=`1.46e-9`；解析代价最大差=`1.42e-14`；1820 个掩码全部唯一且数值有限，1654 个闭区间连续可辨识，eta=0.9 可行掩码=8。PFRS 最强旁峰最低，较低旁峰替代数=0，支配掩码数=0，证书=`FIXED_MASK_SATURATED`。编译、Ruff、科学完整性和输出审计全部通过。首次 smoke 因读取旧 manifest 字段名而在创建输出前停止，修正后从头通过；组合命令中的 `python -m ruff` 因环境无模块失败，按项目既有 `ruff` 命令重跑通过。 |
| 未解决问题 | 证书只覆盖冻结的固定掩码有限空间和当前 nuisance 模型，不能外推到逐层、反馈、自适应或可变率编码。 |
| 下一轮建议 | 停止 C-PFRS 和固定掩码调优；先提交逐层/可变率有限比特 TDOA 统计编码的完整方案供审批，再决定是否新增独立验证脚本。 |

## 2026-07-16 PFRS 连续 TDOA 独立验证

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 15:10 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行已批准的阶段 1：在不接入现有主项目的前提下，把 PFRS 的整数二元判别风险推进到连续/亚采样 TDOA 实际估计验证。 |
| 修改/生成文件 | 新增 `pfrs_continuous_tdoa_validation.py`；生成最终 smoke 目录 `运行结果/PFRS_CONTINUOUS_20260716_150949_continuous_smoke`；更新 `当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。未修改 `pfrs_finite_delay_selection.py`、`main.py` 或任何训练/数据/定位/基线 Python 文件。 |
| 核心改动 | 读取并哈希锁定 PFRS 正式确认协议和七个冻结掩码；新增连续对称 TDOA 生成、向量化剖面似然细格点搜索、抛物线亚采样细化、共同随机数、互斥 development/locked seeds、Full16 参考、RMSE/MAE/偏差/分位数/错峰率、Fisher/CRLB 检查、seed-block 成对 CI、三类 SNR 分工、双候选自动门禁、压缩 NPZ 和三张 SVG。新增连续相位周期判据，区分旧整数格点可辨识性；识别 Uniform 掩码的 `3.2-sample` 连续别名。 |
| 验证方式 | 新脚本与源 PFRS 脚本 `py_compile`；全项目 `ruff check *.py --select F,E9`；`validate_project_integrity.py`；三轮故障导向 smoke 与最终完整 smoke；剖面格点/标量代价逐点对照；正负连续时延无噪声恢复；NPZ→CSV 独立复算；种子集合、tensor shape、manifest 文件清单、SVG XML 和图例/坐标人工检查；`git diff --check`。 |
| 验证结果 | 最终 smoke 退出码 0，无 warning/error；格点代价最大误差 `4.44e-15`，连续可辨识方法无噪声最大误差 `3.54e-6 samples`；development/locked tensor shape=`[1,4,8,32]/[2,4,8,48]`；源 selection/confirmation、development、locked seeds 全部互斥；编译、Ruff、项目科学完整性、NPZ/CSV 重建、SVG 和 manifest 均通过。前两次 smoke 分别因 Uniform 连续别名和 smoke 缺少 stress SNR 被门禁阻止，修正协议后未保留失败目录。 |
| 风险或未解决问题 | smoke 样本极小，任何方法排名和 gate 状态均不可用于性能结论。当前仍是固定源谱、固定复增益和等接收噪声的 N=16/M=4 理论验证；Uniform 只能作为连续歧义压力基线。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `pfrs_continuous_tdoa_validation.py`；默认即正式 `continuous_validation`，不训练模型。完成后优先回读 gate、locked summary、paired comparison、Fisher check、trial NPZ 和三张 SVG。 |

## 2026-07-16 PFRS 独立统计确认协议

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 14:14 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 消除 PFRS 同一噪声样本选优并报告性能的选择偏差，补齐成对置信区间和逐时延归因，同时继续隔离现有项目代码。 |
| 修改/生成文件 | 仅修改独立脚本 `pfrs_finite_delay_selection.py`；生成最终 smoke 目录 `运行结果/PFRS_20260716_141431_confirmation_smoke`；更新四份长期文档。未修改任何现有训练、模型、数据、评估或主入口 Python 文件。 |
| 核心改动 | 新增互斥 selection/confirmation seed 集、跨 seed 全掩码冻结、高试验数冻结候选确认、PFRS/Zhai 逐试验成对错误 NPZ、逐时延风险、seed-block bootstrap、选频稳定性、四张确认图和自动 GO/HOLD/NO-GO。正式配置为 6 个 selection seeds × 500 trials/hypothesis 与 12 个 confirmation seeds × 5000 trials/hypothesis；PyCharm 默认模式设为 `confirmation`。 |
| 验证方式 | 新脚本编译；全项目 `ruff check *.py --select F,E9`；三次 confirmation smoke 迭代；旧 smoke 路径回归；`validate_project_integrity.py`；NPZ 对 CSV 逐 SNR/时延精确重建；manifest 与目录文件集合一致；4 个 SVG XML/大小检查；UTF-8 和 `git diff --check`。 |
| 验证结果 | 最终 smoke 退出码 0，自动状态 HOLD；selection/confirmation seeds 无交集，全部表格行数和张量 shape 断言通过，PFRS/Zhai 风险可从 error2 张量以 `1e-12` 精度重建。编译、Ruff、旧路径回归和项目科学完整性门禁均通过。 |
| 风险或未解决问题 | smoke 规模极小，只验证统计协议和输出链路；正式性能仍未知。当前确认仅比较固定确定性 nuisance 条件，不能替代后续源谱、增益和不等接收噪声敏感性。 |
| 下一轮建议 | 用户直接在 PyCharm 运行独立脚本正式 `confirmation`；回读 gate、paired comparison、paired-by-delay、selection stability 和 manifest 后决定 GO/HOLD/NO-GO。 |

## 2026-07-16 候选 A：PFRS 独立理论门禁实现

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 13:17 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 按用户审批实现候选 A，同时严格隔离现有训练、数据生成、评估和主入口代码。 |
| 修改/生成文件 | 新增 `pfrs_finite_delay_selection.py`；生成独立 smoke 目录 `运行结果/PFRS_20260716_131523_smoke`；更新 `当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。未修改任何既有 `.py` 文件。 |
| 核心改动 | 实现未知确定性公共频谱与全局复增益的闭式剖面代价、有限时延二元 GLRT Monte Carlo 设计风险、均匀先验距离权重、Zhai 型局部 Fisher 信息、离散频点掩码可辨识周期、严格预算穷举、Fisher 下限约束选择、eta/Pareto 分析及五类方法对照。脚本内置 `smoke/research` 两档普通变量配置，可在 PyCharm 直接运行。 |
| 验证方式 | 新脚本 `py_compile`；全项目 `ruff check *.py --select F,E9`；`validate_project_integrity.py`；默认 smoke；向量化剖面代价与 SciPy 广义特征值逐样本对照；固定种子重复性；CSV/JSON/SVG 完整性检查；`git diff --check`。 |
| 验证结果 | 编译、Ruff、项目 22 个根目录 Python 文件和全部科学不变量通过。smoke 穷举 495 个掩码并通过内部 validation；广义特征值等价误差约 `1.78e-15`，局部极限最大偏差约 `1.65e-7`，重复计算完全一致；3 个 SVG 均为合法非空 XML。 |
| 风险或未解决问题 | smoke 只有单 seed、单源谱和每假设 120 trials；PFRS 风险是剖面 GLRT 设计风险而非 ZZB。尚未证明对不同 nuisance、先验和真实 urban8 TDOA 的外部有效性，也尚未完成与项目 CR16 的预算映射。 |
| 下一轮建议 | 先运行脚本 `research` 模式并按预注册门槛判断 PFRS 相对 Zhai-Fisher 的风险改善和 eta 稳定性；不通过则停止，不接入主项目。 |

## 2026-07-16 ZZB 正式版全文复核与 CLIGS 目标修正

| 字段 | 内容 |
|---|---|
| 日期时间 | 2026-07-16 |
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 全文复核 Zotero 中新导入的 Xiong/Liu 2023、Nanzer 2016、Sadler 2010 正式发行版，修正综述证据、参考文献元数据和候选创新目标。 |
| 修改/生成文件 | 修改 `Obsidian Vault/文献综述_20260714.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`。未修改 Python、配置、模型、运行结果或 `实验结果记录.md`。 |
| 核心改动 | 补充三篇正式版的模型、推导、工作区和适用边界；修正 Sadler 2010 作者为三人并更新 Zotero 主条目；补齐 Nanzer/Xiong Zotero 链接。根据 Xiong/Liu 的低信噪比结论，废止统一旁瓣抑制/单个最坏时延目标，将 CLIGS 候选目标修正为“先验积分错误风险最小化 + Zhai 局部 Fisher 信息约束”，并区分固定鲁棒掩码与信噪比自适应 oracle 上界。 |
| 验证方式 | 核对 Zotero 正式条目、DOI、卷期页码和 PDF 全文；复读综述相关章节；检查参考文献编号、数学定界符、Mermaid 代码围栏和过时表述。 |
| 验证结果 | 参考文献共 80 条且编号连续唯一；三条正式版 Zotero 链接均存在；Sadler 旧条目、错误作者和“缺少全文”过时表述均已清除；36 个独立公式定界符、Markdown 围栏和 Mermaid 围栏均成对。`git diff --check` 未发现空白错误；仅有既存行尾转换提示。 |
| 风险或未解决问题 | CLIGS 可辨识性、干扰参数口径、有限时延判别量的局部极限及先验风险排序均未证明；正式版复核只收窄创新边界，不等于候选方法已成立。 |
| 下一轮建议 | 仅开展 CLIGS 理论 go/no-go：先证明可辨识性，再选择确定性代理或 Bayesian 真下界口径；理论通过前不实现选频算法。 |

## 2026-07-15 NLGSC 创新重合审查与综述收敛

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 深检 Zotero 与定向外部全文，判断“干扰参数消除的局部信息安全全局可分辨频谱压缩”与既有 ZZB、复合假设、无源时延和频率资源设计文献的重合程度，并据此修订综述和下一步主线。 |
| 修改/生成文件 | 修改 `Obsidian Vault/文献综述_20260714.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`。未修改 Python、模型、配置、运行结果或 `实验结果记录.md`。 |
| 核心改动 | 补入 Bell 1997 向量/干扰参数 ZZB、Zhou 2024 无源未知照射源联合似然、Nanzer 2016 被动分裂频谱、Xiong/Liu 2023 信噪比自适应 ZZB 波形设计和 Sadler 2010 未知卷积随机信道 ZZB；修正 Defensive IB 2021 “无全文”的过时描述。将广义 NLGSC 判定为高重合风险，窄化为有条件候选 `CLIGS`，新增可辨识性、确定性/Bayesian 干扰参数口径、真下界/设计代理区分和局部极限四项理论门禁。 |
| 验证方式 | UTF-8 回读；核对新增参考文献编号、正文引用、标题层级和数学块；对照已阅读全文逐项复核创新边界；检查 Git diff 和 Markdown 空白。 |
| 验证结果 | UTF-8 回读正常；综述参考文献 `[1]-[80]` 连续、无缺号和重复号，新增 `[76]-[80]` 均有正文引用；34 个数学块分隔符和 2 个 Mermaid 围栏成对；旧的“信息瓶颈论文无全文”描述已清除；`git diff --check` 无空白错误，仅提示既有 LF/CRLF 转换。未运行训练、评估或 Python 冒烟测试，因为本轮只修改研究文档。 |
| 风险或未解决问题 | Zotero 与定向主来源检索不能形式化证明全球不存在相关工作；Sadler 2010 在 Zotero 中缺少可用全文，本轮只采用元数据/摘要。CLIGS 的时延可辨识性和局部极限尚未证明，不能视为已成立创新。 |
| 下一轮建议 | 仅在用户审批后进入纯理论 P0：先证明同模型可辨识性，再分别评估确定性集中设计代理与 Bayesian 真下界路线；任一路线若退化为已有 ACF/ZZB/互信息目标则停止，不进入代码调参。 |

## 2026-07-15 导师汇报重构与插值实验更新

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在不覆盖 V5-A.1/Shared64 创新主线的前提下，重构当前报告 `Obsidian Vault/进展总结_20260715.md`，完整加入双站异构选频与插值伪补全实验，并按初读者可理解的实验报告标准统一章节；根据用户复核意见，进一步展开 Chen-style DAE、各类强基线及 R20 至 Shared64 的创新路线说明。 |
| 修改/生成文件 | 修改 `Obsidian Vault/进展总结_20260715.md`；将10张正文 SVG 统一收纳于 `Obsidian Vault/assets/进展总结_20260715/`；新增报告专属样式 `Obsidian Vault/.obsidian/snippets/progress-report-tables.css` 并在 `appearance.json` 中启用；更新本条 `修改记录.md`。未修改 Python、模型、实验配置或运行结果。 |
| 核心改动 | 将原15章整合为“项目概述—理论基础—近似复现与基线—严格 CR16 创新主线—独立插值验证—总结与计划”六章及附录；统一“一→1→1.1”标题层级。第三章新增 DAE 输入输出、复数卷积、latent 维度、MSE、训练与定位链路，并分别推导 Raw/Clean/Geometry、DFT、Hadamard、PCA、Cao AML、Zhai-CRLB 和 GeoHybrid 的信息流与公式。第四章逐项解释 R20/R20.1、FreqDAE v1/v2/v3、V4、GeoHybrid、V5-A、V5-A.1、Top-K、旧 V5-B 和 Shared64 的动机、目标、结构、结果与停止原因；历史目录已删除的结果显式降级为 `实验结果记录.md` 历史证据。进一步按论文式证据链重排实验结果：增加 FreqDAE/V4 容量对比和 V5-A/V5-A.1 分层性能表，将 Shared64、插值实验的曲线紧邻对应数据分析；12幅图均采用图下居中标题，18个表均采用表上居中标题并连续编号，正文逐一引用；运行路径集中移至附录证据索引。为兼顾源码简洁性和 Obsidian 预览居中效果，18个完整 HTML 表格恢复为 Markdown 表格，表题保留单行居中 HTML，并通过报告专属 `cssclasses` 和 CSS snippet 使表格整体居中。公式兼容性复查进一步消除了块公式中独占一行的 `+`、`-`，防止 Obsidian 在识别数学块前将其解析为 Markdown 列表项。 |
| 验证方式 | UTF-8 回读；对照 `model.py`、`train.py`、`baselines.py`、`task_baselines.py`、`compressed_tdoa.py` 核对公式与实现；检查全部标题层级、数字列表、块公式和代码围栏；逐个验证相对链接和图片路径存在；对照 `实验结果记录.md`、正式结果目录和最新 Shared64 状态；核对10张 SVG 与原始结果文件的 SHA-256；检查图表编号、标题长度、标题位置和正文交叉引用；检查全部 `$$` 是否为独立 ASCII 定界符并具有前后空行；对 DAE/PCA 修复公式执行本地数学语法解析；`git diff --check` 和只读 Git 状态检查。 |
| 验证结果 | 报告仍保持六章及附录；无“阅读指南”或旧章节残留，无数字型 Markdown 列表。12幅图和18个表均连续编号，标题不超过15字且在正文中至少引用一次；10张外部 SVG 均以相对路径引用、位于独立 assets 目录、不包含外部图片依赖，来源哈希一致。18个表的标题、单元格和链接与转换前逐项一致；完整 HTML 表格标记为0，源码由2165行缩短至1445行。表题使用单行居中 HTML，表格由已启用且仅作用于本报告的 CSS snippet 居中。108个块公式分隔符均为精确的独立 `$$` 行，4个代码围栏成对，全部相对链接有效。已移除 Obsidian 不兼容的 `\boldsymbol`，将未分组的 `\widehat I`、`\widehat X` 等改为显式花括号写法，并修复块公式中12处独占一行的 `+`、2处独占一行的 `-` 及1处行首裸竖线；全体数学块内不再存在可能触发 Markdown 列表、表格、标题、引用、分隔线或代码围栏的行。DAE/PCA 目标公式均通过本地数学语法解析。第三、四章所述方法与当前代码实现一致；三个已删除历史结果目录不再作为链接引用。插值正式结果、三组 smoke 证据级别和 Shared64 独立路线边界未改变。未重新运行训练或评估。 |
| 风险或未解决问题 | 文档反映截至2026-07-15已落盘的开发证据。Shared64 专家互补性上界、legacy Power64 当前协议重训、真实 bit-rate、公平复杂度和 locked pools 仍未完成；双站独立 Zhai 与原论文非对称 Zhai 尚无正式大样本性能排名，文中均保留边界。 |
| 下一轮建议 | 先完成 pair-level 专家互补性上界诊断；插值路线按停止规则冻结，不再运行 `project_full` 或继续调插值器。 |

## 2026-07-14 双站异构选频与频谱伪补全独立验证模块

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在不接入 V5-A.1/Shared64 主线的前提下，独立验证双站选频不一致时，IFFT/插值/滤波/FFT 是否能恢复缺失复频谱并改善 TDOA。 |
| 修改/生成文件 | 新增 `standalone_frequency_alignment/` 下的配置、严格 Zhai 选频、频谱对齐核心、项目适配层、运行入口、绘图、单元测试和 README；仅追加四份长期文档的独立路线记录。未修改 `main.py`、V5-A.1/Shared64 或现有 baseline 代码。 |
| 核心改动 | 实现学位论文式严格 0-1 Zhai 逐点交换；实现受控重叠和双站本地独立选频；实现原论文非对称对照、Full、真实交集连续 ML、零填充、理想周期、多相 FIR、周期线性/三次样条、Oracle-Union；记录通信量、选频收敛、频谱 NMSE/相位、TDOA、失败率、旁瓣、耗时和配对 bootstrap；加入理想插值恒等式、算子秩和少于两个共同频点时无效的硬门禁。 |
| 验证方式 | 涉及文件 `py_compile`；`ruff check *.py standalone_frequency_alignment/*.py --select F,E9`；7 项 `unittest`；`validate_project_integrity.py`；受控重叠、双站本地 Zhai 和 urban8/LOS 项目场景 smoke；读取逐试验 CSV 和算子诊断核对。 |
| 验证结果 | 编译、Ruff、7 项测试和项目完整性门禁全部通过。理想周期插值相对零填充最大复数误差约 `6.1e-16`，算子秩严格等于 M；各伪补全方法与真实交集连续估计逐试验相同，缺失频点生成能量仅约 `1e-30`（N=64）至 `4.5e-28`（N=1024）。 |
| 风险或未解决问题 | 现有结果均为小样本流程验证，不能用于论文性能结论；严格 Zhai 的连续松弛 SPIE 版本未混入本路线，避免把未完全确认的惩罚超参数误称为严格复现。完整 `synthetic_full`/`project_full` 尚未运行。 |
| 下一轮建议 | 用户先在 PyCharm 运行 `synthetic_full`。若完整结果仍与交集基线严格等价，则按停止规则否定“周期插值恢复缺失原始 DFT 频点”这一假设，不继续调插值器。 |

## 2026-07-13 导师汇报报告文档

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 生成一份面向导师汇报的完整研究进展报告，统一说明 Chen 论文近似复现、传统与强基线、创新方案演进、严格 CR16 当前结果、现存问题和后续 go/no-go 路线。 |
| 修改/生成文件 | 新增并同步 `报告文档.md`、`Obsidian Vault/报告文档.md`；更新本条 `修改记录.md`。 |
| 核心改动 | 报告以系统模型、GCC/TDOA/WLS、压缩预算、Fisher 信息、全局歧义和压缩域 posterior 公式解释研究内容；补充零基础阅读指南、urban8 数据生成、复数 DAE 架构、DFT/Hadamard/PCA/Cao/Zhai 实现、R20/FreqDAE/V4 细节、V5-A.1 损失和参数、Shared64 门控、指标定义及图表阅读方法。所有行内公式使用 Markdown 数学插件支持的 `$...$`，块公式使用 `$$...$$`。针对怡氧不支持 SVG 数据 URI 的问题，将 6 张源 SVG 按浏览器 96 DPI 完整栅格化为 PNG，并以 Base64 PNG 数据 URI 嵌入单个 `<img>` 节点；报告仍可脱离项目目录独立阅读，同时规避原始 SVG DOM、SVG 数据 URI和外部图片路径的兼容性问题。 |
| 验证方式 | UTF-8 回读；Markdown 块公式、行内公式和代码围栏配对检查；拒绝残留的 `\(...\)`/`\[...\]` 分隔符；检查原始 `<svg>` 标签、SVG 数据 URI 和外部 SVG 引用均为 0；逐张视觉检查 PNG 的标题、坐标、图例、曲线、裁切与留白；逐个 Base64 解码并用 Pillow 校验格式、尺寸和非空内容；核对内嵌 PNG 与临时渲染结果 SHA-256 一致；核对两份报告字节一致；`git diff --check`。 |
| 验证结果 | 每份报告约 1.72 MB，包含 6 个单节点 Base64 PNG 数据图片；Base64 每 120 字符分行，文档最长行 325 字符。6 张图的尺寸分别为 1430×816、1333×840、1451×472、1180×502、896×440、893×456，均通过格式、尺寸、非空内容和哈希校验；视觉抽查未发现裁切、重复标题或异常留白。SVG 数据 URI、原始 `<svg>` 标签、错误图题和外部 SVG 引用均为 0。100 个块公式分隔符、所有 `$...$` 行内公式和 10 个代码围栏均成对；两份报告字节级一致；未运行训练或评估。 |
| 风险或未解决问题 | 报告反映截至 2026-07-13 的开发阶段结论。Shared64 专家互补性诊断、legacy Power64 当前协议重训、真实 bit-rate、公平复杂度和 locked pools 尚未完成，文中均按候选或待验证表述。 |
| 下一轮建议 | 先执行 pair-level expert complementarity 上界诊断；依据预注册门槛决定冻结 Shared64，或仅在独立 calibration pools 做一次门控校准。 |

## 2026-07-11 P0/P1 独立多池 Native-Only 公平入口

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 清理 Shared64 公平入口，并实现与训练、校准和 locked 隔离的 native-only 多 snapshot-pool 开发评估。 |
| 修改/生成文件 | `evaluate_topk_fairness.py`、`benchmark_fairness_complexity.py`、`main.py`、`validate_project_integrity.py` 及长期文档；smoke 输出写入 `运行结果/20260711_154042/tables/topk_fairness`。 |
| 核心改动 | 默认结果源切换至 Shared64；新增 calibration/development/locked pool manifests 与交集硬门禁；新增 `dev_native_multi_pool`；独立导出 Shared52、Shared64、legacy Power64/GeoHybrid64 和强基线 OriginalTop1；native-only 跳过 Common Top-K、PCA/Hadamard；新增 pool-wise cluster CI；bin source 分成 requested/effective 元数据。 |
| 验证方式 | 涉及文件 `py_compile`；`ruff check *.py --select F,E9`；`validate_project_integrity.py`；PyCharm 等价直接运行 `evaluate_topk_fairness.py` smoke；临时目录 pool-wise comparison 导出 smoke。 |
| 验证结果 | 编译、Ruff 和完整性门禁全部通过。首次 smoke 捕获 native-only 仍写 Common Raw evidence 的 `KeyError`，修复后重新运行成功；10 个方法均 valid=2、failure=0，只生成 native FigFair1；pool-wise comparison 临时导出成功。 |
| 风险或未解决问题 | 未运行 P1 全量，因此没有新的性能结论。legacy 单专家 checkpoint 与当前 Shared52 训练协议不完全同代，只能显式作为历史 exact-CR16 对照。 |
| 下一轮建议 | 默认模式已切为 `dev_native_multi_pool`；用户在 PyCharm 直接运行 `evaluate_topk_fairness.py`。完成后先判断 V5B-Shared64 相对 GeoShared52 和 GeoHybrid64 的跨池稳定性，再决定是否批准独立 calibration。 |

## 2026-07-11 严格 Shared64 双专家实现与结果链路对齐

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 把旧 88-bin/CR11.64 双专家修正为严格 64-bin/CR16 方案，并让主入口、公平评估、复杂度基准和完整性门禁识别新方法。 |
| 修改/生成文件 | 用户在 ChatGPT 指导下修改 `main.py`、`compressed_tdoa.py`、`experiment_profiles.py`、`evaluate_topk_fairness.py`、`benchmark_fairness_complexity.py`、`validate_project_integrity.py`；生成 `运行结果/20260711_154042`。Codex 本轮只审查代码与结果并更新文档，未修改 Python。 |
| 核心改动 | 构造 40 shared + 12 Power-only + 12 Geo-only 的 64-bin 并集；两个 hard-bin expert 各使用 52 bins；新增 `V5B-Shared64` 标签、预算元数据、绘图/导出识别和完整性断言。 |
| 验证方式 | 读取 `run.log`、`plot_data.pkl`、SVG 与 `tables/*.csv`；pandas 分区复算；`ruff check *.py --select F,E9`；本轮六个修改文件 `py_compile`；`validate_project_integrity.py`。 |
| 验证结果 | 正式运行成功，预算元数据为 experts=`52/52`、overlap=`40`、union=`64`、128 real、CR16 exact；静态检查、编译和完整性门禁全部通过。Shared64 整体优于 DFT/Cao2017 等多数压缩基线，但融合 all-SNR RMSE=`25.95m`，略弱于 GeoShared52=`25.77m`，未证明门控融合增益。 |
| 风险或未解决问题 | 公平/复杂度脚本默认源路径仍指向旧 `20260709_120019`；`main.py` 存在外部编辑引入的 UTF-8 BOM；`v5a1_bin_sources` 配置标签与实际 Shared52 source 名称不一致；full 使用训练同源 snapshot pool；门控沿用旧专家规则；专属频点抽取仍是启发式。 |
| 下一轮建议 | 先做小范围代码清理和独立 development-pool 原生 Top1 复核；若融合仍弱于 GeoShared52，则把当前门控降级为消融，或只用训练验证组进行一次有约束校准。通过后重新生成 Shared64 top-K、公平后端和复杂度结果，再运行 locked pools。 |

## 2026-07-11 全项目科学完整性审查与自动门禁

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 排查会影响算法性能结论的基础性问题，修复已确认缺陷，并把资源预算、数据协议、缓存、标签、模型输出和评估口径固化为运行前自动门禁。 |
| 修改/生成文件 | 新增 `experiment_integrity.py`、`validate_project_integrity.py`；修改 `main.py`、`signal_gen.py`、`train.py`、`evaluate.py`、`baselines.py`、`task_baselines.py`、`compressed_tdoa.py`、`experiment_cache.py`、`evaluate_topk_fairness.py`、`benchmark_fairness_complexity.py`、诊断/导出/重绘脚本及长期文档。 |
| 核心改动 | V5-B 按 expert 频点并集计费，当前 88-bin 历史实现默认在训练集构造前阻断；频点/CR/PCA state 加严格校验；缓存增加完整协议和 baseline 代码指纹；dev mid fixed set 与主开发集对齐；PCA 只在训练池拟合，禁止 locked 泄漏；urban pair 随机选两个 LOS UAV 并使用浮点标签；嵌套 CR 验证按 CR 轮换；V5-A.1 分桶指标改为逐样本；评估拒绝未知定位器/GCC、重复方法名和非法模型输出。 |
| 验证方式 | `ruff check *.py`；PyTorch 环境 `compileall`；21 个根目录 Python 文件模块导入；`validate_project_integrity.py`；V5-A.1 per-sample loss 等价检查；legacy fairness rows 安全读取检查。 |
| 验证结果 | 全部正式检查通过。完整性脚本确认 fixed eval 同源、正负 TDOA、两路接收信号非同一、GCC/direct 符号一致、geometry-oracle WLS 精确、DFT/Hadamard/PCA/DAE CR16 预算正确、88-bin 融合被识别为超预算、缓存跨 snapshot pool 被拒绝。未运行完整训练或完整评估。 |
| 风险或未解决问题 | 严格 Budget64 V5-B 尚未实现；历史缓存/CSV 缺少新协议字段，只能作为 legacy 诊断。当前通信量仍按未量化实数标量计数；合成 urban8 的 per-link SNR 和 observation-level noisy RMS 需后续敏感性实验。 |
| 下一轮建议 | 先实现共享 64-bin 双 expert，并在新完整性协议下依次运行 dev、mid 和 locked；正式论文同时报告失败率、common-valid 配对结果、通信标量数、参数量与端到端耗时。 |

## 2026-07-10 公平验证扩展、复杂度基准与 V5-B 预算审计

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行公平验证下一阶段：补充 mid-SNR 入口、跨方法配对 cluster CI、冻结后端角色、复杂度/帕累托导出，并在进入 locked 前审计通信预算。 |
| 修改/生成文件 | `evaluate_topk_fairness.py`、新增 `benchmark_fairness_complexity.py`、`compressed_tdoa.py`、`evaluate.py`、`main.py`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md`、`实验结果记录.md`；生成 `fair_topk_dev_method_comparison.csv`、`fair_topk_dev_solver_decision.json`、`fairness_*runtime.csv`、`fairness_model_complexity.csv`、`FigFair3_Accuracy_Complexity_Pareto.svg`。 |
| 核心改动 | 新增 `dev_mid_gate`、开发 rows 合并、样本配对 snapshot-cluster CI 和 `plot_existing` 重绘；冻结 GeoBeam 为精度增强后端、ScreenRefit 为复杂度消融。复杂度脚本统计传输量、参数量、模型体积、前端/evidence/WLS/端到端耗时。预算审计发现 V5-B 两 expert 并集为 88 个复频点，等效 CR11.64；正式 dev/mid/locked 现会强制阻断旧 V5-B，`plot_existing` 成为安全默认，图例和新运行文件名改为 OverBudget。 |
| 验证方式 | `plot_existing` 重绘；0 dB × 10 trials × 3 repeats 复杂度基准；预算字段断言；`dev_mid_gate` 负向守卫测试；SVG 渲染检查；全项目编译与 `git diff --check`。 |
| 验证结果 | 配对比较表生成 132 行；复杂度和三张公平 SVG 正常导出。V5-B 预算审计值为 Power64/Geo64/overlap/union=`64/64/40/88`，176 real，CR=11.636，严格 CR16 断言为 false；`dev_fair` 按设计抛出预算阻断。该阻断是保护性验证，不是待修运行错误。 |
| 风险或未解决问题 | 旧 V5-B 的所有性能只能作为超预算机制诊断。严格 Budget64 需要改变 selected bins 和 expert 输入后重训，不能直接补 mid 或运行 locked。在线复杂度还受逐 pair 双 expert 和重复 WLS 支配，参数少不等于端到端更快。 |
| 下一轮建议 | 审批并实现 40 shared + 12 Power-only + 12 Geo-only 的严格 64-bin 双 expert；通过预算/shape smoke 后完整重跑 dev_fair，再依次运行 mid 与 locked。 |

## 2026-07-10 公平结果 SVG 可视化

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 补齐 `evaluate_topk_fairness.py` 运行后无可视化结果的问题，并允许不重新评估即可从现有 rows CSV 重绘。 |
| 修改/生成文件 | `evaluate_topk_fairness.py`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`；生成 `FigFair1_Native_Strong_Baselines.svg`、`FigFair2_Common_TopK_Attribution.svg`。 |
| 核心改动 | 正常 smoke/dev/locked 运行结束后自动输出两张 SVG。FigFair1 分别展示原生 Top1 的定位 RMSE 和 TDOA MAE；FigFair2 用四个子图展示 CommonTop1/GeoBeam/ScreenRefit 的全开发集 RMSE、低 SNR RMSE、TDOA MAE 和 WLS 次数。新增 `plot_existing` 模式，可直接读取 `fair_topk_dev_rows.csv` 重绘，不加载模型、不重新训练或评估。 |
| 验证方式 | 全项目 `compileall`；执行 `--run-mode plot_existing`；SVG 文本字段检查；将 SVG 渲染为 PNG 后人工检查图例、坐标轴、颜色和文字重叠。 |
| 验证结果 | 全部通过。两张 SVG 正常生成，图例位于图外底部，坐标轴和方法颜色清楚，无曲线/文字遮挡；没有重跑 46.6 min 的 dev_fair。 |
| 风险或未解决问题 | 当前可视化仍基于缺少 2/4/6 dB 的开发集；补 mid-SNR 后需使用合并后的 rows 重新生成最终开发图。 |
| 下一轮建议 | 先实现/运行 `dev_mid_gate`，再冻结 solver 和独立测试协议。 |

## 2026-07-10 统一 WLS、公平 Top-K 与锁定泛化入口

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行 V5-B+ full 后批准方案：修复共享 WLS 病态几何收敛，分离 method/protocol gate，并把同 solver 公平消融从 DAE/V5/DFT 扩展到当前强基线集合，实现低复杂度后端和不可静默覆盖的独立测试入口。 |
| 修改/生成文件 | `evaluate.py`、`task_baselines.py`、`diagnose_v5b_topk_wls.py`、新增 `topk_localization.py`、新增 `evaluate_topk_fairness.py`、`研究总结.md`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | 共享 all-pair WLS 正常预算 250 nfev，全部初值失败时统一重试 1000 nfev；诊断 gate 拆为方法与协议两层。公共 Top-K 模块统一独立峰、亚采样、物理 lag 和 solver。公平入口现包含 Raw、DAE-CR16、V5-B、两种 DFT、Cao2017、GeoHybrid-C40F24、Zhai-CRLB、Hadamard、PCA：全部参加原生 `OriginalTop1-WLS` 宽排名，能合法导出 score 的方法再参加统一 CommonTop1/Top3。DFT bins 从源 pkl 冻结读取，PCA 按历史协议确定性重建并缓存。GeoHybrid 因 coarse-conditioned 局部细搜索不伪造全局 Top-K。locked 还必须读取 dev 阶段冻结的 solver 决策文件。 |
| 验证方式 | 病态 snapshot 回归；人工多峰/错峰单元断言；Direct/Cao/Zhai 原生 score 与 Top1 等价断言；GeoHybrid 误用阻断断言；公平入口端到端 smoke；全项目 `compileall`；CSV/manifest 语义检查。 |
| 验证结果 | 全部通过。病态 snapshot 自适应重试成功；Direct、Cao2017、Cao2020、Zhai 的 score 接口保持原 Top1；最终公平 smoke 为 `-10dB × 2 trials`，10 个方法、37 个方法×求解器组合，共 74 行且全部有效。PCA 冻结缓存成功生成。性能数值仅为 smoke，不作研究结论。 |
| 风险或未解决问题 | dev_fair 和 locked_holdout 尚未正式运行。共享 WLS 新协议会改变旧定位 valid_count，旧 Fig9 不能与新协议结果混用。统一 score 会改变部分方法的 pair weight，所以宽基线排名必须使用 `OriginalTop1-WLS`，CommonTop1 只作为统一后端归因基准。GeoBeam 计算量较高。 |
| 下一轮建议 | 用户 PyCharm 直接运行 `evaluate_topk_fairness.py` 默认 dev_fair（6 SNR × 30 trials）；分析 broad leaderboard、native/common alignment 和 solver attribution 后冻结唯一 common solver，再开启 locked_holdout。 |

## 2026-07-10 Beam-selected TDOA 与复杂度审计

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 在 low_gate 通过后补齐正式 full 所需的方法审计，区分“真实 TDOA 改善”和“仅定位残差更自洽”，并量化几何 Beam 搜索成本。 |
| 修改/生成文件 | `diagnose_v5b_topk_wls.py`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `_beam_wls()` 返回求解次数、被考虑和被修改的 pair；保存 pair-level true/Top1/Beam lag、候选排名/概率、换峰标记、TDOA/定位误差、WLS cost 和坐标；新增 Beam 前后 TDOA MAE/RMSE/within-1/within-2、每 SNR 复杂度统计、定位与 TDOA 的 snapshot-cluster bootstrap。默认 PyCharm 模式切换为 `full`。未修改候选数、置信度规则、Beam passes 或正式 `main.py` 评估链路。 |
| 验证方式 | Beam 状态/求解次数单元断言；`diagnose_v5b_topk_wls.py --smoke --progress-every 10`；审计 CSV 语义断言；根目录 16 个 `.py` 全量编译；`git diff --check`。 |
| 验证结果 | 全部通过。Smoke 为 `-10dB × 20 trials`、240 pairs，preflight 全通过；定位和 TDOA 指标均同向改善，snapshot-cluster CI 为正；审计 CSV 中 selected rank、changed 标记、误差复算和每样本 WLS 次数一致。该性能只用于流程验证。 |
| 风险或未解决问题 | full 尚未运行；固定 seed/snapshot 的结果不能证明跨几何泛化。Beam 使用几何信息且重复调用 WLS，后续必须做相同 solver 的公平消融与复杂度报告。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `diagnose_v5b_topk_wls.py` 完成 full；回读新增审计 CSV 后再决定集成 V5-B+ 或冻结路线。 |

## 2026-07-10 Top-K/BeamWLS 最终诊断语义修正

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的最后一轮诊断修正：消除相邻 bin 假 Top-K、Beam 状态不一致、质心回退污染 RMSE 和方法级 pair 统计错配，并建立低 SNR 优先的分阶段 go/no-go。 |
| 修改/生成文件 | `compressed_tdoa.py`、`diagnose_v5b_topk.py`、`diagnose_v5b_topk_wls.py`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | 新增独立局部极大值候选提取，每个峰单独做抛物线亚采样，CSV 同时保存 refined lag 与 bin lag；Power/GeoHybrid oracle top1 也改为亚采样口径。Beam 只处理 V5-B 已标记 `low_confidence_prior` 的 pair，并以 proposal-copy 方式原子提交 lag/position/cost，修复接受后状态回退。正式诊断移除 RejectBeamWLS；WLS 失败保存为无效样本和 failure rate，不再以质心伪造结果；Top1/Beam 各自保存正确的 pair 数、考虑数和修改数。新增配对 bootstrap RMSE 增益区间与自动 go/no-go CSV。 |
| PyCharm 运行方式 | `diagnose_v5b_topk_wls.py` 顶部新增普通变量；默认 `PYCHARM_RUN_MODE="low_gate"`，执行低 SNR 6 点 × 200 trials，并以 4/12 dB 各 50 trials 做中高 SNR 守门。通过后才将变量改为 `full`。脚本会拒绝旧版相邻-bin CSV，避免误跑。 |
| 代码审查 | 确认 `main.py` 正式 V5-B Top1 延迟、pair weight、训练和 Fig9 定位链路未改变；`compressed_tdoa.py` 的变化只影响 Top-K 假设及诊断字段。检查 TDOA lag window、亚采样定义、fixed geometry、方法有效样本、pair 统计和输出前缀。 |
| 验证方式 | 人工 posterior 独立峰断言；Beam 原子状态提交单元 smoke；`diagnose_v5b_topk.py --smoke`；`diagnose_v5b_topk_wls.py --smoke --progress-every 10`；全项目根目录 `.py` 编译；`git diff --check`。 |
| 验证结果 | 全部通过。Top-K smoke 生成 240 pairs，Top1 与 `estimate_lag` 最大差为 0，前三个 bin 两两间距均不少于 2；WLS smoke preflight 240 pairs 全通过，20/20 样本有效，无质心回退、无 RuntimeWarning、退出码 0。`git diff --check` 仅有 Windows 行尾提示。 |
| 是否产生实验结果 | 是，仅 `-10dB × 20 trials` smoke；Top1/Beam RMSE=`93.22/70.63m`，样本量过小，不用于方法结论。 |
| 风险或未解决问题 | 正式 `v5b_pair_topk_diagnostics.csv` 仍是旧版候选，必须先全量运行 `diagnose_v5b_topk.py` 覆盖；之后才能直接点击运行默认 low_gate。low_gate 是否通过仍待正式结果。 |
| 下一轮建议 | 用户先在 PyCharm 运行 `diagnose_v5b_topk.py`，再直接运行 `diagnose_v5b_topk_wls.py`。若 gate 未通过，冻结 Top-K WLS；若通过，再把 `PYCHARM_RUN_MODE` 改为 `full` 完成全 SNR 复核。 |

## 2026-07-10 方案A：top-K WLS 协议对齐与研究总结迁移

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的方案A：修复 top-K WLS 诊断与主 Fig9 评估链路的定位协议差异；新增根目录 `研究总结.md`，系统梳理已尝试创新方案和复现/强基线方法。 |
| 修改/生成文件 | `diagnose_v5b_topk_wls.py`、`compressed_tdoa.py`、新增 `研究总结.md`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `diagnose_v5b_topk_wls.py` 的 `_localize_wls()` 改为复用 `evaluate._localize_from_tdoa_pairs(standard)`，避免离线诊断脚本和主评估链路使用不同 WLS 求解器。`compressed_tdoa.V5BExpertGatedTDOAEstimator.pair_diagnostic()` 新增 `estimate_lag`、`estimate_hit1`、`estimate_hit2`，保存与正式 `estimate_pair(sub_sample=True)` 一致的亚采样 TDOA。WLS 脚本优先读取新版 CSV 的 `estimate_lag`，旧 CSV 缺失时回退 `top1_lag`。新增 `研究总结.md`，迁移并重组 Obsidian 综述中“研究总结”部分，补齐 R20/FreqDAE/V4/V5/top-K 与强基线总结。 |
| 验证方式 | 全项目根目录 `.py` 编译；`diagnose_v5b_topk_wls.py --smoke --progress-every 5`；代码 diff 审查。 |
| 验证结果 | 编译通过。Smoke 通过：`[OK] TDOA preflight passed: checked 240 CSV pairs`；`[OK] All TDOA assertions passed`；Top1-WLS vs V5B-Expert64 max per-SNR RMSE diff=`0.1208 m`，小于 1 m。 |
| 是否产生实验结果 | 是，仅 1 SNR × 20 trials smoke，非正式 Stage 2A 全量结论。 |
| 风险或未解决问题 | 当前 smoke 使用旧 `v5b_pair_topk_diagnostics.csv`，因此日志显示 `top1_lag_col=top1_lag`。正式复核前需先重新运行 `diagnose_v5b_topk.py` 生成包含 `estimate_lag` 的新版 CSV，再运行 `diagnose_v5b_topk_wls.py`。 |
| 下一轮建议 | 用户按顺序在 PyCharm 运行 `diagnose_v5b_topk.py` 和 `diagnose_v5b_topk_wls.py`。若协议对齐后的 Top3-BeamWLS low-SNR 改善仍不足 3m，则冻结 top-K WLS，V5-B 进入 Pareto direct compressed-domain 方法收尾。 |

## 2026-07-09 22:10:00 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 修复 `diagnose_v5b_topk_wls.py` 的 geometry 重建错配，执行 A1-A5：统一 top-K CSV 与 WLS meta，新增运行前 preflight，并完成 smoke。 |
| 修改/生成文件 | `diagnose_v5b_topk_wls.py`、`compressed_tdoa.py`、`当前任务.md`、`对话上下文.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `diagnose_v5b_topk_wls.py` 不再手工使用 `seed+10000` 生成 `snap_exp`。脚本现在优先按 CSV `snapshot_id` 重建 geometry；旧 CSV 无 `snapshot_id` 时使用与 `diagnose_v5b_topk.py` 一致的 `generate_urban_batch(..., snapshot_indices=None, seed=seed)`。新增 `_check_tdoa_consistency()`，在 WLS 前全量检查 CSV `true_tdoa` 与重建 `meta["delay_float"]` 是否一致，失败会立即报错。`compressed_tdoa.run_v5b_topk_diagnostics()` 后续会把 `snapshot_id` 写入 pair-level top-K CSV，降低未来重建歧义。 |
| 代码审查 | 全项目搜索 `generate_urban_batch`、`snapshot_indices`、`seed+10000`、`delay_float`、`true_tdoa`。结论：`main.py`/`evaluate.py` 正式 Fig6-Fig9 评估链路在同一次 `generate_urban_batch()` 产生的 waveform/meta 上即时计算 Raw/Clean/DAE/direct 方法，不会把外部 CSV 的 TDOA 与另一批 meta 拼接；本次错配是 `diagnose_v5b_topk_wls.py` 的离线 CSV 诊断专有问题。 |
| 验证方式 | 全入口 `py_compile`；`git diff --check`；`diagnose_v5b_topk_wls.py --smoke --progress-every 5`。 |
| 验证结果 | 编译通过。`git diff --check` 仅有 Windows CRLF 提示。Smoke 通过：`[OK] TDOA preflight passed: checked 240 CSV pairs`，`[OK] All TDOA assertions passed`，`Top1-WLS vs V5B-Expert64 max diff=0.1208m`，无最终 error。 |
| 是否产生实验结果 | 是，仅为 20 trials smoke，非正式 Stage 2A 结果。 |
| 风险或未解决问题 | 修复前生成的非 smoke `v5b_topk_wls_*.csv` 来自错配 geometry，仍不能用于 go/no-go；需要用户在 PyCharm 全量重跑覆盖。 |
| 下一轮建议 | 用户直接运行 `diagnose_v5b_topk_wls.py`，无需参数。运行完成后回读 `v5b_topk_wls_baselines.csv`、`v5b_topk_wls_summary_by_snr_bucket.csv`、`v5b_topk_wls_alignment.csv` 和终端日志，正式判断 Top3-BeamWLS 是否进入 V5-B+。 |

## 2026-07-09 18:16:59 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 实现 V5-B top-K WLS go/no-go 诊断脚本 `diagnose_v5b_topk_wls.py`，完成 smoke 验证，等待全量运行。 |
| 修改/生成文件 | 新增 `diagnose_v5b_topk_wls.py`；修改 `compressed_tdoa.py`（`pair_diagnostic` 中 `pair_weight` 计算修复）；更新 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | 新增独立脚本 `diagnose_v5b_topk_wls.py`：复用 `v5b_pair_topk_diagnostics.csv` 的 top-K 候选 + 重建 geometry，实现 Top1-WLS（基线对齐）、Top3-BeamWLS（低 confidence pair 尝试 top2/3 替换）、Top3-RejectBeamWLS（剔除低 confidence pair + BeamWLS）。关键设计：`area_size` 从 `sim.area_size` 获取（修复了 hardcode 1500×1500 导致的偏离）；pair weights 优先使用 CSV `pair_weight`，NaN 时 fallback 到 `_pair_weight()` 计算；TDOA 一致性断言 `abs(csv - meta) <= 1e-4`，不通过直接 RuntimeError；BeamWLS 只对最低 confidence 的 `beam_max_pairs` 个 pair 尝试替换，`grid_size=1`（利用 prev_best 初始猜测）；支持 `--smoke`、`--progress-every`、`--beam-max-pairs`、`--beam-passes`、`--grid-size`、`--save-partial-every-snr`；每 SNR 完成后保存 partial CSV。`compressed_tdoa.py` 中 `pair_diagnostic()` 的 `pair_weight` 改用与 `estimate_pair()` 一致的 `_quality_from_score` + `_peak_from_score` 计算。 |
| 验证方式 | `py_compile`；`--smoke --progress-every 5`（1 SNR, 20 trials）。 |
| 验证结果 | Smoke 通过（65s）：TDOA assertions 0 失败；centroid_fallbacks=1；Top1-WLS vs V5B-Expert64 max diff=2.10m（-10dB, 20 trials 统计噪声内，≤5m 免 RuntimeError）。进度每 5 trial 打印正常，partial CSV 保存正常。 |
| 是否产生实验结果 | 仅 smoke（20 trials），非正式全量结果。全量运行待用户在 PyCharm 执行。 |
| 风险或未解决问题 | 全量预测 160 min；若中途中断可从 partial CSV 恢复。Top3-RejectBeamWLS 在 -10dB 初步显示优于 Top1-WLS（93.66m vs 103.88m），但 20 trials 不足以做 go/no-go 判定。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `diagnose_v5b_topk_wls.py`（无需参数）；完成后回读终端输出和 `tables/v5b_topk_wls_baselines.csv`，根据 low-SNR RMSE 改善幅度（目标≥3m 且 mid/high 不劣化 >5%）决定进入阶段 2A（保留 top-K WLS）或阶段 2B（冻结 V5-B 写论文）。 |

## 2026-07-09（阶段 1 诊断包）

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 实现 V5-B+ top-K 诊断包：新增 `pair_diagnostic()` 方法、`run_v5b_topk_diagnostics()` 函数与独立脚本 `diagnose_v5b_topk.py`。 |
| 修改/生成文件 | `compressed_tdoa.py`（新增 2 个方法/函数）、新增 `diagnose_v5b_topk.py`、`当前任务.md`、`修改记录.md`、`对话上下文.md`、`实验结果记录.md` |
| 核心改动 | `V5BExpertGatedTDOAEstimator.pair_diagnostic()` 返回单 pair 的 fused + individual expert top-K lags/probs、hit/miss、quality metrics 与 gating weights。`run_v5b_topk_diagnostics()` 遍历 eval SNR 范围的所有 LOS pair，输出 pandas DataFrame。`diagnose_v5b_topk.py` 为独立脚本：加载 plot_data.pkl 配置和已训练的 V5-A.1 模型权重，构造 V5B estimator，运行诊断，输出 4 个 CSV 到 `tables/` 并打印 low/mid/high SNR 汇总。 |
| 验证方式 | `py_compile compressed_tdoa.py diagnose_v5b_topk.py`；inline dummy expert smoke 测试 `pair_diagnostic()` 返回完整字段和合理 hit/miss；`signal_gen` 导入测试。 |
| 验证结果 | 编译通过。Smoke: `pair_diagnostic` 返回 37 个字段，top1_hit1=True（mock 5.0 lag vs 5.5 true_tdoa），expert_weights=(0.65, 0.35)，oracle_expert_hit1=True。`signal_gen` 可正常导入。 |
| 是否产生实验结果 | 否；仅完成代码级 smoke，不包含任何 V5-B 性能指标。正式诊断结果待用户运行 `diagnose_v5b_topk.py` 后产生。 |
| 风险或未解决问题 | `diagnose_v5b_topk.py` 需要 `运行结果/20260709_120019` 中的 `model_v5a1_power64.pt`、`model_v5a1_geohybrid64.pt` 和 `v5a1_bin_meta`。若 bin_meta 缺失，脚本会尝试从 training history 恢复。预计 eval-only 运行 5-10 min。 |
| 下一轮建议 | 用户在 PyCharm 中直接运行 `diagnose_v5b_topk.py`；运行完成后回读终端输出和 `tables/v5b_pair_topk_diagnostics.csv`，根据 top-K hit rate 决定是否进入阶段 2A（top-K WLS）或阶段 2B（冻结写论文）。 |

## 2026-07-09 11:51:06 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 V5-B：在 V5-A.1 hard-bin exact likelihood 基础上，新增轻量 expert-gated compressed-domain TDOA posterior mixture，并让 PyCharm 直接运行 `main.py` 进入待运行状态。 |
| 修改/生成文件 | `compressed_tdoa.py`、`main.py`、`experiment_profiles.py`、`evaluate.py`、`export_results.py`、`replot.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`、`Obsidian Vault/文献总结&创新方案/研究总结_文献综述_创新方案.md`。 |
| 核心改动 | `compressed_tdoa.py` 新增 `V5BExpertGatedTDOAEstimator`，融合 `V5A1-Power64` 与 `V5A1-GeoHybrid64` 的 hard-bin posterior，门控依据 top1/top2 margin、entropy、posterior variance 等非 oracle 质量指标；`train_v5a1_hardbin_tdoa()` 新增 bucket-weighted ValMAE best-state restore，避免只按 ValLoss 保存导致点估计不优。`main.py` 默认 profile 切到 `compressed_tdoa_v5b_fast`，训练 V5-A.1 experts 后构造 `V5B-Expert64` 并接入 Fig7/Fig8/Fig9、TDOA-only 结果和 `plot_data` 配置。绘图、导出、重绘脚本同步识别 V5-B。 |
| 验证方式 | `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py compressed_tdoa.py experiment_profiles.py`；inline dummy posterior experts 冒烟测试 V5-B 门控、top-K hypotheses 和 alias diagnostics。 |
| 验证结果 | 编译通过。V5-B 冒烟测试返回 finite lag、positive pair weight、top-3 posterior hypotheses 与 expert mixture quality 字段，说明 estimator 接口可进入现有 direct TDOA/WLS 链路。 |
| 是否产生实验结果 | 未产生正式训练或完整评估结果；仅完成代码级逻辑冒烟。 |
| 风险或未解决问题 | V5-B 尚未完整运行，低 SNR 是否能改善 V5-A.1 错峰未知。V5-B 是轻量/半参数 compressed-domain TDOA 方法，不应只按“是否低 SNR 超过完整训练 DAE”判断价值，还需看训练复杂度、推理复杂度、泛化性和同预算压缩基线优势。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `main.py`，确认 profile 为 `compressed_tdoa_v5b_fast`。运行后回读 `run.log`、`plot_data.pkl`、Fig9/TDOA SVG 和 `tables/tdoa*_method_tdoa_metrics.csv`，分 low/mid/high SNR 比较 V5-B、V5-A.1 experts、DAE-CR16 与强压缩基线。 |

## 2026-07-09 11:18:58 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 TDOA-first 评估轨道：在不改变训练/定位链路的前提下，从现有 method diagnostics 中导出纯 pairwise TDOA 指标，用于区分“估计器本身不准”和“定位 WLS/几何放大误差”。 |
| 修改/生成文件 | `evaluate.py`、`main.py`、`export_results.py`、`replot.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `evaluate.py` 新增 `build_tdoa_metrics_from_method_comparison()`，优先使用 direct estimator 的 TDOA 诊断，其次使用 GCC 诊断，并把 Geometry 作为 0 误差 oracle 下界；`plot_method_comparison()` 支持 TDOA MAE、median、weighted abs、within-1/2 和 sidelobe 的正确纵轴。`main.py` 新增 `tdoa_results`/`tdoa_focus_results` 写入 `plot_data.pkl`，并在运行结束输出 `Fig_TDOA_MAE_CR*.svg`、`Fig_TDOA_Within1_CR*.svg` 和 focus MAE 图。`export_results.py` 新增 `tdoa_method_tdoa_metrics.csv` / `tdoa_focus_method_tdoa_metrics.csv` 导出；`replot.py` 支持旧 pkl 直接补画 TDOA 图。 |
| 验证方式 | `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile main.py evaluate.py export_results.py replot.py task_baselines.py compressed_tdoa.py`；对 `运行结果/20260708_235611` 执行 `export_results.py --csv-only`；读取新 CSV 前若干行检查字段和值。 |
| 验证结果 | 编译通过。`运行结果/20260708_235611/tables/tdoa_method_tdoa_metrics.csv` 与 `tdoa_focus_method_tdoa_metrics.csv` 已生成；sanity check 显示 Raw/DAE 使用 `gcc`，V5A1/DFT/Cao/Zhai 等使用 `direct`，Geometry 使用 `geometry_oracle`，TDOA MAE/median/within-rate 字段正常。 |
| 是否产生实验结果 | 未产生新的训练或完整评估结果；只对既有 `20260708_235611` 的 `plot_data.pkl`/diagnostics 做补导出。 |
| 风险或未解决问题 | 目前导出的是 per-SNR aggregate TDOA 指标，不是所有 pair 的全量明细；pair-level 明细仍主要存在 worst localization details 中。如需定位某个样本级误差传播，后续可再加可选全量 pair detail 导出。 |
| 下一轮建议 | 先用 `tdoa_method_tdoa_metrics.csv` 做全/低/中/高 SNR 分段分析，并与 Fig9 localization RMSE 对照；再决定是否进入 V5-B top-K posterior WLS/calibration，避免盲目继续优化定位 RMSE。 |

## 2026-07-08 23:51:00 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 `V5-A.1 hard-bin exact likelihood + posterior calibration 诊断包`，修正 V5-A.0 soft filterbank/CE 与点估计错位问题，使代码进入 PyCharm 待运行状态。 |
| 修改/生成文件 | `compressed_tdoa.py`、`main.py`、`experiment_profiles.py`、`evaluate.py`、`Obsidian Vault/文献总结&创新方案/研究总结_文献综述_创新方案.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `compressed_tdoa.py` 新增 `HardBinCompressedTDOALikelihood`、V5-A.1 explicit-SNR LOS pair dataset、`V5A1TrainConfig`、`train_v5a1_hardbin_tdoa()` 与 `LearnedHardBinCompressedTDOAEstimator`。V5-A.1 固定 64 个复频点，使用精确 selected-bin cross-spectrum steering 生成 lag posterior，并导出 entropy、top1/top2 margin、posterior variance 等校准诊断。`main.py` 新增默认 profile `compressed_tdoa_v5a1_hardbin_fast`，支持 `V5A1-Uniform64/Power64/Cao64/GeoHybrid64` 四类 hard-bin 方法，接入 Fig7/Fig8/Fig9 与 `plot_data.pkl`。 |
| 验证方式 | `py_compile`；inline V5-A.1 1 epoch 小样本训练；inline `run_urban_method_comparison()` direct estimator smoke；文档同步；`git diff --check` 和 Git 状态复核。 |
| 验证结果 | 编译通过。小样本 smoke：10 个 urban snapshot、96 个 LOS pair、1 epoch，训练/验证 loss finite；`ValMAE≈4.839 samples`；`V5A1-Smoke64` 可进入 all-pair WLS，SNR `[0,20]` 小样本 valid count 为 `[4,4]`。`git diff --check` 无空白错误，仅 Windows CRLF 提示。该结果只证明链路可运行，不代表正式性能。 |
| 是否产生实验结果 | 是，但仅为代码级 smoke/快速链路验证，不是正式训练或论文结果。 |
| 风险或未解决问题 | V5-A.1 尚未完整运行。它是诊断包而非已验证最终方案；如果 hard-bin exact likelihood 仍不能改善低 SNR 错峰，应冻结 V5 系列或转向 deterministic strong frequency set + learned reliability/calibrated WLS。 |
| 下一轮建议 | 用户在 PyCharm 直接运行 `main.py`，确认 profile 为 `compressed_tdoa_v5a1_hardbin_fast`；运行后回读 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig9 与 tables，重点分析四类 hard-bin 方法的 low/mid/high SNR posterior 校准和 TDOA/定位误差。 |

## 2026-07-08 19:30:00 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 V5-A：`CR16-only Learnable Compressed-Domain TDOA Likelihood`，新增不重构 waveform 的压缩域 direct TDOA 轨道，并使 `main.py` 进入 PyCharm 待运行状态。 |
| 修改/生成文件 | 新增 `compressed_tdoa.py`；修改 `main.py`、`experiment_profiles.py`、`evaluate.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。 |
| 核心改动 | `compressed_tdoa.py` 新增 `LearnableCompressedTDOALikelihood`、`train_v5a_compressed_tdoa()` 和 `LearnedCompressedTDOAEstimator`。V5-A 使用 64 个复数压缩特征，构造物理 steering 的 TDOA lag posterior，不输出 waveform。`main.py` 新增 `compressed_tdoa_v5a_fast` 模式：加载冻结 Chen DAE 基线，同时训练 `V5A-CR16` direct estimator，并加入 Fig7/Fig8/Fig9 同链路比较。`experiment_profiles.py` 新增同名 profile 并设为默认；`evaluate.py` 新增 V5-A 曲线样式。 |
| 验证方式 | `py_compile`；inline V5-A 1 epoch 小样本训练；inline `run_urban_method_comparison()` direct estimator smoke；`git diff --check`。 |
| 验证结果 | 编译通过。小样本 smoke：10 个 urban snapshot、80 个 LOS pair、1 epoch，训练/验证 loss finite；`ValMAE≈6.133 samples`、`Val<=1≈0.667`；`V5A-CR16` 可进入 all-pair WLS，20 dB 小样本 valid count 为 6/6。`git diff --check` 只有 Windows CRLF 提示。 |
| 是否产生实验结果 | 是，但仅为代码级 smoke/快速链路验证，不是正式训练或论文结果。 |
| 风险或未解决问题 | V5-A 目前只实现 CR16，尚未验证正式性能；第一版 filterbank/aux loss 仍是最小可行实现，若正式结果失败，需要优先分析 posterior、filterbank entropy、pairwise TDOA error 和定位 outlier，而非回到 waveform decoder。 |
| 下一轮建议 | 用户在 PyCharm 运行 `main.py`，确认 profile 为 `compressed_tdoa_v5a_fast`。运行后回读 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig9 与 tables，再判断是否进入 V5-B。 |

## 2026-07-08 16:29:46 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 执行用户批准的 V4-min 方案：冻结 GeoHybrid 为 baseline/消融，新增 `NestedTaskSufficientDAE / FreqDAE-v4-min`，增加 `freq_task_v4_min_fast` profile，只做编译、小样本 smoke 和 CR16 快速诊断，不跑完整训练；同步瘦身长期文档并修复实验记录表格。 |
| 修改/生成文件 | `model.py`、`train.py`、`main.py`、`experiment_profiles.py`、`evaluate.py`、`replot.py`、`export_results.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`、`Obsidian Vault/文献总结&创新方案/研究总结_文献综述_创新方案.md` |
| 核心改动 | `model.py` 新增 `NestedTaskSufficientDAE`，在 nested latent 基础上加入 pairwise task head 与 uncertainty head。`train.py` 新增 `freq_task_v4_min_pair`：共享 full latent，按 CR4/8/16 前缀 mask 计算 task loss、uncertainty NLL 和 CR 单调约束，并保持训练/验证目标一致。`main.py` 新增 V4 模式与默认 profile `freq_task_v4_min_fast`，当前 PyCharm 直接运行会进入 V4-min fast。图表、重绘和导出支持 `FreqDAE-v4-CR4/8/16`。 |
| 验证方式 | 全入口 `py_compile`；`git diff --check`；`NestedTaskSufficientDAE` forward smoke；CR16 2 epoch 快速训练 smoke；CR16 task-head 快速诊断。 |
| 验证结果 | 编译通过；`git diff --check` 无空白错误，仅 Windows CRLF 提示。Forward smoke 输出 waveform/latent/logits 形状正常。CR16 快速训练 smoke 成功，loss finite。CR16 task-head 快速诊断能输出 CR4/8/16 的 TDOA error 与 uncertainty，说明链路可运行。 |
| 是否产生实验结果 | 是；仅为代码级 smoke/快速诊断，不是正式训练或论文结果。 |
| 风险或未解决问题 | V4-min 仍未完整训练，性能未知。当前结构是最小验证版，不保证解决 CR 无区分度或假窄带问题。若完整结果仍失败，应停止继续微调 waveform decoder loss，转向 task-direct / hybrid estimator。 |
| 下一轮建议 | 用户在 PyCharm 运行 `main.py`，确认 `experiment_profile=freq_task_v4_min_fast`、`active_model=NestedTaskSufficientDAE`、`LOSS_MODE=freq_task_v4_min_pair`。回读新结果目录的 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3/Fig9 和 tables 后再判断 V4 是否保留。 |

## 2026-07-08 13:53:55 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 修正 GeoHybrid 预算公平性，新增严格 CR16 Budget64 消融，新增 direct-only 精简输出 profile。 |
| 修改/生成文件 | `task_baselines.py`、`main.py`、`experiment_profiles.py`、`evaluate.py`、`replot.py`、`export_results.py`、`diagnose_go_no_go.py` 及长期文档 |
| 核心改动 | 超预算 `GeoHybrid v2` 改名为 `GeoHybrid-OverBudget-C64F64`，只作 supplement/诊断。新增 `GeoHybrid-B64-C48F16/C40F24/C32F32` 及 no-consistency、no-uncertainty、coarse-only、fine-only 消融。新增 `geohybrid_direct_only_eval`，默认只输出 direct/baseline 相关 SVG/CSV，关闭不变核心图和 Markdown 大表。 |
| 验证结果 | 编译通过；`git diff --check` 通过；预算 sanity 显示 fair 组合 `union=64`、`effective_cr=16.0`，over-budget 组合 `union=124`、`effective_cr≈8.26`；小样本 smoke 与导出 smoke 通过。 |
| 风险或未解决问题 | 正式 Budget64 结果后来显示公平预算下 GeoHybrid 不足以作为主创新路线，已冻结为 baseline/消融。 |

## 2026-07-08 11:30:00 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 清理 `运行结果/` 中明确过度过时目录，保留归档目录和当前代码引用目录。 |
| 核心变化 | 保留 `历史记录_6.13-6.30`、`20260702_000925`、`20260704_195946`、`20260706_103701`、`20260706_230643`、`20260707_143945`、`20260707_193226`、`20260708_111614`、`go_no_go_20260707_183216`；删除过时运行目录。 |
| 验证结果 | 删除前检查 Python/Markdown 引用；删除后保留目录均存在。 |

## 2026-07-08 10:56:07 +08:00

| 字段 | 内容 |
|---|---|
| 分支名 | `NewMethod-Codex` |
| 本轮目标 | 实现 `GeoHybrid v2` 假设检验版：coarse high-coherence bins + fine low-ambiguity bins 的 compressed-domain TDOA estimator。 |
| 核心变化 | `task_baselines.py` 新增 `GeoHybridDFTTDOAEstimator`；`main.py` 新增 `geohybrid_v2_eval`，不训练网络，重算含 GeoHybrid 的 Fig6-Fig8/Fig8_supp；图例、导出、go/no-go 同步。 |
| 验证结果 | 编译通过；已知时移 sanity 通过；小样本 smoke 成功输出 hybrid diagnostics。 |
| 后续结论 | 后续公平预算审查表明该版本超预算，不能作为主创新结果，只保留为 over-budget 上界/消融。 |

## 2026-07-07 关键修改摘要

| 时间 | 主题 | 结论 |
|---|---|---|
| 19:15 | `GeoAmbi-DFT Direct` 阶段 A | 新增 geometry/ambiguity-aware DFT 选频与 direct/AML/PHAT estimator；正式结果显示低 SNR 失败，高 SNR pair TDOA 可很准但定位 mean 受离群影响。 |
| 18:42 | go/no-go 诊断包 | 新增 `diagnose_go_no_go.py`。Task head 学到一定 TDOA 信息，但没有超过强 classical direct baseline，CR 仍无区分。 |
| 14:28 | `FreqDAE v3` nested latent + task head | 新增 `NestedFrequencyPairwiseDAE` 与 `freq_task_nested_pair`；正式结果仍是假窄带/宽峰模板，CR 无区分。 |
| 13:46 | 静态 baseline cache 与 reconstruction-only 诊断 | 新增 `experiment_cache.py` 和 reconstruction-only 诊断；确认重构失败不只是 pairwise loss 冲突。 |

## 2026-07-06 关键修改摘要

| 主题 | 结论 |
|---|---|
| `FreqDAE v2/v2.1` | 引入 pair-aware 训练和 NMSE/phase/soft peak 修复；改善部分低 SNR 但仍输出假窄带/宽峰模板，高 SNR 地板明显。 |
| `FreqDAE v1` | 频域重构路线初始验证；CR16 有局部低 SNR 改善，但整体不如 Chen-style DAE 的 CR4/CR8，训练公平性和 CR 区分度不足。 |

## 更早历史摘要（2026-06 至 2026-07-05）

| 阶段 | 压缩摘要 |
|---|---|
| paper_repro | 从 Chen 论文复现出发，逐步建立 `paper_repro_v3.1`：urban8 fixed eval set、all-pair WLS、noisy RMS 归一化、final retrain/fixed epoch。当前近似复现基线冻结于 `运行结果/20260702_000925`。 |
| R20/R20.1 loss 路线 | 尝试 MSE/GCC/peak、SNR-adaptive、固定混合 NMSE 等 loss-level 改进；结论是 loss 调权不足以稳定兼顾 waveform reconstruction 与 TDOA。 |
| 传统/强基线 | 复现和扩展 DFT、Hadamard、PCA、Cao、Zhai、DFT-Fisher 等 baseline；形成 Fig6-Fig8/强基线对比链路。 |
| 文档体系 | 建立 `AGENTS.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`，要求每轮修改后同步记录，实验结论必须来自实际日志/pkl/svg/CSV。 |
