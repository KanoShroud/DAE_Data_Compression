# 修改记录

> 本文件只保留近期重要修改摘要。2026-07-27 及以前的逐项完整记录保存在
> `文档归档/修改记录_截至2026-07-27_完整历史.md`。
>
> 历史记录按当时状态保留，判断当前状态时以 `当前任务.md` 和
> `对话上下文.md` 为准。更新本文件属于记录流程，不触发新的修改记录。

## 2026-08-13 RAPTQ RelativePhase 24-bit最终可行性周期实现

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 冻结失败的两坐标稀疏表示后，为完整`RelativePhase`建立一次受限且可止损的连续信息与24-bit可行性验证 |
| 新增文件 | `raptq_tdoa/relative_phase_rate.py`、`run_relative_phase_rate.py`、`test_relative_phase_rate.py` |
| 修改文件 | `raptq_tdoa/README.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心实现 | 连续公共相位`CondMI-Matched24`区间似然；固定`5/5/5/5/4 bit`的五维相对相位码；量化相位区间内采样并在似然域边缘化未传输幅度；不做伪波形重构 |
| 公平与隔离 | 同6频点、同24 payload bit、同连续相位/先验/Bayesian后端，无索引与反馈；design/evaluation seed互斥；历史Stage 0模型字段和目录树哈希受保护；正式后验只使用公开模型proposal，观测条件proposal仅作数值交叉检查 |
| 串联止损 | CondMI阈值边界/局部参考或数值门禁失败即停止；连续RelativePhase相对CondMI的headroom未达到`LCB>0.0625 sample²`则不运行最终24-bit阶段；只有headroom通过才评价Bayes风险、经验MSE和严重错峰 |
| 验证 | 3个新增Python文件编译与Ruff F/E9通过；新旧RAPTQ共27项单元测试通过；项目科学完整性检查通过；smoke退出代码0，预算、旋转不变性、码字区间、真值不泄漏、高分辨率极限、种子/模型/哈希和结果字段链路正常。smoke仅为工程证据，临时目录已清理 |
| 当前状态 | `MODIFICATION_COMPLETE_PENDING_PYCHARM_RUN`；尚无正式性能结果 |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/run_relative_phase_rate.py`，回读正式目录后按`RAPTQ-RP-*`门禁一次性裁决 |

## 2026-08-13 RAPTQ SparseAmpPhase 36-context纠正性重算实现

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 在不覆盖历史Stage 0及数值证书的前提下，使用已认证积分器纠正原36-context的`SparseAmpPhase`连续信息审计，并显式检查跨proposal和跨QMC种子的可复现性 |
| 新增文件 | `raptq_tdoa/recompute_stage0_sparse_amp_phase.py`、`raptq_tdoa/test_sparse_correction.py` |
| 修改文件 | `raptq_tdoa/README.md`、`当前任务.md`、`对话上下文.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 固定重建原2 seed×3 trajectory×6 SNR；每个context使用`model/defensive_mixture`两种proposal与两套独立seed family；四分量全部scramble在似然层固定等权合并；导出分量、context、数值汇总、逐SNR指标、trajectory效应、聚类CI和审计图 |
| 公平与边界 | 三个历史结果目录均做运行前后树哈希；`FullComplex`逐context与历史CSV精确对齐；只重算`SparseAmpPhase`，不选择有利分量，不实现CondMI、全新trajectory或24 bit方法；历史整体HOLD保持不变 |
| 门禁 | 正式预算固定`2^16×8`；四分量逐context风险跨度不得超过`delta_guard=0.0625 sample²`。6条历史trajectory的效应与CI仅作纠正性描述，不承担独立信息充分性确认 |
| 验证 | 本轮4个涉及Python文件编译通过；Ruff `--select F,E9`通过；新旧共19项RAPTQ测试通过；项目完整性检查全部通过；2-context smoke退出代码0，历史对齐约`1e-16`、源目录哈希未变、结果行完整。smoke状态为`SMOKE_ONLY`，不形成数值或信息效应结论 |
| 当前状态 | `MODIFICATION_COMPLETE_PENDING_PYCHARM_RUN` |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/recompute_stage0_sparse_amp_phase.py`，正式回读后按`RAPTQ-SC-N01/R01`裁决；不得用本次36-context结果调参或宣称独立确认 |

## 2026-08-13 RAPTQ稀疏表示积分器认证实现

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 仅修正`SparsePhase/SparseAmpPhase`诱导似然的积分坐标和数值提案，不改变冻结统计量、模型、历史入口或正式结果 |
| 新增文件 | `raptq_tdoa/sparse_integrator.py`、`diagnose_sparse_integrator.py`、`test_sparse_integrator.py` |
| 修改文件 | `raptq_tdoa/README.md`、`当前任务.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 新增无冗余Dirichlet stick-breaking；为两个稀疏表示实现模型提案与固定等权防御性混合提案；所有样本使用完整混合密度校正；导出风险、TV、后验均值、scramble离散度、最大归一化积分权重和启发式ESS |
| 机械与公平性 | 加入Dirichlet解析矩、人工径向测度解析积分、三频点独立张量积分和新旧坐标低SNR一致性；冻结Stage 0及既有数值证书目录运行前后哈希；7个压力context明确为开发子集，不冒充独立统计确认 |
| 分阶段止损 | 机械证书失败则不运行压力测试；表示未通过7-context筛查则不进入原12-context；通过表示独立裁决。`delta_guard=0.0625 sample²`保持工程数值裕量定义，最高预算固定`2^16×8` |
| 验证 | 本轮5个涉及Python文件编译通过；Ruff `--select F,E9`通过；新旧共14项单元测试通过；项目完整性门禁全部通过；最终smoke退出代码0、机械证书和源哈希保护通过。smoke仅为工程证据，不形成收敛结论 |
| 当前状态 | `MODIFICATION_COMPLETE_PENDING_PYCHARM_RUN`；历史`HOLD_NUMERICAL_CONVERGENCE`不变，尚未运行正式稀疏积分器认证 |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/diagnose_sparse_integrator.py`；正式结果至少一个表示通过后再审批原36-context纠正性重算，不进入CondMI-Matched24或Stage 0-R |

## 2026-08-05 RAPTQ Stage 0-I数值收敛证书

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 修复正式Stage 0-I中由无信息积分维度和高SNR尖锐似然造成的QMC不确定性，并用独立证书判断连续表示风险排序是否可靠 |
| 修改文件 | `raptq_tdoa/core.py`、`run_stage0.py`、`test_stage0.py`、`README.md` |
| 新增文件 | `raptq_tdoa/diagnose_stage0_numerical.py` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 从QMC中解析删除公共相位；将匹配循环模型的`RelativePower`实现为精确TDOA先验负对照；为`RelativePhase`新增带完整重要性权重的模型/观测防御性混合提案；新增四档QMC功率、独立参考、逐SNR排序、误差方向和门槛敏感性诊断 |
| 公平与边界 | 固定读取原正式manifest和预注册12个context，运行前后验证源目录哈希；观测条件提案只用于积分效率，不算作可部署信息或方法收益；不修改表示、候选频点、统计样本或24 bit方案 |
| 验证 | 本轮5个Python文件`py_compile`通过；Ruff `--select F,E9`通过；7项单元测试通过；新证书和原Stage 0入口smoke均退出代码0；解析负对照误差为0、源目录不变；项目完整性门禁全部通过。smoke不构成正式研究性能证据 |
| 当前状态 | `PENDING_DEVELOPMENT`；正式数值证书尚未运行，RAPTQ继续保持`HOLD_NUMERICAL_INTEGRATION` |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/diagnose_stage0_numerical.py`，回读`validation.json`和四类CSV后按`RAPTQ-NC-R01`裁决是否允许全新trajectory扩展Stage 0-I |

## 2026-08-04 RAPTQ-TDOA Stage 0独立实现

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 在设计任何24 bit量化器前，验证未知公共复增益下的商空间不变性，并审计候选连续统计量保留的TDOA Bayes信息 |
| 新增文件 | `raptq_tdoa/__init__.py`、`core.py`、`run_stage0.py`、`test_stage0.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 建立连续均匀增益相位及Bessel解析边缘化模型；实现完整复观测、`radius + projector`无损商表示以及5类有损相对/稀疏统计量；用显式复极坐标Jacobian和scrambled Sobol QMC构造诱导似然；增加有限窗口非酉时延机械证书 |
| 信息与公平边界 | 沿用SITQ冻结频点和匹配频谱模型但不读取其evaluation结果；全部低能量context保留；manifest记录源码哈希、种子和配置；本轮不实现24 bit、码本、联合选频、神经网络或真实波形性能比较 |
| 验证 | 本轮Python文件`py_compile`通过；Ruff `--select F,E9`通过；5项单元测试通过；smoke退出代码0并验证结果保存、任意公共相位不变性、无损商表示和QMC诊断链路；smoke不构成研究性能证据 |
| 风险与边界 | 高维有损统计量的QMC误差可能影响排序，正式development必须先通过独立高预算参考审查；当前状态为`PENDING_DEVELOPMENT`，不得提前主张RAPTQ优于SITQ/CondMI |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/run_stage0.py`；回读manifest、validation、数值诊断、配对风险CSV和SVG，再按`RAPTQ-R01`裁决是否另行审批固定24 bit码率审计 |

## 2026-08-04 文献综述同步BRSR与SITQ完整证据链

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 将综述从FBRSA当前候选叙事同步到截至2026-08-03的BRSR终止裁决和SITQ条件任务量化证据，重新界定当前研究缺口与候选创新 |
| 修改文件 | `Obsidian Vault/文献综述_20260714.md`、`修改记录.md` |
| 核心变化 | 更新摘要、系统模型、24 bit账本和结论；新增解码器侧信息下的观测MSE、条件互信息与TDOA Bayes risk量化坐标；按固定观测、逐样本主动采集、共享状态和条件任务量化重组方法；补齐PASR、BRSR Stage 2A/2A.1、动作可观测性、共享状态、转折区及SITQ Stage A/A.1证据链 |
| 创新边界 | FBRSA/BRSR改为已检验并终止的历史候选；SITQ总体思想未被整体否定，但单一全局对称阈值组件=`HOLD_SELECTION_UNCERTAIN`；当前不存在已通过门禁的最终创新方案 |
| 候选假设 | 仅保留待文献核验和另行审批的逐频点非均匀任务阈值、固定24 bit离散位宽分配、结构化向量量化；未新增未经Zotero全文核验的文献，参考文献保持`[1]`--`[83]` |
| 证据来源 | `研究门禁注册表.md`、`研究总结.md`、`实验结果记录.md`及PFRS/PASR/BRSR/SITQ正式结果目录；未新增、重算或重解释实验指标 |
| 验证 | UTF-8回读；37个显示数学块、110个行内数学片段、LaTeX花括号/环境和代码围栏配对通过；数学块无原始`<`；6个Markdown表格列数一致；83条参考文献顺序与正文引用闭合；本地链接及8个主要证据目录存在；旧“FBRSA为当前候选”表述已清除 |
| 实验影响 | 未修改Python代码、配置、门禁或运行结果；未运行训练、评估或诊断 |

## 2026-08-03 SITQ-TDOA Stage A.1阈值稳定性诊断

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 在不修改Stage A算法和不读取evaluation结果选阈值的前提下，检验Bayes-risk全局阈值相对CondMI阈值的跨design轨迹稳定性 |
| 新增文件 | `sitq_tdoa/diagnose_stage_a1_threshold_stability.py`、`sitq_tdoa/test_stage_a1.py` |
| 修改文件 | `sitq_tdoa/README.md`及本轮状态、结果、门禁和研究总结文档 |
| 核心变化 | 新增逐context阈值风险面、轨迹分组bootstrap、六折交叉冻结、分seed方向和近似MDE诊断；源结果及源码哈希前后校验；参数命中率仅描述，不作为硬门禁 |
| 验证 | 本轮Python文件编译通过；Ruff `--select F,E9`通过；14项单元测试通过；项目完整性检查全部通过；正式诊断退出代码0，源目录不变 |
| 正式结果 | `运行结果/SITQ/SITQ_STAGE_A1_20260803_140306_diagnostic`；交叉冻结改善=`0.0138 sample²`，95% CI跨0，两个design seed方向相反，裁决=`HOLD_SELECTION_UNCERTAIN` |
| 风险与边界 | 结果只裁决单一全局对称阈值组件，不重新解释Stage A evaluation，也不否定逐频点阈值、有限bit分配或向量量化 |
| 下一步 | 当前组件冻结，不生成新evaluation确认；后续如重启SITQ，须另行审批具有新理论自由度的新development方案 |

## 2026-08-03 SITQ-TDOA Stage A独立实现

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 独立验证固定24 bit下，直接最小化TDOA后验贝叶斯风险的条件任务量化是否优于观测MSE量化和条件互信息量化 |
| 新增文件 | `sitq_tdoa/__init__.py`、`core.py`、`run_stage_a.py`、`test_stage_a.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 建立匹配复高斯频域模型、有限TDOA先验、未知复增益边缘化和精确量化区间似然；实现MSEQuant、CondMIQuant、SITQ-BayesRisk及浮点上界；design冻结阈值，evaluation独立评价 |
| 公平性 | 三个量化方法共享6个固定频点、每个I/Q 2 bit和24 bit总载荷；无索引、反馈、停止或动态动作；浮点上界排除在同bit裁决外；评估后强制检查方法完整性、键唯一性和预算一致性 |
| 原理修正 | 生成TDOA与有限先验网格完全一致；Bayes-risk设计最小化后验方差；未知公共复增益下按频点差而非绝对频点判定全局模糊周期；阈值落在搜索边界时强制HOLD |
| 验证 | 涉及文件`py_compile`通过；Ruff `--select F,E9`通过；10项单元测试通过；最终smoke退出代码0且机械/公平性检查全通过；`validate_project_integrity.py`现有项目完整性检查全通过 |
| 运行边界 | smoke不构成性能证据，未更新`实验结果记录.md`；正式development由用户在PyCharm直接运行`sitq_tdoa/run_stage_a.py` |
| 下一步 | 回读正式CSV、JSON和SVG，按`SITQ-R01`裁决是否进入联合频点/位宽设计和连续BPSK-RRC模型失配阶段 |

## 2026-08-01 BRSR转折区频谱异质性独立证书

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 独立验证中等SNR模糊度转折区内，块相干且可观测的频谱异质性是否提供可重复、非事后的主动精化动作价值 |
| 新增文件 | `brsr_transition_regime/__init__.py`、`experiment.py`、`run_experiment.py`、`test_experiment.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 主效应预注册为0/5 dB，-10/15 dB保留为负对照；S2采用三类块相干公共频谱陷波，ContextStatic获得相同类别；StateOracle只用planning冻结B24动作；加入奇偶交叉拟合、同总bit时间共享、S2减S1差分归因和分层停止裁决 |
| 公平性 | 2-bit类别索引和5-bit动作反馈均按L=16计入；静态方法按结果无关hash匹配总bit；planning/evaluation符号、噪声、ID和种子互斥；不读取locked或OutcomeOracle |
| 验证 | 涉及文件`py_compile`通过；Ruff `--select F,E9`通过；4项协议单元测试通过；`BRSR_TRANSITION_20260801_174253_smoke`的14项机械检查全部通过 |
| 运行边界 | smoke只证明链路，临时性能状态不可用于路线结论；正式development由用户在PyCharm运行`brsr_transition_regime/run_experiment.py` |
| 下一步 | 回读正式CSV、JSON、SVG和日志，按`BRST-R01`裁决是否允许另立可部署ObservablePolicy development；本轮不生成locked |

## 2026-08-01 进展总结全文同步BRSR完整研究链

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 将7月17日以后BRSR从机制验证、同bit部署、Oracle语义审计到共享状态精度复验的完整进展同步到整份导师向进展报告，而非仅追加后部章节 |
| 修改文件 | `Obsidian Vault/进展总结_20260731.md`、`修改记录.md` |
| 核心变化 | 更新摘要、当前阶段和贡献边界；将FBRSA候选与BRSR实现衔接；重构前四章中的信息流图、核心术语、双轨资源口径、双站观测模型、四层评价、强基线、路线演进图和路线表；新增BRSR独立章节、阶段裁决表及planning精度复验表；最终状态明确为`NO_GO_ROUTE_PLANNING_NONCONVERGENCE` |
| 证据来源 | 仅整理既有正式结果：Stage A/B、Stage 1、Stage 2A/2A.1、可观测性证书、StateOracle证书和S1精度复验；未新增或重算实验数据 |
| 审慎边界 | 明确区分OutcomeOracle、StateOracle和DeployablePolicy；当前裁决只终止既定场景与协议组合，不外推为所有主动频谱采集理论失败 |
| 验证 | 全文Markdown结构、章节及图表编号、数学分隔符和高风险公式行、本地结果路径、Mermaid节点、UTF-8读取及`git diff --check`检查 |
| 实验影响 | 未修改Python代码、配置、门禁或运行结果；未运行训练、评估或诊断 |

## 2026-07-31 BRSR S1 StateOracle确认性精度复验

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 在不调算法和门禁的前提下，对原S1/L16小幅正效应执行一次planning收敛校准和功效充分的独立确认 |
| 新增文件 | `brsr_s1_precision_replication/__init__.py`、`replication.py`、`run_replication.py`、`test_replication.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | calibration与confirmation状态互斥；以`32/64/128`包候选、256包参考和128包独立check选择最小收敛planning配置；增加参考动作交叉拟合充分性；正式确认固定48个独立状态、S1和`L=16` |
| 功效与门槛 | 历史最小MSE点效应=`0.30455 sample²`、最大cluster SD=`0.69355`，双侧5%和80%功效近似需41状态，预注册48；planning主等价界为历史效应10%，并固定5%/20%敏感性 |
| 公平性 | 只比较StateOracle-B24与BestStatic/SNR-only的B24及同总bit L16基线；反馈按`5/16 bit/packet`计入；不使用OutcomeOracle、locked或确认结果选planning包数 |
| 工程优化 | confirmation planning只保存状态/SNR/配置充分统计量，不保存百万级逐包B32行；策略选择与原逐包均值数学等价 |
| 验证 | 本轮文件`py_compile`和Ruff `F,E9`通过；6项单元测试通过；87个项目Python文件编译通过；`validate_project_integrity.py`全部通过；SVG视觉检查通过 |
| Smoke | `运行结果/BRSR/BRSR_S1_PRECISION_20260731_175641_smoke`；20项机械检查全部通过，状态固定为`SMOKE_ONLY`，不构成性能结论 |
| 未完成 | 正式development尚未由用户在PyCharm运行；不得依据smoke调整预注册配置 |
| 下一步 | PyCharm直接运行`brsr_s1_precision_replication/run_replication.py`并回读planning与确认性门禁；任何非PASS正式结果终止当前共享状态BRSR路线 |

## 2026-07-31 BRSR共享状态StateOracle路线可行性证书

| 字段 | 内容 |
|---|---|
| 分支 | `NewMethod-Codex` |
| 目标 | 在实现ObservablePolicy前，以非事后StateOracle检验共享物理状态是否提供能够超过同总bit强静态前沿的系统性动作余量 |
| 新增文件 | `brsr_state_oracle_certificate/__init__.py`、`certificate.py`、`run_certificate.py`、`test_certificate.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md`、`实验结果记录.md` |
| 核心变化 | 独立实现S0/S1/S2；planning/evaluation符号和噪声分离；StateOracle只按planning条件期望损失冻结B24动作；加入B24/B32、SNR-only、S2 ContextStatic及`L=4/8/16`同总bit时间共享；输出配对cluster CI、逐seed方向、MDE、planning动作裕量与分半稳定性 |
| 公平性 | 5 bit动作反馈按块长摊销；S0强制退化为SNR-only；S2显式加入已知陷波类别的ContextStatic；不读取locked或evaluation结果选动作 |
| 兼容修复 | 历史冻结源码的原始哈希受Git LF与Windows CRLF影响；新入口只在换行归一化SHA完全匹配时接受，并在manifest记录匹配方式，不放宽语义源码冻结 |
| 验证 | 4个Python文件`py_compile`通过；Ruff `F,E9`通过；6项单元测试通过；项目科学完整性检查通过；最终smoke端到端成功 |
| Smoke | `运行结果/BRSR/BRSR_STATE_ORACLE_20260731_164622_smoke`，10项机械检查全部通过，状态固定为`SMOKE_ONLY`，不构成性能结论 |
| 未完成 | 尚未运行正式development，S1/S2是否存在稳定状态余量待确认 |
| 下一步 | 用户在PyCharm直接运行`brsr_state_oracle_certificate/run_certificate.py`；回读`run.log`、`gate_decision.json`、`paired_effects.csv`、`planning_policy_diagnostics.csv`和主图 |

## 2026-07-30 BRSR Oracle语义审计与动作价值可观测性证书

| 字段 | 内容 |
|---|---|
| 目标 | 修正“事后Oracle优势等于可学习余量”的错误外推，停止IID样本块协议，并用独立数据检验动作前信息能否预测条件动作风险 |
| 新增文件 | `brsr_observability_certificate/__init__.py`、`audit_oracles.py`、`features.py`、`certificate.py`、`run_certificate.py`、`test_certificate.py`、`README.md` |
| 文档更新 | `当前任务.md`、`对话上下文.md`、`研究总结.md`、`研究门禁注册表.md`、`修改记录.md` |
| 核心变化 | 历史Oracle统一分类为事后全知或干扰参数Oracle；IID Block改判`NO-GO-PROTOCOL-IID-BLOCK`；新增严格动作前特征白名单、互斥train/calibration/test seeds、预注册Ridge/HGB、校准集选模、29-bit静态时间共享强基线、配对cluster CI、MDE与分级裁决 |
| 冻结边界 | 未修改 `brsr_tdoa/`、`brsr_route_feasibility/` 或 `brsr_stage2_rate_adaptive/`；候选池、似然、`5x64`积分、RiskGreedy、SafeGate、温度、通信预算和历史结果保持不变 |
| 验证 | 本轮6个Python文件通过`py_compile`和Ruff `F,E9`；6项单元测试通过；真实链路smoke完成冻结工件加载、独立数据生成、条件风险学习、公平基线、审计和导出，所有方法平均总量均为29 bit，partial文件清零 |
| Smoke | `运行结果/BRSR/BRSR_OBSERVABILITY_20260730_144448_smoke`；状态固定为`SMOKE_ONLY`，只证明工程链路，不作为性能结论 |
| 下一步 | 用户在PyCharm直接运行`brsr_observability_certificate/run_certificate.py`的默认development；正式结果决定`GO_OBSERVABLE`、`HOLD_POWER`、`HOLD_PROXY_MISMATCH`或`NO_GO_OBSERVABILITY_CURRENT_FEATURES` |

## 2026-07-29 Smoke 记录分层与实验归档表格优化

| 字段 | 内容 |
|---|---|
| 目标 | 将常规 smoke 从长期实验事实中分离，保留工程追溯能力，并改善 12 列历史实验表的阅读宽度 |
| 修改文件 | `AGENTS.md`、`实验结果记录.md`、`修改记录.md`、`文档归档/README.md`、三份相关完整历史归档；新增 `文档归档/smoke验证历史_截至2026-07-27.md` |
| 核心变化 | 常规 smoke 默认只写修改验证；14 个独立 smoke 章节和 49 条代码级/smoke 表格记录迁入非默认读取的专项历史附件；对话上下文和研究总结删除已被正式结果覆盖的 smoke 细节；正式实验、重要诊断和独立协议事实不变 |
| 表格优化 | 历史实验表由12列精简为7列：日期、分支、实验名称和脚本合并为“记录”，证据来源、可信度和论文用途合并为“证据与适用性”；最终采用1920px固定布局，较原2400px缩窄20%，列宽比例为10/6/9/26/22/19/8 |
| 实验影响 | 未修改 `.py`、模型、配置、运行结果或门禁裁决；未运行训练和评估 |

## 2026-07-29 历史归档结构与路径治理

| 字段 | 内容 |
|---|---|
| 目标 | 整理三份完整历史归档的时间顺序、章节层级、重复内容和结果路径，降低历史状态对当前任务的歧义 |
| 修改文件 | `文档归档/实验结果记录_截至2026-07-27_完整历史.md`、`文档归档/对话上下文_截至2026-07-27_完整历史.md`、`文档归档/研究总结_截至2026-07-27_完整历史.md`、其余两份完整历史归档及 `文档归档/README.md` |
| 核心变化 | 早期实验表按时间倒序；对话上下文建立 1–5 章并收拢 V5 专题；研究总结将原第 5 章并入 3.8；所有归档明确历史状态不构成当前指令；可唯一映射的旧结果路径改为当前实际路径 |
| 验证 | 日期顺序、标题层级、长段落重复、结果路径存在性、归档 SHA-256 和 Markdown 差异检查 |
| 实验影响 | 未修改 `.py`、模型、配置、运行结果或门禁裁决；未运行训练和评估 |

## 2026-07-29 根目录研究文档治理与门禁权威收口

| 字段 | 内容 |
|---|---|
| 目标 | 降低每次对话恢复上下文的成本，清除旧状态冒充当前状态的问题，并统一研究门禁的权威来源 |
| 修改文件 | `AGENTS.md`、`当前任务.md`、`对话上下文.md`、`研究总结.md`、`修改记录.md`、`实验结果记录.md`、`研究门禁注册表.md` |
| 新增内容 | `文档归档/README.md` 及五份截至 2026-07-27 的完整历史归档 |
| 核心变化 | 当前任务切换为 BRSR 新块级评分/协议理论方案；研究总结补齐 BRSR Stage 1 至 Stage 2A.1；大型上下文和日志改为“当前摘要+归档索引”；`AGENTS.md` 改为按需读取；门禁以仓库规则和注册表为准 |
| Memory 处理 | 删除 Codex memory 中项目专属的重复门禁规则正文，不删除仓库内的权威规则 |
| 验证 | UTF-8 回读、标题与链接检查、状态关键词交叉检查、归档文件大小和内容保留检查 |
| 实验影响 | 未修改 `.py`、模型、配置、数据或运行结果；未运行训练、评估或冒烟实验 |

## 近期重要修改索引

| 日期 | 事件 | 结果 |
|---|---|---|
| 2026-07-27 | Stage 2A.1 块级协议证书 | `UPPER-BOUND-ONLY`；BlockOracle 有余量，BlockPredicted 未通过 |
| 2026-07-27 | Stage 2A.1 正式 development | `NO-GO-CONFIG-STAGE2A1`；冻结 SafeGate，阻塞 Stage 2B/locked |
| 2026-07-27 | Stage 2A 失败机理诊断 | 弱评分关系存在但重尾严重，只允许全新数据上的新设计 |
| 2026-07-24 | Stage 2A 同平均总 bit 验证 | Oracle 通过，可部署策略和校准未通过 |
| 2026-07-24 | BRSR Stage 1 认证积分与正式重算 | 限定协议内通过，允许进入 Stage 2 设计 |
| 2026-07-23 | 统一研究门禁治理 | 建立分级裁决、敏感性审计和注册表 |

完整的修改文件、验证命令、结果和遗留问题均可在历史归档中按日期检索。
