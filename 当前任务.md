# 当前任务

## 1. 当前任务状态

| 字段 | 内容 |
|---|---|
| 当前任务 | 执行 A+B+C 三阶段修改，建立更接近 Chen 2025 的 `paper_repro_v2_urban8` 复现轨道 |
| 任务来源 | 用户批准 A+B+C，并要求同步考虑整数 TDOA、K-fold 泄漏、路径损耗、GCC、归一化、Raw 公平性等因素 |
| 当前阶段 | 代码修改已完成；编译、城市小样本冒烟、旧 R20.1 路径冒烟已通过；完整训练尚未运行 |
| 是否允许修改代码 | 本轮已按批准完成；后续除非用户继续批准，不再扩展修改 |
| 是否允许修改 md 文档 | 是，本轮已按项目规则更新长期上下文文档 |
| 是否允许运行训练 | 否；本轮未运行完整 `python main.py`，只运行编译和小样本冒烟验证 |
| 背景 | `paper_repro_v1` 使用 pairwise TDOA 代理指标，Raw 在高 SNR 饱和，无法复现论文的定位误差趋势；因此需要转向 8 UAV 几何定位近似复现 |
| 范围内文件 | `main.py`、`train.py`、`signal_gen.py`、`evaluate.py`、`replot.py`、根目录上下文文档 |
| 范围外文件 | Git 提交、完整训练、Wireless InSite 数据生成、论文全部对比基线实现 |
| 当前阻塞点 | 无代码阻塞；需要用户确认是否运行完整 `paper_repro_v2_urban8` |
| 下一步动作 | 运行默认 `python main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3，再判断复现趋势 |

## 2. 待办清单

- [x] 检查 Git 状态、当前分支和最近 commit。
- [x] 读取上下文文档与当前代码。
- [x] 在 `signal_gen.py` 增加 `urban8`：单目标、8 UAV、建筑物遮挡、LOS mask、路径损耗、分数时延、多径和 per-observation RMS。
- [x] 在 `train.py` 增加 snapshot-level GroupKFold，避免同一城市 snapshot 同时进入 train/val。
- [x] 在 `evaluate.py` 增加亚采样 TDOA、8 UAV 非线性最小二乘定位、Raw/DAE/Clean oracle 公平评估。
- [x] 在 `main.py` 将默认 `paper_repro` 切换为 `paper_repro_v2_urban8`，并保留 `$env:DAE_EXPERIMENT_MODE='r20_1'` 切回旧创新轨道。
- [x] 更新 `replot.py` 对新配置字段的打印。
- [x] 运行 `py_compile` 和小样本冒烟验证。
- [x] 更新 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。
- [ ] 用户确认后运行完整 `python main.py`。
- [ ] 读取完整结果并判断是否接近论文趋势。

## 3. 用户审批记录

- 2026-06-30：用户要求进行一次“文档治理修改”，并明确要求全部采纳 GPT 网页端给出的修改方案后执行。
- 2026-07-01：用户批准先建立严格的 `paper_repro` 论文复现基线轨道，并要求修改后更新文档、审查代码进入待运行状态。
- 2026-07-01：用户提供 `paper_repro_v1` 完整运行结果 `20260701_122523`，要求分析代码与结果，判断无法复现论文趋势的原因。
- 2026-07-01：用户批准并要求执行 A+B+C 三阶段修改，同时考虑整数 TDOA 分辨率、K-fold 泄漏、路径损耗、GCC、归一化和 Raw baseline 公平性。

## 4. 完成标准

- 默认 `python main.py` 进入 `paper_repro_v2_urban8`，评估指标从 pairwise TDOA RMSE 转为 8 UAV localization RMSE meters。
- 训练数据采用 snapshot-level GroupKFold，减少同一固定城市快照泄漏。
- Raw、DAE、Clean oracle 经过同一 LOS 选择、GCC、亚采样 TDOA 和定位求解流程。
- 修改后的 Python 文件通过编译和小样本冒烟验证。
- 不编造完整实验结论；完整训练运行前只记录代码验证结果。
