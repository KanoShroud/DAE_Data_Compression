# 当前任务

## 1. 当前任务状态

| 字段 | 内容                                                                        |
|---|---------------------------------------------------------------------------|
| 当前任务 | 实验流程待继续 / R20.1 完整训练待确认                                                   |
| 任务来源 | 用户根据 GPT 网页端进一步审查方案，要求 Codex 全部采纳并执行修改                                    |
| 当前阶段 | 文档治理已完成，已启动 R20.1 完整训练，待运行结束后读取新实验结果                                      |
| 是否允许修改代码 | 否，除非用户后续批准具体修改方案                                                          |
| 是否允许修改 md 文档 | 是，仅限本任务范围内的根目录 md 文档                                                      |
| 是否允许运行训练 | 否，本轮不运行完整训练                                                               |
| 背景 | 第一轮文档治理已解决主要问题；本轮进一步修正 py_compile 规则过固定、论文可用结果标准不够细、R20.1 判断标准缺失、修改记录可能递归嵌套等细节 |
| 范围内文件 | `AGENTS.md`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`                    |
| 范围外文件 | 所有 `.py` 代码、`运行结果/`、`.gitignore`、Git 历史、模型权重和中间数据                         |
| 当前阻塞点 | 无；完整训练与代码验证不属于本轮范围                                                        |
| 下一步动作 | 待 R20.1 运行结束后读取新 run.log/plot_data.pkl               |

## 2. 待办清单

- [x] 检查 Git 状态、当前分支和最近 commit。
- [x] 读取现有 5 个根目录 md 文档。
- [x] 将 `AGENTS.md` 项目路径改为本地参考路径。
- [x] 调整 `AGENTS.md` 中 py_compile 规则，要求覆盖本轮涉及和新增/修改的 Python 文件。
- [x] 在 `AGENTS.md` 与 `修改记录.md` 中加入非递归记录原则。
- [x] 在 `对话上下文.md` 中新增 R20.1 结果判断标准。
- [x] 在 `实验结果记录.md` 中补充论文可用结果的复现要求。
- [x] 在 `修改记录.md` 中追加本轮轻量精修记录。

## 3. 用户审批记录

- 2026-06-30：用户要求进行一次“文档治理修改”，并明确要求全部采纳 GPT 网页端给出的修改方案后执行。

## 4. 完成标准

- `AGENTS.md` 保留事件驱动文档更新规则，不写入任何当前版本状态。
- py_compile 规则既保留主流程最低检查，也覆盖本轮新增或修改的 Python 文件。
- `对话上下文.md` 明确 R20.1 完整训练后的判断标准。
- `实验结果记录.md` 明确论文可用结果必须可复现。
- `修改记录.md` 明确非递归、非琐碎、事件级记录原则。
- 本轮不修改代码、不运行训练、不提交 Git。
