# 当前任务

## 2026-07-08 当前任务：V5-A 已实现，待 PyCharm 完整运行

| 字段 | 内容 |
|---|---|
| 当前任务 | 执行用户批准的 `V5-A：CR16-only Learnable Compressed-Domain TDOA Likelihood`，使代码进入 PyCharm 待运行状态。 |
| 是否允许修改代码 | 是。用户已批准进入 V5-A。 |
| 任务背景 | V4-min waveform decoder + task head 路线在 `运行结果/20260708_163957` 未通过 go/no-go：Fig2 仍是假窄带/低信息量输出，Fig9 中 CR4/8/16 无区分，task head direct 诊断也不够强。 |
| 本轮目标 | 新增不重构 waveform 的 CR16 压缩域直接 TDOA likelihood 模型，并通过现有 direct estimator + all-pair WLS 评估链路与 Chen DAE 和强基线比较。 |
| 范围内文件 | `compressed_tdoa.py`、`main.py`、`experiment_profiles.py`、`evaluate.py`、`对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`、`Obsidian Vault/文献总结&创新方案/研究总结_文献综述_创新方案.md` |
| 不修改的文件 | 冻结基线结果目录 `运行结果/20260702_000925`；已归档目录 `运行结果/历史记录_6.13-6.30`；旧实验结果目录除非用户再次批准清理。 |
| 已完成事项 | 新增 `compressed_tdoa.py`；新增 profile `compressed_tdoa_v5a_fast`；默认 profile 已切换；V5-A 训练历史与模型保存接入；`V5A-CR16` 进入 Fig7/Fig8/Fig9 方法比较；编译和小样本 smoke 通过。 |
| 未完成事项 | 尚未运行完整 `main.py`；尚未读取正式 V5-A 结果目录；尚未判断 V5-A 是否通过 go/no-go。 |
| 完成标准 | 代码可编译；小样本 smoke 可训练、可估计、可定位；PyCharm 点击 `main.py` 可进入 V5-A profile；正式结果生成后能看到 V5-A 与 Chen DAE/DFT/Cao/Zhai/GeoHybrid/Hadamard/PCA 同图比较。 |
| 当前验证方式 | `py_compile`；inline V5-A 1 epoch 小样本训练；inline `run_urban_method_comparison()` direct estimator smoke；`git diff --check`。 |
| 当前验证结果 | 全部通过。小样本 smoke 只证明链路可运行，不作为正式性能结论。 |
| 下一步优先级 | 1. 用户用 PyCharm 运行 `main.py`。2. 回读新目录 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig9、tables。3. 判断 V5-A CR16 是否优于当前压缩基线，若成功再进入 V5-B。 |
| 风险与注意事项 | V5-A 第一版只验证 CR16，不验证 CR4/CR8 资源阶梯；训练 loss 权重仍是最小可行实现，正式结果若失败，需要先看 pairwise TDOA posterior 与 filterbank diagnostics，而不是直接回到 waveform decoder。 |
