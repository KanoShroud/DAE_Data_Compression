# 当前任务

## 2026-08-13 当前任务：RAPTQ RelativePhase 24-bit最终可行性周期

| 字段 | 内容 |
|---|---|
| 当前任务 | 使用全新trajectory完成一次受限的`RelativePhase`最终可行性周期：先确认连续信息headroom，再在同24 bit下比较`RAPTQ-RelativePhase24`与模型匹配`CondMI-Matched24`。 |
| 任务来源 | 两坐标`SparseAmpPhase`已为`HOLD_COMPUTABILITY_36_CONTEXT`且中高SNR信息损失明确；完整`RelativePhase`数值稳定、连续信息更完整，但尚未证明有限码率价值。 |
| 当前阶段 | `MODIFICATION_COMPLETE_PENDING_PYCHARM_RUN`。独立实现、单元测试和smoke已经完成；尚无正式性能结果。 |
| 新增入口 | `raptq_tdoa/run_relative_phase_rate.py`；顶部`RUN_MODE="development"`，可直接在PyCharm运行，不需要环境变量。 |
| 新增实现 | `raptq_tdoa/relative_phase_rate.py`：连续公共相位`CondMI-Matched24`量化似然和固定`5/5/5/5/4 bit`相对相位区间似然。 |
| 公平预算 | 两种有限码率方法均使用冻结6频点和24 payload bit；无索引、无反馈。CondMI为每复频点`2-bit I+2-bit Q`；RAPTQ传输五个相对相位。A站含噪观测、SNR及冻结先验作为融合端公开侧信息。 |
| 位宽边界 | 唯一4-bit坐标在运行前按`source_power*(omega_k-omega_anchor)^2`最小局部杠杆冻结；不根据design或evaluation性能选位宽，不联合选频或训练码本。 |
| 数据隔离 | design seeds=`7301/7302`，evaluation seeds=`8301--8304`；每条trajectory共享其SNR扫描。历史Stage 0目录只读并做模型字段与树哈希核对。 |
| 串联门禁 | 先冻结CondMI阈值并做128/256节点局部参考；数值失败则止损。随后只计算连续`RelativePhase` headroom；其CI未超过`delta_guard=0.0625 sample²`则不运行24-bit RAPTQ。只有headroom通过才比较同bit Bayes风险、经验MSE与严重错峰率。 |
| 统计规模 | evaluation共4 seed×40 trajectory=`160`独立cluster，每cluster含6个SNR；报告效应量、trajectory-cluster bootstrap 95% CI和近似MDE。该规模用于一次development可行性裁决，不是locked结果。 |
| 当前禁止事项 | 不修改历史`SparsePhase/SparseAmpPhase`；不依据本轮结果调频点、位宽、先验、积分预算、门槛或阈值网格；不进入真实波形、联合选频、神经网络或locked。 |
| 验证状态 | 本轮3个新增Python文件编译与Ruff F/E9通过；新旧RAPTQ共27项测试通过；项目完整性门禁通过；smoke退出代码0、预算/种子/模型/哈希/结果字段链路正常。smoke目录已清理且不构成性能证据。 |
| 下一步 | 用户在PyCharm直接运行`raptq_tdoa/run_relative_phase_rate.py`。回读`validation.json`、`config_manifest.json`、数值审计、配对效应、逐SNR汇总和SVG后，按新注册门禁裁决RAPTQ是否结束或进入后续模型失配验证。 |

## 已冻结路线

- SITQ单一全局对称阈值：`HOLD_SELECTION_UNCERTAIN`，不使用新evaluation继续确认。
- BRSR转折区频谱异质性：`NO_GO_TRANSITION_STATE_HEADROOM`。
- BRSR共享状态确认性复验：`NO_GO_ROUTE_PLANNING_NONCONVERGENCE`。
- Stage 2A.1 SafeGate：`NO-GO-CONFIG-STAGE2A1`。
- IID BlockPredicted/BlockOracle：`NO-GO-PROTOCOL-IID-BLOCK`。
- PASR-TDOA v1：动作评分与停止配置为`NO-GO-CONFIG`。
- PFRS固定掩码：冻结空间内为限定范围的`NO-GO-ROUTE`。
- R20/R20.1、FreqDAE v1-v4：不再作为当前主创新。

详细裁决以`研究门禁注册表.md`为准，正式实验事实以`实验结果记录.md`为准。
