# 当前任务

## 2026-07-16 当前任务：PASR-TDOA v1 正式结果审查与路线判定

| 字段 | 内容 |
|---|---|
| 当前任务 | 在不接入现有训练、urban8 和定位主链路的前提下，验证“剖面后验驱动的主动逐层精化 TDOA 编码”（PASR-TDOA）能否突破固定四频点 PFRS 的风险—精度边界。 |
| 当前状态 | **正式 `research` 已完成并回读，预注册6-bit主检验以及4/8-bit敏感性均为 `NO_GO`。当前任务进入“结果已分析、下一版理论方案待审批”；不得在本 locked 集合上继续扫阈值。** |
| 本轮已完成 | 根目录新增 `pasr_tdoa/`，含核心剖面后验、模态提取、主动动作、有限比特量化、bit 账本、静态强基线、消融、统计门禁、绘图、README 和测试。修复 locked 选对手的数据泄漏、静态位宽扫描不一致、边界后验伪早停和 bootstrap 聚类过粗问题；新增开发集冻结对手表及逐行协议门禁。默认 PyCharm 模式为 `research`；未修改 `main.py`、DAE、V5、PFRS、urban8 或既有评估 Python 文件。 |
| 核心协议 | 基础层由全部可辨识二频点掩码的确定性设计代理穷举得到，当前为 `[-1,0]`；每层根据当前多峰后验选择下一频点，最多发送4个复频点。I/Q 位宽、尺度、继续/停止标志和动作索引全部计入通信预算。所有静态方法扫描4--10 bit I/Q；同平均 bit 时分组合、同风险组合和高 SNR 参考只在 development 上选择并冻结，locked 只评估。6-bit I/Q 为预注册主检验，4/8-bit 仅作敏感性分析。 |
| 已实现基线与消融 | Exhaustive-Proxy-M2/M3/M4、PFRS、Zhai-Fisher、Power、Uniform、Fixed-Nested、Random-Request、Oracle-Next-Bin、PASR-Always4、PASR-OraclePower、Fisher-Only-Request、HardTop1-Request 和 Full16 参考。`Exhaustive-Proxy` 表示全掩码枚举后按确定性代理选出，不等于经验风险全局最优。 |
| smoke 证据 | `运行结果/PASR_20260716_192922_smoke`：无噪声最大 TDOA 误差=`1.863e-6 sample`；固定分层与同一频点并集的后验最大差=`2.082e-17`；5568条 trial rows、330条 action rows、16个方法、3个 development 冻结对手；逐行 bit 恒等式、方法键唯一、预算上限、种子隔离、同源 TDOA、manifest 文件集合和3张 SVG 均通过。`PASR-Always4` 和 `Fixed-Nested` 的平均/最大 bit 分别为 `64/64`、`57.8/58`，证明消融未被多收无必要反馈。smoke 只有1个 locked seed、每 SNR 20 trials，`overall_status=SMOKE_ONLY`，方法排名不得用于结论。 |
| 未完成事项 | 尚未建立“动作评分与真实终端风险下降”的校准关系；尚未验证同时约束全局错峰风险和局部精度的停止证书；尚未用全新 development/locked seeds 验证任何 PASR v2。 |
| 下一步优先级 | P0：冻结 v1 及当前 locked 集合，只做现有数据的反事实诊断。P1：在新 development 集合上比较“当前代理、一步真实后验风险下降、全局歧义项、局部Fisher项”，判断Oracle上界能否由可观测准则逼近。P2：仅在动作排序和双证书停止通过开发门禁后实现v2，并使用全新locked seeds；否则停止PASR主动选频路线。 |
| 完成标准 | v1任务已完成：正式运行、结果复算和NO-GO归因均有保存证据。若审批v2，其进入新locked测试前必须先在新development集合证明动作评分与终端风险下降正相关，并证明停止后相对“再发一个频点”的预期风险增幅不超过预设容差。 |
| 风险与注意事项 | 当前Oracle-Next-Bin的巨大优势只证明“存在更好动作”，不代表真实系统可获得Oracle信息。开发集PASR看似优于静态、locked优势消失，说明小开发集选择不稳定。新的动作准则不得使用当前locked结果调参；主动反馈、尺度和动作索引仍须完整计bit。当前结论仅适用于固定确定性源谱、复增益和等噪声的非对称双站最小模型。 |

## 已冻结路线

- PFRS 固定掩码：连续性能 `NO_GO` 且有限空间 `FIXED_MASK_SATURATED`，不再调整 eta、风险权重或同一 locked set。
- Shared64 双专家：未超过最强合法 Power64，冻结为基线/消融。
- 双站异构选频的确定性频谱伪补全：与交集零填充等价，路线结束。
- 波形重构型 FreqDAE v1-v4：反复出现假窄带、宽峰和压缩率无区分度，不再作为当前主创新。
