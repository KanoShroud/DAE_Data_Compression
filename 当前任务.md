# 当前任务

## 2026-07-10 当前任务：方案A已执行，等待新版 top-K/WLS 全量复核

| 字段 | 内容 |
|---|---|
| 当前任务 | 已执行用户批准的方案A：修复 top-K WLS 诊断与主评估链路的定位器协议漂移，并新建根目录 `研究总结.md`。当前代码已通过编译和 smoke，等待用户在 PyCharm 中按顺序重新运行诊断脚本。 |
| 是否允许修改代码 | 本轮代码修改已完成。下一步默认不继续改代码，先等待用户运行新版诊断并回传结果。 |
| 任务背景 | 阶段 2A 全量结果显示 Top3-BeamWLS 有小幅收益但未达 go/no-go；同时 Top1-WLS 与 Fig9 `V5B-Expert64` 存在 max diff=3.94m，需先消除定位器协议差异后再最终判断 top-K WLS 是否保留。 |
| 本轮已完成 | `diagnose_v5b_topk_wls.py` 改为复用 `evaluate._localize_from_tdoa_pairs(standard)`；`compressed_tdoa.py` 的 `pair_diagnostic()` 新增 `estimate_lag/estimate_hit1/estimate_hit2`，使新版 top-K CSV 可保存与正式 `estimate_pair()` 一致的亚采样 lag；WLS 脚本优先读取 `estimate_lag`，旧 CSV 缺失时回退 `top1_lag`。新增 `研究总结.md`，系统梳理复现基线、传统强基线、R20/FreqDAE/V4/V5/top-K 等创新尝试。 |
| 验证方式 | 全项目根目录 `.py` 编译；`diagnose_v5b_topk_wls.py --smoke --progress-every 5`。 |
| 验证结果 | 编译通过。Smoke 通过：`[OK] TDOA preflight passed: checked 240 CSV pairs`，`[OK] All TDOA assertions passed`，Top1-WLS vs V5B-Expert64 max per-SNR RMSE diff=`0.1208 m`，小于 1 m。 |
| 下一步运行顺序 | 1. 在 PyCharm 运行 `diagnose_v5b_topk.py`，覆盖生成新版 `v5b_pair_topk_diagnostics.csv`（包含 `estimate_lag`）。2. 再运行 `diagnose_v5b_topk_wls.py`，生成协议对齐后的 `v5b_topk_wls_*.csv`。3. 回传日志和结果后，判断 Top3-BeamWLS 是否达到 low-SNR 改善 >=3m 且 mid/high 不劣化超过 5%。 |
| 完成标准 | 若新版 Top1-WLS 与 Fig9 V5B-Expert64 每 SNR RMSE 差异仍 <1m，则诊断协议有效。若 Top3-BeamWLS 不达 go/no-go，则冻结 V5-B，top-K WLS 只作为补充诊断。 |
| 风险与注意事项 | 当前 smoke 使用旧 CSV，因此 `top1_lag_col=top1_lag`；只有重新运行 `diagnose_v5b_topk.py` 后，WLS 才会使用新增的 `estimate_lag` 字段。旧的非 smoke `v5b_topk_wls_*.csv` 不能代表协议对齐后的最终结果。 |
