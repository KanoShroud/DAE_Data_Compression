# 当前任务

## 2026-07-16 当前任务：PFRS 固定掩码极限已确认，下一主线待审批

| 字段 | 内容 |
|---|---|
| 当前任务 | 在不接入现有训练/评估主链路的前提下，验证“剖面有限时延风险选频”（Profiled Finite-delay Risk Selection, `PFRS`）能否形成区别于纯 Fisher/功率选频的局部精度与全局可分辨折中。 |
| 当前状态 | **阶段 1 正式连续 TDOA 验证和固定掩码理论证书均已完成。自动状态分别为 `NO_GO` 与 `FIXED_MASK_SATURATED`：在冻结的 `N=16、M=4、J/Jmax≥0.9` 空间内不存在连续旁峰低于当前 PFRS 的固定掩码，也不存在同时在 Fisher 信息和旁峰上支配 PFRS 的掩码。固定掩码调优路线正式停止。** |
| 本轮已完成 | 新增独立脚本 `pfrs_fixed_mask_feasibility.py`；以 `1/4096 sample` 连续网格穷举全部 1820 个四频点掩码，计算精确剖面相干函数、闭区间连续可辨识性、主瓣边界、最强旁峰、旁峰积分能量、Fisher 比和有限掩码 Pareto 前沿；生成正式结果 `PFRS_FIXED_MASK_20260716_161036_certificate`。未修改 `main.py` 或任何现有训练/评估方法。 |
| 核心结论 | 1820 个掩码中 1654 个在闭合 8-sample 先验区间严格连续可辨识；`eta=0.9` 下仅 8 个可行。当前 PFRS `[-8,-5,6,7]` 的 Fisher 比=`0.900967`、最强旁峰=`0.867747`、位置=`1.237017 sample`，是这 8 个掩码中旁峰最低者且位于 Pareto 前沿；更低旁峰掩码数=0，支配 PFRS 的掩码数=0。Zhai-Fisher 的旁峰=`0.974978@1.146574 sample`；Power 的旁峰仅 `0.222654`，但主瓣半宽约 `4.206 samples`、Fisher 比仅 `0.05645`。这证明固定四频点方案存在结构性“窄主瓣/高旁峰”与“宽主瓣/低旁峰”冲突。 |
| 当前证据 | 正式证书解析闭式剖面代价与原 `profiled_cost()` 最大差=`1.42e-14`；正式/烟雾网格的 PFRS 旁峰差仅=`1.46e-9`；1820 个掩码无重复、数值全有限，8 个输出文件、源哈希和 manifest 完全一致；两张 SVG 已渲染检查。编译、全项目 Ruff 和 `validate_project_integrity.py` 均通过。 |
| 未完成事项 | 固定掩码 PFRS 不再进入 C-PFRS、nuisance 或 urban8 扩展。下一主线应从固定每样本四频点选择，转向“低频粗定位 + 高频精化”的逐层/可变率有限比特统计编码；其预算、是否允许反馈、门禁和强基线尚需详细设计并由用户审批。 |
| 下一步优先级 | P0：冻结 PFRS v1、连续验证和固定掩码证书，不再调整 eta、风险权重或同一 locked set。P1：先输出逐层精化编码的完整数学模型、通信预算、无反馈/有反馈边界、比较基线和 go/no-go，不直接写代码。P2：用户批准后，仍以独立小脚本做最小可证伪验证；通过后才考虑接入项目主链路。 |
| 完成标准 | 高 SNR 下 PFRS 或同约束期望风险方案的经验方差应接近 Fisher 预测并显著优于 Power；中低 SNR 下相对 Zhai-Fisher 的错峰率或 RMSE 应有独立置信区间支持，且候选位于“局部精度—全局错峰”Pareto 前沿。若连续时延估计不能验证该折中，则停止 PFRS 主创新路线。 |
| 风险与注意事项 | 固定掩码证书只对当前有限候选空间、确定性源谱、复增益、等噪声和 8-sample 先验成立，不能外推成所有压缩编码的不可能性定理。降低 eta 可以换取更低旁峰，但这是改变局部精度要求，不是对当前方案的无代价修复；不得在看到 locked 结果后改 eta 并继续宣称同一预注册方法。当前仍是 N=16/M=4 理论门禁，不代表项目 CR16 主结果。 |

## 已冻结路线

- Shared64 双专家：未超过最强合法 Power64，冻结为基线/消融，除非独立互补性上界给出新证据。
- 双站异构选频的确定性频谱伪补全：已证明与交集零填充等价，无法恢复未观测复频谱，路线结束。
- 波形重构型 FreqDAE v1-v4：反复出现假窄带、宽峰和压缩率无区分度，不再作为当前主创新。
