# 当前任务

## 2026-07-09 当前任务：阶段 2A top-K WLS 已修复，待 PyCharm 全量重跑

| 字段 | 内容 |
|---|---|
| 当前任务 | `diagnose_v5b_topk_wls.py` 的 geometry 重建错配已修复；smoke 通过。下一步由用户在 PyCharm 直接全量运行 `diagnose_v5b_topk_wls.py`，生成有效 Stage 2A WLS 结果。 |
| 是否允许修改代码 | 本轮修复已完成；当前不继续修改代码，等待全量运行结果。 |
| 任务背景 | 阶段 1 诊断显示 low-SNR top3/top5 posterior 仍保留额外信息：low SNR top1=59.3%，top3=71.9%，top5=77.4%，top5±2=81.1%。需要验证这些 top-K 信息能否通过 BeamWLS 转化为 localization RMSE 改善。 |
| 范围内文件 | `diagnose_v5b_topk_wls.py`、`diagnose_v5b_topk.py`、`compressed_tdoa.py`、`运行结果/20260709_120019/tables/v5b_pair_topk_diagnostics.csv`；下一步输出 `v5b_topk_wls_*.csv`。 |
| 不修改的文件 | 所有 `.py` 文件；冻结基线 `运行结果/20260702_000925`。 |
| 已完成事项 | 修复 `diagnose_v5b_topk_wls.py`：优先按 CSV `snapshot_id` 重建 geometry；旧 CSV 无 `snapshot_id` 时退回到与 `diagnose_v5b_topk.py` 一致的 `snapshot_indices=None, seed=seed`；新增 WLS 前 TDOA preflight。`compressed_tdoa.py` 后续 top-K CSV 会写入 `snapshot_id`。全项目 py_compile 通过；smoke 通过：preflight checked=240，TDOA assertions=0，alignment max diff=0.1208m。 |
| 未完成事项 | 需要用户在 PyCharm 全量运行 `diagnose_v5b_topk_wls.py`，覆盖此前错配 geometry 的无效 WLS CSV。 |
| 下一步优先级 | PyCharm 全量运行 `diagnose_v5b_topk_wls.py` → 回读 `v5b_topk_wls_baselines.csv`、`v5b_topk_wls_summary_by_snr_bucket.csv`、`v5b_topk_wls_alignment.csv` 和终端日志 → 正式 go/no-go。 |
| 完成标准 | Top3-BeamWLS 相对 Top1-WLS：low-SNR mean RMSE 改善 ≥3m，且 mid/high 不劣化超过 5%。若达标，保留为 V5-B+ 候选；否则冻结 V5-B Pareto 结果。 |
| 风险与注意事项 | 当前非 smoke 的 `v5b_topk_wls_*.csv` 仍来自修复前错配 geometry，不能用于方法性能判断；全量重跑后会覆盖。运行时不要加 `--strict-align`，alignment 只作诊断。 |
