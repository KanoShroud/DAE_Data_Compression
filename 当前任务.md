# 当前任务

## 1. 当前任务状态

| 字段 | 内容 |
|---|---|
| 当前任务 | 执行 P1-P4：冻结 v3.1 基线、结果导出、论文图导出、fast/eval 模式 |
| 任务来源 | 用户批准 P1-P4，并要求注意复现分支与创新分支公平性、评估是否需要拆分臃肿 Python 文件 |
| 当前阶段 | 代码修改、结果导出、编译和小样本冒烟均已完成；未运行完整训练 |
| 是否允许修改代码 | 本轮批准范围内修改已完成；后续扩展需再次审批 |
| 是否允许修改 md 文档 | 是，按项目规则记录已审查的完整实验结果和当前任务状态 |
| 是否允许运行训练 | 否；本轮不运行完整训练 |
| 背景 | v3.1 已可作为近似 paper_repro 基线；后续需要固化表格/论文图，并提供不重复 563 min full audit 的快速复现实用模式 |
| 范围内文件 | `main.py`、`train.py`、`export_results.py`、`运行结果/20260702_000925/tables`、根目录上下文文档 |
| 范围外文件 | 未经审批的训练代码修改、Git 提交、完整训练、Wireless InSite 数据生成、论文全部对比基线实现 |
| 当前阻塞点 | 无代码阻塞；完整创新方法公平比较仍需后续单独设计 shared urban8 协议 |
| 下一步动作 | 用户可直接使用 `export_results.py` 的表格/论文图；若继续开发，建议先实现 shared urban8 innovation mode 或补 DFT/Hadamard/PCA 基线 |

## 2. 待办清单

- [x] 检查 Git 状态、当前分支和最近 commit。
- [x] 读取上下文文档与当前代码。
- [x] 读取并分析 `运行结果/20260701_150204`。
- [x] `signal_gen.py`：固定 urban snapshot 完整信道；缩短 `urban_base_delay`；新增 noisy RMS；新增 LOS-only 训练展开。
- [x] `evaluate.py`：固定 evaluation set；新增 all-pair WLS + multi-start；新增 Geometry oracle、trimmed/p90/p95/max/failure 统计；Fig3 保留 mean 主图并增加补充图。
- [x] `train.py`：新增 `early_stopping`、`restore_best`、`final_retrain`、`training_protocol` 开关；paper_repro 默认每折固定 200 epoch，恢复 best checkpoint，选择 best-fold 模型。
- [x] `main.py`：默认升级为 `paper_repro_v3_urban8_fair_eval`；训练协议为 `cv_fixed200_best_fold`；保留 R20.1 切换；记录 v3 配置。
- [x] `replot.py`：打印 v3 配置字段。
- [x] 运行两套 `py_compile`。
- [x] 运行 urban8/WLS/fixed-epoch best-fold 小样本冒烟。
- [x] 更新 `对话上下文.md`、`当前任务.md`、`实验结果记录.md`。
- [x] 用户提供并审查完整 v3 结果目录 `运行结果/20260701_185455`。
- [x] 用户审批并执行 A1-A3+B1-B2。
- [x] `train.py`：新增 fixed snapshot/UAV 计划下的 refreshable urban waveform dataset；CV 后按 fold best epoch 中位数做全训练集 final train。
- [x] `signal_gen.py`：新增 `build_urban_training_plan()` 与 `generate_urban_training_dataset_from_plan()`，明确训练样本单位为 single-UAV waveform。
- [x] `main.py`：默认协议改为 `cv_best_epoch_final_train`，启用 `RESAMPLE_TRAIN_EACH_EPOCH=True`、`RESAMPLE_INTERVAL=5`，并按 Fig1/Fig2/Fig3 顺序创建图。
- [x] `evaluate.py`：Fig3 收窄为约 15×4.8 英寸，默认关闭 mean RMSE 置信区间浅色填充。
- [x] `replot.py`：窗口标题改为本地 SVG 文件名，并打印训练重采样配置。
- [x] 运行两套 `py_compile`、小样本 final retrain 冒烟和 Fig3 绘图冒烟。
- [x] 用户提供并审查完整 v3.1 结果目录 `运行结果/20260702_000925`。
- [x] 用户审批并执行 P1-P4。
- [x] 新增 `export_results.py`，导出 v3.1 表格、论文主图和补充图。
- [x] 新增 `paper_repro_fast_final` 模式，跳过 CV，按冻结 epoch schedule 直接 final train。
- [x] 新增 `paper_repro_eval_only` 模式，加载冻结模型，仅重跑评估/出图。
- [x] 新增 smoke-test 环境变量覆盖：样本数、epoch、Monte Carlo 次数、Fig2 诊断数、batch、固定 snapshot 数和 `DAE_NO_SHOW`。
- [x] 运行编译、导出脚本、fast_final 小样本冒烟、eval_only 小样本冒烟。
- [ ] 用户确认下一步：补传统压缩基线，或迁移创新方法到 shared urban8 公平评估协议。

## 3. 用户审批记录

- 2026-06-30：用户要求进行一次“文档治理修改”，并明确要求全部采纳 GPT 网页端给出的修改方案后执行。
- 2026-07-01：用户批准先建立严格的 `paper_repro` 论文复现基线轨道，并要求修改后更新文档、审查代码进入待运行状态。
- 2026-07-01：用户提供 `paper_repro_v1` 完整运行结果 `20260701_122523`，要求分析代码与结果，判断无法复现论文趋势的原因。
- 2026-07-01：用户批准并要求执行 A+B+C 三阶段修改，同时考虑整数 TDOA 分辨率、K-fold 泄漏、路径损耗、GCC、归一化和 Raw baseline 公平性。
- 2026-07-01：用户提供 `paper_repro_v2_urban8` 完整运行结果 `20260701_150204`，要求全面审查并给出进一步修改方案。
- 2026-07-01：用户批准执行 v3 修改，并明确要求 TDOA 可正可负、Fig3 mean 保持主图、paper_repro 固定 200 epoch、定位按 WLS 思路实现。
- 2026-07-01/02：用户批准执行 A1-A3+B1-B2，并额外要求 Fig3 适配显示/论文插入、去除或解释第一子图浅色轮廓、修正图窗序号与本地 Fig 文件序号一致。

## 4. 完成标准

- 默认 `python main.py` 进入 `paper_repro_v3_1_epoch_final_train`。
- 编译和小样本冒烟通过。
- 不编造完整实验结论；完整训练运行前只记录代码验证结果。
