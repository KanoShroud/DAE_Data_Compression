# 当前任务

## 2026-07-20 当前任务：BRSR-TDOA 阶段 B 失败机理诊断与路线裁决

| 字段 | 内容 |
|---|---|
| 当前任务 | 在完整 `1024` 点 BPSK-RRC、连续 TDOA/复增益和线性分数时延下，验证后验风险驱动的逐层频谱采集是否优于同预算最优静态方案及 SNR-only 开放环路调度。 |
| 当前状态 | **阶段 B v1 维持 `DEVELOPMENT_NO_GO`；正式第一阶段失败机理诊断已经完成。当前处于“诊断完成、待审批 Stage B v2 模型重构边界”状态。不得进入 locked，也不得在原 evaluation 集上调 lambda、停止阈值、候选频点或 Rollout 参数。** |
| 本轮已完成 | 正式诊断目录=`运行结果/BRSR_STAGE_B_DIAG_20260720_180002_diagnostic`，只读冻结 Stage B development，状态=`DIAGNOSTIC_ONLY_NO_POLICY_CHANGE`。完成60条无噪声有限窗口残差、18个预注册样本加1个已知失败案例的复增益/动作上界诊断、4个样本的Rollout Q敏感性以及12频点候选池离线审计；正式算法、候选池和原结果未修改。 |
| 核心协议 | `Fs=40 MHz`，BPSK、`2 samples/symbol`、RRC 滚降 `0.2`、跨度 `8 symbols`；TDOA 连续位于 `[-8,8] samples`；单径 AWGN。A 站位于融合端且不计通信，B 站缓存候选 FFT。两个基础频点发送 4-bit I/Q 共16 bit；12频点动作码为5 bit；最多两次 acquire/refine，提前停止收费、硬上限后不收费，总预算约21--42 bit。 |
| development 证据 | `运行结果/BRSR_STAGE_B_20260718_173940_development`：5760 条结果、16 个方法，链路与预算校验通过。Rollout-T32 总体 RMSE=`3.3567 sample`，相对最强同预算 SNR-only-T32 的 `3.5683` 改善约5.93%，平均通信=`31.636 bit`；但15 dB RMSE反弹到`1.2872`，相对固定比较基线退化约68.5%，逐轨迹配对MSE改善95% CI=`[-1.03,3.92]`跨0，因此门禁失败。 |
| 正式诊断结论 | 有限窗口失配真实存在：线性模型NMSE中位数约`0.00605`，边界均值约为内部的`2.29`倍；但单复标量剖面仅带来约`4.7%`中位相对改善，不是唯一主因。将复增益从24个状态加密到576个状态，Greedy/Rollout RMSE仅由`3.2935/4.0199`变为`3.2891/4.0108 sample`，说明“网格太粗”不是主因；真值复增益上界约为`1.953 sample`，说明未知复增益的建模/消除方式仍是重要瓶颈。预注册样本中Rollout动作选择遗憾约`9.32 sample²`，高于Greedy的`4.03`；Q=16与Q=64在4个诊断样本上首动作和最终估计完全一致，暂不支持“Rollout采样数不足”作为首要原因。当前简化分数与消除增益后的频点贡献相关系数约`-0.524`，候选池设计准则需要重做。 |
| 未完成事项 | 当前诊断的复增益和动作上界只覆盖18个预注册样本，不能直接形成正式性能主张；真值复增益和事后最优动作均是不可实现上界。尚未实现解析/连续复增益剖面似然，也未完成同数量、同预算、同量化条件下的 Schur-Fisher、Zhai-style 和全局风险型候选池消融。 |
| 下一步优先级 | P0：先在独立开发脚本中建立“线性有限窗观测下的连续复增益解析剖面似然”，用固定频点和固定动作重算，禁止同时改候选池或策略。P1：若固定码RMSE和高SNR错峰明显改善，再在全新 development seeds 上公平比较当前、Schur-Fisher、Zhai-style、全局风险型候选池。P2：仅当新似然与候选池通过门禁后，先比较Greedy与静态/SNR-only，再决定是否保留Rollout；不得继续增加Q或在旧集合调阈值。 |
| 完成标准 | 同平均 bit 的 TDOA RMSE 相对最强 Best-Static/SNR-only 改善不少于5%，或同 RMSE 平均 bit 节省不少于10%；任一 SNR 退化不超过5%，轨迹聚类95%置信区间支持正收益，90%覆盖率、风险/MSE比和量化截幅同时通过。 |
| 风险与注意事项 | 事后动作oracle使用真实TDOA，只能证明现有动作空间包含更优选择，不能作为可部署算法或公平性能。`All-4bit`需要96 bit，不能与T32直接比较。候选池审计是未量化的投影信息代理，不是精确Schur-Fisher或最终性能。SNR-only仍存在小校准集从4920个配置选优的问题；任何 Stage B v2 都必须更换独立开发种子，并冻结后再生成新locked集合。 |

## 已冻结路线

- PASR-TDOA v1：通信协议成立，但动作评分与停止规则正式 `NO_GO`；保留为启发式消融和失败机理证据。
- PFRS 固定掩码：连续性能 `NO_GO` 且有限空间 `FIXED_MASK_SATURATED`，不再调整 $\eta$、风险权重或同一 locked 集合。
- Shared64 双专家：未超过最强合法 Power64，冻结为基线/消融。
- 双站异构选频的原网格确定性补全与时域补零：已完成含 Urban8 同参数 BPSK-RRC、`N=512`、四种 `M`、五种 SNR 和五种重叠率的150,000次扩展验证；120,000个正重叠配对 trial 中五种原网格确定性补全与真实交集逐点完全相同。固定8倍时域补零密集频谱的 MAE/RMSE 则由`2.6089/7.9421`变为`2.9004/8.3158 sample`，试验等权 MAE 改善95%区间为`[-0.3178,-0.2671]`；400条件审计为更差/更好=`316/84`，故路线结论限定为当前密集匹配器下无总体收益，不延伸为所有条件或所有补零算法均恶化。路线冻结结束，不影响当前 BRSR 主任务。
- 波形重构型 FreqDAE v1--v4：反复出现假窄带、宽峰和压缩率无区分度，不再作为当前主创新。
