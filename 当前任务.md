# 当前任务

## 2026-07-11 当前任务：P1 多池结果已分析，专家互补性诊断待审批

| 字段 | 内容 |
|---|---|
| 当前任务 | 在严格 64 个复频点并集预算下，判断 Shared64 双专家及其融合是否具有可复核的精度、复杂度和泛化优势。 |
| 当前状态 | **P1 `dev_native_multi_pool` 已完成并分析，下一阶段待审批。** Shared64 相对 GeoShared52 的收益跨池成立，但相对合法单专家 Power64 的 TDOA 显著更差，因此暂不进入 calibration、Common Top-K 或 locked。 |
| 本轮已完成 | 默认公平/复杂度结果源切到 `20260711_154042`；新增 training/calibration/development/locked 四组互斥池；新增 `dev_native_multi_pool`；Shared52、Shared64、legacy Power64/GeoHybrid64 和 DAE/DFT/Cao/Zhai 进入 native-only 方法集合；新增 pool-wise 与 combined cluster CI；修正 requested/effective bin-source 元数据。 |
| 关键门禁 | 正式同 CR 比较按实际传输特征并集计费；oracle clean 归一化被阻止；缓存必须匹配 snapshot 池、种子、归一化、SNR、定位协议、样本数和 baseline 代码指纹；未知 GCC/定位器、重复方法名、非法频点和非有限输出直接报错。 |
| 验证结果 | `ruff check *.py --select F,E9`、本轮六个修改文件编译、`validate_project_integrity.py` 均通过；完整性脚本确认 Shared64 的 52/52/40/64 预算、finite posterior、fixed-set、TDOA/GCC 符号、WLS 和缓存隔离。正式运行耗时约 97.9 min。 |
| 未完成事项 | 现有 sample-level rows 只能比较每个 trial 的最终平均 TDOA，不能判断 PowerShared52/GeoShared52 在同一 pair 上是否互补；legacy Power64 来自旧 checkpoint，若成为最终候选需按当前协议重训确认。 |
| 下一步优先级 | P0：审批 Shared64 pair-level expert complementarity eval-only 诊断；若 oracle 共享专家选择不能接近/超过 Power64，则冻结双专家融合；只有 oracle 上界充分时，才使用独立 calibration pools 做一次预注册校准。 |
| 完成标准 | 新方法频点并集严格为 64；所有入口通过完整性门禁；训练池、开发池和锁定池隔离；OriginalTop1 与共同后端分轨；独立池上的精度、失败率、复杂度和 cluster CI 均有可复核证据。 |
| 风险与注意事项 | 当前 `20260711_154042` 是训练 snapshot pool 上的同分布开发证据，不是独立泛化证据；不得直接据此调参后再称为测试结果。通信量仍按未量化实数标量计数；urban8 每链路 SNR 与 observation-level noisy RMS 仍需敏感性实验。 |

## 2026-07-14 并行独立验证路线：双站异构选频与频谱伪补全

> 本节与 V5-A.1、Shared64 完全隔离，不改变上方当前主线任务及其 go/no-go 状态。

| 字段 | 内容 |
|---|---|
| 验证问题 | A、B 两站分别选择不同频点后，能否通过“稀疏频谱 IFFT → 整数倍插值 → 抗镜像滤波 → FFT”补出缺失频点，并改善 TDOA。 |
| 当前状态 | **`synthetic_full` 已完成并分析，插值伪补全假设未成立，本独立路线按停止规则结束。** 完整运行包含 60000 个基础试验、9 种方法和 540000 行结果；不再推进插值器调参。 |
| 隔离边界 | 新代码只位于 `standalone_frequency_alignment/`，独立入口为 `run_interpolation_alignment.py`；未修改 `main.py`、现有训练/评估链、V5-A.1 或 Shared64。结果单独保存到 `运行结果/频谱插值对齐/`。 |
| Zhai 口径 | A、B 分别用本地周期图运行学位论文的严格 0-1 逐点交换选频；另保留“B 本地选频、A 全频匹配 B”的原论文非对称对照。当前项目中的候选裁剪/贪心代理不用于本路线的严格复现。 |
| 硬门禁 | 理想周期插值映射回原始 DFT 网格后必须与零填充一致；线性算子秩不得超过实际观测数；少于两个共同有效频点时 TDOA 必须判为无效。 |
| 当前事实 | 正式运行中，五种伪补全方法在 48000 个可估计配对试验上的 TDOA 与 `Intersection-Continuous` 逐个完全相同，MAE 配对增益及 95% CI 均为 `0 [0, 0]`；缺失频点 NMSE 为 `0 dB`、相位不可定义、生成能量仅为机器误差，说明没有恢复任何缺失复频谱。 |
| 下一步 | 不再运行 `project_full`，也不再调整插值倍数或滤波器。若继续研究双站频点失配，应转向“联合/协调选频、公共核心频点或显式信号模型预测”，并作为新的独立路线审批；不得把确定性插值称为频谱恢复。 |
