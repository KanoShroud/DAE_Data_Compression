# 当前任务

## 2026-07-11 当前任务：全项目科学完整性审查完成，严格 Budget64 待实现

| 字段 | 内容 |
|---|---|
| 当前任务 | 全面审查数据生成、训练/验证、特征预算、缓存、TDOA、WLS、公平评估和结果保存链路，并把基础性约束固化为自动门禁。 |
| 当前状态 | **修改完成并通过轻量验证，待用户审阅。** 当前历史 V5-B 仍是 88 个复频点的 CR11.64 诊断；默认 `main.py` 会在构造训练集前阻止其作为 CR16 正式运行。 |
| 本轮已完成 | 新增 `experiment_integrity.py` 与 `validate_project_integrity.py`；修复 V5-B 并集预算、缓存协议、PCA 锁定集泄漏、dev mid fixed-set 错配、训练/验证目标不对称、urban pair 固定低编号偏置和 V5-A.1 分桶统计；评估入口新增数据/输出/方法名/定位器校验。 |
| 关键门禁 | 正式同 CR 比较按实际传输特征并集计费；oracle clean 归一化被阻止；缓存必须匹配 snapshot 池、种子、归一化、SNR、定位协议、样本数和 baseline 代码指纹；未知 GCC/定位器、重复方法名、非法频点和非有限输出直接报错。 |
| 验证结果 | 根目录 21 个 Python 文件通过 `ruff check *.py`、`compileall` 和模块导入；完整性脚本通过 fixed-set、正负 TDOA、GCC/direct 符号、geometry-oracle WLS、CR16 预算、DAE latent、缓存隔离等检查。未运行完整训练或正式评估。 |
| 未完成事项 | 严格共享 Budget64 V5-B 尚未设计训练；历史缓存/公平 CSV 缺少新协议字段，只能按 legacy 诊断读取，不能进入新正式结论。 |
| 下一步优先级 | P0：审批并实现共享 64 复频点双 expert；P1：在新协议下重跑 dev_fair，再补 2/4/6 dB；P2：冻结方法和 solver 后运行独立 snapshot-pool locked 测试；P3：报告实数标量数、参数量、端到端时延和失败率。 |
| 完成标准 | 新方法频点并集严格为 64；所有入口通过完整性门禁；训练池、开发池和锁定池隔离；OriginalTop1 与共同后端分轨；独立池上的精度、失败率、复杂度和 cluster CI 均有可复核证据。 |
| 风险与注意事项 | 当前资源口径是未量化的实数标量数，不等于真实比特率；urban8 采用合成场景且每链路按自身信号功率设 SNR；`per_observation_noisy_rms` 需要跨 UAV 观测级尺度。上述属于协议假设，后续论文必须声明并做量化/本地归一化敏感性实验。 |
