# 当前任务

## 2026-07-20 当前任务：BRSR-TDOA 阶段 B 失败机理诊断与路线裁决

| 字段 | 内容 |
|---|---|
| 当前任务 | 在完整 `1024` 点 BPSK-RRC、连续 TDOA/复增益和线性分数时延下，验证后验风险驱动的逐层频谱采集是否优于同预算最优静态方案及 SNR-only 开放环路调度。 |
| 当前状态 | **阶段 B development 为 `DEVELOPMENT_NO_GO`；第一阶段失败机理诊断入口已实现并通过 smoke，当前处于“修改完成、待用户在 PyCharm 运行正式 diagnostic”状态。不得进入 locked，也不得在同一 evaluation 集上调 lambda、停止阈值、候选频点或 Rollout 参数。** |
| 本轮已完成 | 新增独立 `diagnose_stage_b_failures.py` 和 `test_stage_b_failures.py`，按冻结manifest/哈希/种子重建：有限窗相移残差、粗/细/真值复增益固定码敏感性、同一两动作空间事后上界、Rollout Q敏感性和候选池投影信息/多尺度模糊审计。最终 smoke=`运行结果/BRSR_STAGE_B_DIAG_20260720_154417_smoke`；编译、Ruff、18项BRSR测试、项目完整性、JSON/CSV/SVG和冻结源码哈希全部通过。正式Stage B算法和原结果未修改。 |
| 核心协议 | `Fs=40 MHz`，BPSK、`2 samples/symbol`、RRC 滚降 `0.2`、跨度 `8 symbols`；TDOA 连续位于 `[-8,8] samples`；单径 AWGN。A 站位于融合端且不计通信，B 站缓存候选 FFT。两个基础频点发送 4-bit I/Q 共16 bit；12频点动作码为5 bit；最多两次 acquire/refine，提前停止收费、硬上限后不收费，总预算约21--42 bit。 |
| development 证据 | `运行结果/BRSR_STAGE_B_20260718_173940_development`：5760 条结果、16 个方法，链路与预算校验通过。Rollout-T32 总体 RMSE=`3.3567 sample`，相对最强同预算 SNR-only-T32 的 `3.5683` 改善约5.93%，平均通信=`31.636 bit`；但15 dB RMSE反弹到`1.2872`，相对固定比较基线退化约68.5%，逐轨迹配对MSE改善95% CI=`[-1.03,3.92]`跨0，因此门禁失败。 |
| 未完成事项 | 用户尚未运行正式 `diagnostic`。需回读全60条无噪声轨迹、预注册18个样本及已知失败案例的诊断结果，量化四类根因；细网格敏感性仍不是连续解析 nuisance 处理，候选池量化后判别力也尚未比较。当前4→8 bit精化几乎未使用，需待正式诊断后决定是否退出主叙事。 |
| 下一步优先级 | P0：用户在PyCharm运行 `brsr_tdoa/diagnose_stage_b_failures.py` 默认 `diagnostic`，不改参数。P1：若高SNR错误主要来自可修正的似然/增益失配，重新定义模型后用全新development seeds、优先验证Greedy；同时再批准四类候选池的严格公平消融。若事后动作上界仍低或收益依赖少数异常样本，则冻结BRSR主动路线。P2：不得在当前集合把Greedy事后升级为主方法。 |
| 完成标准 | 同平均 bit 的 TDOA RMSE 相对最强 Best-Static/SNR-only 改善不少于5%，或同 RMSE 平均 bit 节省不少于10%；任一 SNR 退化不超过5%，轨迹聚类95%置信区间支持正收益，90%覆盖率、风险/MSE比和量化截幅同时通过。 |
| 风险与注意事项 | 总体指标掩盖了高SNR模型失配：Rollout-T32 在15 dB的后验风险/经验MSE仅约0.226，90%覆盖率约0.683，表现为对错误峰过度自信。Rollout总体和各预算均未优于Greedy，且Q敏感性样本在5/10 dB出现动作变化。SNR-only仅用每SNR 12个校准样本从4920个配置中选优，存在明显选择过拟合，当前平均优势不能作为可靠论文主张。 |

## 已冻结路线

- PASR-TDOA v1：通信协议成立，但动作评分与停止规则正式 `NO_GO`；保留为启发式消融和失败机理证据。
- PFRS 固定掩码：连续性能 `NO_GO` 且有限空间 `FIXED_MASK_SATURATED`，不再调整 $\eta$、风险权重或同一 locked 集合。
- Shared64 双专家：未超过最强合法 Power64，冻结为基线/消融。
- 双站异构选频的确定性频谱伪补全：与交集零填充等价，路线结束。
- 波形重构型 FreqDAE v1--v4：反复出现假窄带、宽峰和压缩率无区分度，不再作为当前主创新。
