# 当前任务

## 2026-07-22 当前任务：BRSR-TDOA 阶段 B v2 三步门禁已裁决

| 字段 | 内容 |
|---|---|
| 当前任务 | 在完整 `1024` 点 BPSK-RRC、连续 TDOA/复增益和线性分数时延下，验证后验风险驱动的逐层频谱采集是否优于同预算最优静态方案及 SNR-only 开放环路调度。 |
| 当前状态 | **BRSR三步方案已在步骤1得到明确 `STEP1_NO_GO`，当前任务已完成并停止。步骤2候选池消融和步骤3主动策略重验按预注册门禁不执行；不得进入 locked，也不得绕过门禁继续调候选频点、lambda、停止阈值或Rollout参数。** |
| 本轮已完成 | 新增独立 `diagnose_stage_b_v2_profile.py` 和测试，不修改 Stage B v1 核心。正式目录=`运行结果/BRSR_STAGE_B_V2_STEP1_20260722_192558_research`：使用3个全新种子、每种子20条跨6个SNR复用轨迹，共360个观测；固定BestStatic/SNR-only T32频点和32-bit预算，比较当前24状态边缘化、理想相移剖面、线性A端模板量化剖面、无量化剖面和真值复增益辅助诊断。 |
| 核心协议 | `Fs=40 MHz`，BPSK、`2 samples/symbol`、RRC 滚降 `0.2`、跨度 `8 symbols`；TDOA 连续位于 `[-8,8] samples`；单径 AWGN。A 站位于融合端且不计通信，B 站缓存候选 FFT。两个基础频点发送 4-bit I/Q 共16 bit；12频点动作码为5 bit；最多两次 acquire/refine，提前停止收费、硬上限后不收费，总预算约21--42 bit。 |
| development 证据 | `运行结果/BRSR_STAGE_B_20260718_173940_development`：5760 条结果、16 个方法，链路与预算校验通过。Rollout-T32 总体 RMSE=`3.3567 sample`，相对最强同预算 SNR-only-T32 的 `3.5683` 改善约5.93%，平均通信=`31.636 bit`；但15 dB RMSE反弹到`1.2872`，相对固定比较基线退化约68.5%，逐轨迹配对MSE改善95% CI=`[-1.03,3.92]`跨0，因此门禁失败。 |
| 步骤1结论 | 主方法 `LinearA-QuantProfile` 在冻结BestStatic-T32下的RMSE=`3.5587 sample`，差于当前`DiscreteMarginal24`的`3.3999 sample`；候选减基准的配对MSE差=`+1.1051 sample²`，轨迹聚类95% CI=`[0.1470,2.0677]`，45%轨迹受益。高SNR严重错峰率由`3.33%`升至`4.17%`，最坏单SNR RMSE退化`10.32%`。覆盖率`0.8389`和风险/MSE=`0.7399`虽合格，但五项门禁中三项失败。理想相移剖面与当前边缘化基本等价，说明“边缘化改剖面”本身无有效收益；线性A端模板在10 dB改善但低SNR显著恶化。真值复增益和无量化结果仅是真值辅助/信息损失诊断，在失配模型下不保证构成性能上界。 |
| 未完成事项 | 三步方案范围内无未完成实验：步骤2和步骤3是条件任务，因步骤1未通过而按规则跳过。尚未验证四类候选池，不得把本轮结论外推为“所有候选池设计都无效”；但当前BRSR v2连续复增益剖面修复路线已经否决。 |
| 下一步优先级 | 当前任务结束，等待用户决定是否在三步方案之外提出新的研究问题。默认冻结 BRSR Stage B v1/v2，不进入locked、不继续调参，也不将步骤1局部10 dB改善包装成主结论。 |
| 完成标准 | 已满足：步骤1使用独立种子、固定频点/预算/策略完成正式门禁；结果为`STEP1_NO_GO`后停止步骤2/3；代码、21项BRSR测试、项目完整性和结果文件审计全部通过。 |
| 风险与注意事项 | 事后动作oracle使用真实TDOA，只能证明现有动作空间包含更优选择，不能作为可部署算法或公平性能。`All-4bit`需要96 bit，不能与T32直接比较。候选池审计是未量化的投影信息代理，不是精确Schur-Fisher或最终性能。SNR-only仍存在小校准集从4920个配置选优的问题；任何 Stage B v2 都必须更换独立开发种子，并冻结后再生成新locked集合。 |

## 已冻结路线

- PASR-TDOA v1：通信协议成立，但动作评分与停止规则正式 `NO_GO`；保留为启发式消融和失败机理证据。
- PFRS 固定掩码：连续性能 `NO_GO` 且有限空间 `FIXED_MASK_SATURATED`，不再调整 $\eta$、风险权重或同一 locked 集合。
- Shared64 双专家：未超过最强合法 Power64，冻结为基线/消融。
- 双站异构选频的原网格确定性补全与时域补零：已完成扩展验证并冻结，详细证据保留在实验记录和专项报告中；不再作为当前任务，也不影响 BRSR 路线判断。
- 波形重构型 FreqDAE v1--v4：反复出现假窄带、宽峰和压缩率无区分度，不再作为当前主创新。
