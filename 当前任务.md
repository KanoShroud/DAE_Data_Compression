# 当前任务

## 2026-07-05 本轮状态更新：Fig8 主/补充轨道增强已执行，代码待 PyCharm 运行

| 字段 | 内容 |
|---|---|
| 当前任务 | 执行用户批准的 A1-A3 + C1-C2 + B1-B2：精简 Fig8 正式强基线主图、补充诊断图；增强 Cao2020 与 Zhai phase 候选实现；新增 pandas 结果分析脚本；清理一次性脚本状态。 |
| 是否允许修改代码 | 是。用户明确“批准执行A1-A3 + C1-C2 + B1-B2”。 |
| 已完成代码修改 | `task_baselines.py` 新增 `Cao2020SegmentedFCEstimator`，并将 `ZhaiPhaseSuperpositionEstimator` 改为 `sqrt_power` 加权相位叠加；`main.py` 新增 `USER_RUN_STRONG_DIAGNOSTIC_SUPPLEMENT`，Fig8 主图只保留正式强基线，新增 `fig8_supp_results` 保存 Cao2020/Zhai phase 诊断候选；`evaluate.py` 更新图例；`export_results.py` 与 `replot.py` 支持 Fig8 supplement；新增 `analyze_results.py`。 |
| 当前默认运行 | `USER_EXPERIMENT_MODE="paper_repro_eval_only"`；PyCharm 直接运行 `main.py` 不训练，加载 `运行结果/20260702_000925` 的冻结 v3.1 模型，重新生成 Fig1-Fig8 和 tables。 |
| 验证方式 | 两套 `py_compile`；已知整数时移 sanity；小样本 urban8 Fig8 主/补充图 smoke；`analyze_results.py` 读取旧结果目录；`git diff --check`。 |
| 验证结果 | 编译通过；四个 direct estimator 对 `np.roll(x, 7)` 均返回 `-7.0`；小样本 smoke 输出 `SMOKE_OK`；`analyze_results.py` 成功为 `运行结果/20260704_195946` 写出 `tables/analysis_*`；`git diff --check` 仅有 Windows 行尾提示。 |
| 完成标准是否满足 | 代码级完成标准已满足；正式性能结论未产生，需要用户运行完整 `main.py` 后回读新结果。 |
| 未完成事项 | 尚未正式运行新 `main.py`，因此不知道 segmented Cao2020 与 weighted Zhai phase 在完整 Fig8/Fig8 supplement 中是否改善。 |
| 下一步优先级 | 1. 用户在 PyCharm 运行 `main.py`。2. 回读新目录的 `run.log`、`plot_data.pkl`、`Fig8*.svg`、`tables/fig8*.csv` 和 `tables/analysis_*.csv`。3. 若 Cao/Zhai 候选仍弱，进入创新方案设计验证，不再继续把 proxy 强行放入主图。 |

## 2026-07-05 本轮状态更新：`20260704_195946` Fig8 结果审查，方案待审批

| 字段 | 内容 |
|---|---|
| 当前任务 | 审查最新结果 `运行结果/20260704_195946`，分析 Fig8 中 `Zhai-Phase-Superposition` 最差、`Cao2020-HighFC` 次差且高 SNR 接近 PCA 的原因；比较当前复现方法与论文来源方法的差异；给出下一步方案供用户审批。 |
| 是否允许修改代码 | 否。本轮用户要求全面分析并给出方案供审批，未要求执行代码修改。 |
| 已完成结果读取 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv`，并检查 Fig8 SVG 图例文本。 |
| 关键结果 | Fig8 全 SNR mean RMSE 均值：Raw≈15.720 m，DAE-CR16≈21.380 m，Cao2017-DFT-AML≈29.387 m，DFT≈32.162 m，PCA≈34.834 m，Zhai-CRLB-Decimation≈37.209 m，Hadamard≈38.783 m，DFT-Fisher-Direct≈40.662 m，Cao2020-HighFC≈43.099 m，Zhai-Phase-Superposition≈51.584 m。 |
| 关键诊断 | Cao2020 重做后相对旧结果有明显改善：全 SNR mean RMSE 约 `92.340 -> 43.099 m`，高 SNR 约 `76.841 -> 16.244 m`；但其 direct sidelobe ratio 仍约 0.96。Zhai phase-superposition 高 SNR direct within-1 约 0.751，明显低于 Zhai-CRLB 约 0.998，说明相位叠加实现/权重不稳。 |
| 当前结论 | 本轮不建议继续把 `Zhai-Phase-Superposition` 作为正式强基线；Cao2020 当前是可运行但仍偏弱的 CRB/high-FC proxy；下一步应先清理强基线命名和图表口径，再决定是否实现更严格的 Cao incoherent processing 或进入创新方案。 |
| 下一步优先级 | 等待用户审批：建议 A. 删除或归档一次性 `update_dft_fisher_direct.py`；B. 将 Fig8 正式强基线精简为 Raw/DAE/DFT/Hadamard/PCA/Cao2017/Zhai-CRLB/DFT-Fisher，Cao2020 与 Zhai phase 放入消融或标注 proxy；C. 若继续修 Cao2020，实现分段 incoherent averaging 与 SNR/相干度加权。 |

## 2026-07-04 本轮状态更新：Cao2020-HighFC 重做完成，待 PyCharm 运行

| 字段 | 内容 |
|---|---|
| 当前任务 | 按用户要求重做 Cao2020，修正 Fig8 中 `Cao2020-HighFC` 旧的最高频硬选 proxy，使其更接近 Cao 2020 的 CRB/partial Fourier coefficient 高时延敏感性动机，然后让用户直接运行 `main.py`。 |
| 是否允许修改代码 | 是。用户明确要求“重做 Cao2020，然后让我执行 main”。 |
| 已完成代码修改 | `baselines.py` 新增 `select_cao2020_crb_dft_bins()` 与 `_cao2020_crb_fc_bins()`：扫描单侧连续高频 DFT 窗，综合 Fisher/CRB 延迟信息、带内功率、高频偏好和物理 lag 窗旁瓣惩罚选频；新增 `DFT-Cao2020-CRB` strong variant 和元信息。`main.py` 中 Fig8 的 `Cao2020-HighFC` 优先使用该新选频结果；`evaluate.py` 图例改为 `Cao2020 CRB high-FC`。 |
| 理论/逻辑检查 | 新实现保留 Cao 2020 中“高频 Fourier coefficient 对时延更敏感”的动机，但避免旧实现把正负 Nyquist 边缘硬拼成两端梳状窗口。当前仍是可运行的 CRB/high-FC 代理，不是完整 Cao 2020 incoherent segmented estimator；正式论文表述应避免说成完全复现原文全部工程细节。 |
| 验证方式 | 两套 `py_compile`；`git diff --check`；小样本 urban8 clean direct TDOA smoke；代码路径检查确认 `Cao2020-HighFC` 默认使用 `DFT-Cao2020-CRB.selected_bins`。 |
| 验证结果 | 编译通过；`git diff --check` 无空白错误，仅有 Windows 行尾提示；小样本中旧 two-edge high-frequency 约 mean abs 4.08 samples、within1 约 0.23，新 Cao2020 约 mean abs 1.07 samples、within1 约 0.80，方向明显改善但不是正式结果。 |
| 当前状态 | 代码已进入待 PyCharm 直接运行 `main.py` 状态。默认仍由 `main.py` 顶部 `USER_*` 普通变量控制，不需要命令行环境变量。 |
| 未完成事项 | 尚未完整运行新的 `main.py`，因此没有正式 Fig8 RMSE；Cao2020 的完整 incoherent processing 分段平均版本仍未实现。 |
| 下一步优先级 | 用户运行 `main.py` 后，回读新结果目录的 `run.log`、`plot_data.pkl`、`Fig8_Strong_TaskAware_Baselines_CR16*.svg` 与 `tables/fig8_*`，重点比较新 `Cao2020-HighFC` 的高 SNR direct TDOA 诊断是否较 `20260704_163145` 明显下降。 |

## 2026-07-04 本轮状态更新：`20260704_163145` 审查与 Fig8/zoom 修正完成

| 字段 | 内容 |
|---|---|
| 当前任务 | 审查最新结果 `运行结果/20260704_163145`，解释 Cao2020-HighFC 高 SNR 异常和 Zhai-CRLB 部分 SNR 优于 Raw 的原因；补充正确 phase-superposition；把 Fig6/Fig7/Fig8 的高低 SNR zoom 合并为单张双子图，减少结果目录文件数。 |
| 是否允许修改代码 | 是。用户提出“正确的 phase-superposition 要补充”和“zoom 图要合并”，属于明确执行要求。 |
| 已完成结果审查 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv`。结论：Cao2020-HighFC 当前是高频硬选 proxy，direct TDOA 本身在高 SNR 已失败；Zhai-CRLB 高 SNR direct TDOA 很准，部分 SNR 优于 Raw 属于可能合理的任务导向选频效果，但低 SNR 仍差。 |
| 已完成代码修改 | `task_baselines.py` 改正 `ZhaiPhaseSuperpositionEstimator`；`main.py` 默认打开 `USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=True` 并用 Zhai-CRLB bins 构造 phase-superposition；`evaluate.py` 新增 `plot_method_zoom_pair()`；`main.py`、`export_results.py`、`replot.py` 改为保存合并版 `*_SNR_Zooms.svg`。 |
| 已完成历史结果整理 | 已用 `replot.py` 离线重绘 `运行结果/20260703_191104` 和 `运行结果/20260704_163145`；旧 Low/High 单独 zoom SVG 已在这两个目录中删除，保留合并版 zoom SVG。 |
| 验证方式 | `python -m py_compile ...`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；已知整数时移 sanity；PyTorch Conda 运行 `replot.py` 重绘两个历史目录。 |
| 验证结果 | 编译通过；`np.roll(x, 7)` 时 Direct DFT 和新的 phase-superposition 均返回 `-7.0`；合并 zoom 图已生成，旧单图已清理。 |
| 未完成事项 | 尚未完整运行新的 `main.py`，因此 `Zhai-Phase-Superposition` 没有正式 RMSE 指标；Cao2020-HighFC 仍只是待校准 proxy，尚未按 Cao 2020 原文完整重写。 |
| 下一步优先级 | 1. 用户用 PyCharm 直接运行 `main.py` 生成含 phase-superposition 的新 Fig8；2. 回读新目录确认 Fig8 方法排序和 zoom 可读性；3. 按 Cao 2020 原文重做 HighFC/incoherent/CRB 路线，或将当前 Cao2020-HighFC 明确降级为 diagnostic proxy。 |


## 2026-07-04 本轮状态更新：Fig8 强基线轨道代码已完成

| 字段 | 内容 |
|---|---|
| 当前任务 | 按用户批准补充新的强基线方法，并判断是否接入原 Fig7。最终采用独立 Fig8 强基线轨道，同时保留 Fig7 作为任务感知概览。 |
| 是否允许修改代码 | 是。用户明确同意判断并要求执行代码修改、审查代码、更新相关文档。 |
| 已完成代码修改 | `task_baselines.py` 新增 Cao-style DFT-AML direct estimator、Zhai phase-superposition 诊断类和 direct estimator 公共诊断 helper；`baselines.py` 新增 Zhai-style CRLB/FIM greedy 选频器与 `DFT-Zhai-CRLB` strong variant；`main.py` 新增 `USER_RUN_STRONG_BASELINES`、`USER_STRONG_INCLUDE_PHASE_SUPERPOSITION` 和 `fig8_results` 生成保存流程；`evaluate.py` 支持 `plot_kind="strong"` 与新方法样式；`export_results.py`/`replot.py` 支持 Fig8 导出和重绘。 |
| 当前默认 Fig8 方法 | `Raw`、`DAE-CR16`、`DFT`、`DFT-Fisher-Direct`、`Cao2017-DFT-AML`、`Cao2020-HighFC`、`Zhai-CRLB-Decimation`、`Hadamard`、`PCA`。`Zhai-Phase-Superposition` 类已实现但默认 `USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=False`，待严格复现 Zhai 第三章后再打开。 |
| 验证方式 | 两套 `py_compile`；direct estimator 已知延迟 sanity；小样本 urban8 method-comparison + Fig8 plotting smoke；`git diff --check`。 |
| 验证结果 | 编译全部通过；Direct/Cao 对已知整数延迟返回正确 lag；简化 phase-superposition 未通过该 sanity，因此默认不进 Fig8；小样本 urban8 smoke 输出 `FIG8_SMOKE_OK`；`git diff --check` 仅提示 Windows 行尾转换。 |
| 当前状态 | 代码已进入待 PyCharm 运行 `main.py` 状态。正式运行会在新时间戳目录中输出 Fig8 主图、Fig8 low/high SNR zoom、`plot_data.pkl` 中的 `fig8_results`，以及 `tables/fig8_*` 表格。 |
| 未完成事项 | 尚未运行完整 `paper_repro_eval_only`，因此没有新的正式 Fig8 性能指标；Cao/Zhai baseline 仍需基于正式结果和原文细节继续校准。 |
| 下一步优先级 | 1. 用户用 PyCharm 直接运行 `main.py`。2. 回读新结果目录的 `run.log`、`plot_data.pkl`、Fig8 SVG 和 `tables/fig8_*`。3. 根据 Fig8 判断强基线是否足够强，再开启创新方案在同一 urban8/WLS 协议下的验证。 |

## 2026-07-03 本轮状态更新：Balanced DFT-Fisher 增量更新已完成

| 字段 | 内容 |
|---|---|
| 当前任务 | 按用户批准修复 `DFT-Fisher-Direct`：将 naive `power*f^2` 硬选高频改为 balanced Fisher/FIM 选频，并单独重算该方法后合并更新 `运行结果/20260703_191104`。 |
| 是否允许修改代码 | 是。用户明确批准“按照建议执行”，并要求审查代码、更新文档、进入待运行状态；同时询问能否单独跑修复后的 DFT-Fisher 避免完整重跑。 |
| 已完成代码修改 | `baselines.py`：新增 `_balanced_fisher_bins()` 与 `select_balanced_fisher_dft_bins()`，在 Fisher/FIM 频点选择中加入物理 lag 窗旁瓣惩罚，并让 `build_traditional_baselines()` 接收 `dft_lag_limit_samples`。`main.py`：构建传统 baseline 时传入当前物理 TDOA lag gate。`evaluate.py`：图例从 `Naive Fisher direct` 改为 `Balanced Fisher direct`。`export_results.py`：summary 说明 DFT-Fisher-Direct 的 balanced/FIM 选频含义。`update_dft_fisher_direct.py`：新增单方法增量更新脚本，只重算 `DFT-Fisher-Direct` 并更新旧结果目录的 pkl、表格和 SVG。 |
| 已完成增量运行 | 已运行 `D:\Software\anaconda3\envs\PyTorch\python.exe update_dft_fisher_direct.py "F:\PythonWorkspace\DAE Data Compression\运行结果\20260703_191104"`，未重训 DAE，未重跑所有 baseline，仅重算 `DFT-Fisher-Direct`、重导出 tables 并重绘 Fig1/Fig2/Fig3/Fig6/Fig7 与 zoom SVG。 |
| 验证结果 | 两套 `py_compile` 通过；小样本 alias smoke 中旧 Fisher 物理 lag 内最大旁瓣约 `0.907`，balanced 版本约 `0.483`；正式目录增量更新后 `DFT-Fisher-Direct` 平均 mean RMSE 从 `53.8813 m` 降至 `40.6645 m`，完整 50 snapshot 选频诊断为 inside sidelobe `0.456`、outside sidelobe `0.274`；SVG 检查确认 Fig7/zoom 显示 `Balanced Fisher direct`，无 `Naive Fisher direct` 和重复 `DFT-train-power-Direct`。 |
| 当前状态 | 代码已进入待 PyCharm 运行 `main.py` 状态；`运行结果/20260703_191104` 的 `plot_data.pkl`、tables 和 SVG 已被增量更新。 |
| 未完成事项 | Balanced DFT-Fisher 高 SNR 已明显变合理，但低 SNR 仍很差：Fig7 中 `DFT-Fisher-Direct` 低 SNR(≤0 dB)平均约 `91.79 m`，高 SNR(≥8 dB)平均约 `7.94 m`。说明修复了强梳状 alias，但还没有解决低 SNR 选频/加权抗噪问题。 |
| 下一步优先级 | 若继续完善 DFT task-aware 基线，优先研究低 SNR 稳健的频点加权/PHAT/多窗融合，而不是再回到零填充 IDFT 重构。若转向创新方案，应保留当前 Fig6/Fig7 双轨结构，使用同一 urban8 + fixed eval set + all-pair WLS + lag gate 评估链路。 |

## 2026-07-03 本轮状态更新：Fig6/Fig7 精简与重绘已执行

| 字段 | 内容 |
|---|---|
| 当前任务 | 按用户批准执行 Fig6/Fig7 图表精简、重复曲线移除、zoom order 修复，并直接重绘覆盖 `运行结果/20260703_191104` 的 SVG。 |
| 是否允许修改代码 | 是。用户明确要求“按照建议的下一步修改方案执行”，并要求保留 `DFT-Fisher-Direct`、更新文档、重绘覆盖原图。 |
| 已完成代码修改 | `baselines.py`：新增 `include_legacy_variants`，默认不构建 DFT/Hadamard random 和 Hadamard-sequency，保留 Hadamard-block-2。`main.py`：默认 `USER_BASELINE_INCLUDE_LEGACY_VARIANTS=False`、`USER_BASELINE_RANDOM_VARIANT_SEEDS=0`；跳过与主线 `DFT` source 相同的重复 direct estimator；精简 Fig6 supplement 和 Fig7 order；zoom order 继承完整图 order。`evaluate.py`：`DFT` 图例显示 selected-bin source，`DFT-Fisher-Direct` 标注为 `Naive Fisher direct`。`replot.py`：对旧 pkl 应用当前推荐方法集合，并支持 `DAE_REPLOT_NO_SHOW=1` 无窗口重绘。 |
| 已完成重绘 | 已运行 `D:\Software\anaconda3\envs\PyTorch\python.exe replot.py "F:\PythonWorkspace\DAE Data Compression\运行结果\20260703_191104"` 并设置 `DAE_REPLOT_NO_SHOW=1`，覆盖 Fig1/Fig2/Fig3/Fig6/Fig7 及 Fig6/Fig7 low/high zoom SVG。 |
| 验证结果 | 两套 `py_compile` 通过；小样本 smoke 输出 `SMOKE_OK`，确认默认不构建 legacy random/sequency，Fig6/Fig7 order 正确且保留 `DFT-Fisher-Direct`；`git diff --check` 仅提示 Windows 行尾转换；SVG 文本检查确认 Fig7 无 `DFT-train-power-Direct`，保留 `Naive Fisher direct`。 |
| 当前状态 | 代码进入待 PyCharm 运行状态；`运行结果/20260703_191104` 的 SVG 已替换为新版本。 |
| 下一步优先级 | 用户检查新 SVG 可读性；若认可，可继续实现更强的 balanced/FIM-greedy DFT 选频，或进入创新方法与 Chen/传统基线的公平对比。 |

## 2026-07-03 本轮状态更新：`20260703_191104` Fig6/Fig7 结果审查，方案待审批

| 字段 | 内容 |
|---|---|
| 当前任务 | 审查最新结果 `运行结果/20260703_191104`，解释 Fig7 重复曲线、Fig6/Fig7 zoom 少线、Fig6 supplement 方法过多、DFT-Fisher 性能差等问题，并给出下一步修改方案供用户审批。 |
| 是否允许修改代码 | 否。本轮只分析结果并提出方案；未修改 Python 代码。 |
| 已完成结果审查 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig6_traditional_baselines.csv`、`tables/fig7_baselines.csv`、`tables/fig6_method_diagnostics.csv`、`tables/fig7_method_diagnostics.csv`，并对照 `main.py`、`evaluate.py`、`baselines.py`、`task_baselines.py`。 |
| 关键发现 | `DFT` 与 `DFT-train-power-Direct` 在 Fig7 中逐 SNR 完全重合，因 `DFT` 主线本来就是用 `DFT-train-power` selected bins 构造的 direct estimator；Fig6 zoom 少线是因为 `supplement_zoom_method_order` 包含 Fig6 过滤后不存在的 direct 方法；Fig7 zoom 少线是因为 zoom order 本身比完整 Fig7 order 少 `DFT-Fisher`；DFT-Fisher 性能差的根因是选中两段高频边缘 bins，窗函数在物理 lag 内产生强旁瓣，GCC sidelobe ratio 约 0.978，within-1-sample rate 仅约 0.27。 |
| 当前待审批方案 | 1. 去除 Fig7 中重复的 `DFT-train-power-Direct` 曲线，仅保留 `DFT` 并在配置/图例中说明其 source 是 `DFT-train-power`；2. 将 zoom order 自动派生自主图/补充图 order，保证曲线数量一致；3. 精简 Fig6 supplement，移除无论文依据的 Hadamard random/sequency 等方法，保留 Salari block-1 和可选 block-2 消融；4. DFT 变体分为 Chen-style、工程诊断、task-aware 三层，Fig6 supplement 只保留必要少数，Fig7 保留任务导向方法。 |
| 下一步优先级 | 等待用户审批后执行代码修改、编译、小样本 smoke、更新文档，使代码进入待 PyCharm 运行状态。 |

## 2026-07-03 本轮状态更新：Fig6/Fig7 direct DFT lag-gate 与 zoom 改造已执行

| 字段 | 内容 |
|---|---|
| 当前任务 | 执行用户批准的 `20260703_164540` 后续修正方案：统一 direct/GCC 的物理可行 lag gate，调整 Fig6 主线 DFT direct source，补充 Fig6/Fig7 低/高 SNR zoom 图，并增强 DFT 曲线可读性。 |
| 是否允许修改代码 | 是。用户明确回复“按你说的方案，批准执行”。 |
| 已完成代码修改 | `task_baselines.py`：`DirectDFTTDOAEstimator` 支持 `lag_limit_samples`、alias diagnostics 和 gated diagnostics。`evaluate.py`：Raw/Clean/DAE/waveform baseline 与 direct estimator 均支持统一 `tdoa_lag_limit_samples`；`plot_method_comparison` 支持 `snr_min/snr_max/method_order/title_suffix`，并区分 DFT direct/recon/Fisher 样式。`main.py`：新增 `USER_BASELINE_DFT_DIRECT_SOURCE="DFT-train-power"`、`USER_USE_PHYSICAL_TDOA_LAG_GATE=True`、zoom 输出开关；自动按 urban8 区域尺寸计算 lag gate，保存 Fig6/Fig7 low/high zoom SVG。`export_results.py`/`replot.py`：同步支持 zoom 图导出/重绘。 |
| 当前代码状态 | 已通过编译、静态检查和小样本 smoke，进入待 PyCharm 直接运行 `main.py` 状态。默认仍为 `paper_repro_eval_only`，不训练，加载冻结 v3.1 模型。 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；`git diff --check`；PyTorch 小样本 urban8 method comparison + direct DFT + zoom plotting smoke；临时目录 `export_results` zoom SVG 导出 smoke。 |
| 验证结果 | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；小样本 smoke 输出 `SMOKE_OK ...`，direct DFT 在 lag gate 下可运行；导出 smoke 输出 `EXPORT_SMOKE_OK 11`，确认 Fig6/Fig7 主图、补充图和 low/high zoom SVG 可生成。 |
| 未完成事项 | 尚未运行完整正式 `paper_repro_eval_only`，因此没有新的 Fig6/Fig7 正式指标；需要用户在 PyCharm 直接运行 `main.py` 后读取新结果目录。 |
| 下一步优先级 | 1. PyCharm 直接运行 `main.py`；2. 读取新 `run.log`、`plot_data.pkl`、Fig6/Fig7 主图与 zoom SVG、`tables/`；3. 判断 `DFT` direct 在物理 lag gate 与 `DFT-train-power` source 下是否恢复合理，若仍异常再进入 Cao/Zhai/FIM-greedy 选频。 |

## 2026-07-03 本轮状态更新：`20260703_164540` Fig6/Fig7 结果审查，方案待审批

| 字段 | 内容 |
|---|---|
| 当前任务 | 审查最新完整 eval-only 结果 `运行结果/20260703_164540`，解释 Fig6/Fig7 中主线 `DFT` direct 误差变大的原因，并给出下一步修改方案供用户审批。 |
| 是否允许修改代码 | 否。本轮只审查结果和给出方案；等待用户审批后再改代码。 |
| 已完成结果审查 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig6_traditional_baselines.csv`、`tables/fig7_baselines.csv`、`tables/fig6_method_diagnostics.csv`，并对照 `baselines.py`、`task_baselines.py`、`evaluate.py` 中 DFT 选频、direct-TDOA 和绘图逻辑。 |
| 关键发现 | 主线 `DFT` 使用 `DFT-SCS-lite` 的近均匀 bins `[0,16,32,49,...,1023]` 做 selected-bin cross-spectrum direct-TDOA，形成强周期歧义：选频窗自身在 lag `±63/±126/±189/±252` samples 附近有接近主峰的旁瓣。Fig6 中 `DFT` mean RMSE≈103.63 m，direct mean abs TDOA error≈122.57 samples，direct sidelobe ratio≈0.996。 |
| 额外验证 | 同一 urban8 clean 小样本中，全频 direct DFT 与现有 `gcc_standard` 一致，mean TDOA error≈0.26 samples；`DFT-SCS-lite` direct mean≈47-51 samples；加入物理可行 lag limit≈48 samples 后，`DFT-SCS-lite` direct clean mean error 降至≈0.26 samples。说明理论和符号不是主因，主因是均匀梳状选频 + 全 lag 搜索。 |
| 当前待审批方案 | 1. 给 direct DFT 和 waveform GCC 增加统一物理可行 lag gate；2. Chen Fig6 主线 `DFT` 的 direct source 从 `DFT-SCS-lite` 改为更合理的非 oracle `DFT-train-power` 或保留 `DFT-SCS-lite` 但必须启用 lag gate；3. 为 Fig6 supplement/Fig7 增加 low-SNR/high-SNR zoom SVG；4. 调整 DFT/direct/recon 曲线颜色、marker、linestyle，解决可读性问题。 |
| 下一步优先级 | 等待用户审批后执行代码修改、编译、小样本 smoke、更新文档，使代码进入待 PyCharm 运行状态。 |

## 2026-07-03 本轮状态更新：Chen Fig6 的 DFT 改为频域互谱 direct-TDOA

| 字段 | 内容 |
|---|---|
| 当前任务 | 按用户最新判据修改 Fig6/DFT 口径：方法能否进入 Chen Fig6 主图，以是否严格对应 Chen 引用文献的方法为准；DFT 文献若未明确零填充 IDFT 重构，则默认采用频域互谱估计 TDOA。 |
| 是否允许修改代码 | 是。用户明确要求“根据你的判断，执行代码修改”。 |
| 已完成代码修改 | `main.py`：Fig6 主图的 `DFT` 改为 `DirectDFTTDOAEstimator`，优先使用 `DFT-SCS-lite` 的 partial-Fourier bins，经频域互谱直接估计 TDOA；`DFT-SCS-lite/DFT-Fisher` 等零填充 IDFT 波形重构路径只保留在补充/任务感知消融中。`task_baselines.py`：补充互谱 direct-TDOA 的理论说明。`evaluate.py`：修正 waveform 方法与 direct estimator 的说明和图例标签。`baselines.py`/`export_results.py`：更新 DFT 主图与补充消融的元信息和导出解释。 |
| 当前方法口径 | Chen Fig6 主图：`Raw`、`DAE-CR16`、`DFT`（selected-bin cross-spectrum direct TDOA）、`Hadamard`（Salari 连续行块 `H_M^T H_M` 投影伪重构）、`PCA`。Fig6 补充图：保留 `DFT recon`、`DFT Fisher recon`、`DFT-train-*`、随机/已知频带等消融。Fig7：保留任务导向 direct DFT/Fisher 变体，供创新方案对比。 |
| 理论判断 | `IFFT{X_i(f) conj(X_j(f))}` 得到互相关是互相关定理/维纳-辛钦类结果在互谱上的应用；这不是对每一路信号做零填充 IDFT 伪重构。零填充单路波形重构会额外引入波形重构误差，当前不再作为 Chen Fig6 DFT 主线。 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`；`D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...`；PyTorch 小样本 urban8 baseline/direct smoke；`git diff --check`。 |
| 验证结果 | 两套 `py_compile` 通过；小样本 smoke 输出 `SMOKE_OK DFT-SCS-lite ['DFT', 'DFT-Fisher-Direct'] ...`；`git diff --check` 仅提示 Windows 行尾转换，无空白错误。 |
| 当前代码状态 | 已进入待 PyCharm 直接运行 `main.py` 状态。默认仍为 `paper_repro_eval_only`，不训练，加载冻结 v3.1 模型，生成 Fig1/Fig2/Fig3/Fig6/Fig7 和 `plot_data.pkl`。 |
| 未完成事项 | 尚未运行完整正式 eval-only 来得到新 Fig6/Fig7 指标；尚未实现更严格的 Cao/Zhai/FIM-greedy direct-TDOA 频点选择。 |
| 下一步优先级 | 1. 用户在 PyCharm 直接运行 `main.py`；2. 读取新结果目录的 `run.log`、`plot_data.pkl`、Fig6/Fig7 SVG 和 `tables/`；3. 判断 direct DFT 是否适合作为 Chen Fig6 主线，若仍异常，再进入 Cao/Zhai/FIM-greedy 实现。 |

## 2026-07-03 本轮状态更新：Fig6/Fig7 双轨 baseline 已执行

| 字段 | 内容 |
|---|---|
| 当前任务 | 审查 `运行结果/20260703_134722` 中 DFT-Fisher 进入主图后的正式结果，并按用户批准执行 Fig6 严格 Chen-style 复现轨道 + Fig7 task-aware 强基线轨道分离 |
| 是否允许修改代码 | 是；用户已批准执行 14:58 双轨方案，并要求审查代码、更新文档 |
| 已完成代码修改 | `main.py` 默认主 DFT 从 `fisher_power` 改回 `scs_lite`；新增 `task_baselines.py` 的 `DirectDFTTDOAEstimator`；`evaluate.py` 支持 reconstruction models 与 direct-TDOA estimators 共用同一 fixed eval set 和 all-pair WLS；`baselines.py` 增加 `DFT-SCS-lite` 命名和元信息；`export_results.py`/`replot.py` 支持 Fig7 导出与重绘 |
| 已完成结果审查 | 读取并分析 `运行结果/20260703_134722/run.log`、`plot_data.pkl`、`tables/fig6_method_diagnostics.csv`。确认 DFT-Fisher 在主图中明显恶化：全 SNR mean RMSE 约 57.36 m，高 SNR 均值约 32.03 m，显著差于 DFT-bandlimited/DFT-train-power/DAE-CR16；根因是 `power*f^2` 硬选择高频边缘 bins 后再做伪重构 GCC，破坏了可恢复波形和稳定互相关结构 |
| 验证方式 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py task_baselines.py`；`git diff --check`；PyTorch 小样本 urban8 baseline/direct-TDOA smoke；direct DFT 简单移位信号相位符号 sanity check；`export_method_baselines` Fig7 导出 smoke |
| 验证结果 | 编译通过；`git diff --check` 仅提示 Windows 行尾转换；小样本 baseline/direct/plot smoke 输出 `SMOKE_OK`；全频 direct DFT 对简单移位信号输出 lag 与现有 GCC 一致；Fig7 表格/SVG 导出 smoke 成功 |
| 当前代码状态 | 已进入待 PyCharm 直接运行 `main.py` 状态。默认 `paper_repro_eval_only`，不训练；默认输出严格 Chen-style `Fig6_Chen_Traditional_Baselines_CR16.svg` 与 task-aware `Fig7_TaskAware_Baselines_CR16.svg`；`plot_data.pkl` 保存 `fig6_results`、`fig7_results` 和 `baseline_all_results` |
| 未完成事项 | 尚未运行完整正式 eval-only 验证新 Fig6/Fig7 输出；尚未实现 Zhai/FIM-greedy 之类更强 direct-TDOA 基线；尚未把创新 DAE 方法迁移到同一 urban8/WLS 公平协议 |
| 下一步优先级 | 1. 在 PyCharm 直接运行 `main.py` 生成新结果目录；2. 审查新 `run.log`、`plot_data.pkl`、Fig6/Fig7 SVG 和 tables；3. 若 Fig7 direct DFT 有价值，再继续实现 Zhai/FIM-greedy 或创新方法对比 |

## 1. 当前任务状态

| 字段 | 内容 |
|---|---|
| 当前任务 | 设计双轨 baseline：严格 Chen Fig6 复现轨道 + 创新方案强基线验证轨道 |
| 任务来源 | 用户认同 Chen-style Fig6 应严格参照原论文基线，同时指出后续创新论文应对比更新、更好的 TDOA-aware baseline，并要求给出分析结论和具体执行方案供审批 |
| 当前阶段 | 已明确两条轨道的目标差异：`Fig6_Chen` 用于近似复现 Chen 2025；新增 task-aware 图用于创新方案对比。当前只给方案，等待用户审批 |
| 是否允许修改代码 | 否；本轮只提出代码修改方案，未修改 Python 代码 |
| 是否允许修改 md 文档 | 是，已按项目规则更新 |
| 是否允许运行训练 | 否；本轮未运行训练或完整 eval-only，只读取文献与代码 |
| 已完成工作 | 形成双轨建议：严格 Chen 轨道主图使用 `DFT-SCS-lite/partial-Fourier`、Hadamard Salari projection、PCA、DAE-CR16；创新强基线轨道新增 TDOA-aware DFT/Fisher、partial-spectrum direct GCC/ML、Zhai-style FIM/CRLB greedy decimation 等；Hadamard 后续统一指 Salari 连续行块 `H_M^T H_M` 投影伪重构 + GCC/WLS，不再拆分 direct/projection 两条主方法 |
| 验证结果 | 未运行代码；这是方案设计。核心判断是：有重构的方法可进入重构质量 + 定位双指标；无重构的 direct-TDOA 方法必须通过统一 TDOA constraint + same WLS localizer 纳入定位对比，但不参加 waveform reconstruction 指标 |
| 当前判断 | 方案待审批：建议先改出 `Fig6_Chen_Traditional_Baselines_CR16`，再新增 `Fig7_TaskAware_Baselines_CR16` 和补充消融图，为后续创新方法与 Chen 方法、强基线三方比较做接口准备 |
| 下一步动作 | 等待用户审批；审批后分阶段修改代码，先做严格 Chen Fig6 默认口径，再扩展 direct-TDOA baseline 统一接口和 Fig7，不默认运行完整训练 |

## 2. 待办清单

- [x] 检查 Git 状态、当前分支和最近 commit。
- [x] 读取上下文文档、参考文献和当前代码接口。
- [x] 新增 `baselines.py`：DFT、Hadamard、PCA 三种传统 baseline。
- [x] `evaluate.py`：新增按方法名运行的 urban localization Fig6 评估函数和绘图函数。
- [x] `main.py`：接入传统基线默认评估，保存 `fig6_results`、baseline 元信息和 `Fig6_Traditional_Baselines_CR16.svg`。
- [x] `replot.py`：支持从 `plot_data.pkl` 重绘 Fig6。
- [x] `export_results.py`：支持导出 Fig6 CSV/Markdown 表格和 SVG。
- [x] 运行两套 `py_compile`。
- [x] 运行 `paper_repro_eval_only` 小样本冒烟。
- [x] 运行 `export_results.py` 小样本导出验证。
- [x] 更新 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`。
- [x] 修复 PyCharm 默认运行模式：`DEFAULT_EXPERIMENT_MODE="paper_repro_eval_only"`。
- [x] 增加运行日志中的模式来源字段。
- [x] 运行无 `DAE_EXPERIMENT_MODE` 小样本冒烟，确认默认不训练。
- [x] 实现 DFT 非 oracle/center/random 选择。
- [x] 实现 Hadamard Salari/random/sequency 行选择，并将 Salari `H_M^T H_M` 重构保留在主评估链路。
- [x] 实现 PCA noisy/clean/fixed-SNR 训练源配置。
- [x] 保存 Fig6 每方法 NMSE/PSD/GCC/TDOA 诊断字段。
- [x] 导出 `fig6_method_diagnostics.csv/md`。
- [x] 运行默认 eval-only 小样本冒烟和 diagnostic variants 小样本冒烟。
- [x] 将常用 `DAE_*` 环境变量入口改为 `main.py` 顶部 `USER_*` 普通变量。
- [x] 默认关闭环境变量覆盖：`ALLOW_ENV_OVERRIDES=False`。
- [x] 默认打开 Fig6 额外变体：`USER_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=True`。
- [x] 将主 DFT 改为 `DFT-train-power`，避免中心带宽 oracle。
- [x] 将 Fig6 主图和补充消融图分离，并将图例放在图下方避免遮挡曲线。
- [x] 随机 DFT/Hadamard 变体按多 seed 输出，补充图中按均值和范围显示。
- [x] 增加 Hadamard `sequency` 和 `offset` 消融。
- [x] 增加 Fig6 active PSD、linear PSD NMSE、weighted TDOA error、within-1/2 rate 诊断。
- [x] `main.py` 运行结束自动导出 `tables/`。
- [x] `export_results.py` 支持 `--result-dir`。
- [x] 用户运行完整 `paper_repro_eval_only`，生成新的正式 Fig6：`运行结果/20260702_220605`。
- [x] 审查 `20260702_220605` 的 `run.log`、`plot_data.pkl`、SVG 和 `tables/`。
- [x] 编译检查当前 Python 文件。
- [x] 用户审批下一轮 Fig6 baseline v3/导出增强/耗时优化方案。
- [x] 新增 `DFT-train-band` 连续训练功率频带，并将默认 `USER_BASELINE_DFT_MODE` 改为 `"train_band"`。
- [x] 将 `DFT-train-power` 保留为补充消融，记录每个 DFT 变体实际保留的 bins。
- [x] 明确 Salari Hadamard 连续行块规则，补充图中使用 `Hadamard-block-2` 作为行块消融。
- [x] Fig6 主图增加高 SNR zoom inset，不单独生成 zoom 图。
- [x] 自动 `tables/` 导出默认跳过 Fig6 SVG，避免与结果根目录重复。
- [x] 导出 `fig6_random_group_summary.csv/md`，记录随机 DFT/Hadamard 组的均值、标准差、最小值和最大值。
- [x] 运行编译、函数级 smoke、Fig6 inset 绘图 smoke、导出 smoke 和 `git diff --check`。
- [x] 用户运行完整 `paper_repro_eval_only`，生成 Fig6 v3 正式结果：`运行结果/20260703_110113`。
- [x] 审查 `20260703_110113` 的 `run.log`、`plot_data.pkl`、SVG 和 `tables/`。
- [x] 用户审批 A+B：Fig6 图表口径调整与 eval-only 结果目录瘦身。
- [x] 参考综述中 RMS 带宽/Fisher 信息/高频分量对 TDOA 贡献更大的研究，新增 `DFT-Fisher` 频谱抽取方法。
- [x] 将 `main.py` 默认 `USER_BASELINE_DFT_MODE` 改为 `"fisher_power"`，主 Fig6 默认画 `DFT-Fisher`。
- [x] 补充图保留 `DFT-Fisher/DFT-train-band/DFT-train-power/DFT-bandlimited/DFT-uniform/DFT-random*`。
- [x] 新增 `USER_EVAL_ONLY_COPY_MODELS=False`，eval-only 默认不复制模型权重。
- [x] 运行编译、PyTorch 函数级 smoke 和 `git diff --check`。
- [ ] 用户运行完整 `paper_repro_eval_only`，生成 Fig6 v4 正式结果。
- [ ] 读取并审查新结果目录的 `run.log`、`plot_data.pkl`、Fig6 SVG 和 `tables/`。
- [ ] 用户审批下一轮：结果目录归档方案、DNN-DAE/CNN-DAE 补充基线或创新方法公平对比迁移。
- [ ] 用户确认是否继续补 DNN-DAE/CNN-DAE 或 DFT/Cao/Zhai 类 direct-TDOA 补充消融。

## 3. 完成标准

- 当前轮完成标准：代码可编译；`DFT-Fisher` baseline 可构建并输出正确形状；Fig6 绘图函数可处理 `DFT-Fisher` 和 zoom inset；eval-only 模型复制开关写入日志和 pkl 配置；文档状态更新。该标准已满足。
- 正式结果完成标准：用冻结 v3.1 模型和完整 Monte Carlo trials 运行 `paper_repro_eval_only`，读取并审查正式 Fig6 v4 的 `run.log`、`plot_data.pkl`、SVG、`fig6_traditional_baselines.csv`、`fig6_method_diagnostics.csv` 和 `fig6_random_group_summary.csv/md`。

## 4. 风险与注意事项

- 当前 smoke 目录 `运行结果/20260702_195744` 和 `运行结果/20260702_195918` 只用于流程验证，PCA 样本数很小，指标不可用于论文结论。
- 完整 Fig6 默认 `PCA` 使用 `BASELINE_PCA_SAMPLES=N_SAMPLES` 的 noisy waveform 拟合；若用户缩小该值，必须在论文或记录中说明。
- 当前主 Fig6 默认 `DFT-Fisher`：训练集平均频谱功率 × 归一化频率² 选频，并用功率阈值屏蔽低功率噪声频点；`DFT-train-band`、`DFT-train-power` 和 `DFT-bandlimited` 放入补充图。
- Hadamard 主图采用 Salari-style 连续行块 `H_M^T H_M` 投影伪重构后统一 GCC/WLS 链路；后续不再把 Hadamard direct 与 projection 作为两条主方法区分。
- Chen 2025 未交代传统 baseline 的完整工程细节；当前实现应表述为“按相同 CR 预算与统一定位链路实现的可复现实验 baseline”。
