# 当前任务

## 2026-07-30 当前任务：BRSR 动作价值可观测性证书

| 字段 | 内容 |
|---|---|
| 当前任务 | 检验仅用动作发生前可部署信息，能否预测候选频点的条件风险，并在独立测试集上稳定超过同为 29 bit 的 BestStatic 与 SNR-only 强基线。 |
| 任务来源 | 历史 Oracle 语义审计发现：事后全知 Oracle 的优势不能证明动作价值可学习；IID 样本的 BlockOracle 也不能证明共享块协议存在。 |
| 当前阶段 | 正式 development 已完成并复核；本任务已完成，下一研究任务待审批。 |
| 当前裁决 | 可观测性证书=`HOLD_PROXY_MISMATCH`；Stage 2A.1=`NO-GO-CONFIG-STAGE2A1`；IID Block=`NO-GO-PROTOCOL-IID-BLOCK`。 |
| 已完成 | 独立实现、代码验证、smoke、正式`4×30/2×30/3×40`运行、29-bit公平审计、动作预测诊断、配对cluster CI和门禁裁决。 |
| 正式结果 | `运行结果/BRSR/BRSR_OBSERVABILITY_20260730_153403_development`；HGB相对BestStatic/SNR-only的配对MSE改善95% CI均严格为负，3个test seed全部退化。 |
| 科学结论 | 动作前信息含弱排序信号，但HGB/Ridge只会排除明显差动作，不能可靠区分固定动作附近的最优候选；收益没有转化为部署策略。 |
| 允许范围 | 只读总结现有证据，或提出理论上不同的可观测量、条件风险目标或协议假设并先供审批。 |
| 暂不允许 | 不调HGB/Ridge超参数，不重新选择特征，不在当前test上设计门控，不继续IID Block，不进入Stage 2B或locked。 |
| 下一步优先级 | 冻结当前单样本均值残差学习组件；决定是以负结果收束BRSR，还是另立“尾部风险/改善概率等理论上不同的决策目标”并使用全新development。 |
| 完成标准 | 已满足：正式结果同时报告效应量、CI、逐seed一致性、MDE和预测诊断，并按预注册规则得到`HOLD_PROXY_MISMATCH`。 |

## 已冻结路线

- Stage 2A.1 SafeGate：`NO-GO-CONFIG-STAGE2A1`。
- IID BlockPredicted/BlockOracle：`NO-GO-PROTOCOL-IID-BLOCK`。
- PASR-TDOA v1：动作评分与停止规则为 `NO-GO-CONFIG`。
- PFRS 固定掩码：冻结空间内为限定范围的 `NO-GO-ROUTE`。
- Shared64、GeoHybrid、DFT、Zhai 等：保留为基线或机制消融。
- R20/R20.1、FreqDAE v1-v4：不再作为当前主创新。

详细裁决以 `研究门禁注册表.md` 为准，正式实验事实以 `实验结果记录.md` 为准。
