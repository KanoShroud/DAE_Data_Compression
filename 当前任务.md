# 当前任务

## 2026-08-19 当前任务：NP-NBQ-DPD Stage 0结构余量证书

| 字段 | 内容 |
|---|---|
| 当前任务 | 在离散小规模被动定位模型中，穷举全部严格24-bit节点--频点--位宽分配，检验联合利用几何与公开链路可靠性的直接位置Bayes风险设计是否存在结构性余量。 |
| 任务来源 | Jiang 2026全文复核后，将混合量化、多快拍几何结构与直接定位原则迁移到未知源谱、未知复增益和网络总预算约束的被动TDOA压缩；先做可穷举证书，避免直接进入urban8或启发式优化。 |
| 当前阶段 | `RESULT_ANALYZED_HOLD`。正式development已完成并回读，自动裁决=`HOLD_STRUCTURE_EFFECT`。 |
| 独立入口 | `np_nbq_dpd/run_stage0.py`；顶部`RUN_MODE="development"`，可直接在PyCharm运行，不需要环境变量。 |
| 模型边界 | 3节点、2频点、离散二维ROI、单径AWGN和逐链路BSC；参考节点复增益固定为规范，第二频点源幅度比、源相位及两个非参考节点的频带内常复增益幅相采用有限先验并在量化位置似然中边缘化。 |
| 公平预算 | 每个节点--频点的I/Q采用相同标量量化器家族，位宽为`0--3 bit/real`，全部336个合法方案均严格24 payload bit；所有方法共享evaluation观测、BSC随机数和直接位置后端。 |
| 对比方法 | `EqualBit`、外生链路等级驱动的`JiangStylePresetHybrid`、`GeometryOnly`、`ReliabilityOnly`、`ProjectedFisher`、`JointRisk`、design内冻结的`StrongestFrozenBaseline`及非bit可比的`UnquantizedAll-Diagnostic`。 |
| 信息边界 | 分配只使用ROI先验、节点坐标、独立pilot给出的SNR/BER和外生链路等级；禁止真实位置、clean信号、动作后误差或evaluation结果进入分配。Jiang-style仅为设计原则迁移，不声称严格复现其能量观测和GA-PDR。 |
| 统计协议 | 固定profile分层bootstrap：各profile内重采样，再对4个profile等权汇总；`1.96×SE`明确记为正态CI半宽，近似MDE采用双侧5%显著性与80%功效。另输出逐profile效应、matched-Bayes风险校准和design赢家/逐cell位宽稳定性。 |
| 门禁 | `JointRisk`相对design冻结的最强合法基线、`GeometryOnly`和`ReliabilityOnly`的分层配对位置MSE改善95% CI下界均须大于0；三个非平衡profile均不得对最强基线出现CI上界小于0的明确回归。校准比值超出`[0.5,2]`只触发人工审查，不作为性能门禁。 |
| 编码边界 | 量化区间索引冻结为`natural_binary`，每个标签bit独立通过BSC；该结果不外推到Gray码、信道编码或软译码。manifest保存逐profile派生种子、软件版本和源码哈希。 |
| 验证状态 | 正式目录=`运行结果/NP_NBQ_DPD/NP_NBQ_STAGE0_20260819_165042_development`；完整性、公平性和概率校准通过。`JointRisk`相对最强冻结基线的经验MSE改善=`30.5877 m²`，95% CI=`[-16.6939,79.0925]`；但evaluation后验风险差为`-22.7345 m²`，自身设计目标方向反转，design赢家频率仅`19.10%--68.20%`。 |
| 当前禁止事项 | 不修改现有DAE/BRSR/SITQ/RAPTQ和`main.py`；不把有限先验Stage 0外推为连续任意信道结论；不依据本次development调profile、分配空间、基线或门禁；`HOLD`期间不扩展urban8、整数规划、多径、NLOS或locked。 |
| 下一步 | 冻结本次development及其`HOLD`裁决。若继续该路线，先审批只使用全新design随机流的风险数值收敛与选择regret证书，不生成新的性能结论；只有经验JointRisk分配能稳定逼近高预算参考时，才允许另行审批全新evaluation有界复验。 |

## 已冻结路线

- SITQ单一全局对称阈值：`HOLD_SELECTION_UNCERTAIN`，不使用新evaluation继续确认。
- BRSR转折区频谱异质性：`NO_GO_TRANSITION_STATE_HEADROOM`。
- BRSR共享状态确认性复验：`NO_GO_ROUTE_PLANNING_NONCONVERGENCE`。
- Stage 2A.1 SafeGate：`NO-GO-CONFIG-STAGE2A1`。
- IID BlockPredicted/BlockOracle：`NO-GO-PROTOCOL-IID-BLOCK`。
- PASR-TDOA v1：动作评分与停止配置为`NO-GO-CONFIG`。
- PFRS固定掩码：冻结空间内为限定范围的`NO-GO-ROUTE`。
- R20/R20.1、FreqDAE v1-v4：不再作为当前主创新。
- RAPTQ有限码率路线：`HOLD_RELATIVE_PHASE_HEADROOM`，不再回调频点、位宽或门槛。

详细裁决以`研究门禁注册表.md`为准，正式实验事实以`实验结果记录.md`为准。
