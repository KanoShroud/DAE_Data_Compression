# AGENTS.md

项目级长期规则。进入本仓库工作时，先读本文件，再读 `对话上下文.md`、`当前任务.md`、`修改记录.md`、`实验结果记录.md`，了解项目基本情况和工作进度。

## 项目背景

- 项目名称：`DAE Data Compression`。
- 研究主题：面向`TDOA 参数估计`和`定位解析`任务的`数据压缩`。
- 基准论文：Chen 等，`Collaborative Localization Using Multiple UAVs: A Deep Denoising Autoencoder-Based Data Compression Approach`，IEEE TVT 2025。
- 研究主线：设计新的、任务驱动的压缩方法，在相同压缩比下，合理权衡复杂度与精度，实现比现有基线更高的TDOA估计精度和定位解析精度（任意条件或特定条件下）。

## 运行/环境规则

- 本地参考路径为 `F:\PythonWorkspace\DAE Data Compression`；如仓库迁移，以当前工作目录为准。
- 项目使用 `D:\Software\anaconda3\envs\PyTorch` 虚拟环境。缺少依赖时，提供相应的 Conda 安装命令，由用户安装。
- 读取中文路径或中文文档时，PowerShell 优先使用 `Get-Content -Encoding UTF8 -LiteralPath ...`。若显式按 UTF-8 读取后仍乱码，应检查文件实际编码，不得基于乱码内容修改文件。
- 编辑含中文注释的 `.py` 文件时，只修改任务相关内容，避免无关格式化、批量替换或因编码转换重写整个文件。
- 对项目新建或已确认采用 UTF-8 的文本文件，Python 读写时显式指定 `encoding="utf-8"`；外部文件应保持其实际编码。
- 处理中文路径时优先使用 `pathlib.Path`；确需字符串路径时使用原始字符串。

## 角色安排与协作分工

- Codex 按信号处理研究协作标准负责代码修改、轻量验证、结果分析和方案设计。
- 用户默认用 PyCharm 运行 `main.py`、完整训练、完整的仅评估运行（eval-only）和长耗时脚本。

## 工作原则

- 用户明确要求“只给方案/不执行”时，不得修改py代码。
- 用户要求执行改动时，保持改动小而可解释，优先沿用现有代码和文档风格。
- 每次修改后检查原理、逻辑、可读性和结果保存与展示，不只检查语法。
- 不编造实验结果、指标、模型权重、运行目录或结论，不确定就写“待确认”。
- 未经用户明确批准，不进行大规模重构或修改任务范围外文件。
- 未经用户明确批准，不通过`Computer Use`等插件代替用户操作电脑。
- 若执行任务时，命中/触发了用户明确要求之外的skills，需要在回答中明确指出。

## 研究门禁与路线裁决

- 所有研究门禁均登记在`研究门禁注册表.md`，并区分理论/协议不变量、公平性约束、统计证据、工程最小效应和研究止损规则。
- 裁决必须同时给出效应量、置信区间、样本量/统计功效和门槛敏感性；对合理阈值敏感时标记为`HOLD`。
- 使用`GO`、`HOLD`、`NO-GO-CONFIG`、`NO-GO-COMPONENT`和`NO-GO-ROUTE`分级表述，不得把配置或组件失败直接外推为路线失败。
- development 可用于设计，locked 只用于一次最终确认；根据 locked 反馈修改方法或门禁后必须更换 locked 集合。

## 语言与术语规范

- 输出回答、代码注释、日志说明和项目文档时，优先使用准确、自然、符合电子信息、信号处理、统计估计和机器学习领域习惯的中文表述。
- 术语准确性高于中文化程度。不得为了追求“全中文”而进行生硬直译、逐词翻译或自行创造未经学术界广泛使用的中文术语。
- 翻译英文术语前，必须结合其在当前代码、实验协议和研究语境中的实际含义判断，不得仅根据英文单词的一般词典含义翻译。
- 对已有稳定、规范中文译名的术语，首次出现时采用“中文名称（英文名称或缩写）”，后续使用中文名称或通行缩写。例如“克拉美–罗下界（Cramér–Rao lower bound，CRLB）”。
- 对没有稳定中文译名、中文译法容易产生歧义、或项目中通常直接使用英文的术语，保留英文，并在首次出现时用中文解释其具体含义。
- 现有代码标识符、配置键、文件名及第三方接口名称原则上保持不变；除非用户明确要求，不得擅自翻译或重命名。解释时可说明其中文含义，但必须保留原始名称。新增名称应遵循项目现有命名规范。

## 代码修改与验证

- 文本源码、配置和文档的手工修改优先使用 `apply_patch`；不得通过格式化、批量替换、编码转换或 PowerShell 整体写回方式重写无关内容。
- 新增或修改 `.py` 文件后，必须使用项目指定 Python 解释器对本轮所有涉及文件执行编译检查。
- 新增或修改 `.py` 文件后，必须对本轮涉及文件执行 `python -m ruff check <文件列表> --select F,E9`；不得仅检查仓库根目录或以无关历史问题代替本轮检查。
- 修改模型、数据处理、损失函数、评估、定位或结果保存逻辑后，应执行与改动对应的轻量冒烟测试。完整训练、完整评估及长耗时脚本由用户通过 PyCharm 运行。
- 更改实验或运行模式时，优先使用项目已有配置项或命令行参数，其次使用集中管理的配置变量；除非项目原有机制要求，不使用临时环境变量控制实验。
- 修改代码时同步检查相关注释、日志、配置说明、结果字段和输出文件，避免实现与说明不一致。
- 涉及损失函数时，必须检查训练集与验证集目标定义是否一致。
- 涉及 GCC 或 TDOA 时，必须检查训练、验证和评估中的时延窗口、正负号约定、单位及索引定义是否一致。
- 涉及多分支输入、频点选择或方法融合时，压缩预算按实际传输特征并集计算；正式同压缩比比较前必须通过预算检查。
- 复用缓存或历史结果前，必须核对随机种子、数据划分、snapshot 池、归一化、SNR 设置、时延窗口、定位器、样本数、指标定义、方法预算及代码版本或指纹；协议不完整或不一致时不得复用，并应明确说明。
- 生成、重绘或导出实验图表时，必须核对数据来源、图例、坐标轴、单位、曲线含义、指标定义及 `pkl`、CSV 等结果字段的一致性。
- 代码修改并完成轻量验证后，最终回复应说明修改文件、验证命令及结果；如需用户在 PyCharm 中正式运行，还应说明入口脚本、关键配置、是否训练、预期输出目录、需要回读的文件及尚未完成的验证。

## Git规则

- 用户默认用 PyCharm Git 插件管理提交、分支、合并、推送和回滚；Codex 只做必要的只读 Git 检查。
- Codex 不主动 commit、push、merge、rebase、checkout 或创建分支，除非用户明确要求。
- 当任务涉及修改文件、分析工作树状态、准备提交，或运行可能产生/覆盖项目文件的脚本时，必须检查：
  - `git status --short --branch`
  - `git branch --show-current`
  - `git log -5 --oneline --decorate`
- 纯讨论、纯方案、纯文本解释时可不检查 Git。
- 如果发现工作树已有未提交修改，默认是用户或前序工作留下的，不要回滚，除非用户明确要求。
- 若需要新分支，默认使用 `codex/` 前缀，除非用户指定。
- 如用户只需要版本管理建议，Codex 只给出建议文件列表和提交说明草稿，不直接执行 Git 写操作。
- `.gitignore` 以仓库当前文件为准。修改忽略规则前需确认是否会影响用户希望纳入版本管理的文件。通常不应提交运行结果、缓存、模型权重、大型中间数据和本地环境。

## 文档与结果记录

根目录维护以下长期文档。文档按事件更新，不要求每轮全部更新；用户明确要求“不修改文档”时，不更新任何文档。

### 文档职责与更新规则

- `AGENTS.md`：记录项目级长期规则。
  - 仅在用户明确要求或批准规则变更时更新。
  - 不记录当前版本、实验结果、分支状态、临时任务或下一步计划。

- `对话上下文.md`：记录当前研究阶段、核心方法、关键结论、主要风险和下一步研究方向。
  - 上述状态发生实质变化时更新。
  - 实验指标只保留综合结论，详细事实指向 `实验结果记录.md`。
  - 仅提出方案、任务自然流转或未改变研究结论的轻量验证不更新。

- `当前任务.md`：记录当前唯一的主线任务及其审批和执行状态。
  - 用户确认新的主线任务，或任务进入“方案待审批、已批准待执行、修改完成待运行、结果待分析、已完成”等阶段时更新。
  - 临时文档治理、格式修正、规则小修和纯讨论不更新。

- `修改记录.md`：记录已经完成且有项目意义的代码或文档修改。
  - 同一修改事件的修改原因、涉及文件和验证结果合并为一条记录。
  - 纯讨论、未执行方案、任务自然流转、单独追加实验结果、轻微排版、错别字和仅追加本文件记录不单独记录。
  - 更新本文件不触发新的修改记录。

- `实验结果记录.md`：记录实际运行或读取实验、评估和结果文件后形成的事实，包括正式训练、完整评估、冒烟实验、重绘导出和结果审查。
  - 单独执行 `py_compile` 或仅检查代码结构不更新。
  - 完整训练、仅评估（eval-only）或重要结果分析完成后必须更新。
  - 若研究结论或主线任务状态同时改变，再相应更新 `对话上下文.md` 和 `当前任务.md`。

- `研究门禁注册表.md`：记录跨路线门禁的定义、来源、统计口径、敏感性和分级裁决。
  - 新增、修改、废止门禁，或实验导致门禁裁决和适用边界变化时更新。
  - 代码中的阈值必须与注册表一致；事后修改须保留原版本、原因及其对既有结论的影响。

### 实验结果证据规范

- 没有实际运行实验、读取结果文件或获得明确的用户反馈时，不写具体指标；用户反馈必须标注“用户反馈/待复核”。
- 分析 PyCharm 结果时优先读取 `run.log`、`plot_data.pkl`、SVG、`tables/*.csv`、`tables/*.md`。
- 必须区分正式运行、小样本冒烟测试（smoke test）、单方法增量更新、仅重绘/导出和用户反馈。
