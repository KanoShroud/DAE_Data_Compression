# 研究总结

> 当前摘要更新至 2026-08-19。2026-07-27 及以前的完整逐路线叙述保存在
> `文档归档/研究总结_截至2026-07-27_完整历史.md`。

## 1. 研究定位

- 目标：在相同通信预算下，为多 UAV 协作定位设计比现有压缩基线更适合 TDOA 参数估计和定位解析的任务驱动方法。
- 评价原则：同时核算传输特征、量化、索引、反馈、缓存和计算复杂度；分别报告 TDOA 与定位误差、效应量、不确定性和条件稳健性。
- 当前主线：NP-NBQ-DPD Stage 0，以可穷举的小规模直接定位模型验证“干扰参数边缘化 + 网络总bit联合分配”是否存在结构性收益。
- 当前路线状态：BRSR共享状态及转折区配置、SITQ单一全局阈值和RAPTQ有限码率路线均冻结；NP-NBQ-DPD正式development为`HOLD_STRUCTURE_EFFECT`，暂不扩展urban8或复杂信道。

## 2. 基线与评估基础

| 轨道 | 当前定位 | 结论 |
|---|---|---|
| Chen-style DAE | 近似论文复现基线 | `paper_repro_v3.1` 可复现主要工程链路，但不声称严格复现原 Wireless InSite 结果 |
| Raw / DAE / DFT / Hadamard / PCA | 波形及传统压缩基线 | 已建立统一数据和定位评估链路 |
| Cao / Zhai / Fisher / GeoHybrid | 任务型频域强基线 | 用于判断创新方法是否真正超越任务导向压缩 |
| V5 / Shared64 / Top-K | 压缩域似然与共同后端消融 | 证明部分机制有效，但未形成严格预算下的全面支配 |

正式结果路径、参数和指标以 `实验结果记录.md` 为准。

## 3. 方法演化与当前裁决

| 路线 | 核心尝试 | 当前结论 |
|---|---|---|
| R20/R20.1 | MSE、GCC、峰值与混合 NMSE 损失 | 可改善部分 TDOA 指标，但波形恢复和压缩率区分度不足，停止作为主创新 |
| FreqDAE v1-v4 | 频域重构、嵌套潜变量与任务头 | 反复出现假窄带、宽峰和压缩率无区分，冻结为失败机制证据 |
| GeoHybrid / V5-A / V5-B | 直接压缩域 TDOA 似然、专家与 Top-K 几何决策 | 保留为强基线、机制上界和复杂度消融 |
| V5B-Shared64 | 严格 CR16 双专家融合 | 相对 Geo 专家有收益，但未稳定优于 Power 专家，保持 `HOLD` |
| 双站异构插值补全 | 对不重合频点做原网格插值或补零 | 不产生新的跨站互谱信息，路线冻结 |
| PFRS | Fisher 安全与有限时延风险联合选频 | 固定掩码在冻结空间内饱和，连续性能未超过最强 Power 基线 |
| PASR-TDOA v1 | 后验驱动的主动逐层选频与停止 | 通信协议成立，但动作评分、停止准则和同 bit 性能门禁失败，配置为 `NO-GO-CONFIG` |
| BRSR Stage A | 贝叶斯风险顺序精化的机制验证 | 理论不变量、预算和基础校准通过，只批准进入 development |
| BRSR Stage B v1/v2 | 真实波形、量化似然、Greedy/Rollout 与连续复增益修复 | 平均收益不稳且存在高 SNR 重尾；v1 为 `NO-GO-CONFIG`，v2 组件为 `NO-GO-COMPONENT` |
| BRSR 路线可行性 Stage 1 | 冻结候选池和似然，验证事后下界与可观测 RiskGreedy | RiskGreedy 在限定协议内有正收益；事后 Oracle 数值仅保留为描述性下界 |
| BRSR Stage 2A | stop/acquire 与同实际平均总 bit 静态前沿比较 | 可部署策略、校准和工程收益未通过；事后 Oracle 不再作为可学习潜力证据 |
| BRSR Stage 2A.1 | 独立安全动作门控 | 机械门禁通过，但相对同 29-bit 强基线显著不足，配置冻结 |
| Stage 2A.1 协议证书 | 分解反馈成本、动作评分和块级共用动作 | BlockPredicted 全部未通过；BlockOracle 基于 IID 样本事后拼块，不构成共享协议证据，改判 `NO-GO-PROTOCOL-IID-BLOCK` |
| BRSR 可观测性证书 | 用动作前信息学习每个动作的条件风险，并与同29-bit静态前沿独立比较 | 弱排序关系存在，但HGB/Ridge均显著弱于静态强基线；正式=`HOLD_PROXY_MISMATCH` |
| BRSR共享状态StateOracle证书 | 用独立planning包估计共享状态下各动作的条件期望损失，再在独立evaluation包上与同总bit静态前沿比较 | 正式=`HOLD_POWER_STATE_HEADROOM`：S1有小幅、跨seed一致但CI未闭合的点收益；S2均值收益由少数状态主导且不稳；均未批准ObservablePolicy |
| BRSR S1确认性精度复验 | 冻结S1/L16和全部算法，在独立calibration states选择最小收敛planning包数，再用48个新状态确认效应 | 正式=`NO_GO_ROUTE_PLANNING_NONCONVERGENCE`：256包参考充分，但32/64/128包均未通过等价门禁；48状态确认未执行，当前共享状态路线终止 |
| BRSR转折区频谱异质性证书 | 将主问题限定到`0/5 dB`模糊度转折区，并以块相干公共陷波构造可观测频谱异质性；StateOracle与知道同一陷波类别的ContextStatic按同总bit比较 | planning交叉拟合通过，但同总bit主效应和S2减S1归因CI均跨0；正式=`NO_GO_TRANSITION_STATE_HEADROOM`。5 dB单点正效应只作新假设线索 |
| SITQ-TDOA Stage A | 固定6个频点和24 bit，以精确量化似然直接设计TDOA后验贝叶斯风险；与MSE量化、条件互信息量化共享频点、码率和解码器 | 正式=`HOLD_STAGE_A_UNCERTAIN`：SITQ相对CondMI点改善为负、CI跨0且3个seed均负；当前单阈值配置冻结 |
| SITQ-TDOA Stage A.1 | 只重建design集合的逐轨迹、逐阈值风险面，采用轨迹分组bootstrap和六折交叉冻结检验Bayes-risk阈值能否稳定优于CondMI阈值 | 正式=`HOLD_SELECTION_UNCERTAIN`：交叉冻结改善=`0.0138 sample²`且CI跨0，两个design seed方向相反；不批准使用新evaluation确认 |
| RAPTQ-TDOA Stage 0 | 将B站复频谱按公共相位群作用分解为径向量、复投影以及进一步有损的相对幅度/相位统计量；连续均匀增益相位解析边缘化，先比较未量化统计量的TDOA Bayes风险 | 无损商表示成立；解析负对照、排序和完整相对相位收敛通过，但稀疏幅相/相位最大风险误差仍为`0.0867/0.3964 sample²`，正式=`HOLD_NUMERICAL_CONVERGENCE`，不得进入24 bit |
| RAPTQ稀疏表示积分器认证 | 以无冗余stick-breaking和严格双proposal重新认证`SparsePhase/SparseAmpPhase`诱导似然，不修改统计量或历史Stage 0结果 | 机械证书通过；`SparseAmpPhase`开发/复核最大风险差=`0.05483/0.01568 sample²`，正式=`CERTIFIED`；`SparsePhase`开发最大差=`0.12030 sample²`，正式=`HOLD_COMPUTABILITY_DEVELOPMENT`。这是表示级数值结论，不是RAPTQ性能GO |
| RAPTQ SparseAmpPhase 36-context纠正性重算 | 将已认证积分器扩展到原全部36个development context；固定双proposal、双QMC seed family和似然层等权合并，检查局部认证能否外推到全部历史样本 | 34/36个context通过；两个10 dB context风险跨度=`0.09126/0.08228 sample²`越线，正式=`HOLD_COMPUTABILITY_36_CONTEXT`。描述性全SNR/5--10 dB风险超额=`2.2951/6.7693 sample²`，说明中高SNR信息损失结构未改变，但仍非独立确认 |
| RAPTQ RelativePhase 24-bit最终周期 | 将完整5维相对相位按固定`5/5/5/5/4 bit`区间量化，与连续公共相位、相同6频点和相同24 bit的`CondMI-Matched24`比较；先确认连续headroom再执行有限码率阶段 | 公平性和数值门禁通过；连续相对相位相对CondMI的风险改善=`0.14670 sample²`、95% CI=`[-0.06890,0.38179]`，正式=`HOLD_RELATIVE_PHASE_HEADROOM`，24-bit阶段按协议未执行。当前RAPTQ有限码率路线冻结 |
| NP-NBQ-DPD Stage 0 | 将Jiang式混合量化、几何结构直接定位和任务型Bayes风险设计迁移到被动TDOA网络总预算场景；在3节点、2频点、离散ROI和有限未知源谱/复增益下，穷举全部严格24-bit节点--频点--位宽分配 | 正式development完整且概率/公平性证书通过；`JointRisk`相对最强冻结基线MSE改善=`30.5877 m²`、95% CI=`[-16.6939,79.0925]`，相对两个单因素基线的CI也跨0；赢家bootstrap频率仅`19.10%--68.20%`。正式=`HOLD_STRUCTURE_EFFECT`，不扩展urban8或复杂信道 |

## 4. 当前科学结论

1. 事后全知 Oracle 的优势混合了可预测结构和不可预测的噪声偶然性，不能单独证明候选动作空间存在可学习收益。
2. 逐样本 SafeGate 和“块内平均预测风险最小”的 BlockPredicted 已有充分负证据，不应继续在原 development 上调阈值。
3. Stage 1 RiskGreedy 的正收益说明动作前物理风险含有弱信息，但能否在严格 29 bit 下稳定超过静态强基线仍未确认。
4. 单一均值改善不足以形成路线结论；必须同时检查配对或轨迹聚类置信区间、样本量或功效、逐 SNR 稳健性、重尾和门槛敏感性。
5. IID 样本没有块内共享状态，因此历史 BlockOracle 不能支持块级协议；若未来重启 Block，必须先建立真实共享源、信道或慢变状态，并分离选动作与评动作数据。
6. 可观测性证书表明，当前动作前信息主要能排除明显差动作，却不能稳定解析固定动作附近的细微价值差；按期望平方误差回归后取argmin还会放大乐观预测误差和重尾损失。
7. 在严格29 bit下，5 bit反馈挤占前向载荷；只有动作选择足够准确才能补偿该成本。当前HGB/Ridge没有达到这一要求，且失败由3个独立test seed和严格负CI共同支持。
8. 主动选频只有在动作价值随可重复的共享物理状态变化时才有充分必要性。新的StateOracle证书先检验这一“状态余量”，并严格分离planning与evaluation；它不是可部署算法，也不使用未来噪声或动作结果。
9. 正式证书表明S1比S2更接近可复验状态：S1的`L=16`点改善跨3个seed均为正，但12个独立状态的CI略跨0；S2虽有更大均值改善，却由单个近时延边界状态强烈主导，仅6/12个状态相对SNR-only为正。
10. 32个planning包仍不足以稳定冻结状态动作：S1/S2分半动作一致率约15%--17%，因此当前`HOLD`不仅是样本功效问题，也包含条件动作估计不稳定。
11. 一次性精度复验进一步表明：128个planning包虽把相对256包参考的平均regret降至`0.01325 sample²`，但95% CI上界仍为`0.09234`且seed间方向不稳；在预注册5%--20%容差范围内均不等价。此前S1小幅正效应不能转化为计算可行、可重复的状态动作选择，因此当前共享状态BRSR路线按门禁终止。
12. 新转折区路线不是缩小SNR后选择性报告：`0/5 dB`是预注册主效应区，`-10/15 dB`继续作为负对照；真正新增的信息结构是块内稳定的公共频谱陷波，而不是放宽通信预算或读取未来结果。
13. 新路线先做StateOracle可行性证书而非直接训练策略。若独立planning仍不能稳定识别状态动作，或S2相对S1的差分收益不显著，则应停止该信息结构，不再用阈值调参挽救。
14. 转折区正式结果表明，状态动作在planning中确实可辨识，但“可辨识”不等于“足以补偿反馈和静态时间共享资源”。同总bit主效应只有`2.22%` RMSE点改善且CI跨0，24个状态仅11个为正。
15. 5 dB下目标方法相对ContextStatic-rate-matched有约`4.50%` RMSE点改善，事后分层CI为正；0 dB则典型误差和`>2 sample`比例略差。该差异支持“阈值区可能更窄”的探索性解释，但不能事后替换0/5 dB合并门禁。
16. S2相对S1的差分收益不稳定，移除贡献最大的seed后点差分转负。因此当前证据不能把收益因果归于公共频谱陷波，块内频谱异质性信息结构尚未获得独立支持。
17. SITQ绕开BRSR最薄弱的逐样本动作预测：量化阈值仅在design集合离线冻结，部署时没有动作选择和反馈。Stage A先在严格匹配的有限TDOA模型中隔离验证“任务风险量化”本身；连续TDOA、BPSK-RRC波形和模型失配必须留到后续独立阶段，不能混入当前机制门禁。
18. SITQ正式结果的后验风险与经验MSE比接近1，90%覆盖率约93%，说明量化似然和概率模型工作正常。失败来自设计收益没有跨集合兑现，而不是基础概率链路错误。
19. Bayes-risk阈值`0.4414`在design上比CondMI阈值`0.4917`低`0.0711 sample²`后验风险，但evaluation中后验风险和经验MSE方向均反转。单一全局阈值的设计自由度有限，且CondMI与Bayes-risk在当前近似单峰后验中高度接近。
20. SITQ与CondMI的典型样本几乎相同，净负收益由0/5 dB少数灾难性错峰主导；因此不能用总体NLL略优或15 dB点优势替代平方误差主门禁。
21. Stage A.1进一步说明，原阈值差异不是稳定的design结构：固定原阈值时风险改善CI跨0，交叉冻结效应只有`0.0138 sample²`，小于近似95% MDE=`0.1005 sample²`，且两个design seed方向相反。
22. 六折交叉冻结中有四折的Bayes-risk和CondMI目标选择同一阈值；48个未见轨迹中32个收益严格为0。当前单一全局对称阈值的自由度不足以稳定区分两个设计目标，但该结果不否定逐频点阈值、有限bit分配或向量量化。
23. RAPTQ不是直接把SITQ阈值改成另一组参数，而是先重新界定压缩对象：在连续未知公共增益相位下，完整复观测对公共相位旋转的TDOA后验不变，因此`radius + rank-one projector`是候选无损商表示；但`projector only`是否安全不能仅凭群不变性推出。
24. 对循环频域相移模型，时延算子是酉的，径向量通常不携带时延；对有限窗口线性时延，算子可能非酉，观测能量会随时延变化。新路线显式保留并审计该差异，避免把模型匹配结论错误外推到真实窗口。
25. RelativePhase、RelativePower和稀疏幅相都是真正有损统计量，其诱导似然需要对被删除坐标积分。当前实现使用显式复极坐标与scrambled Sobol QMC，并输出独立高预算参考误差；在数值误差未证明足够小时，不允许比较方法优劣或进入有限bit设计。
26. RAPTQ正式Stage 0-T证书通过：任意公共相位旋转和`radius + projector`重构引起的后验TV约为`1e-14`，有限窗口测试仍保持相位不变但径向跨度为`0.3851`，说明“去公共相位”成立，而“无条件去幅度”不成立。
27. 36个context的初步结果中，`ProjectorOnly`相对完整观测的平均后验风险超额为`-0.1727 sample²`，95% CI=`[-0.9705,0.3814]`；完整相对相位为`0.0071`，95% CI=`[-1.4237,1.5600]`。两者均未检出稳定信息损失，但只有6个独立trajectory cluster，不能主张统计等价。
28. 相对幅度的平均风险约维持在先验方差`22 sample²`，说明匹配循环相移模型中时延主要由跨频相位关系承载。该表示在10/15 dB的QMC误差最大，定量曲线必须由解析负对照替代后再解释。
29. 稀疏相位和稀疏幅相相对完整观测的平均风险超额分别约`2.519/2.339 sample²`，现有bootstrap CI为正；但最大QMC TV分别约`0.370/0.187`，且样本仅6个cluster，因此当前只能视为“稀疏化可能造成明显损失”的线索，不能形成正式比较。
30. 匹配循环相移模型中，删除全部频点相位后只保留相对功率不会保留TDOA信息；因此`RelativePower`后验可以解析写成TDOA先验，不应继续用高维QMC估计。该解析负对照既减少数值维度，也能暴露实现中任何虚假的幅度时延信息。
31. 全局公共相位是群作用下的无信息坐标，已从`ProjectorOnly`、相对相位和稀疏表示的QMC中解析删除；这属于模型降维，不是通过增加积分点掩盖收敛问题。
32. 观测幅度条件重要性提案只改变积分效率，完整混合提案密度仍进入重要性权重；其结果必须与独立模型提案和更高预算参考一致，不能被当作部署时可用的额外侧信息。
33. 正式数值证书表明，原Stage 0的最大风险差从`7.7878`降至本证书最高预算的`0.3964 sample²`，且全部目标表示的误差随预算增加而下降；原异常主要是无信息维度和低效积分，而非商空间理论错误。
34. 完整相对相位已达到主数值分辨率：防御性提案最大风险差=`0.00231 sample²`；模型提案仍为`0.04526`但也低于单网格平方。防御性提案的收益属于积分效率，不是算法性能。
35. 稀疏幅相只略高于单网格平方门槛并通过双网格敏感性，稀疏相位在双网格下仍失败；因此不能把两者合并称为“已收敛”，也不能因逐SNR排序一致而跳过绝对误差门禁。
36. 以独立参考得到的12-context平均风险结构为`Full < ProjectorOnly < RelativePhase < SparseAmpPhase < SparsePhase < RelativePower`。这是稳定的机制线索，但样本数和当前门禁只支持数值排序，不支持统计等价或有限bit优势主张。
37. 无冗余stick-breaking和完整混合密度校正使`SparseAmpPhase`在预注册最高预算下通过两阶段内部的双proposal风险一致性，说明此前`0.08674 sample²`的阻塞至少部分来自积分坐标与提案效率，而不是统计量本身不可计算；但同一困难context的防御性proposal跨两套QMC种子仍出现`0.07055 sample²`差异，后续必须继续报告数值不确定性。
38. `SparsePhase`仍未通过，且高SNR权重退化更严重；失败表示不得因另一表示通过而删除，也不应继续通过增加预算或调整混合比追逐门槛。
39. `SparseAmpPhase`认证只说明其连续诱导似然在当前协议下可数值计算。是否保留足够TDOA信息、是否相对模型匹配的24 bit CondMI存在headroom、以及实际量化后能否获益，仍是后续独立问题。
40. 36-context纠正性重算暴露了局部认证覆盖不足：原12-context只含每个seed的trial 0，扩展后新增发现seed 4302 trial 2的10 dB数值失败；因此表示级`CERTIFIED`不能外推为全样本可计算性。
41. 两个越线context的TV和后验均值差很小，而后验风险差超过门槛，说明主峰位置基本稳定、尾部方差仍对QMC采样敏感。正式门禁必须HOLD，但不能把它误解为估计器完全崩溃。
42. 数值修正相对原Stage 0的平均风险变化仅`-0.0443 sample²`，没有推翻稀疏幅相在5--15 dB丢失信息的定性结论。特别是5 dB的6个context全部通过数值门禁，但平均风险超额约`10.0184 sample²`。
43. SparseAmpPhase纠正性重算当时只削弱“两坐标稀疏幅相”分支，并据此开放了完整`RelativePhase`最终周期；该后续周期现已完成，其正式冻结结论见下一条。
44. RAPTQ最终周期进一步表明，连续相对相位相对模型匹配CondMI的平均headroom虽为正，但置信区间跨0且trajectory方向仅一半为正；因此继续调相位码本缺少稳定的信息余量依据，路线按预注册协议冻结。
45. NP-NBQ-DPD改变了研究对象：不再单独压缩某一路波形或TDOA统计量，而是在统一直接位置后验下联合决定“哪个节点的哪个频点使用多少bit”，同时显式边缘化未知源谱与频带内常复增益。
46. Stage 0采用有限干扰先验和离散ROI不是最终应用模型，而是为了让全部336个合法分配可穷举，使`JointRisk`的结构余量、单因素贡献和Fisher代理排序可以被直接证伪；若该精确小模型都没有余量，不应进入urban8启发式优化。
47. Jiang 2026只作为混合位宽和几何结构直接定位的迁移来源。当前`JiangStylePresetHybrid`不复现其能量观测、阈值FIM和GA-PDR，正式论文中必须继续称为设计原则基线；所有主结论由最强合法基线兜底，避免以弱迁移基线制造收益。
48. 固定四个profile不是从未知profile总体随机抽样，因此正式总体效应采用profile内bootstrap和profile间等权汇总，不能让重采样随机改变各条件权重。逐profile效应用于阻止总体均值掩盖非平衡场景中的明确回归。
49. matched-Bayes模型下，平均后验位置风险应与后验均值估计器的经验MSE一致。该校准身份及90%可信集覆盖率是似然链路证书；当前`[0.5,2]`比值范围只用于异常审查，不承担方法性能门禁。
50. design从336个方案中选择一个赢家会产生有限样本argmin不稳定。赢家bootstrap频率、top-k风险间隔和逐cell位宽频率用于区分“稳定单一结构”与“多个近等价优良方案”，但不得据此重新选择evaluation方案。
51. NP-NBQ-DPD正式结果证明基础概率链路可信：全部32个method×profile的风险/MSE比位于约`0.812--1.240`，90%可信集覆盖率位于`0.875--0.975`，没有校准警告。当前HOLD不是似然、预算或后端错误造成的。
52. `JointRisk`总体RMSE比最强冻结基线低约`1.66%`，但profile分层MSE改善CI跨0且当前近似MDE超过点效应两倍；相对GeometryOnly和ReliabilityOnly也没有稳定正CI。因此只能说存在有利点估计，不能确认联合结构余量。
53. balanced和left-reliable中JointRisk与单因素方法选择同一allocation，说明联合目标在两个条件下没有新增结构；right-reliable和top-degraded虽产生不同allocation，但独立效应重尾且逐profile CI跨0。
54. design稳定性揭示了比单纯功效不足更深的限制：四个profile的冻结赢家频率仅`19.10%--68.20%`，尤其top-degraded最优/次优风险差只占最优风险约`0.23%`。当前更合理的解释是优良分配形成平坦近等价集合，而不是存在唯一稳定的联合最优结构。
55. Schur-Fisher与全局风险排序具有中等正相关，但其正式冻结分配在四个profile中均退化为EqualBit。相关性不足以保证极值区域或最终argmin正确，当前不能用Fisher代理直接替代全局风险设计。
56. 正式门禁使用经验位置平方误差是合理的最终任务指标，但只读机制补算发现JointRisk在evaluation后验Bayes风险上平均落后最强冻结基线`22.7345 m²`；right-reliable/top-degraded分别落后`62.3159/28.6220 m²`。经验MSE的有利点估计没有得到方法自身优化目标支持。
57. 理论上的总体JointRisk最优解在同一可行分配集合内必然不劣于任一单因素分配；当前方向反转不是该支配关系失效，而是32条design样本对336个候选做经验argmin引入了赢家诅咒和选择过拟合。下一步若继续，首要任务是design风险收敛和选择regret证书，而不是直接扩大evaluation。

## 5. 当前下一步

1. 冻结`NP_NBQ_STAGE0_20260819_165042_development`及其`HOLD_STRUCTURE_EFFECT`裁决，不在该结果上重选profile、基线、分配或门禁。
2. 当前不批准urban8、连续干扰参数、整数规划、多径/NLOS或locked。若继续，先另立只使用全新design随机流的风险数值收敛与选择regret证书，以高预算参考判断经验argmin能否可靠逼近总体JointRisk最优解。
3. 只有design证书通过，才允许另行审批保持模型、profile、基线和门禁不变的全新evaluation有界复验；若设计选择不收敛或复验仍不能闭合最强冻结基线及两个单因素基线的主效应，应结束当前网络联合分配路线。

## 6. 证据索引

- 当前任务与允许范围：`当前任务.md`
- 当前研究状态：`对话上下文.md`
- 门禁定义及裁决：`研究门禁注册表.md`
- 实验事实：`实验结果记录.md`
- 已执行修改：`修改记录.md`
- 完整历史总结：`文档归档/研究总结_截至2026-07-27_完整历史.md`
