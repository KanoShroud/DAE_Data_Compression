# 实验结果记录

## 2026-07-14 新增记录：独立频谱插值对齐路线

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-14 | `NewMethod-Codex` | 双站异构选频与频谱插值对齐完整合成 Monte Carlo | `standalone_frequency_alignment/run_interpolation_alignment.py` | `运行结果/频谱插值对齐/20260714_184753_411401_synthetic_full` | profile=`synthetic_full`；N=300，M=20，L=8；SNR=`2/6/10/14/18/22 dB`；重叠=`0/0.25/0.5/0.75/1`；4 类频谱；500 trials/点；连续 lag 步长 0.01；受控重叠掩码 | 60000 个基础试验、9 方法、540000 rows，零重复，理论门禁通过。五种伪补全在 48000 个有效配对试验上与 `Intersection-Continuous` 的时延逐个完全相同，MAE gain/95% CI=`0 [0,0]`。缺失 NMSE=`0 dB`、相位不可定义、缺失能量最大约 `1.64e-29`。交集 5/10/15/20 bins 的 MAE=`7.749/2.681/1.322/0.597 samples`；Oracle-Union MAE=`0.092/0.177/0.299/0.597`；Full MAE/RMSE=`0.0138/0.0226`；Zhai-Asymmetric MAE/RMSE=`0.973/4.947`，低 SNR 平滑谱存在明显长尾。 | 周期 IFFT/插值/抗镜像滤波/FFT 不能恢复原始网格上的未观测复频点，也不能改善相同连续 TDOA 估计器；性能缺口来自真实跨站相位信息缺失。该插值伪补全路线未成立并按停止规则结束。结果可作为否定性方法学证据，但不是双站独立 Zhai 与原论文非对称 Zhai 的最终比较，因为本次独立掩码使用受控随机重叠。 | `config.json`、`protocol_manifest.json`、`run.log`、`operator_diagnostics.csv`、`trial_results.csv`、`summary.csv`、`paired_comparisons.csv`、SVG、本轮 pandas 分层复算 | 高（合成机制结论） | 可用于否定插值恢复假设和方法消融；不可外推为真实部署性能 | 若写入论文，需明确理想化单时延模型、受控随机掩码、无量化 bit-rate 和运行时间不含补全前处理；不建议继续 `project_full` |
| 2026-07-14 | `NewMethod-Codex` | 受控重叠频谱对齐 smoke | `standalone_frequency_alignment/run_interpolation_alignment.py` | `运行结果/频谱插值对齐/20260714_175153_381904_smoke` | N=64，M=12，L=4；SNR=2/14 dB；重叠=0/0.5/1；2 类信号；每点 4 trials；连续时延步长 0.02 sample | 432 method rows；理论门禁全通过，算子秩=12；理想插值与零填充最大误差约 `6.1e-16`；五种伪补全与 `Intersection-Continuous` 的有效/无效状态和逐试验 TDOA 完全一致；生成缺失频点能量最大约 `2.6e-30` | 在相同原始 DFT 网格上，周期插值没有补出新的有效复频点；该 smoke 只验证理论恒等式和流程，不做方法性能排名 | `operator_diagnostics.csv`、`trial_results.csv`、`paired_comparisons.csv`、`run.log` | 高（流程） | 否 | 已被 `synthetic_full` 覆盖，仅保留为过程回归证据 |
| 2026-07-14 | `NewMethod-Codex` | 双站本地严格 Zhai smoke | 同上 | `运行结果/频谱插值对齐/20260714_175510_177406_local_zhai_smoke` | N=64，M=12；A、B 分别使用本地周期图运行严格 0-1 逐点交换；2 个 SNR、2 类信号、每点 4 trials | 两站实际重叠比例约 0.0833 至 0.9167，选频均收敛；当共同有效频点少于 2 个时统一判无效；其余试验中全部伪补全方法与交集连续估计逐试验完全一致 | 双站各自运行 Zhai 确实会产生不同掩码，但周期插值仍不能解决缺失跨站相位观测 | 逐试验 CSV、选频元数据、算子诊断 | 高（流程） | 否 | 仅在另行审批“独立/协调选频策略比较”路线时扩大样本，不再服务于插值伪补全路线 |
| 2026-07-14 | `NewMethod-Codex` | urban8/LOS 项目适配 smoke | 同上 | `运行结果/频谱插值对齐/20260714_175605_038956_project_smoke` | N=1024，M=64，L=4；现有 fixed urban8/LOS、per-observation noisy RMS；SNR=0/12 dB；每点 2 trials；lag=±48；双站本地严格 Zhai | 36 method rows；项目 TDOA 符号检查通过；算子秩=64；两站重叠比例约 0.1875 至 0.625；理想插值与零填充误差约机器精度，生成缺失能量最大约 `4.5e-28`；全部伪补全 TDOA 与交集连续估计逐试验一致 | 独立模块能读取现有项目模拟器且不影响主流程；小样本指标不作为 urban8 正式性能结论 | `run.log`、`config.json`、`protocol_manifest.json`、CSV、`plot_data.pkl`、SVG | 高（流程） | 否 | 插值机制已被完整合成实验否定，不再运行 `project_full` |

## 2026-07-11 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-11 | `NewMethod-Codex` | Shared64 独立三池 Native-Only 开发评估 | `evaluate_topk_fairness.py` | `运行结果/20260711_154042/tables/topk_fairness` | mode=`dev_native_multi_pool`；pools=`242/342/442`；SNR=`-10/-6/-2/0/2/6/12/20dB`；60 trials/pool/SNR；10 methods；OriginalTop1-WLS；不训练、不运行 Common Top-K | 14400 rows，零重复/NaN/failure。pooled all RMSE/TDOA MAE：DAE16=`37.23m/2.317`、Power64 legacy=`38.88m/3.450`、V5B=`41.39m/3.677`、PowerShared52=`41.74m/3.745`、Cao=`44.36m/3.563`、Geo64 legacy=`44.78m/3.907`、GeoShared52=`46.78m/3.964`、DFT=`51.58m/4.640`、Zhai=`57.86m/5.925`。V5B vs GeoShared52 all RMSE gain=`5.39m` CI=`1.94..9.02`，TDOA gain=`0.287` CI=`0.154..0.398`；vs PowerShared52 两项 CI 均跨零；vs Power64 TDOA gain=`-0.227` CI=`-0.375..-0.089`。 | 独立池确认 Shared64 融合明显优于 GeoShared52 和 DFT，但没有证明优于 PowerShared52，且 TDOA 显著弱于合法 Power64 单专家。当前双专家主张未通过最终 go/no-go；需先检查 pair-level 专家互补性上界，再决定 calibration 或冻结。高 SNR定位 RMSE 的少量反常波动来自病态几何长尾，不是 TDOA MAE 恶化。 | manifest、sample-level rows、pool/SNR summaries、combined/pool-wise cluster CI、FigFair1；本轮 pandas 复算 | 高（独立开发证据）；非 locked | 可用于开发消融和路线决策，不可作为最终测试 | 审批 eval-only pair-level complementarity 诊断；legacy Power64 若进入最终候选需当前协议重训复核 |
| 2026-07-11 | `NewMethod-Codex` | Shared64 native-only 公平入口 smoke | `evaluate_topk_fairness.py`；pool-wise export inline smoke | `运行结果/20260711_154042/tables/topk_fairness` | mode=`smoke`；训练 pool seed42；-10dB × 2 trials；OriginalTop1-WLS；Raw、DAE16、Power/Geo Shared52、V5B-Shared64、legacy Power/Geo64、DFT、Cao2017、Zhai；不运行 Common Top-K | 最终退出码0；10个方法均 requested=valid=2、failure=0、mean solver calls=1；生成 `FigFair1_Native_Strong_Baselines_Smoke.svg`；pool-wise comparison 临时导出20行。首次 smoke 曾因 native-only 对 Raw 写 Common evidence 触发 `KeyError: Raw`，修复后完整重跑通过。 | P0/P1 代码链路可运行，当前结果只验证加载、预算、方法覆盖、WLS和导出，不用于性能排名。独立 development 三池全量尚未运行。 | 终端日志；smoke CSV/SVG；完整性门禁；临时 pool-wise CSV 断言 | 高（代码链路） | 否 | 默认已切为 `dev_native_multi_pool`；用户在 PyCharm 直接运行 `evaluate_topk_fairness.py`，完成后回读多池 rows/CI/FigFair1 |
| 2026-07-11 | `NewMethod-Codex` | V5B-Shared64 严格 CR16 正式运行与结果审查 | `main.py`；pandas 复算；`validate_project_integrity.py` | `运行结果/20260711_154042` | profile=`compressed_tdoa_v5b_shared64_fast`；40 shared + 12 Power-only + 12 Geo-only；experts 52/52 bins；union=64 complex=128 real；6000 pairs、20 epochs；16 SNR × 200 trials；冻结 DAE 源 `20260702_000925`；OriginalTop1-WLS | 总耗时约 97.9 min。定位 RMSE low/mid/high/all：V5B=`54.20/9.55/8.78/25.95m`，Geo52=`54.70/8.86/8.22/25.77m`，Power52=`56.88/9.81/9.69/27.41m`，DAE16=`41.00/9.53/9.64/21.38m`，Cao2017=`61.25/12.38/9.36/29.39m`，DFT=`65.27/14.08/11.54/32.17m`。V5B TDOA MAE low/mid/high/all=`7.286/0.249/0.164/2.851 samples`，DAE=`4.277/0.145/0.171/1.706`；all within-1 V5B=`0.824`、DAE=`0.925`。相对旧超预算 V5-B，Shared64 all/low RMSE 分别劣化约 `2.03/4.29m`。 | 严格预算修正成功且性能仍优于多数压缩基线，说明压缩域 direct 路线未失效；但当前融合仅低 SNR略优于 Geo52，中高 SNR及总体均更差，不能把门控融合作为已验证贡献。结果使用训练同源 snapshot pool，只能作为开发证据，不能作为独立泛化或最终论文结论。 | `run.log`、`plot_data.pkl`、Fig7/8/9 与 TDOA SVG、`tables/fig9_focus_baselines.csv`、`tables/tdoa_focus_method_tdoa_metrics.csv`；本轮完整性门禁全部 PASS | 中高（同协议开发结果）；泛化可信度待独立池验证 | 可用于严格预算开发消融，不可作为最终 locked 结论 | 修正脚本默认源路径和元数据后，在独立 development pools 上复核 expert/fusion；重新生成 Shared64 top-K、公平 solver 与复杂度结果；冻结后再做 locked 多池测试 |
| 2026-07-11 | `NewMethod-Codex` | 全项目科学完整性 smoke | `validate_project_integrity.py`；`ruff`；`compileall`；模块导入；V5-A.1 loss inline smoke | 无正式结果目录；不写运行结果 | urban8 8 snapshots；fixed eval `-10/20dB × 4`；已知 7-sample 时移；geometry-oracle WLS；DAE CR4/8/16 单样本；DFT/Hadamard/PCA CR16；缓存 snapshot-pool mismatch | `ruff check *.py`、`compileall`、15 个核心模块导入均通过；完整性脚本全部 PASS：fixed geometry/label、clean waveform direction、正负 TDOA、pair seed 重现与两路差异、GCC/direct 符号、geometry WLS、CR16 预算、DAE latent、缓存隔离；V5-A.1 per-sample loss 与原 batch mean 数值等价。两次单行定位器 guard 命令因 PowerShell/Python 引号构造错误返回 SyntaxError，随后已把同一断言纳入完整性脚本并通过。 | 本轮只证明代码和科学不变量门禁可运行，不产生方法性能结论。当前旧 V5-B 的 88-bin 并集会被识别为超预算，严格 CR16 需新 Budget64 模型。 | 终端检查输出；代码审查；`validate_project_integrity.py` PASS 日志 | 高（代码/协议） | 否 | 修改严格 Budget64 后必须再次运行完整性脚本，再由用户在 PyCharm 做正式训练和评估 |

## 2026-07-09 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-10 | `NewMethod-Codex` | 公平比较复杂度基准与 V5-B 通信预算审计 | `benchmark_fairness_complexity.py`；`evaluate_topk_fairness.py --run-mode plot_existing`；预算守卫负向测试 | `运行结果/20260709_120019/tables/topk_fairness` | runtime：0 dB、10 trials、3 repeats；参数量/模型体积/传输量静态统计；旧 dev rows 配对 snapshot-cluster bootstrap；不训练模型 | DAE-CR16：约 107.45 万 trainable params，encoder 约 53.48 万，artifact 约 4.20 MB，原生总在线时间约 47.63 ms/trial。V5-B：130 trainable params，artifact 约 0.015 MB，原生总在线时间约 88.88 ms/trial；Power64/GeoHybrid64 频点交集 40、并集 88，传输 176 real，有效 CR=`11.636`，严格 CR16 预算检查失败。配对方法比较表 132 行，复杂度帕累托 SVG 导出成功。 | **关键更正：现有 V5-B/V5-B+ 并非严格 CR16。** 旧 `20260709_120019`、top-K full 和 dev_fair 中 V5-B 结果全部降级为超预算机制上界，不能进入 CR16 主排名；Top-K 共享后端机制结论仍可保留。正式 dev/mid/locked 已按设计阻断该旧方法，`plot_existing` 保留为安全重绘入口。参数量小不代表端到端更快，当前双 expert 和多 WLS 是主要运行开销。 | `fairness_model_complexity.csv`、三类 runtime CSV、`fair_topk_dev_method_comparison.csv`、solver decision JSON、FigFair1/2/3、预算守卫日志 | 高 | 仅可用于预算审计、复杂度和机制消融；不可作为严格 CR16 主结果 | 实现并重训严格共享 64-bin V5-B；从 dev_fair 起重新验证，再补 mid/locked |
| 2026-07-10 | `NewMethod-Codex` | 公平强基线 `dev_fair` 正式结果 | `evaluate_topk_fairness.py`，PyCharm 默认 `dev_fair` | `运行结果/20260709_120019/tables/topk_fairness/fair_topk_dev_*` | seed=42；SNR=`-10/-8/-6/-4/0/8dB`；30 trials/SNR；180 samples；10 methods；OriginalTop1 + CommonTop1/GeoBeam/ScreenRefit；GeoHybrid 仅原生 Top1；不训练神经网络 | 退出码 0、耗时 46.6 min、所有组合 failure=0。原生 Top1 压缩方法 RMSE：DAE=57.90、Cao2017=59.55、V5B=59.93、GeoHybrid=66.20、PCA=74.71、DFT=75.42m。Common GeoBeam：DAE=43.58、V5B=49.72、Cao=51.90、PCA=58.64、DFT=61.30m。V5B GeoBeam 相对 CommonTop1 增益=11.69m，cluster CI=2.49..22.23m；ScreenRefit 增益仅1.68m且CI跨0，收益保留率约14.4%。V5B/DAE 原生 TDOA MAE=6.13/4.29 samples。 | V5B 在开发集明显优于多数传统压缩基线，但未超过 DAE，也未与 Cao 显著拉开。统一 Top-K 对 DAE、DFT、Cao、PCA 等同样有效，不能作为 V5B 独有贡献。当前开发 SNR 缺少2–6dB，且只有22个snapshot clusters；不得直接进入 locked。 | 用户完整日志；dev rows、broad leaderboard、summary、native/common alignment、solver attribution、manifest；pandas 配对和 snapshot-cluster bootstrap 复算 | 高（当前开发集） | 可作方法筛选，不是最终独立测试结果 | 补2/4/6dB dev_mid；导出跨方法 paired cluster CI；冻结 solver 后使用独立 snapshot-pool seeds 做 locked |
| 2026-07-10 | `NewMethod-Codex` | 公平 dev 结果 SVG 重绘 | `evaluate_topk_fairness.py --run-mode plot_existing` | `运行结果/20260709_120019/tables/topk_fairness/FigFair1_Native_Strong_Baselines.svg`；`FigFair2_Common_TopK_Attribution.svg` | 直接读取既有 `fair_topk_dev_rows.csv`；不训练、不重新评估；FigFair1=原生定位/TDOA 曲线；FigFair2=共同后端精度/TDOA/复杂度四子图 | 重绘退出码 0；两张 SVG 分别约 90.6KB/135.4KB；字段检查包含正确的 RMSE、TDOA MAE 和 WLS calls 坐标；渲染人工复核无重叠和图例遮挡。 | 可视化补齐且没有改变任何实验数据。FigFair1 用于判断 estimator 本体，FigFair2 用于区分共同 solver 收益与计算成本。 | 重绘日志；SVG 文本检查；PNG 临时渲染人工检查；全项目编译 | 高（展示） | 可作为开发诊断图；最终论文图需补 mid/locked 后更新 | 补 mid-SNR 后基于合并数据重绘 |
| 2026-07-10 | `NewMethod-Codex` | 统一 WLS + 公平强基线 Top-K 端到端 smoke | `evaluate_topk_fairness.py --run-mode smoke`；共享 WLS 回归；原生 score 等价断言 | `运行结果/20260709_120019/tables/topk_fairness/fair_topk_smoke_*` | `-10dB × 2 trials`；Raw、DAE-CR16、V5-B、DFT-train-power、DFT-Fisher、Cao2017、GeoHybrid-C40F24、Zhai-CRLB、Hadamard、PCA；原生 Top1 + 统一 CommonTop1/Top3；不训练神经网络；PCA 按历史 10000 noisy 样本协议重建一次并缓存 | 退出码 0；10 个方法、37 个方法×求解器组合，共 74 行且全部有效。Direct/Cao2017/Cao2020/Zhai 的 score 与原生 Top1 数值一致；GeoHybrid 的不合法全局 score 调用被阻断。ScreenRefit 不超过 2 次 WLS，GeoBeam 约 21 次；全项目 `compileall` 通过。 | 公平入口已覆盖当前主要强基线，并把原生方法排名与统一 solver 增益分开。2-trial 指标方差极大，不得用于性能排名；正式证据来自 dev_fair，locked 在 solver 决策冻结前不得运行。 | 终端输出；74-row fair CSV；broad leaderboard；native/common alignment；solver attribution；manifest；PCA cache；语义断言 | 高（流程）/低（性能） | 否 | PyCharm 直接运行默认 dev_fair（6 SNR × 30 trials）；分析并冻结唯一 common solver 后再运行 locked_holdout |
| 2026-07-10 | `NewMethod-Codex` | V5-B+ 独立峰 Top3 GeoBeam full 审计 | `diagnose_v5b_topk_wls.py`，`PYCHARM_RUN_MODE="full"` | `运行结果/20260709_120019/tables/v5b_topk_wls_*.csv` | 16 SNR × 200 trials；3200 samples、36000 pairs；Top3 独立峰；仅 `low_confidence_prior` pair 搜索；snapshot-cluster bootstrap；复杂度审计 | 退出码 0、耗时 15.3 min、TDOA preflight 全通过。low Top1/Beam pooled RMSE=`61.87/54.05m`，gain=`7.827m`，paired CI=`4.61..10.95m`，snapshot-cluster CI=`3.69..11.63m`；TDOA MAE=`6.882/3.514 samples`，cluster gain CI=`3.129..3.616`。mid/high 定位与 TDOA 完全不变。全 SNR按点平均定位 RMSE Beam=`21.515m`、DAE-CR16=`21.380m`；TDOA MAE Beam=`1.435`、DAE=`1.706 samples`。平均 WLS 次数 low/all=`11.85/5.08`。 | 方法性能条件全部通过，且 TDOA-only 证明确实减少错峰，不只是产生几何自洽解。自动总 gate 因 alignment 单项 NO-GO：20dB 一个病态 snapshot 在离线诊断中未收敛，max RMSE diff=`1.1635m`、valid count 差 1；其余 15 个 SNR diff 均小于 `0.009m`。应视为“方法条件通过、定位协议尚未锁定”，不能直接作为最终独立测试结论。 | 用户完整日志；baselines/tdoa_metrics/pair_decisions/complexity/alignment/go_no_go CSV；pandas 复算；病态 snapshot WLS 收敛复核 | 高（当前开发集） | 候选，尚不可直接作为最终论文结果 | 统一共享 WLS 数值协议；同 solver 公平消融；冻结参数后用全新 snapshot pools 多 seed 验证 |
| 2026-07-10 | `NewMethod-Codex` | Beam-selected TDOA/复杂度审计 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 10` | `运行结果/20260709_120019/tables/v5b_topk_wls_smoke_*.csv` | `-10dB`、20 trials、240 pairs；独立峰 Top3；仅低置信度 pair 参与 Beam；新增 pair decisions、TDOA-only、complexity 和 snapshot-cluster bootstrap | 退出码 0；preflight 240 pairs 全通过。定位 Top1/Beam RMSE=`93.22/70.63m`；TDOA MAE=`17.29/8.60 samples`、RMSE=`23.67/13.92 samples`；snapshot-cluster 定位 RMSE gain=`22.60m`，95% CI=`2.03..51.16m`；TDOA MAE gain=`8.68 samples`，95% CI=`7.29..10.58`；平均每样本约 21 次 WLS。 | 审计链路证明可以同时追踪定位、pairwise TDOA 和计算成本；小样本中三者语义一致。20 trials 方差大，不能作为正式方法性能或论文结果。 | 终端输出；`pair_decisions`/`tdoa_metrics`/`complexity`/`go_no_go` CSV；字段复算断言 | 高（流程）/低（性能） | 否 | PyCharm 直接运行默认 full；再据全 SNR 审计判断 V5-B+ 是否保留 |
| 2026-07-10 | `NewMethod-Codex` | 独立峰 Top-K + 低置信度 BeamWLS low_gate 正式诊断 | `diagnose_v5b_topk_wls.py`，`PYCHARM_RUN_MODE="low_gate"` | `运行结果/20260709_120019/tables/v5b_topk_wls_gate_*.csv`；新版 `v5b_pair_topk_diagnostics.csv` | 低 SNR `-10..0dB` 各 200 trials；4/12dB 各 50 trials；总 1300 samples、14630 pairs；独立峰逐峰亚采样；Beam 仅搜索 `low_confidence_prior` pair；2000 次 paired bootstrap | 运行 13.9 min、退出码 0；TDOA preflight 全通过。Top1/Beam low pooled RMSE=`61.87/54.05m`，gain=`7.827m`，95% CI=`4.608..10.952m`；median=`10.49/7.20m`，trimmed=`49.97/41.05m`，>20m=`43.2%/37.8%`，>50m=`26.8%/21.9%`，两者 failure≈`0.1%`。Top1 与 Fig9 max diff=`0.007m`、valid count 完全一致；4/12dB Beam 与 Top1 完全相同；自动 gate 全 PASS。样本级 low：改善/恶化/不变=`34.95%/18.93%/46.12%`，>20m 大改善/大恶化=`13.93%/6.26%`。 | low_gate 通过且收益同时体现在 RMSE、median、trimmed 和 outlier，较旧相邻-bin Beam 的 2.46m 增益有实质提升。收益集中在 -10/-8/-6dB，并由原有非 oracle low-confidence gate 自动关闭于中高 SNR。但当前尚未保存 Beam 最终 pair lag，不能证明 TDOA-only 同步改善；bootstrap 未按 snapshot 聚类，因此暂不作为论文最终结果。 | 用户日志；gate baselines/alignment/go_no_go/partial_rows；新版 36000-pair Top-K CSV；pandas 样本级复算；`plot_data.pkl` DAE/V5B 对比 | 高（当前 fixed eval） | 候选，需 full/审计后确认 | 补 Beam-selected TDOA 和复杂度字段；做 snapshot-cluster bootstrap；运行 full；随后多 seed 与公平 solver 消融 |
| 2026-07-10 | `NewMethod-Codex` | 独立峰 Top-K + 低置信度 BeamWLS 修正 smoke | `diagnose_v5b_topk.py --smoke`；`diagnose_v5b_topk_wls.py --smoke --progress-every 10` | `运行结果/20260709_120019/tables/*_smoke.csv` | `-10dB`、20 trials、240 LOS pairs；独立局部峰、逐峰亚采样；Beam 仅处理 `low_confidence_prior` pair；无质心回退 | Top-K：Top1/Top3/Top5 ±1=`15.8%/27.1%/34.2%`，所有样本得到 5 个独立候选，Top1 与 `estimate_lag` 最大差=0。WLS：Top1/Beam RMSE=`93.22/70.63m`，median=`66.10/40.82m`，trimmed=`87.87/60.70m`；两方法均 valid=20、failure=0；配对 RMSE gain=`22.60m`，95% bootstrap CI=`2.06..48.61m`；运行 45.4s，退出码 0。 | 修正后的候选、Beam、failure 和 CSV 统计链路可运行；20 trials 结果方差很大，脚本按设计输出 smoke `NO-GO`，不能作为正式性能证据。 | 终端输出；smoke CSV；人工断言；全项目编译 | 高（流程）/低（性能） | 否 | 先全量重跑 `diagnose_v5b_topk.py`，再运行默认 `low_gate`，按 6 个低 SNR 全量结果判定 |
| 2026-07-10 | `NewMethod-Codex` | 新版亚采样 Top-K + Top3 BeamWLS 全量诊断 | `diagnose_v5b_topk.py`；`diagnose_v5b_topk_wls.py` | `运行结果/20260709_120019/tables` | 16 SNR × 200 trials；36000 LOS pairs；Top1 使用 `estimate_lag`；Top3-BeamWLS/RejectBeamWLS；主评估 WLS 定位器 | TDOA preflight 36000 pairs 全通过，运行退出码 0、耗时 59.6 min。low-SNR 亚采样 Top1 ±1=`60.53%`、整数 Top1=`59.32%`、Top3=`71.93%`；亚采样 Top1 MAE/RMSE/median=`6.882/14.558/0.610 samples`。Top1/Top3-Beam low pooled RMSE=`61.90/59.44m`（改善 `2.46m`），mid=`9.37/9.39m`，high=`9.02/8.52m`；low median=`10.50/9.74m`。Top1 与保存 V5B 最大 SNR 点差=`2.245m`。 | 当前结果未达到 low-SNR `>=3m` go/no-go。Beam 的净收益主要来自少数离群：low 样本改善/恶化比例=`45.5%/46.6%`。Top-K 多数为同峰相邻 bin，且 failure 质心回退、Beam 状态维护及 pair 计数仍有方法学问题，因此本轮只能作为诊断证据，不能作为论文正式结果。 | 用户回传完整日志；`v5b_pair_topk_diagnostics.csv`、`v5b_topk_wls_baselines.csv`、`v5b_topk_wls_summary_by_snr_bucket.csv`、`v5b_topk_wls_alignment.csv`、样本级 partial rows；pandas 复算 | 中高（TDOA CSV 高；WLS 正式结论中等） | 否 | 修正独立峰 Top-K、Beam 状态、failure/valid_count 和方法级 pair 统计后，先做分阶段最终 go/no-go |
| 2026-07-10 | `NewMethod-Codex` | 方案A：top-K WLS 主定位器协议对齐 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 5`；全项目 `.py` 编译 | `运行结果/20260709_120019/tables` | 1 SNR(-10dB)、20 trials；`diagnose_v5b_topk_wls.py` 复用 `evaluate._localize_from_tdoa_pairs(standard)`；旧 CSV 暂无 `estimate_lag`，本次 smoke 回退 `top1_lag` | 编译通过。Smoke 输出：`[OK] TDOA preflight passed: checked 240 CSV pairs`；`[OK] All TDOA assertions passed`；Top1-WLS vs V5B-Expert64 max per-SNR RMSE diff=`0.1208 m`；无最终 error。 | 方案A代码路径可运行，且 smoke 下 Top1-WLS 已与主 Fig9 V5B 曲线基本对齐。该记录只证明协议对齐修复和脚本链路可用，不代表正式 Stage 2A 性能。 | 命令输出；`py_compile` 输出；`v5b_topk_wls_smoke_alignment.csv` | 高 | 否 | 需先重新运行 `diagnose_v5b_topk.py` 生成含 `estimate_lag` 的新版 top-K CSV，再全量运行 `diagnose_v5b_topk_wls.py` 做最终 go/no-go |
| 2026-07-10 | `NewMethod-Codex` | V5-B top-K WLS 修复后全量诊断 | `diagnose_v5b_topk_wls.py` | `运行结果/20260709_120019/tables` | 16 SNR × 200 trials；Top1-WLS、Top3-BeamWLS、Top3-RejectBeamWLS；beam_max_pairs=12、beam_passes=1、grid_size=3；使用修复后的 geometry preflight | 运行成功，退出码 0，耗时约 32.1 min。preflight checked=36000，TDOA assertions=0，centroid fallbacks=56。Top3-BeamWLS 相对 Top1-WLS：low pooled RMSE `62.15 -> 60.05m`（改善 2.10m），low per-SNR 平均 `50.92 -> 48.49m`（改善 2.42m）；mid pooled `11.69 -> 9.68m`，high pooled `10.81 -> 9.78m`；all pooled `39.05 -> 37.57m`。Top3-RejectBeamWLS：low pooled `60.44m`，但 mid/high `13.99/11.23m` 劣化。Top1-WLS vs saved V5B max diff=3.94m。 | Top3-BeamWLS 没有通过原定 go/no-go（low-SNR 改善不足 3m），但确实降低了一部分 RMSE/outlier tail。样本级看 Top3-Beam 只改善约 25.8% 样本，median/trimmed 在 mid/high 变差，说明收益主要来自少数 low-SNR 离群而非稳定提升。Top3-Reject 不建议主推。 | 用户提供完整日志；已读取 `v5b_topk_wls_baselines.csv`、`v5b_topk_wls_summary_by_snr_bucket.csv`、`v5b_topk_wls_alignment.csv`、`v5b_topk_wls_partial_rows.csv`、`v5b_topk_wls_partial_worst.csv`；pandas 分段汇总 | 高 | 可作为补充诊断；暂不建议作为主创新结果 | 若要最终否决或保留 Top3-BeamWLS，建议先做一次定位器协议对齐复核，消除脚本自带 WLS 与 Fig9 主链路的 max diff=3.94m |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS geometry 修复 smoke | `diagnose_v5b_topk_wls.py --smoke --progress-every 5` | `运行结果/20260709_120019/tables` | 1 SNR(-10dB)、20 trials；使用已生成的 `v5b_pair_topk_diagnostics.csv`；修复后 geometry reconstruction=`seeded generator`（旧 CSV 无 `snapshot_id`）；新增 WLS 前 TDOA preflight | Smoke 输出：`[OK] TDOA preflight passed: checked 240 CSV pairs`；`[OK] All TDOA assertions passed`；centroid_fallbacks=2；Top1-WLS RMSE=101.90m，Top3-BeamWLS=111.61m，Top3-RejectBeamWLS=94.84m；Top1-WLS vs V5B-Expert64 max diff=0.1208m；无最终 error。 | 修复有效：WLS 诊断脚本现在能在正式计算前确认 CSV 与 meta 一致，且 smoke 下 Top1-WLS 与原 V5B 曲线对齐。该结果只证明链路修复，不代表 Stage 2A 正式性能。 | 命令输出；读取 `v5b_topk_wls_smoke_summary_by_snr_bucket.csv` 与 `v5b_topk_wls_smoke_alignment.csv`；全入口编译输出 | 高 | 否 | 需要用户全量运行 `diagnose_v5b_topk_wls.py` 覆盖修复前错配的非 smoke CSV |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS 全量运行失败：geometry 重建错配 | `diagnose_v5b_topk_wls.py` | `运行结果/20260709_120019/tables` | 16 SNR × 200 trials；Top1-WLS、Top3-BeamWLS、Top3-RejectBeamWLS；beam_max_pairs=12、beam_passes=1、grid_size=3 | 运行完成 3200/3200 samples 后触发 `RuntimeError: TDOA consistency check FAILED: 34880 pairs have |CSV true_tdoa - meta delay_float| > 1e-4.` 小样本复核：CSV 与 `generate_urban_batch(snapshot_indices=None, seed=42)` 0 mismatch；与 WLS 当前 `seed+10000` snap_exp 方式 190/200 mismatch。 | 本次 WLS localization RMSE 无效，不能用于 Stage 2A go/no-go。错误不是 V5-B 方法失败，而是 WLS 脚本重建的 fixed eval metadata 与 pair-level top-K CSV 不一致。应先修复 geometry 重建和运行前 preflight，再重跑 WLS。 | 用户提供终端日志；本轮读取 `signal_gen.py`、`compressed_tdoa.py`、`diagnose_v5b_topk.py`、`diagnose_v5b_topk_wls.py` 并执行小样本一致性复核 | 高 | 否 | 需审批并修复脚本；修复后重新全量运行 |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K 诊断重新运行 | `diagnose_v5b_topk.py` | `运行结果/20260709_120019/tables` | 16 SNR × 200 trials；输出 pair-level top-K posterior diagnostics；加载 `V5B-Expert64` 所需 Power64/GeoHybrid64 hard-bin experts | `v5b_pair_topk_diagnostics.csv` 共 36000 rows，覆盖 16 个 SNR 和 200 trials；low SNR top1=59.3%、top3=71.9%、top5=77.4%、top5±2=81.1%；mid top1=99.7%；high top1=99.9%；oracle expert low=65.2%；low bad-pair rate=34.8%。 | 新 top-K CSV 已成功生成且字段完整，可进入 Stage 2A WLS 全量诊断。top-K 信息在低 SNR 仍有明显增益，但 oracle expert 上界有限，后续重点应验证 BeamWLS 是否能把 top-K 信息转成 RMSE 改善。 | 用户提供完整终端日志；本轮读取 CSV 行数/字段并编译相关脚本 | 高 | 可作为 Stage 2A 诊断依据；不是最终论文性能曲线 | 需运行 `diagnose_v5b_topk_wls.py`，以新 CSV 重新生成 WLS 全量结果 |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS 全量诊断结果初读（已作废） | `diagnose_v5b_topk_wls.py` | `运行结果/20260709_120019/tables` | 16 SNR × 200 trials；3 methods：Top1-WLS、Top3-BeamWLS、Top3-RejectBeamWLS；beam_max_pairs=12、beam_passes=1、grid_size=3；从 `v5b_pair_topk_diagnostics.csv` 读取 top-K candidates 并重建 fixed eval geometry | 曾读取到：low SNR RMSE Top1=60.72m、Top3-Beam=57.43m、Top3-Reject=58.26m；mid Top1=9.29m、Top3-Beam=8.21m、Top3-Reject=11.09m；high Top1=8.35m、Top3-Beam=8.00m、Top3-Reject=9.92m。 | 已确认该记录对应的 WLS CSV 存在 geometry 重建错配风险，不再作为 Stage 2A go/no-go 或论文候选结果。保留该行仅用于追踪错误来源。 | 后续 TDOA consistency error 与小样本复核 | 高 | 否 | 需用修复后的脚本全量重跑覆盖 |
| 2026-07-09 | `NewMethod-Codex` | V5-B top-K WLS smoke 验证 | `diagnose_v5b_topk_wls.py --smoke --progress-every 5` | `运行结果/20260709_120019` | 1 SNR(-10dB), 20 trials, beam_max_pairs=12, beam_passes=1, grid_size=3 | Top1-WLS RMSE=103.88m, Top3-BeamWLS=103.13m, Top3-RejectBeamWLS=93.66m; TDOA asserts 0 fail; centroid_fallbacks=1; align max diff=2.10m vs V5B-Expert64; elapsed=60s | Smoke 通过。Top3-RejectBeamWLS 在 -10dB 初步显示优于 Top1-WLS（93.66m vs 103.88m），但 20 trials 不足以做 go/no-go 判定。对齐差 2.10m（≤5m 阈值，20-trial 统计噪声范围内）。 | 命令输出、CSV | 高 | 否 | 需全量运行（200 trials）后判定 go/no-go |

## 2026-07-09 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-09 | `NewMethod-Codex` | V5-B+ top-K 诊断包代码级 smoke | `py_compile`；inline dummy expert smoke | 无正式结果目录；仅内存 mock 数据 | `V5BExpertGatedTDOAEstimator.pair_diagnostic()`；mock Power64 fav_lag=5.0、GeoHybrid64 fav_lag=-3.0；true_tdoa=5.5；top_k=5 | Smoke: `pair_diagnostic` 返回 37 个完整字段；top1_lag=5.0 hit1=True；expert weights (0.65, 0.35)；oracle_expert_hit1=True | `pair_diagnostic()` 方法和 `run_v5b_topk_diagnostics()` 函数语法正确、逻辑合理、字段完整。`diagnose_v5b_topk.py` 可编译。该记录只证明代码链路可运行，不包含正式诊断结果。 | 命令输出与代码审查 | 高 | 否 | 需要用户运行 `diagnose_v5b_topk.py` 产生正式结果后，再分析 top-K hit rate 与后续路线 |

## 2026-07-09 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-09 | `NewMethod-Codex` | V5-B expert-gated compressed-domain TDOA 正式结果审查 | `main.py`；pandas 分析 `tdoa_*_method_tdoa_metrics.csv`、`fig9_baselines.csv`、`fig9_method_diagnostics.csv` | `运行结果/20260709_120019` | profile=`compressed_tdoa_v5b_fast`；V5-A.1 训练 6000 LOS pair、20 epoch；V5-B 非 oracle posterior quality 门控融合 Power64+GeoHybrid64；冻结 Chen DAE 源 `运行结果/20260702_000925`；CR16；direct-only | 总耗时 94.2 min。训练：Power64 ValMAE 2.48→2.56（ValLoss 2.05→2.02），GeoHybrid64 MAE 2.52→2.57（ValLoss 3.00→2.35）。TDOA：V5B all SNR MAE=2.70 samples（Power64=2.78、GeoHybrid=3.07、DAE-CR16=1.71）；低 SNR V5B MAE=6.88（DAE=4.28）；高 SNR V5B MAE=0.178（DAE=0.171）。Localization：V5B all SNR mean=23.92m（Power64=26.50、GeoHybrid=25.12、DAE=21.38）；高 SNR V5B mean=8.21m 优于 DAE 9.64m（4dB: 7.84 vs 9.51；10dB: 7.44 vs 9.56）。 | V5-B expert gating 机制有效：V5B 在所有 SNR 区域优于独立 expert。高 SNR（≥4 dB）持续超越 DAE-CR16。低 SNR TDOA 仍有 ~60% 差距但优于所有其他压缩基线。ValLoss↓≠ValMAE↓ 训练瓶颈未解决。当前处于路线决策点：A）论文级收尾；B）攻克低 SNR；C）Pareto 对比框架。 | 已读取 `run.log`、`plot_data.pkl`、`tdoa_*_method_tdoa_metrics.csv`、`fig9_baselines.csv`、`fig9_method_diagnostics.csv`；pandas 全/低/高 SNR 汇总 | 高 | 可作为候选主创新结果，建议先决定路线后补充消融与多 seed 复核 | 若进入论文，需补：V5-B vs expert-only 消融、learned likelihood vs 纯 GCC 对比、posterior quality 分解；建议 3 seed 复核报告均值±标准差 |

## 2026-07-08 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-09 | `NewMethod-Codex` | `20260708_235611` TDOA-only 完整分段分析 | pandas 读取 `tdoa_method_tdoa_metrics.csv` 与 `fig9_baselines.csv` | `运行结果/20260708_235611/tables` | 不新增训练；按全 SNR、低 SNR(≤0)、中 SNR(2..6)、高 SNR(≥8)汇总 TDOA MAE/median/within-1/sidelobe，并与 localization mean/median/outlier rate 排名对照 | TDOA MAE 与 localization mean 相关性：全 SNR≈`0.925`，低 SNR≈`0.952`，中 SNR≈`0.956`，高 SNR≈`0.784`。全 SNR TDOA MAE：Raw≈`0.943`、DAE-CR16≈`1.706`、V5A1-Power64≈`2.765`、Cao2017≈`2.836`、PCA≈`3.014`、V5A1-GeoHybrid64≈`3.016`。低 SNR：DAE-CR16≈`4.277` 优于 V5A1-Power64/GeoHybrid64≈`6.892/7.775`。高 SNR：Hadamard≈`0.0587`、Zhai≈`0.0665`、GeoHybrid-B64≈`0.0761`、V5A1-GeoHybrid64≈`0.1470`、DAE-CR16≈`0.1709`。MAE≤1 sample 转折：DAE-CR16≈`-4 dB`，V5A1-Power64/GeoHybrid64≈`-2 dB`。 | 低 SNR 性能差主要不是定位器导致，而是 pairwise TDOA 估计仍落后 DAE-CR16；高 SNR 下不少 direct 方法 TDOA 已非常准，定位 mean 的差距才更可能来自 WLS/几何离群放大。V5-B 若继续，主攻方向应是低 SNR 错峰抑制和 posterior/频点/expert gating，而不是单纯定位器调参。 | pandas 输出；`tdoa_method_tdoa_metrics.csv`；`fig9_baselines.csv` | 高 | 可作为内部机制诊断；若做论文图，建议多 seed/正式重绘后再引用 | 若后续要定位样本级原因，需要新增全量 pair-level TDOA detail 或扩展 worst-localization detail |
| 2026-07-09 | `NewMethod-Codex` | `20260708_235611` TDOA-first 补导出验证 | `export_results.py`；读取新生成 CSV；pandas 分段汇总 | 输入/输出：`运行结果/20260708_235611`；新增表：`tables/tdoa_method_tdoa_metrics.csv`、`tables/tdoa_focus_method_tdoa_metrics.csv`；新增 TDOA SVG 位于 `tables/` | 不重新训练、不重新评估 waveform/localization；从既有 `plot_data.pkl` 的 `method_diagnostics` 构造 TDOA-only 视图；direct 方法优先使用 `direct_*` 指标，波形方法使用 `gcc_*` 指标，Geometry 为 0 误差 oracle | 编译通过；导出成功。CSV sanity：`Raw`/`DAE-CR16` 的 `tdoa_source=gcc`，V5A1/DFT/Cao/Zhai 等为 `direct`，`Geometry` 为 `geometry_oracle`。全 SNR TDOA MAE：Raw≈`0.943 samples`、DAE-CR16≈`1.706`、V5A1-Power64≈`2.765`、Cao2017≈`2.836`、PCA≈`3.014`、V5A1-GeoHybrid64≈`3.016`。低 SNR(≤0 dB)：Raw≈`2.466`、DAE-CR16≈`4.277`、V5A1-Power64≈`6.892`、V5A1-GeoHybrid64≈`7.775`。高 SNR(≥8 dB)：Hadamard≈`0.0587`、Zhai≈`0.0665`、GeoHybrid-B64≈`0.0761`、V5A1-GeoHybrid64≈`0.1470`、DAE-CR16≈`0.1709`。 | TDOA-first 导出链路可用，并揭示当前关键事实：V5-A.1 低 SNR 定位差不是单纯 WLS 放大，TDOA 本身仍弱于 DAE-CR16；高 SNR 下部分 direct 方法 TDOA 已很准，定位 mean 的差异更可能受 WLS/几何离群影响。 | 命令输出；读取 `tdoa_method_tdoa_metrics.csv`；pandas 分段汇总；`py_compile` 输出 | 高 | 可作为补充诊断数据；正式论文图需后续审图与必要多 seed/重跑 | 下一步建议基于 TDOA-first + localization RMSE 对照，优先判断 V5-B 是否应解决低 SNR TDOA 错峰，还是只做高 SNR/localizer robustification |
| 2026-07-09 | `NewMethod-Codex` | `20260708_235611` V5-A.1 hard-bin exact likelihood 正式结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/fig9_baselines.csv`、`tables/fig9_focus_baselines.csv`、`tables/fig9_focus_method_diagnostics.csv`、`fig9_focus_worst_localization_errors.csv`；pandas 分段汇总 | `运行结果/20260708_235611` | profile=`compressed_tdoa_v5a1_hardbin_fast`；direct-only；V5-A.1 hard-bin exact likelihood；6000 LOS pairs；20 epoch；64 complex bins；sources=`uniform/power/cao/geohybrid`；weight_mode=`combined`；冻结 Chen DAE 源目录 `运行结果/20260702_000925` | 总耗时约 91.8 min。训练：`Power64` ValLoss `2.0465->2.0204` 但 ValMAE `2.483->2.557 samples`；`GeoHybrid64` ValLoss `2.9966->2.3537` 但 ValMAE `2.543->2.566`。Fig9 focused mean RMSE 全 SNR：Raw≈15.72 m，DAE-CR16≈21.38 m，V5A1-GeoHybrid64≈24.98 m，V5A1-Power64≈26.48 m，Cao2017≈29.39 m，GeoHybrid-B64-C40F24≈31.84 m，DFT≈32.17 m，PCA≈34.84 m，Zhai≈37.21 m，Hadamard≈38.83 m，DFT-Fisher≈40.67 m，V5A1-Cao64≈40.70 m。低 SNR(≤0 dB)：DAE-CR16≈41.00 m，V5A1-GeoHybrid64≈53.00 m，V5A1-Power64≈53.12 m，V5-A.0≈90.39 m。高 SNR(≥8 dB)：V5A1-GeoHybrid64≈7.85 m，Zhai≈7.65 m，Hadamard≈7.48 m，DAE-CR16≈9.64 m。 | V5-A.1 通过“机制有效”诊断：hard-bin exact likelihood 显著优于 V5-A.0，并超过多数压缩 direct baseline；但未通过“最终创新主结果”门槛，因为全 SNR 和低 SNR 仍弱于冻结 DAE-CR16。频点集合主导性能，`Power64/GeoHybrid64` 明显优于 `Uniform64/Cao64`；posterior confidence 有一定 SNR 区分，但 single-argmax + WLS 在低 SNR 多峰场景下仍不够稳。下一步应做 V5-B：best-by-ValMAE checkpoint、统一 selected-bin cache、posterior top-K multi-hypothesis WLS、非 oracle expert gating 与 validation calibration。 | 已读取日志、pkl、CSV/tables；pandas 全/低/中/高 SNR 汇总；检查 V5-A.1 training history 和 selected-bin meta；对比 `20260708_205930` V5-A.0 | 高 | 否 | 若进入论文，需先做 V5-B 或多 seed 复核；当前结果可作为 V5-A.1 机制消融/强基线，但不宜作为最终主创新曲线 |
| 2026-07-08 | `NewMethod-Codex` | V5-A.1 hard-bin exact likelihood 代码级 smoke | `py_compile`；inline `train_v5a1_hardbin_tdoa()`；inline `run_urban_method_comparison()` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`compressed_tdoa_v5a1_hardbin_fast`；hard-bin 方法先用 `V5A1-Smoke64` uniform 64 bins；10 个 urban snapshot；96 个 LOS pair；1 epoch；batch=24；SNR smoke=`[0,20]`、4 trials | 编译通过。Smoke 输出 `[V5-A.1][V5A1-Smoke64][Epoch 001/1] ValLoss=13.3383 ValMAE=4.839 | lowMAE=9.869 highMAE=0.326`；method comparison 输出 `V5A1_SMOKE_OK 4.8387 [4, 4]`，说明 estimator 可进入 all-pair WLS 并生成 valid localization counts。 | V5-A.1 的 hard-bin exact likelihood、posterior diagnostics、direct estimator 和 Fig7/Fig8/Fig9 接线可运行。该记录只证明代码进入待运行状态，不代表正式性能；正式结论需用户完整运行 `main.py` 后读取新时间戳目录。 | 命令输出、inline smoke 输出、编译输出 | 高 | 否 | 需要用户在 PyCharm 运行完整 `main.py`；回读 `run.log`、`plot_data.pkl`、`tables/*.csv` 和 Fig7/Fig8/Fig9 后补正式结果 |
| 2026-07-08 | `NewMethod-Codex` | `20260708_205930` V5-A compressed-domain TDOA 正式结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/fig9_focus_baselines.csv`、`tables/fig9_focus_method_diagnostics.csv`、`tables/fig9_focus_worst_localization_errors.csv`；pandas 分段汇总 | `运行结果/20260708_205930` | profile=`compressed_tdoa_v5a_fast`；V5-A CR16-only direct likelihood；64 个复数压缩特征；10000 LOS pair；40 epoch；lag gate=48 samples；初始化=`wide`；冻结 Chen DAE 源目录 `运行结果/20260702_000925`；direct-only 输出 Fig7/Fig8/Fig9 | 运行耗时约 96.0 min。训练 `TrainLoss 11.2865->3.1138`，`ValLoss 10.6283->3.0750`，`ValCE 10.5161->3.0304`；但 `ValMAE 6.0813->6.2070 samples`，`Val<=1≈0.75` 基本不变，`ValAmb 0.2387->0.3633`、`ValWidth 0.0773->0.1170` 变差。Fig9 focused mean RMSE 全 SNR：Raw≈15.72 m，DAE-CR16≈21.38 m，Cao2017≈29.39 m，GeoHybrid-B64-C40F24≈31.84 m，DFT≈32.17 m，Zhai≈37.21 m，DFT-Fisher≈40.67 m，V5A-CR16≈40.69 m。低 SNR(≤0 dB) V5A mean/median/trimmed≈90.39/62.30/81.69 m，direct TDOA mean/median error≈16.90/13.37 samples；高 SNR(≥8 dB) V5A mean≈9.53 m、median≈0.719 m、trimmed≈1.115 m，direct TDOA mean≈0.173 samples、within-1=1.0。 | V5-A.0 未通过 go/no-go。代码链路可运行且高 SNR 典型样本可用，但低 SNR 成片选错峰，且训练 CE 下降没有转化为 point TDOA MAE 改善；当前 wide learnable filterbank 更像“宽带相位 likelihood”，缺少 SNR/coherence-aware reliability 与不确定性校准。下一步不应简单加 epoch，应先做 posterior calibration、softargmax/variance-weighted WLS、confidence gating、强基线频点初始化与 hard-negative/point-aligned loss。 | 已读取日志、pkl、CSV/tables；pandas 分段汇总；检查 worst localization 样本与 V5 training/filterbank summary | 高 | 否 | 适合作为 V5-A.0 失败消融/路线诊断。若继续 V5，应先审批 V5-A.1 轻量诊断与修正，不建议直接长训 |
| 2026-07-08 | `NewMethod-Codex` | `20260708_163957` FreqDAE-v4-min 正式 fast 结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/training_summary.csv`、`fig2_diagnostics.csv`、`fig9_focus_baselines.csv`、`fig9_focus_method_diagnostics.csv`；补充 inline task-head pairwise TDOA 诊断 | `运行结果/20260708_163957` | profile=`freq_task_v4_min_fast`；active model=`NestedTaskSufficientDAE`；loss=`freq_task_v4_min_pair`；CR4 supernet 训练 40 epoch，CR8/CR16 复用同一权重换 mask；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples | 训练耗时约 10.1 min，总耗时约 96.4 min。最终 Train/Val TaskSufficientFreqNMSE≈`1.00135/1.00135`，ValTotal≈`0.60625`。Fig2：CR4/8/16 complex NMSE≈`1.001-1.005`，mag NMSE≈`0.955-0.980`，PSD dB MSE≈`501-786`，三 CR 几乎重合。Fig9 focused mean RMSE：V4 CR4/8/16 全 SNR≈`20.151/20.142/20.133 m`；低 SNR(≤0 dB)≈`23.914/23.940/23.963 m`；高 SNR(≥8 dB)≈`16.626/16.575/16.525 m`。对照：Chen DAE-CR4/8/16 全 SNR≈`13.878/17.027/21.380 m`，高 SNR≈`8.044/9.160/9.643 m`。V4 GCC high-SNR mean abs TDOA error≈`0.63 samples`、within-1≈`0.79`、peak width≈`11.6 samples`，显著弱于 Chen DAE-CR4 的约 `0.03 samples`、peak width≈`3.0 samples`。补充 V4 task-head direct pairwise 诊断：20 dB CR4/8/16 MAE≈`0.544/0.542/0.552 samples`，within-1≈`0.884/0.884/0.873`，仍无 CR 区分。 | V4-min 比 v3 high-SNR mean 有小幅改善，但没有解决根因：时频域仍是假窄带/低信息量输出，CR4/8/16 几乎完全重合，task head 本身也没有形成足够强的 direct estimator。当前 waveform-decoder + nested latent + task-head 辅助路线应视为未通过 go/no-go，不建议继续围绕该路线加 epoch 或微调 loss。 | 已读取日志、pkl、CSV/tables；pandas 分段汇总；inline 批量 task-head pairwise TDOA 诊断 | 高 | 否 | 若继续创新，应跳出当前 waveform decoder 思路，优先转向 compressed-domain differentiable TDOA likelihood / learnable filterbank + uncertainty WLS，或真正 task-direct pair/set architecture；V4 可保留为失败消融 |
| 2026-07-08 | `NewMethod-Codex` | `FreqDAE-v4-min` 代码级 smoke 与 CR16 快速诊断 | `py_compile`；inline `NestedTaskSufficientDAE` forward smoke；inline CR16 2 epoch 训练 smoke；inline task-head 快速诊断 | 无正式结果目录；临时小样本 synthetic/urban pair 数据 | 默认 profile=`freq_task_v4_min_fast`；active model=`NestedTaskSufficientDAE`；loss=`freq_task_v4_min_pair`；嵌套 CR masks=`(4,8,16)`；快速训练 smoke 使用 64 samples、2 epochs；task-head 诊断使用 32 pair eval | 编译通过；`git diff --check` 无空白错误，仅 CRLF 提示。Forward smoke 输出 waveform `(3,2,1024)`、latent `(3,512)`、task logits `(2,1024)`。CR16 快速训练 smoke 输出 `V4_CR16_QUICK_OK ... 2 ...`，loss finite。CR16 task-head 快速诊断输出 CR4/8/16 mean_abs 约 `5.415/6.450/6.839 samples`，within1 约 `0.156/0.062/0.031`，mean_logvar 约 `2.727/1.403/0.723`。 | V4-min 的模型、loss、profile、训练/验证接线和 task-head 诊断链路可运行；该记录只证明代码进入待运行状态，不代表正式性能。GeoHybrid 当前冻结为 baseline/消融，不再继续调参。 | 命令输出、inline smoke 输出、CR16 快速诊断输出 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py` 生成新结果目录后，再读取 `run.log`、`plot_data.pkl`、Fig1/Fig2/Fig3/Fig9 和 tables 判断 V4 是否有效 |
| 2026-07-08 | `NewMethod-Codex` | `20260708_140117` GeoHybrid Budget64 direct-only 正式结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/fig8*_baselines.csv`、`tables/fig8*_method_diagnostics.csv`、`tables/fig8_supp_worst_localization_errors.csv`；pandas 汇总 | `运行结果/20260708_140117` | profile=`geohybrid_direct_only_eval`；`EXPERIMENT_MODE=paper_repro_eval_only`；`run_training=False`；direct-only 输出；冻结 DAE 源 `运行结果/20260702_000925`；baseline CR=16；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples；Budget64 并集 64 复频点、128 实数特征 | 总耗时约 85.2 min。OverBudget 与上一轮 `GeoHybrid v2` 完全一致，max abs diff=0。低 SNR(≤0 dB) mean RMSE：Raw≈30.04 m、DAE-CR16≈41.00 m、OverBudget≈52.33 m、B64-C48F16≈61.42 m、B64-C40F24≈67.00 m、B64-C32F32≈71.93 m、FineOnly/GeoAmbi-AML≈78.17 m。高 SNR(≥8 dB) mean RMSE：Raw≈6.70 m、Hadamard≈7.48 m、Zhai≈7.65 m、DFT-Fisher≈7.93 m、DAE-CR16≈9.64 m、B64-C40F24≈10.48 m、B64-C48F16≈10.64 m、B64-C32F32≈10.73 m、OverBudget≈10.64 m。低 SNR direct TDOA MAE：OverBudget≈6.94 samples、B64-C48F16≈8.07、B64-C40F24≈8.76、B64-C32F32≈12.54、FineOnly≈11.83；高 SNR direct TDOA MAE：FineOnly≈0.05、OverBudget≈0.057、B64-C40F24≈0.076、B64-C32F32≈0.078、B64-C48F16≈0.130。预算字段确认 B64 方法 `effective_cr=16.0/fair_cr16`，OverBudget `effective_cr≈8.26/over_budget`。 | 公平预算修正后，GeoHybrid 的低 SNR 优势明显缩水；上一轮 v2 的部分收益来自 124 个复频点的超预算冗余。Budget64 仍优于 FineOnly/GeoAmbi v1，但没有超过 Raw、DAE-CR16，也没有超过 OverBudget；高 SNR 的 TDOA 已较准，但定位 mean 仍被少数固定坏几何样本拉高。当前 Budget64 GeoHybrid 不足以作为主创新结果，可作为 fair-budget negative/diagnostic baseline。 | 已读取日志、pkl、CSV/tables；pandas 分段汇总；比较上一轮 `20260708_111614`；检查 worst localization high-SNR 样本 | 高 | 否 | 若继续该路线，只建议做一次有限机制诊断，不建议继续大量调 bin split/权重。下一步更应转向结构级 V4 或重新定义任务直达架构，并保留 Budget64 作为公平失败消融 |
| 2026-07-08 | `NewMethod-Codex` | `20260708_111614` GeoHybrid v2 完整 eval-only 结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/fig8*_baselines.csv`、`tables/fig8*_method_diagnostics.csv`；pandas 汇总 | `运行结果/20260708_111614` | profile=`geohybrid_v2_eval`；`EXPERIMENT_MODE=paper_repro_eval_only`；`run_training=False`；冻结 DAE 源 `运行结果/20260702_000925`；baseline CR=16；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples；GeoHybrid coarse=`DFT-train-power` 64 bins、fine=`DFT-GeoAmbi` 64 bins | 总耗时约 70.3 min。低 SNR(≤0 dB) mean RMSE：Raw≈30.04 m、DAE-CR16≈41.00 m、GeoHybrid≈52.33 m、GeoAmbi-AML≈78.17 m、GeoAmbi-Direct≈85.94 m、DFT-Fisher-Direct≈91.81 m；GeoHybrid 低 SNR TDOA MAE≈6.94 samples、within-1≈0.60，明显优于 GeoAmbi-AML≈11.83/0.51 与 GeoAmbi-Direct≈14.65/0.39。高 SNR(≥8 dB) mean RMSE：Raw≈6.70 m、Hadamard≈7.48 m、Zhai-CRLB≈7.65 m、DFT-Fisher-Direct≈7.93 m、DAE-CR16≈9.64 m、GeoHybrid≈10.64 m；GeoHybrid 高 SNR TDOA MAE≈0.057 samples、within-1=1.0，median≈0.35 m，但 mean 仍受定位离群/几何放大影响。`GeoHybrid-DFT-PHAT-gated` 与 GeoHybrid 基本重合。重要公平性发现：coarse/fine 频点仅重叠 4 个，并集 124 个复频点，约 248 个实数特征，超过 CR16 128 实数预算。 | GeoHybrid v2 的核心假设部分成立：coherence-aware coarse-to-fine 显著修复 GeoAmbi v1 的低 SNR 崩坏；但它仍未超过 Raw/DAE16，且 high SNR 弱于 Hadamard/Zhai/DFT-Fisher 等强基线。当前实现还存在预算不公平风险，不能直接作为 CR16 公平创新方法；应先改为固定预算的 joint bin selection 或明确按更高特征预算标注。导出方面，Fig1/Fig2/Fig3/Fig6 与上一 GeoAmbi v1 运行数据重复，CSV/MD 表成对保存造成目录臃肿。 | 已读取日志、pkl、CSV/tables；pandas 汇总；检查 SVG/CSV 哈希；检查 coarse/fine bin 并集大小 | 高 | 否 | 下一步若继续该路线，必须先修正特征预算公平性，再重跑 direct-only 精简评估；图表导出建议默认只保留 CSV 和必要 SVG，禁用大体积 Markdown 表与不变 Fig1-Fig3/Fig6 重生成 |
| 2026-07-08 | `NewMethod-Codex` | GeoHybrid v2 代码级 smoke 与 sanity | `py_compile`；inline 已知时移 sanity；inline `build_traditional_baselines` + `run_urban_method_comparison` smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`geohybrid_v2_eval`；`GeoHybridDFTTDOAEstimator` 使用 coarse=`DFT-train-power` bins、fine=`DFT-GeoAmbi` bins；CR=16；physical lag gate=48 samples；smoke SNR=`[-10,10]`、4 trials；对照 `GeoAmbi-DFT-AML` | 编译通过；`git diff --check` 无空白错误，仅 CRLF 提示；已知时移 sanity：`np.roll(x, 7)` 返回 `7.0`。小样本 smoke 输出 `GEOHYBRID_SMOKE_OK 64 64 ['Raw', 'Clean', 'Geometry', 'GeoHybrid-DFT', 'GeoHybrid-DFT-PHAT-gated', 'GeoAmbi-DFT-AML']`。smoke 中 `GeoHybrid-DFT` 诊断字段包含 `coarse_coherence/fine_coherence/coarse_fine_deviation/consistency/fine_beta/pair_weight` 等；小样本下 10 dB GeoHybrid 优于 GeoAmbi v1，但 -10 dB 仍较差。 | GeoHybrid v2 的 estimator、direct TDOA、uncertainty diagnostics、all-pair WLS、图表/导出/重绘接线可运行。该 smoke 只证明代码链路与诊断字段正确，不代表正式性能；低 SNR 是否改善需完整 `main.py` 运行后判断。 | 命令输出、inline smoke 输出、编译与 `git diff --check` 输出 | 高 | 否 | 用户需在 PyCharm 运行 `main.py`，读取新结果目录的 `run.log`、Fig8/Fig8_supp、`tables/fig8*_baselines.csv`、`tables/fig8*_method_diagnostics.csv` 和 worst localization details 后记录正式结论 |

## 2026-07-07 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-07 | `NewMethod-Codex` | `20260707_193226` GeoAmbi-DFT Direct 完整 eval-only 结果审查 | `main.py`；读取 `run.log`、`plot_data.pkl`、`tables/fig8_supp_baselines.csv/md`、`tables/fig8_supp_worst_localization_errors.csv`；pandas 汇总 | `运行结果/20260707_193226` | profile=`geoambi_direct_eval`；`EXPERIMENT_MODE=paper_repro_eval_only`；`run_training=False`；冻结 DAE 源 `运行结果/20260702_000925`；baseline CR=16；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples；强基线含 `GeoAmbi-DFT-Direct/AML/PHAT` | 运行耗时约 65.1 min。GeoAmbi 全 SNR mean RMSE：Direct≈39.08 m、AML≈36.27 m、PHAT≈47.38 m，均弱于 Raw≈15.72 m、DAE-CR16≈21.38 m、Cao2017≈29.39 m。低 SNR(≤0 dB) GeoAmbi Direct/AML/PHAT≈85.94/78.17/103.31 m，低 SNR outlier>20m 约 75.2%/67.9%/91.0%。高 SNR(≥10 dB) GeoAmbi Direct/AML/PHAT≈11.33/11.32/9.29 m；Direct/AML 高 SNR direct TDOA MAE 约 0.048/0.047 samples、within-1=1.0，但 mean localization 仍被固定坏样本拖高，outlier>20m 约 2.5%、outlier>50m 约 2.0%。GeoAmbi 静态 alias diagnostics：64 bins，负/正频各 32，physical-lag inside sidelobe≈0.188，优于 DFT-train-power 的约 0.752。 | GeoAmbi v1 的静态 ambiguity 抑制目标生效，但没有解决低 SNR 随机相位噪声；低 SNR 选频过稀疏/偏高频导致 cross-spectrum 方差巨大。高 SNR pairwise TDOA 已很准，mean RMSE 差主要来自 WLS/几何病态样本和少量系统性偏差被放大。当前纯 deterministic GeoAmbi 不能作为最终创新方法，应转向 SNR/coherence-aware 的 hybrid coarse-to-fine direct TDOA，并改进 pair uncertainty/定位离群诊断。 | 已读取日志、CSV/tables；pandas 汇总；worst localization 样本复查 | 高 | 否 | 若继续该路线，需实现 GeoAmbi v2：低 SNR 使用高功率/高相干 coarse bins，GeoAmbi 只做高置信 fine refinement；同时输出 pair-level quality 与 high-GDOP/outlier 分层分析。当前结果可作为失败消融或路线转折证据 |
| 2026-07-07 | `NewMethod-Codex` | GeoAmbi-DFT Direct 阶段 A 代码级 smoke | `py_compile`；inline `build_traditional_baselines` + `run_urban_method_comparison`；选频平衡检查 | 无正式结果目录；临时小样本 urban8 数据 | 默认 profile 已切到 `geoambi_direct_eval`；正式 PyCharm 运行将使用 `paper_repro_eval_only`、不训练；smoke 使用 `SignalSimulator(scenario_mode=urban8, urban_base_delay=16, urban_min_los=5)`、CR=16、`n_pca_samples=64/256`、`include_strong_variants=True`、physical lag gate=48 samples、direct methods=`GeoAmbi-DFT-Direct/AML` | 编译通过。`git diff --check` 无空白错误，仅 Windows CRLF 提示。Smoke 输出 `GEOAMBI_SMOKE_OK 64 ['Raw', 'Clean', 'Geometry', 'GeoAmbi-DFT-Direct', 'GeoAmbi-DFT-AML']`；小样本 0 dB 下 Raw≈0.72 m、Clean≈0.14 m、Geometry≈0、GeoAmbi-DFT-Direct≈21.26 m、GeoAmbi-DFT-AML≈12.06 m。选频平衡检查：64 个复频点中负频 32、正频 32、DC 0，bins 范围约 168..851。 | 阶段 A 的 GeoAmbi 频点选择、direct estimator 构造、WLS 评估、图表/重绘/导出/go-no-go 接线可运行。小样本数值只用于流程检查，不能作为正式性能结论；当前主视图优先采用 `GeoAmbi-DFT-AML`。 | 命令输出、inline smoke 输出、选频检查输出 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录的 Fig7/Fig8/Fig8_supp、`plot_data.pkl`、`tables/fig8*_baselines.csv` 和 alias diagnostics 后记录正式结论；若要纳入创新 Fig9，再切回创新 profile 并使用新 cache |
| 2026-07-07 | `NewMethod-Codex` | go/no-go task-head direct 诊断包正式运行 | `diagnose_go_no_go.py`；读取/导出 `go_no_go_*` CSV/MD/SVG/pkl | 输入：`运行结果/20260707_143945`；输出：`运行结果/go_no_go_20260707_183216`；smoke：`运行结果/go_no_go_smoke` | 不训练；SNR=`[-10,0,10,20]`；每 SNR 40 个 urban observation；sanity 40 observation；all-pair WLS；physical lag gate=48 samples；TaskHead-CR4/8/16 direct TDOA；对照 Raw、Chen DAE-CR4/16、Hadamard、DFT、Cao2017、Cao2020、Zhai-CRLB、Zhai phase-superposition、v3 waveform CR16 | 运行耗时约 9.4 min。TaskHead high-SNR mean RMSE：CR4≈20.18、CR8≈20.19、CR16≈20.22 m；v3 waveform CR16≈20.99 m；Hadamard≈6.68 m，Zhai-CRLB≈6.89 m，Raw≈8.06 m。TaskHead high-SNR TDOA MAE≈0.421-0.422 samples，v3 waveform≈0.466 samples。全 SNR TaskHead mean RMSE≈22.70/22.70/22.73 m，CR 无区分。Sanity CR16：normal TDOA MAE≈0.484；phase randomized≈0.891；shuffle second observation≈5.92；zero≈4.42；alternate geometry seed≈0.567。决策表：G1 FAIL、G2 WARN、G3 FAIL、fake-signal sanity PASS、quantization NOT_RUN。 | v3 task head 学到了一定信号/TDOA 信息，且不是纯几何/标签先验；但它没有明显超过 v3 waveform decoder，也没有进入 high-SNR classical direct 强基线竞争区间，且 CR 仍无区分度。当前不建议继续直接加训 v3 或马上开发 v4 waveform decoder；应转向 ambiguity/geometry-aware frequency-selection 或重新设计 task-direct 架构。 | 脚本运行输出；`go_no_go_baselines.csv`、`go_no_go_method_diagnostics.csv`、`go_no_go_sanity.csv`、`go_no_go_decision.csv/md`、`Fig_GoNoGo_TaskHead_Direct.svg` | 高 | 否 | 该结果是中等规模 go/no-go 诊断，不是完整 16 SNR × 200 trials 论文图。若后续 task-direct 通过新门槛，再补 random-label retraining、8-bit/4-bit quantization、多 geometry/channel 泛化 |
| 2026-07-07 | `NewMethod-Codex` | go/no-go 诊断脚本代码级验证 | `py_compile`；`diagnose_go_no_go.py` smoke；`git diff --check` | `运行结果/go_no_go_smoke` | 新增 `diagnose_go_no_go.py`；smoke 使用 SNR=`[-10,20]`、4 observation/SNR、4 sanity observation；加载 `20260707_143945` v3 权重与 `plot_data.pkl` | 编译通过；新增脚本无空白错误。第一次 smoke 暴露旧 `DAE` 构造函数不支持 `signal_len`，已修复为 `DAE(cr=...)`；第二次 smoke 暴露非数值诊断字段汇总问题，已修复为跳过字符串字段；最终 smoke 成功导出 baseline、diagnostic、sanity、decision 表、SVG 和 pkl。 | 诊断脚本链路可运行。Smoke 只验证流程，不用于性能结论。 | 命令输出与 smoke 目录 | 高 | 否 | 无 |
| 2026-07-07 | `NewMethod-Codex` | `20260707_143945` FreqDAE v3 nested fast 正式结果审查 | `main.py`；读取 `run.log`/`tables/training_summary.csv`/`fig2_diagnostics.csv`/`fig9_focus_baselines.csv`/`fig9_focus_method_diagnostics.csv`；pandas 汇总 | `运行结果/20260707_143945` | `freq_task_v3_fast_final`；profile=`freq_task_v3_nested_fast`；active model=`NestedFrequencyPairwiseDAE`；loss=`freq_task_nested_pair`；CR4 共享 supernet 训练 60 epoch，CR8/CR16 复用同一权重并切换 active mask；静态 baseline cache 源 `运行结果/20260706_230643` | 训练耗时约 13.7 min，总运行约 40.7 min。最终 Train/Val PairFreqNMSE≈`1.00090/1.00091`，ValTotal≈`1.08236`。Fig2：v3 三个 CR 的 complex NMSE≈`1.001-1.003`，mag NMSE≈`0.973-0.981`，PSD dB MSE≈`665-826`。Fig9 focused mean RMSE 全 SNR：FreqDAE-v3 CR4/8/16≈`21.920/22.008/22.042 m`；低 SNR(≤0 dB)约 `23.967/24.008/24.066 m`；高 SNR(≥10 dB)约 `20.072/20.233/20.241 m`。高 SNR TDOA mean abs error≈`0.413-0.415 samples`，peak width≈`9.8 samples`，sidelobe ratio≈`0.74`。 | v3 fast 没有解决时频崩坏和 CR 无区分度。它低 SNR 能压制极端错误，但机制更像宽峰 TDOA 模板，不是 waveform reconstruction，也不是可随 SNR 提升的高精度估计。当前 v3 不宜直接加训到 200 epoch；下一步应先做 task-head direct TDOA 诊断，或转向 residual nested decoder 的 waveform v4。 | 已读取日志、CSV/tables；pandas 汇总；与冻结 Chen DAE/Raw 指标对比 | 高 | 否 | 若用于论文，只能作为失败消融或路线转折证据。需要补 task-head direct 诊断确认 latent task head 是否有效；如继续 waveform 链路，需新 v4 结构而非继续当前 v3 加训 |
| 2026-07-07 | `NewMethod-Codex` | FreqDAE v3 nested supernet 代码级验证 | `py_compile`；inline `NestedFrequencyPairwiseDAE` forward/task-head smoke；inline urban8 `train_with_cv` 1 epoch smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 新默认 profile=`freq_task_v3_nested_fast`；模型=`NestedFrequencyPairwiseDAE`；loss=`freq_task_nested_pair`；smoke 使用 CR4 supernet、nested CR masks `(4,8,16)`、24 LOS-pair samples、1 epoch、batch=8；loss 权重 smoke：corr=0.05、xphase=0.02、peak=0.01、task_head=0.01 | 编译通过。Forward smoke：CR4/8/16 active mask 下 waveform 输出均为 `(2,2,1024)`，task head logits 为 `(2,1024)`。训练 smoke 成功输出 `V3_SMOKE_OK torch.Size([2, 2, 1024]) torch.Size([2, 1024]) freq_task_nested_pair 1`。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | v3 最小结构、nested latent mask、pairwise task head、urban pair 数据链路和 final-only 训练链路均可运行。该记录只证明代码链路，不代表 v3 性能有效。 | 命令输出、编译与 smoke 输出 | 高 | 否 | 需要用户在 PyCharm 完整运行默认 `main.py`，读取新结果目录后才能判断 v3 是否改善时频域重构、GCC 峰宽和 CR 区分度 |
| 2026-07-07 | `NewMethod-Codex` | 静态 baseline cache 与 reconstruction-only 诊断代码验证 | `py_compile`；inline `experiment_cache` smoke；`diagnose_reconstruction_only.py`；`git diff --check` | 缓存源：`运行结果/20260706_230643`；诊断输出：`运行结果/diag_recon_only_20260707_134404` | `main.py` 新增创新模式静态 baseline cache，默认复用 `20260706_230643` 的 Fig6-Fig8 和 `baseline_all_results`；diagnostic 使用 `FrequencyPairwiseDAE-ReconOnly`，CR4/16，1024 LOS-pair samples，12 epochs，batch=128，`corr_weight=0`、`pair_phase_weight=0`、`lambda_peak=0`、`spectral_blend=0.20` | 编译通过。缓存 smoke：`20260706_230643` 含 `fig6_results/fig7_results/fig8_results/fig8_supp_results/baseline_all_results/traditional_baseline_meta`，`baseline_all_results` 方法数=25，merge smoke 成功。Reconstruction-only 诊断：CR4 train/val PairFreqNMSE≈0.93703/0.96484，CR16≈0.99289/0.97043，总耗时≈0.8 min。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | 缓存机制可用，后续创新模式可避免重复重算不变的 Fig6-Fig8。Reconstruction-only 小诊断显示，去掉 pairwise task loss 后重构仍未快速下降到有效区间，说明 v2/v2.1 问题不只是 pairwise loss 冲突，单 waveform 频域重构结构/目标本身也偏弱。 | 命令输出、缓存读取 smoke、`diagnostic_summary.csv`、编译与 `git diff --check` 输出 | 高 | 否 | 该诊断是小样本/短训练，只用于机制判断；若用于论文，需要补正式 reconstruction-only 消融、多 seed 或更长 epoch。下一轮完整创新运行应检查日志 `[BaselineCache]` 是否成功加载 |

## 2026-07-06 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-07 | `NewMethod-Codex` | `20260706_230643` FreqDAE v2.1 正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/`tables/fig2_diagnostics.csv`/`fig9_focus_baselines.csv`/`fig9_focus_method_diagnostics.csv`/`fig9_focus_worst_localization_errors.csv`；pandas 对比 | `运行结果/20260706_230643` | `freq_task_v2_200_final`；active model=`FrequencyPairwiseDAE`；loss=`freq_task_pair`；`soft_peak_sharpness_weight=0.25`；CR4/8/16 均 final-only 200 epochs；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples；冻结 Chen-DAE 源 `运行结果/20260702_000925` | 总耗时≈194.3 min。训练最终 PairFreqNMSE train/val：CR4≈1.00047/1.00051，CR8≈1.00039/1.00037，CR16≈1.00073/1.00075。Fig9 focused 全 SNR mean RMSE：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-v2-CR4≈20.759、CR8≈18.791、CR16≈19.790 m。低 SNR(≤0 dB)：v2.1 CR4/8/16≈24.356/24.725/26.502 m；高 SNR(≥10 dB)：v2.1 CR4/8/16≈16.009/12.460/13.448 m。相对 v2：峰宽约 14→9 samples，high-SNR TDOA mean abs error 约 0.33-0.47→0.25-0.28 samples，但 complex NMSE 仍≈1.0，mag NMSE≈0.96-0.99，PSD dB MSE 很高。Fig6-Fig8 与 `20260706_190513` 公共字段 max diff=0。 | v2.1 修复了损失定义和部分峰宽问题，但未解决根因：模型仍学到窄带/低信息量假信号和宽峰 TDOA 模板，CR 无稳定区分度，高 SNR 仍弱于 Raw/Chen DAE。当前失败不是 TDOA 标签符号错误，clean pair 自检显示 peak 与几何 tdoa mean abs≈0.25 sample、within-1=1.0。应停止继续完整训练 v2 loss 微调，转向 nested latent + 双分支 v3；同时为创新迭代增加 Fig6-Fig8 静态 baseline cache/skip。 | 已读取日志、pkl、CSV/tables；pandas 汇总；与 `20260706_190513`、`20260706_103701` 对比；clean pair 自检 | 高 | 需复核 | 若用于论文，可作为 v2/v2.1 loss-level 修复失败消融；正式创新结果需 v3 结构验证。后续运行应避免重复计算未变化的 Fig6-Fig8 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE v2.1 loss 修复代码级验证 | `py_compile`；inline sanity；inline urban8 `train_with_cv` 1 epoch smoke；`git diff --check` | 无正式结果目录；临时小样本 urban8 数据 | 默认仍为 `freq_task_v2_200_final`；新增 `soft_peak_sharpness_weight=0.25`；smoke 使用 `FrequencyPairwiseDAE` CR16、`freq_task_pair`、12 个 LOS pair、2-fold、1 epoch；修正后的 `frequency_task_reconstruction_loss` | `py_compile` 全套入口通过。sanity 确认零输出的 time/complex NMSE≈1.0，cross-spectrum phase loss 与 sharpness peak loss finite。小样本训练 smoke 输出 `V2_1_SMOKE_OK freq_task_pair 0.25 2`。`git diff --check` 无空白错误，仅 Windows CRLF 提示。 | v2.1 代码链路可运行。该修复只说明损失定义和训练入口正确，尚不代表正式性能改善。由于 NMSE 定义被修正，后续 Fig1 PairFreqNMSE 数值相对 `20260706_190513` 上升是预期现象。 | 编译输出、inline sanity/smoke 输出、`git diff --check` | 高 | 否 | 需要用户完整运行 `main.py` 后读取新结果目录，确认 high-SNR TDOA floor、peak width、complex NMSE 和 CR 区分度是否改善 |
| 2026-07-06 | `NewMethod-Codex` | `20260706_190513` FreqDAE v2 pair-aware 正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/Fig2 diagnostics/Fig9 focused/tables/worst localization diagnostics | `运行结果/20260706_190513` | `freq_task_v2_200_final`；active model=`FrequencyPairwiseDAE`；loss=`freq_task_pair`；CR4/8/16 均 final-only 200 epochs；urban8 fixed eval set；all-pair WLS；physical lag gate=48 samples；冻结 Chen-DAE 源 `运行结果/20260702_000925` | 总耗时≈195.7 min。训练最终 loss 三个 CR 几乎相同：final train/val loss 约 0.600。Fig9 focused 全 SNR mean RMSE：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-v2-CR4≈20.352、FreqDAE-v2-CR8≈19.913、FreqDAE-v2-CR16≈22.474 m。低 SNR(≤0 dB)：v2 CR4/8/16≈24.828/25.708/25.858 m，优于 Raw≈30.038 与 DAE-CR16≈40.998。高 SNR(≥10 dB)：v2 CR4/8/16≈15.867/13.439/18.717 m，弱于 Raw≈6.550 与 DAE-CR4≈7.996。Fig2 诊断显示 v2 complex NMSE≈1.0、mag NMSE≈0.96-0.99、peak width 约 14 samples；高 SNR TDOA mean abs error 仍约 0.28-0.43 sample。 | v2 的低 SNR 优势真实但机制是宽峰 TDOA 模板去噪，不是高质量波形/频谱重构。高 SNR 不随 SNR 明显下降和 CR 无区分度来自固定 TDOA 误差地板、峰过宽、重构崩坏和当前损失定义问题。新增发现：训练 reconstruction NMSE 归一化会低估接近零输出的误差；cross-spectrum reliability 近似恒为 1；soft peak 不约束峰宽。 | 已读取日志、pkl、CSV/tables；pandas 汇总；与 `20260706_103701`、`20260705_202516` 对比 | 高 | 需复核 | 下一步若修 v2.1，需先统一训练/诊断 NMSE 定义并短训验证；若用于论文，只能作为失败/消融证据，不能作为最终创新结果 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE v2 pair-aware 创新链路代码级验证 | `py_compile`；inline urban8 `train_with_cv` 1 epoch smoke；inline `run_urban_method_comparison`；`export_method_outlier_details` smoke | 无正式结果目录；临时小样本 urban8 数据；临时导出目录 | 默认代码已切到 `freq_task_v2_200_final`；smoke 使用 `FrequencyPairwiseDAE` CR16、`loss_mode=freq_task_pair`、12 个 LOS pair、1 epoch、4 个 urban localization trials；定位器 `all_pair_wls`；lag gate=48 samples | `py_compile` 全套入口通过。训练 smoke 输出 `TRAIN_SMOKE freq_task_pair FrequencyPairwiseDAE 1`。小样本 method comparison 可运行并输出 `FreqDAE-v2-CR16`；导出 smoke 显示 worst localization CSV/MD 成功生成，字段包含 `pair_uav_indices`、`pair_true_tdoa_samples`、`pair_est_tdoa_samples`、`pair_tdoa_error_samples`、`pair_weight`、`pair_sidelobe_ratio`。`git diff --check` 无空白错误，仅 CRLF 提示。 | FreqDAE v2 的数据生成、训练损失、模型前向、urban localization 评估、worst TDOA-定位对应诊断和导出链路均可运行。该记录只证明代码链路，不包含正式性能结论。 | 编译输出、inline smoke 输出、导出 smoke 输出、`git diff --check` | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录后才能记录正式 Fig1-Fig9 和 RMSE 指标 |
| 2026-07-06 | `NewMethod-Codex` | `20260706_155312` FreqDAE eval-only + Fig10 localizer ablation 正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/`tables/fig9_focus_baselines.csv`/`fig10_localizer_baselines.csv`/`fig10_localizer_worst_localization_errors.csv`/`fig2_diagnostics.csv` | `运行结果/20260706_155312` | `freq_task_eval_only`；`run_training=False`；active model=`FrequencySelectiveDAE`；FreqDAE 源 `运行结果/20260706_103701`；Chen DAE 参考源 `运行结果/20260702_000925`；Fig10 high-SNR `[10,12,14,16,18,20]`；定位器 `WLS/Grid/Pruned/Grid+Pruned`；physical lag gate=48 samples | 总耗时≈116.6 min。Fig9 focused 与 `20260706_103701` 基本完全一致：全 SNR mean RMSE Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。Fig10 high-SNR mean：Raw WLS≈6.550、Pruned≈6.903；DAE-CR4 WLS≈7.996、Pruned≈7.752；FreqDAE-CR4 WLS≈9.020、Pruned≈8.626；FreqDAE-CR16 WLS≈9.611、Pruned≈9.118；Zhai WLS≈7.694、Pruned≈7.907 m。Grid 与 WLS 基本一致；Pruned 普遍使 median 变差。 | eval-only 模型复用正确，主性能未变化。Fig10 证明 high-SNR mean 离群不是简单 WLS 初值局部最优；pruning 只能小幅改善 DAE/FreqDAE 的少数 mean outlier，却损害 Raw/Zhai 和 median，不适合作为默认主定位器。worst 表显示 sample 16/26/130/168 是固定坏样本；Raw/Zhai 主要是几何/GDOP/边界解放大小 TDOA 误差，FreqDAE 还存在系统性 pairwise TDOA bias。 | 已读取日志、pkl、CSV；pandas 汇总；与 `20260706_103701` 对比 | 高 | 需复核 | 若用于论文，应把 Fig10 作为 supplement/诊断，不改变主链路；如需减少耗时，建议审批后默认关闭 Fig10，仅按需开启 |
| 2026-07-06 | `NewMethod-Codex` | FreqDAE eval-only + Fig10 localizer ablation 代码级验证 | `py_compile`；inline urban8 `run_urban_method_comparison` smoke；inline FreqDAE 权重加载；inline Fig10 plot/export smoke | 模型源 `运行结果/20260706_103701`；小样本 smoke 无正式结果目录 | 默认模式改为 `freq_task_eval_only`；FreqDAE 权重源 `20260706_103701`；Chen DAE 参考源 `20260702_000925`；新增定位器 `all_pair_wls_grid/pruned/grid_pruned`；Fig10 high-SNR localizer ablation 默认 SNR `[10,12,14,16,18,20]` | `D:\Software\anaconda3\envs\PyTorch\python.exe -m py_compile ...` 通过；`git diff --check` 无空白错误，仅 CRLF 提示；urban8 smoke：`all_pair_wls` 下 Raw≈0.29/0.23 m、DFT-smoke≈1.96/1.27 m，`all_pair_wls_grid_pruned` 下 Raw≈0.28/0.27 m、DFT-smoke≈2.11/0.90 m；增强字段如 `geometry_gdop`、`candidate_count` 有值；FreqDAE CR4/8/16 权重均可加载并 forward 输出 `(2,2,1024)`；Fig10 临时 SVG 与 `fig10_localizer_baselines.csv/md` 导出成功 | 代码已进入 PyCharm 直接运行 `main.py` 的 eval-only 待运行状态；本轮不产生正式性能曲线，只证明新 eval-only 源路径、鲁棒定位器、增强 worst diagnostics、Fig10 绘图/导出链路可运行 | 编译输出、函数级 smoke 输出、权重加载输出、临时导出输出 | 高 | 否 | 需要用户运行完整 `main.py` 生成正式 Fig10 与增强 tables，再读取 `run.log`、`plot_data.pkl`、`Fig10_Localizer_Robustness_Ablation.svg` 和 `tables/fig10_localizer_*` |
| 2026-07-06 | `NewMethod-Codex` | `20260706_103701` FreqDAE 200 epoch 完整重跑与 top-k worst diagnostics 正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/`tables/fig9_focus_baselines.csv`/`fig9_worst_localization_errors.csv`/`fig8*.csv`/`fig2_diagnostics.csv` | `运行结果/20260706_103701` | `freq_task_200_final`；active model=`FrequencySelectiveDAE`；loss=`freq_task_mse`；CR4/8/16 均 fixed final-only 200 epochs；冻结 Chen-DAE 源 `运行结果/20260702_000925`；physical lag gate=48 samples；Monte Carlo trials=200；本次生成 `*_worst_localization_errors.csv/md` | 总耗时≈219.0 min。训练最终 ValTotal：CR4≈0.294550、CR8≈0.324834、CR16≈0.390188。Fig9 focused 全 SNR mean RMSE：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。低 SNR(≤0 dB)：DAE-CR4≈23.295、Raw≈30.038、FreqDAE-CR4≈30.330、FreqDAE-CR8≈36.087、FreqDAE-CR16≈37.292、DAE-CR16≈40.998 m。高 SNR(≥10 dB)：Raw≈6.550、DAE-CR4≈7.996、DAE-CR8≈9.158、FreqDAE-CR4≈9.020、FreqDAE-CR16≈9.611、DAE-CR16≈9.661、FreqDAE-CR8≈9.794 m。Fig9 focused 与 `20260705_202516` 公共数值完全一致。 | 结果确认 FreqDAE v1 不是最终创新主线：CR16 相对 Chen DAE-CR16 略有改善，但 CR4/CR8 明显弱于 Chen DAE。新增 worst diagnostics 显示 high-SNR mean 主要由固定坏样本主导：sample 16/26/130/168 在多个 SNR 反复出现 50-90 m 定位误差。Zhai-CRLB high-SNR direct TDOA 很准（mean abs≈0.063 sample、within-1≈0.999），但 sample 168 仍约 78 m，说明主要是 WLS/几何歧义或小系统误差被几何放大。 | 已读取日志、pkl、tables；pandas 汇总；与 `20260705_202516` 对比 | 高 | 需复核 | 若用于论文，需要补充 UAV 坐标/GDOP/WLS 多初值诊断；建议新增 eval-only 模式，避免再次为诊断重训 219 min |
| 2026-07-06 | `NewMethod-Codex` | A+B 图表/离群诊断增强代码验证与 `20260705_202516` 离线重绘 | `py_compile`；inline `run_urban_method_comparison` smoke；inline top-k worst 导出 smoke；`export_results.py --no-fig6-svg`；`replot.py` | `运行结果/20260705_202516`；小样本 smoke 无正式结果目录 | Fig2 图例保持 `DAE`；Cao2017 改青色；新增 Fig9 focused 视图；method-comparison 新增 p90/p95/max/valid/failure/outlier 统计和未来 top-k worst localization details | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK [0] [6.130430977112897] 4`；导出 smoke 输出 `EXPORT_WORST_OK 1395`；旧结果目录新增/覆盖 `Fig9_Focused_DAE_CR_Comparison.svg`、`Fig9_Focused_DAE_CR_Comparison_SNR_Zooms.svg`、`tables/fig9_focus_baselines.csv/md`、`tables/fig9_focus_method_diagnostics.csv/md`；`fig9_focus_baselines.csv` 表头去重正常 | 本轮没有产生新的训练或评估性能；只证明图表 focused 视图、增强导出字段、未来 top-k 离群诊断链路可运行。旧 `plot_data.pkl` 缺少逐样本定位明细，因此不能补生成 `*_worst_localization_errors.csv` | 命令输出、旧结果目录导出文件、代码编译与 smoke | 高 | 否 | 若要获得正式离群样本表，需要重新运行 `main.py` 或 eval-only 生成带 `outlier_details` 的新 `plot_data.pkl` |
| 2026-07-06 | `NewMethod-Codex` | `20260705_202516` FreqDAE 200 epoch 公平复核正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/`tables/fig9*.csv`/`fig2_diagnostics.csv`；pandas 汇总 | `运行结果/20260705_202516` | `freq_task_200_final`；active model=`FrequencySelectiveDAE`；loss=`freq_task_mse`；CR4/8/16 均 fixed final-only 200 epochs；冻结 Chen-DAE 源 `运行结果/20260702_000925`；physical lag gate=48 samples；Fig9 含 Chen-DAE CR4/8/16 与 FreqDAE CR4/8/16 | 训练最终 ValTotal：FreqDAE CR4≈0.29455、CR8≈0.32483、CR16≈0.39019。Fig9 mean RMSE 全 SNR 均值：Raw≈15.720、DAE-CR4≈13.878、DAE-CR8≈17.027、DAE-CR16≈21.380、FreqDAE-CR4≈17.008、FreqDAE-CR8≈19.552、FreqDAE-CR16≈20.053 m。低 SNR(≤0 dB)：DAE-CR4≈23.295、Raw≈30.038、DAE-CR8≈30.157、FreqDAE-CR4≈30.330、FreqDAE-CR8≈36.087、FreqDAE-CR16≈37.292、DAE-CR16≈40.998 m。高 SNR(≥10 dB)：Raw≈6.550、DAE-CR4≈7.996、FreqDAE-CR4≈9.020、DAE-CR8≈9.158、FreqDAE-CR16≈9.611、DAE-CR16≈9.661、FreqDAE-CR8≈9.794 m。Zhai-CRLB direct TDOA mean abs error 随 SNR 单调下降到≈0.055 sample，但 localization mean RMSE 在高 SNR 从≈6.797 m 回升到≈8.866 m，median/trimmed 仍下降 | FreqDAE v1 的 200 epoch 复核未证明其优于 Chen-style DAE：同 CR 下 CR4/CR8 明显弱于旧 DAE，仅 CR16 相对旧 DAE-CR16 有全局改善。训练损失 CR 排序正确，但定位 mean RMSE 未形成稳定 CR 阶梯，说明 v1 的 soft frequency pre-emphasis + Fisher spectral NMSE 不足以成为最终创新主线。Zhai-CRLB 的高 SNR mean 回升主要是少量 WLS/几何/partial-Fourier 离群点主导，不是 direct TDOA 精度随 SNR 变差 | 已读取日志、pkl、fig9/fig2 表格并与 `20260705_165958`、`20260702_000925` 对比 | 高 | 需复核 | 若用于论文，需要补充 outlier-rate/top-k worst diagnostics、修正 Cao2017 颜色与 Fig2/Fig9 语义，并转向 FreqDAE v2 或嵌套 latent/task branch 创新方案 |

## 2026-07-05 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-05 | `NewMethod-Codex` | FreqDAE 200 epoch 公平复核入口与 Fig9 全 CR 对照代码级验证 | `py_compile`；inline `FrequencySelectiveDAE` forward；inline Fig9/zoom plotting smoke | 无正式结果目录；仅内存随机张量与虚拟 method_data | 新默认 `USER_EXPERIMENT_MODE="freq_task_200_final"`；Fig9 方法集合含 Raw、冻结 Chen-DAE CR4/8/16、FreqDAE CR4/8/16、DFT/DFT-Fisher/Cao/Zhai/Hadamard/PCA；绘图样式按方法类别分色 | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK torch.Size([2, 2, 1024]) 13`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | 代码进入待 PyCharm 运行状态；可以生成包含新旧 DAE 全 CR 对照的 Fig9，并使用 200 epoch 训练 FreqDAE。该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录确认 200 epoch FreqDAE 性能、Fig9 可读性和同 CR 对比结论 |
| 2026-07-05 | `NewMethod-Codex` | `20260705_165958` 频域任务感知创新模式 v1 正式结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/`tables/fig9*.csv`；pandas 汇总 | `运行结果/20260705_165958` | `freq_task_fast_final`；active model=`FrequencySelectiveDAE`；loss=`freq_task_mse`；CR4/8/16 均 final-only 120 epochs；冻结 Chen-DAE 源 `运行结果/20260702_000925`；physical lag gate=48 samples；Fig9 对比冻结 `DAE-CR16`、FreqDAE 与强基线 | 训练：CR4/8/16 最终 ValTotal≈0.319/0.347/0.406，CR8/16 到 120 epoch 仍下降，CR4 末期轻微波动。Fig9 mean RMSE 全 SNR 均值：Raw≈15.720、DAE-CR16≈21.380、FreqDAE-CR4≈18.049、FreqDAE-CR8≈18.757、FreqDAE-CR16≈21.566、DFT≈32.162、Hadamard≈38.783、PCA≈34.834 m；低 SNR(≤0 dB)：DAE-CR16≈40.998、FreqDAE-CR4≈32.743、FreqDAE-CR8≈34.714、FreqDAE-CR16≈37.080 m；高 SNR(≥10 dB)：DAE-CR16≈9.661、FreqDAE-CR4≈9.207、FreqDAE-CR8≈9.200、FreqDAE-CR16≈12.375 m。Fig8 与 `20260705_140948` 公共方法 max abs diff=0 | FreqDAE v1 证明了低 SNR 相对冻结 DAE-CR16 的改进，但高 SNR 重构和 FreqDAE-CR16 稳定性不足；Fig6-Fig8 不变是因为它们仍使用冻结 Chen-DAE 和未变的强基线，新增模型只进入 Fig1-Fig3 与 Fig9。120 epoch 与 Chen-DAE 的 200 epoch/CV 复现协议不完全公平，后续需做 200 epoch 同协议或更明确的 fast-screening 标注 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig9_baselines.csv`、`tables/fig9_method_diagnostics.csv`，并对比 `20260705_140948`/`20260702_000925` | 高 | 需复核 | 若用于论文，需要先把 Fig1-Fig3/Fig9 语义标注为 FreqDAE，按同等训练预算重跑创新模型，并修正 FreqDAE-CR16 高 SNR 不稳定问题 |
| 2026-07-05 | `NewMethod-Codex` | 频域任务感知创新模式代码级验证 | `py_compile`；inline `FrequencySelectiveDAE` 训练/评估 smoke；Fig9 绘图/导出 smoke | 无正式结果目录；小样本临时 urban8 数据与临时 SVG/CSV 目录 | 新模式 `freq_task_fast_final`；新模型 `FrequencySelectiveDAE`；loss=`freq_task_mse`；smoke 用 CR=16、16 个 waveform、1 epoch、2 个 urban trials；Fig9 临时数据只验证绘图/导出 | 两套 `py_compile` 通过；训练/评估 smoke 输出 `FREQ_SMOKE_OK FrequencySelectiveDAE freq_task_mse ['Raw', 'Clean', 'Geometry', 'DAE-CR16', 'FreqDAE-CR16']`；Fig9 导出 smoke 输出 `FIG9_EXPORT_SMOKE_OK`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | 新创新链路可编译、可训练、可进入同一 urban8/WLS 定位评估，并可生成 Fig9 表格/图；该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录确认 Fig9/Fig3/Fig2 正式趋势 |
| 2026-07-05 | `NewMethod-Codex` | Fig3 physical lag gate 统一代码级验证 | `py_compile`；inline `UrbanLocalizationExperiment` smoke | 无正式结果目录；小样本临时 urban8 数据 | `tdoa_lag_limit_samples=24`；2 个 SNR 点；4 trials；无 DAE 模型，仅验证 Geometry/Raw/Clean 路径和配置保存 | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK 24.0 2 [...]`；`git diff --check` 无空白错误，仅 Windows 行尾提示 | Fig3 的 `UrbanLocalizationExperiment` 已能接收并保存 lag gate，后续正式运行会与 Fig6-Fig8 使用同一 physical TDOA 搜索窗口；该记录不是正式性能指标 | 命令输出与代码审查 | 高 | 否 | 需要完整运行 `main.py`，读取新目录确认 `mc_results` 的 `localization_config.tdoa_lag_limit_samples≈48`，并比较 Fig3 CR16 与 Fig8 DAE-CR16 是否对齐 |
| 2026-07-05 | `NewMethod-Codex` | `20260705_140948` Fig8 主/补充强基线正式 eval-only 结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/CSV；`analyze_results.py` pandas 汇总 | `运行结果/20260705_140948` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；`baseline_cr=16`；physical lag gate=48 samples；Fig8 主图精简；Fig8 supplement 含 Cao2020/Zhai phase 诊断候选 | Fig8 主图全 SNR mean RMSE 均值：Raw≈15.720、DAE-CR16≈21.380、Cao2017≈29.387、DFT≈32.162、PCA≈34.834、Zhai-CRLB≈37.209、Hadamard≈38.783、DFT-Fisher-Direct≈40.662 m。Fig8 supplement：Cao2020-HighFC≈42.717 m，Zhai-Phase-Superposition≈48.462 m。相对 `20260704_195946`：Cao2020≈43.099→42.717 m；Zhai phase≈51.584→48.462 m。 | Fig8 主图口径更清晰；Cao2020 segmented 与 Zhai weighted phase 均有一定改善，但仍弱于正式强基线，适合保留在 supplement。新发现 Fig3 与 Fig6-Fig8 的 lag gate 不一致，需优先统一评估协议。 | 已读取日志、pkl、fig8/fig8_supp 表格、analysis 表；跨目录 pandas 对比 | 高 | 需复核 | 若用于论文，需要先修正 Fig3 与 Fig6-Fig8 的 physical lag gate 一致性，并重跑 eval-only 生成口径一致的新结果 |
| 2026-07-05 | `NewMethod-Codex` | Fig8 主/补充轨道增强代码级验证 | `py_compile`；inline estimator sanity；小样本 urban8 Fig8 smoke；`analyze_results.py` | 旧结果分析输入：`运行结果/20260704_195946`；小样本 smoke 无正式结果目录 | 新增 `Cao2020SegmentedFCEstimator`；`ZhaiPhaseSuperpositionEstimator(weight_mode="sqrt_power")`；Fig8 主图正式强基线，Fig8 supplement 保存 Cao2020/Zhai phase 诊断候选；`analyze_results.py` 基于 pandas 分 band 汇总 | 两套 `py_compile` 通过；四个 direct estimator 对 `np.roll(x, 7)` 均返回 `-7.0`；小样本 urban8 smoke 输出 `SMOKE_OK`；`analyze_results.py` 成功读取 `20260704_195946/plot_data.pkl` 并生成 `tables/analysis_*`；`update_dft_fisher_direct.py` 当前根目录不存在 | 新代码进入待 PyCharm 正式运行状态；本轮只验证代码链路和分析脚本，不产生新的正式 Fig8 性能结论 | 命令输出、旧结果目录 pkl 读取、生成的 analysis tables | 高 | 否 | 需要用户运行完整 `main.py`，读取新目录的 `run.log`、`plot_data.pkl`、Fig8/Fig8 supplement SVG 与 tables 后，才能记录正式指标 |

## 2026-07-04 新增记录

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-05 | `NewMethod-Codex` | `20260704_195946` Fig8 强基线正式 eval-only 结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/CSV；pandas 汇总 | `运行结果/20260704_195946` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；`baseline_cr=16`；`dft_direct_source=DFT-train-power`；physical lag gate≈48 samples；`strong_baselines=True`；`phase_superposition=True` | Fig8 全 SNR mean RMSE 均值：Raw≈15.720 m，DAE-CR16≈21.380 m，Cao2017≈29.387 m，DFT≈32.162 m，PCA≈34.834 m，Zhai-CRLB≈37.209 m，Hadamard≈38.783 m，DFT-Fisher-Direct≈40.662 m，Cao2020≈43.099 m，Zhai-Phase≈51.584 m。Cao2020 相对旧结果全 SNR 从约 92.340 m 降到约 43.099 m，高 SNR 从约 76.841 m 降到约 16.244 m。Zhai phase 高 SNR direct within-1≈0.751，弱于 Zhai-CRLB≈0.998。 | Cao2020 重做方向有效但仍是较弱 proxy；Zhai phase-superposition 当前实现不适合作为正式强基线；低 SNR 下 Raw/DAE 更稳，高 SNR 下 Zhai-CRLB、DFT-Fisher、Hadamard 的任务型 TDOA 更有优势但离群误差仍影响 mean。 | 已读取 `run.log`、`plot_data.pkl`、`fig8_baselines.csv`、`fig8_method_diagnostics.csv`，并检查 Fig8 SVG 图例 | 高 | 需复核 | 若进入论文主比较，需先审批图表口径：哪些方法作为正式强基线，哪些标注为 proxy/消融；如继续声称 Cao2020，需要实现或说明与原文 incoherent processing 的差异 |
| 2026-07-04 | `NewMethod-Codex` | Cao2020-HighFC 重做代码级验证与小样本 smoke | `py_compile`；PyTorch inline clean urban8 direct TDOA smoke；代码路径检查 | 无正式结果目录；仅小样本临时数据 | 新 `DFT-Cao2020-CRB`：单侧连续高频 FC 窗；Fisher/CRB 延迟信息 + 保留功率 + 高频偏好 + 物理 lag 窗旁瓣惩罚；baseline CR=16 | 两套 `py_compile` 通过；`git diff --check` 无空白错误；小样本 clean urban8 中旧 two-edge high-frequency 约 mean abs 4.08 samples、median 3.58、within1 0.23，新 `DFT-Cao2020-CRB` 约 mean abs 1.07 samples、median 0.14、within1 0.80；Zhai-CRLB 同一 smoke 约 mean abs 0.21、within1 0.98 | 新 Cao2020 代理相对旧最高频硬选明显改善，但仍弱于 Zhai-CRLB，符合“Cao high-FC 是更稳的高频/CRB baseline，而非理论最优选频”的定位；该记录不是正式 Fig8 结果 | 命令输出与小样本 smoke 输出 | 高 | 否 | 需要用户完整运行 `main.py`，读取新结果目录的 `tables/fig8_baselines.csv` 和 `tables/fig8_method_diagnostics.csv` 后记录正式指标 |
| 2026-07-04 | `NewMethod-Codex` | `20260704_163145` Fig8 强基线结果审查与 zoom 离线重绘 | 读取 `run.log`/`plot_data.pkl`/CSV；`replot.py`；`py_compile`；phase-superposition sanity | `运行结果/20260704_163145`；同时重绘 `运行结果/20260703_191104` 的 zoom | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；`baseline_cr=16`；`dft_direct_source=DFT-train-power`；physical lag gate≈48 samples；本次结果中 `phase_superposition=False`，代码修改后默认改为 True | Fig8 mean RMSE 全 SNR 均值：Raw≈15.720 m，DAE-CR16≈21.380 m，DFT≈32.162 m，DFT-Fisher-Direct≈40.662 m，Cao2017≈29.387 m，Cao2020-HighFC≈92.340 m，Zhai-CRLB≈37.209 m，Hadamard≈38.783 m，PCA≈34.834 m。Cao2020 高 SNR direct mean abs TDOA error≈6.410 samples、within1≈0.155；Zhai-CRLB 高 SNR direct mean abs TDOA error≈0.067 samples、within1≈0.998。离线重绘已生成 `*_SNR_Zooms.svg` 并删除旧 Low/High 单图。 | Cao2020-HighFC 当前实现不是可靠原文复现，更像高频硬选 proxy；Zhai-CRLB 部分高 SNR 优于 Raw 有任务选频解释，但低 SNR 不稳。新 phase-superposition 已通过已知时移 sanity，但尚未产生正式完整曲线。 | 已读取 `tables/fig8_baselines.csv`、`tables/fig8_method_diagnostics.csv`、`plot_data.pkl`、`run.log`；命令行重绘输出；sanity 输出 | 高 | 否 | 需要下一次完整 `main.py` eval-only 生成含 `Zhai-Phase-Superposition` 的正式 Fig8；Cao2020 需按原文校准后再用于论文主比较 |


本文件只记录已运行、已明确读取过日志/pkl/svg，或明确来自用户反馈的实验/验证/结果审查信息。不得编造不存在的指标、路径、模型权重或结论；不确定信息写“待确认”。

记录原则：

- `证据来源` 必须区分日志、pkl、svg、用户反馈、冒烟测试或历史记录摘要。
- `是否可用于论文` 用“是 / 否 / 需复核”标注；来自用户反馈或历史摘要的结果默认需复核。
- “可用于论文”不仅要求结果真实，还要求实验设置、随机种子、路径、代码版本和图表导出方式可复现。
- `复核需求` 写明是否需要重新读取原始日志、pkl、svg 或重新运行脚本。
- 纯文档维护、方案讨论、任务看板更新不属于实验结果，不写入本文件。

## 实验结果表

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-09 | `NewMethod-Codex` | V5-B expert-gated compressed-domain TDOA 代码级验证 | `py_compile`；inline dummy posterior experts smoke | 无正式结果目录；仅内存 dummy posterior | 默认 profile 改为 `compressed_tdoa_v5b_fast`；V5-B 融合 `V5A1-Power64` 与 `V5A1-GeoHybrid64`；V5-A.1 experts 使用 bucket-weighted ValMAE restore；dummy posterior 测试 top-K 与 pair weight 接口 | `py_compile` 通过；dummy smoke 返回 finite lag、positive pair weight、top-3 hypotheses 和 expert mixture quality 字段 | V5-B 代码链路可编译，estimator 接口可进入现有 direct TDOA/WLS 评估；该记录不是正式性能结果 | 命令输出与代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig9/TDOA SVG 与 CSV 后才能记录正式指标 |
| 2026-07-08 | `NewMethod-Codex` | V5-A compressed-domain TDOA 代码级 smoke | `py_compile`；inline `train_v5a_compressed_tdoa()`；inline `run_urban_method_comparison()` | 无正式结果目录；小样本临时 urban8 数据 | `compressed_tdoa_v5a_fast` 代码路径；10 个 urban snapshot；80 个 LOS pair；V5-A CR16 训练 1 epoch；lag gate=48；20 dB 小样本定位 smoke | `py_compile` 通过；V5-A smoke 训练/验证 loss finite；`ValMAE≈6.133 samples`，`Val<=1≈0.667`；`V5A-CR16` direct estimator 进入 all-pair WLS 后 20 dB valid count 为 6/6；`git diff --check` 仅 Windows CRLF 提示 | V5-A 模型、训练器、direct estimator 和定位评估链路可运行；该记录不代表正式性能，不能用于论文指标结论 | 命令输出和代码审查 | 高 | 否 | 需要用户在 PyCharm 完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig7/Fig8/Fig9 和 CSV 后再记录正式指标 |
| 2026-07-04 | `NewMethod-Codex` | Fig8 强基线代码级验证与小样本 smoke | `py_compile`；PyTorch inline smoke | 无正式结果目录；仅合成已知延迟信号与小样本 urban8 临时数据 | `USER_RUN_STRONG_BASELINES=True`；默认 `USER_STRONG_INCLUDE_PHASE_SUPERPOSITION=False`；direct estimators: Direct/Cao/Zhai-CRLB；小样本 urban8 使用 2 个 SNR、4 observation，仅验证流程 | 两套 `py_compile` 通过；Direct/Cao 对 `np.roll(x, 7)` 返回 `-7.0`；简化 phase-superposition 未稳定返回正确延迟，因此默认关闭；小样本 urban8 comparison + Fig8 plotting 输出 `FIG8_SMOKE_OK`；`git diff --check` 仅提示 Windows 行尾转换 | Fig8 强基线代码链路可编译、可构建、可绘图；尚未产生正式性能结论 | 命令输出与本轮代码审查 | 高 | 否 | 需用户在 PyCharm 完整运行 `main.py` 后读取新结果目录，才可记录正式 Fig8 指标 |
| 2026-06-23 | 待确认 | R12 Bug 修复后验证 | `main.py` | `运行结果/20260623_184731` | GCC-centric；`epsilon=0.03`；NLOS 最强径标签；CR=4/8/16 | 记录中为全 CR 21/21 优于 Raw，CR 排序 7/21 | TDOA 精度显著优于 Raw，但 CR 区分度不足 | 历史修改记录与上下文摘要 | 中 | 需复核 | 如需论文引用，应重新读取对应 `plot_data.pkl`/日志 |
| 2026-06-27 | 待确认 | R19 纯包络 NMSE | `main.py` | `运行结果/20260627_115432` | 纯包络 NMSE；GCC/Corr 主导；CR=4/8/16 | 记录中为 loss 单调下降；三种 CR 均优于 Raw；CR 无区分度；Fig2 时频域崩坏、伪峰多 | 纯包络可稳定优化 GCC，但不能恢复波形和 CR 阶梯 | 历史上下文与用户反馈 | 中 | 需复核 | 完整数值需重新读取 pkl 和日志 |
| 2026-06-29 | `NewMethod-Codex` | R20 代码级冒烟验证（前序工作） | `python -m py_compile` 与 PyTorch inline smoke | 无；合成随机/小样本 | 固定混合 NMSE；same lag GCC；低 SNR 高 Peak；CR=4/8/16 | 记录中为 `py_compile` 通过；tiny 1 epoch CV 返回新字段；train/evaluate same-window GCC 最大差约 `5.1198735e-05` | R20 代码曾通过基础语法和一致性冒烟测试，但未完整训练 | 当前对话上下文记录 | 中 | 否 | 本轮未重新运行该验证；如需引用需重新执行或查原始输出 |
| 2026-06-30 | `NewMethod-Codex` | R20 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG | `运行结果/20260629_173410` | R20 固定混合 NMSE；CR=4/8/16；标准 GCC；低 SNR 高 Peak | 用户反馈与本轮复核一致：三种 CR 均优于 Raw，但 CR 区分度不足；Fig2 时频域仍崩坏，GCC 伪峰明显减少；R20 RMSE 均值约 Raw 35.456、CR4 6.605、CR8 6.776、CR16 6.712 | R20 证明 TDOA 可优于 Raw，但未证明额外维度能改善重建或形成稳定 CR 排序 | 已读取日志、pkl、svg，并结合用户反馈 | 高 | 需复核 | 论文引用前建议再次导出关键表格并核对图表 |
| 2026-06-30 | `NewMethod-Codex` | R20.1 代码级冒烟验证 | `py_compile`；PyTorch inline smoke | 小样本合成数据，无结果目录 | `mse_weight_max/phase_mix` 解耦；`selection_start_epoch=2` smoke；Fig2 `diagnostic_trials=2` | `py_compile` 两套 Python 均通过；CPU 小样本训练、Fig2 diagnostics、Fig3 CI 绘图均通过；输出 `SMOKE_OK 2 2` | R20.1 代码可编译，关键新增字段可运行；尚未验证完整实验效果 | py_compile 与冒烟测试输出 | 高 | 否 | 未运行完整 `python main.py`，不得写完整训练结论 |
| 2026-07-01 | `NewMethod-Codex` | R20.1 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG | `运行结果/20260630_120041` | R20.1；CR=4/8/16；任务驱动 Corr/Peak + mixed NMSE；固定信道 | 用户反馈与本轮读取一致：Fig2 时频域仍崩坏，GCC 单峰改善；Fig3 全 CR 均优于 Raw 但无稳定区分度；pkl 中 CR4/8/16 平均 RMSE 约 6.96/6.55/6.46，Raw 约 35.46；clean peak 与标签一致性在原 R20.1 多径/NLOS 设置下仍是风险 | R20.1 不是可靠论文复现基线，应先建立 paper_repro 轨道 | 已读取日志、pkl、svg 并结合用户反馈 | 高 | 需复核 | 若需论文引用，应重新导出表格；当前结论主要用于指导代码路线 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v1 代码验证与数据标签自检 | `py_compile`；PyTorch inline smoke；`clean_peak_consistency` | 小样本合成数据；默认 50 固定信道自检，无完整结果目录 | 默认 `EXPERIMENT_MODE=paper_repro`；MSE-only；NLOS=0；LOS label；`multipath_scale=0.1`；训练 SNR -10..20；评估 SNR -10:2:20 | 两套 `py_compile` 通过；小样本端到端输出 `SMOKE_OK 1 paper_repro_v1 2 [-10, 0] 1.0`；默认 50 信道 clean peak 自检：match=1.0、within2=1.0、mean_abs_peak_error=0.0、mean_false_peaks_gt_05≈0.0586 | 代码进入 paper_repro 待完整运行状态；该记录不是完整训练结果 | 编译输出、inline smoke 输出、clean peak 自检输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v1 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG；轻量分析脚本 | `运行结果/20260701_122523` | 默认 `paper_repro`；MSE-only；CR=4/8/16；5-fold；10000 样本；200 epoch；SNR -10:2:20；LOS label；`multipath_scale=0.1` | CV best ValMSE 均值：CR4≈1.108、CR8≈1.302、CR16≈1.455；重构 MSE/PSD 呈 CR4 优于 CR8 优于 CR16；Monte Carlo 平均 RMSE：Raw≈15.21、CR4≈25.69、CR8≈25.73、CR16≈29.19；严格 CR4<CR8<CR16 为 15/16 个 SNR，但 Raw 在 0 dB 以上近乎 0；-10 dB 下 CR8/16 优于 Raw/CR4 | MSE/时频域恢复趋势较合理，但 TDOA/Raw/CR 排序不复现论文定位趋势；主要原因是合成 LOS-dominant 数据和 TDOA 代理指标使 Raw 高 SNR 饱和 | 已读取日志、pkl、SVG 数据；加载模型做按 SNR MSE 诊断 | 高 | 需复核 | 若继续论文复现，应实现多 UAV 几何定位/更真实数据和归一化审查 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v2_urban8 代码验证与冒烟 | `py_compile`；PyTorch inline smoke；clean peak/定位轻量检查 | 小样本合成 urban8 数据；无完整结果目录 | 默认 `paper_repro` 已切换为 `paper_repro_v2_urban8`；8 UAV；单目标；建筑物遮挡；oracle LOS；路径损耗；分数时延；per-observation RMS；snapshot GroupKFold；localization RMSE meters | 两套 `py_compile` 通过；小样本端到端输出 `SMOKE_OK paper_repro_v2_urban8 2 localization_m [-10, 0] 1.0`；默认 50 snapshot clean peak 自检 exact=0.5625、within1=1.0、within2=1.0、mean_abs_peak_error=0.4375；R20.1 旧路径输出 `TASK_SMOKE_OK R20.1 1` | 代码进入 `paper_repro_v2_urban8` 待完整运行状态；该记录不是完整训练结果，不能据此判断论文趋势 | 编译输出、inline smoke 输出、轻量自检输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v2_urban8 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG；轻量 pkl 分析脚本 | `运行结果/20260701_150204` | `paper_repro_v2_urban8`；MSE-only；8 UAV urban8；snapshot GroupKFold；200 max epochs；patience=10；localization RMSE meters；200 Monte Carlo trials | CV best/stop epoch：CR4≈31-32/41-42，CR8≈17-22/27-32，CR16≈15-18/25-28；mean RMSE 平均值 Clean≈20.07、Raw≈28.43、CR4≈33.27、CR8≈48.04、CR16≈55.48 m；median RMSE 平均值 Clean≈0.235、Raw≈5.39、CR4≈9.76、CR8≈14.97、CR16≈28.95 m；Clean max error 多个 SNR 约 145 m，说明 mean 被少数定位失败主导；20 dB Fig2 diagnostics：CR4/8/16 peak within2≈1.0/1.0/0.859，sidelobe ratio≈0.259/0.607/0.936 | v2 重构质量明显改善，但 Fig3 mean 不适合作为论文结论；主要问题是固定评估集缺失、定位求解异常值、单参考 TDOA、动态多径、clean RMS 归一化泄漏、训练含 NLOS 而评估只用 LOS | 已读取日志、pkl、SVG 对应数据并结合用户反馈 | 高 | 需复核 | 建议先修评估和数据协议，形成 `paper_repro_v3_urban8_fair_eval` 后再完整运行 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v3_urban8_fair_eval 代码验证与冒烟 | `py_compile`；PyTorch inline smoke | 小样本合成 urban8 数据；无完整结果目录 | v3：固定 snapshot channel；`urban_base_delay=16`；`urban_min_los=5`；`per_observation_noisy_rms`；LOS-only 训练；fixed eval set；all-pair WLS；fixed epoch；final retrain | 两套 `py_compile` 通过；默认 50 snapshot 生成成功，LOS count min/mean/max=5/5.24/7，clean peak within2=1.0；小样本 WLS 中 Geometry oracle 在 -10/0/20 dB 均为 0，Clean 均约 0.16 m；1 epoch CV + final retrain 输出 `TRAIN_OK paper_repro_v3_urban8_fair_eval 1 True`；Fig2 固定 demo 输出相同 true TDOA `[-8, -8]` | v3 代码进入待完整运行状态；该记录不是完整训练结果，不能据此判断论文趋势 | 编译输出、inline smoke 输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v3 训练协议调整冒烟 | `py_compile`；PyTorch inline smoke | 小样本合成 urban8 数据；无完整结果目录 | `training_protocol=cv_fixed200_best_fold`；`early_stopping=False`；`restore_best=True`；`final_retrain=False`；smoke 使用 2-fold × 2 epoch | 两套 `py_compile` 通过；冒烟输出 `PROTOCOL_SMOKE cv_fixed200_best_fold [2, 2] False`，确认每折跑满指定 epoch、无 final retrain | 代码进入更省时的 v3 待运行状态；完整 paper_repro 将是 5 fold × 200 epoch/CR，不再额外全量 200 epoch | 编译输出、inline smoke 输出 | 高 | 否 | 下一步需完整运行 `python main.py` 并读取新结果目录 |
| 2026-07-01 | `NewMethod-Codex` | paper_repro_v3_urban8_fair_eval 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG | `运行结果/20260701_185455` | `paper_repro_v3_urban8_fair_eval`；MSE-only；固定 evaluation set；fixed snapshot channel；all-pair WLS；noisy RMS；LOS-only 训练；5-fold×200 epoch；best-fold 模型 | 总耗时约 219.6 min；CR4/8/16 best epoch 分别约 56.6/18.0/14.2；Clean mean≈5.10 m、median≈0.083 m、trimmed≈0.238 m，geometry oracle≈0；Raw mean 均值≈20.00 m，CR4/8/16 mean 均值≈22.46/36.45/44.63 m；CR 排序 14/16 个 SNR 基本正确，但 CR4 仅 2/16 个 SNR 的 mean 优于 Raw；Fig2 高 SNR 下 CR4 peak within2=1.0 且 sidelobe ratio≈0.23，CR8/16 旁瓣更高 | v3 评估协议基本可信，已排除 v2 的主要评估错误；但 MSE-only DAE 仍未复现论文中压缩数据优于 Raw 的趋势。根因更可能是当前合成 LOS/oracle 评估使 Raw baseline 过强、训练协议过拟合且 best-fold 未用全训练集、MSE 重构造成带宽/相位损伤 | 已读取日志、pkl、SVG 并结合用户反馈 | 高 | 需复核 | 下一步建议审批 `CV 选 epoch + 全训练集 final train`、训练数据增强、论文式 LOS/NLOS/基线补全；暂不建议继续在 paper_repro 中加入任务 loss |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_v3.1 代码验证与冒烟 | `py_compile`；PyTorch inline smoke；Fig3 plotting smoke | 小样本合成 urban8 数据；无完整结果目录 | `training_protocol=cv_best_epoch_final_train`；`final_epoch_policy=median_cv_best_epoch`；训练 waveform 每 1 epoch 重采样的 smoke；默认代码配置为每 5 epoch 重采样；Fig3 默认不显示 CI 填充 | 两套 `py_compile` 均通过；训练协议冒烟输出 `SMOKE_PROTOCOL cv_best_epoch_final_train median_cv_best_epoch 2 2`；Fig3 绘图冒烟输出 `PLOT_SMOKE [15.0, 4.8] 3` | v3.1 代码可编译，CV 后全训练集 final train、固定计划重采样和 Fig3 版式调整均可运行；尚未验证完整训练效果 | 编译输出、inline smoke 输出、绘图 smoke 输出 | 高 | 否 | 下一步需运行完整 `python main.py` 并读取新结果目录；不得据此写完整实验结论 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_v3.1 完整结果审查 | 读取 `run.log`/`plot_data.pkl`/SVG | `运行结果/20260702_000925` | `paper_repro_v3_1_epoch_final_train`；MSE-only；固定 evaluation set；fixed snapshot channel；all-pair WLS；noisy RMS；LOS-only 训练；5-fold×200 epoch；全训练集 final train；训练 waveform 每 5 epoch 重采样 | 总耗时约 563.4 min；CV best epoch CR4/8/16 均接近 200，中位数均为 197；mean RMSE 平均值 Raw≈20.00、CR4≈17.84、CR8≈21.17、CR16≈27.76 m；-10 dB mean：Raw≈113.24、CR4≈104.72、CR8≈110.54、CR16≈118.61 m；20 dB mean：Raw≈5.87、CR4≈7.84、CR8≈9.15、CR16≈9.74 m；median/trimmed 下 CR4<CR8<CR16 为 16/16 个 SNR；Geometry oracle≈0，Clean mean≈5.10 m、median≈0.083 m、trimmed≈0.238 m | v3.1 显著优于 v3，CR4 低 SNR 已优于 Raw，CR8 部分低 SNR 接近/优于 Raw，CR16 仍受容量限制；当前结果已足够作为合成近似论文复现基线，但不是严格 Chen 2025 Wireless InSite 复现 | 已读取日志、pkl、SVG 尺寸并结合用户反馈 | 高 | 需复核 | 若论文引用，建议导出正式表格并说明合成数据、oracle LOS、WLS、归一化和缺少 Wireless InSite 的差异；不建议为追求曲线完全一致继续盲目调 loss |
| 2026-07-02 | `NewMethod-Codex` | v3.1 结果导出 | `export_results.py` | `运行结果/20260702_000925/tables` | 读取冻结 v3.1 `plot_data.pkl`；导出 CSV/Markdown 表格、正文 mean RMSE 图、补充 median/trimmed 图和 summary | 成功生成 `training_summary`、`rmse_mean/median/trimmed`、`fig2_diagnostics`、`Fig3_Main_Mean_RMSE.svg`、`Fig3_Supp_Median_Trimmed.svg`、`paper_repro_v3_1_summary.md` | 当前 v3.1 结果已固化为可查表格和论文候选图；该记录不包含新训练 | 脚本输出和导出文件检查 | 高 | 需复核 | 论文正式引用前建议人工核对表格格式、caption 和单位 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_fast_final 小样本冒烟 | `main.py` | `运行结果/20260702_140552` | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_fast_final`、`DAE_N_SAMPLES=32`、`DAE_FAST_FINAL_EPOCHS=1`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_NO_SHOW=1` | 三个 CR 均完成 1 epoch final-only 训练、模型保存、UrbanLocalizationSweep、Fig1/Fig2/Fig3 和 pkl 保存；总耗时约 0.4 min | fast_final 代码路径可运行；这是流程冒烟，不可用于结果结论 | 冒烟 run.log 输出 | 高 | 否 | 若要得到可用 fast_final 结果，需使用默认 197 epoch 或指定合理 epoch 后完整运行 |
| 2026-07-02 | `NewMethod-Codex` | paper_repro_eval_only 小样本冒烟 | `main.py` | `运行结果/20260702_140637` | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_eval_only`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_NO_SHOW=1`；模型源 `运行结果/20260702_000925` | 成功加载 `model_cr4/8/16.pt` 与源 `plot_data.pkl`，完成小样本 UrbanLocalizationSweep、Fig1/Fig2/Fig3 和 pkl 保存；总耗时约 0.3 min | eval_only 代码路径可运行；适合后续快速重评估/出图，不重新训练 | 冒烟 run.log 输出 | 高 | 否 | 默认完整 eval_only 仍会按配置运行 200 trials；可用环境变量降低试跑成本 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 传统基线代码验证与小样本冒烟 | `main.py`、`export_results.py` | `运行结果/20260702_154414` | 环境变量：`DAE_EXPERIMENT_MODE=paper_repro_eval_only`、`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1`；模型源 `运行结果/20260702_000925`；baseline CR=16 | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；小样本运行成功生成 `Fig6_Traditional_Baselines_CR16.svg` 和 `plot_data.pkl`；pkl 中 Fig6 方法顺序为 `Raw/Clean/Geometry/DFT/Hadamard/PCA/DAE-CR16`；baseline 元信息显示 latent real dim=128、DFT complex bins=64、Hadamard rows/channel=64、PCA actual components=31；`export_results.py` 成功导出 `fig6_traditional_baselines.csv/md` 和 Fig6 SVG | Fig6 代码链路可运行，已进入待正式完整 eval-only 运行状态；本记录是小样本流程验证，不可用于论文指标结论 | 编译输出、run.log、plot_data.pkl 字段检查、导出脚本输出 | 高 | 否 | 正式 Fig6 需用完整 `paper_repro_eval_only` 配置运行，PCA 样本数不应使用 32 |
| 2026-07-02 | `NewMethod-Codex` | PyCharm 默认模式修复小样本冒烟 | `main.py` | `运行结果/20260702_161432` | 显式移除 `DAE_EXPERIMENT_MODE`；保留小样本覆盖：`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1` | 两套 `py_compile` 通过；运行日志显示 `EXPERIMENT_MODE=paper_repro_eval_only (source=main.py:DEFAULT_EXPERIMENT_MODE)`、`K=0`、`Epochs=0`、`run_training=False`、`model_source_dir=运行结果/20260702_000925`；成功加载冻结 `model_cr4/8/16.pt` 并生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl` | 证明不依赖外部 PowerShell 环境变量时，`main.py` 默认进入 eval-only，不会重新训练；本记录不是正式 Fig6 结果 | 编译输出与冒烟 run.log | 高 | 否 | 正式运行需移除小样本覆盖，只保留默认 `paper_repro_eval_only` |
| 2026-07-02 | `NewMethod-Codex` | Fig6 传统基线正式 eval-only 结果审查 | `main.py` | `运行结果/20260702_162659` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；PCA noisy waveform 训练样本 10000；方法为 Raw/Clean/Geometry/DFT/Hadamard/PCA/DAE-CR16 | Fig6 mean RMSE 平均值：Raw≈20.00、Clean≈5.10、Geometry≈0、DAE-CR16≈27.76、DFT≈35.48、Hadamard≈42.17、PCA≈45.99 m；低 SNR(-10..-4)均值：Raw≈57.82、DAE≈82.45、DFT≈98.90、Hadamard≈102.59、PCA≈107.80；高 SNR(10..20)均值：Raw≈6.54、DAE≈9.67、Hadamard≈9.84、DFT≈10.11、PCA≈18.01 | 与 Chen Fig.6 不一致：当前 DFT 使用中心频带保留，明显受益于基带限带信号；Hadamard/PCA 均通过统一重构+GCC/WLS 链路，性能随 SNR 改善；PCA 虽整体最差且有高 SNR 误差地板，但不像论文中几乎不随 SNR 降低。该结果适合暴露 baseline 实现假设，尚不宜直接作为“严格论文 Fig6 复现”结论 | 已读取 `run.log` 和 `plot_data.pkl`，并结合用户对 Fig6 的反馈 | 高 | 需复核 | 下一步应审批并实现 Fig6 baseline fidelity variants：非 oracle DFT、Salari/随机 Hadamard、PCA 训练/归一化消融和每方法 GCC/TDOA 诊断 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 baseline variants 默认链路小样本冒烟 | `main.py`、`export_results.py` | `运行结果/20260702_195744` | 无 `DAE_EXPERIMENT_MODE`；小样本覆盖：`DAE_N_SAMPLES=32`、`DAE_MONTE_CARLO_TRIALS=4`、`DAE_FIG2_DIAGNOSTIC_TRIALS=2`、`DAE_BASELINE_PCA_SAMPLES=32`、`DAE_NO_SHOW=1`；默认 `dft_mode=uniform`、`hadamard_mode=salari`、`pca_source=noisy` | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；运行日志显示 `EXPERIMENT_MODE=paper_repro_eval_only`、`run_training=False`；成功生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl`；pkl 方法顺序为 `Raw/Clean/Geometry/DFT/Hadamard/PCA/DAE-CR16`，诊断字段包含 `complex_nmse`、`psd_db_mse`、`gcc_mean_abs_tdoa_error_samples` 等 9 项；首次误用 `export_results.py --result-dir ...` 失败，报错 `FileNotFoundError: --result-dir\plot_data.pkl`，随后用位置参数重跑成功生成 `fig6_method_diagnostics.csv/md` | 新版默认 Fig6 代码链路可运行，PyCharm 直接运行仍不会训练；该记录是小样本流程验证，指标不可用于论文结论 | 编译输出、run.log、plot_data.pkl 字段检查、导出脚本输出和失败重跑记录 | 高 | 否 | 正式结果需移除小样本覆盖，在 PyCharm 直接运行完整 `paper_repro_eval_only` |
| 2026-07-02 | `NewMethod-Codex` | Fig6 diagnostic variants 小样本冒烟 | `main.py` | `运行结果/20260702_195918` | 无 `DAE_EXPERIMENT_MODE`；小样本覆盖：`DAE_N_SAMPLES=16`、`DAE_MONTE_CARLO_TRIALS=2`、`DAE_FIG2_DIAGNOSTIC_TRIALS=1`、`DAE_BASELINE_PCA_SAMPLES=16`、`DAE_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=1`、`DAE_NO_SHOW=1` | 运行成功，日志显示 baseline 元信息 `include_diagnostic_variants=True`，方法列表含 `DFT`、`Hadamard`、`DFT-bandlimited`、`DFT-random`、`Hadamard-random`、`PCA`、`DAE-CR16`；生成 Fig1/Fig2/Fig3/Fig6 和 `plot_data.pkl` | 证明额外 DFT/Hadamard 变体链路可运行；该记录只验证流程，不用于论文指标结论 | 冒烟 run.log 输出 | 高 | 否 | 若正式运行额外变体，需显式设置 `DAE_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=1` 并使用完整 PCA 样本数 |
| 2026-07-02 | `NewMethod-Codex` | `main.py` 用户变量配置入口编译验证 | `py_compile`、`git diff --check` | 无新结果目录 | `ALLOW_ENV_OVERRIDES=False`；`USER_EXPERIMENT_MODE="paper_repro_eval_only"`；`USER_BASELINE_INCLUDE_DIAGNOSTIC_VARIANTS=True`；常用配置从环境变量入口改为 `main.py` 顶部 `USER_*` 普通变量 | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；未运行完整 eval-only 或训练 | 配置入口改造语法通过；该记录不包含任何实验指标结论 | 编译输出与静态检查输出 | 高 | 否 | 下一步需用户在 PyCharm 直接运行 `main.py`，读取新结果目录后再记录正式 Fig6 指标 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 baseline variants 正式 eval-only 结果审查 | `main.py` | `运行结果/20260702_203157` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；PCA noisy waveform 训练样本 10000；`dft_mode=uniform`；`hadamard_mode=salari`；`include_diagnostic_variants=True` | mean RMSE 平均值：Raw≈20.00、Clean≈5.10、Geometry≈0、DAE-CR16≈27.76、DFT-uniform≈47.79、Hadamard≈42.17、DFT-bandlimited≈35.48、DFT-random≈44.46、Hadamard-random≈88.23、PCA≈45.99 m；高 SNR(10..20)均值：DFT-random≈8.43、DAE≈9.67、Hadamard≈9.84、DFT-bandlimited≈10.11、DFT-uniform≈14.84、PCA≈18.01、Hadamard-random≈72.85 m；运行耗时约 37.2 min | `DFT-bandlimited` 复现了上一次中心频带 DFT，说明其优势主要来自已知有效频带；`DFT-uniform/random` 排序随 SNR 交替，反映频点选择强敏感；`Hadamard-random` 高 SNR 仍大误差，说明随机行破坏结构性 TDOA 保真；Hadamard Salari 与 PCA 与上一次一致；结果仍不宜作为严格 Chen Fig.6 复现，只能作为当前合成数据下的 baseline 消融 | 已读取 `run.log` 和 `plot_data.pkl`，并结合用户反馈；未自动导出 `tables/` | 高 | 需复核 | 下一步建议审批：主图/补充图分离、自动导出 tables、DFT 训练集功率选频、Hadamard row-offset/sequency 消融、多随机 seed 平均和诊断指标修正 |
| 2026-07-02 | `NewMethod-Codex` | Fig6 baseline v2 代码验证与导出补齐 | `py_compile`、inline smoke、`export_results.py` | 函数级小样本；导出目标 `运行结果/20260702_203157/tables` | 新版默认主 DFT 为 `DFT-train-power`；补充变体含 `DFT-bandlimited/DFT-uniform/DFT-random*`、`Hadamard-random*/sequency/offset`；随机 seed 数 5；主/补充 Fig6 分离；新增 active PSD、linear PSD NMSE、weighted TDOA error 等诊断 | 两套 `py_compile` 通过；函数级 smoke 成功构建 `DFT-train-power`、`Hadamard-offset-64` 和随机变体，运行两点 SNR 小样本 Fig6 并生成主/补充绘图对象；`export_results.py --result-dir 运行结果/20260702_203157` 成功生成 `Fig6_Traditional_Baselines_CR16.svg`、`Fig6_Supp_Baseline_Ablation_CR16.svg`、`fig6_traditional_baselines.csv/md`、`fig6_method_diagnostics.csv/md` | 新版 Fig6 v2 代码链路可编译并通过函数级验证；既有正式结果目录的 tables 已补齐，但新版 `DFT-train-power` 仍需重新完整 eval-only 才有正式指标 | 编译输出、inline smoke 输出、导出脚本输出 | 高 | 否 | 下一步需在 PyCharm 直接运行 `main.py`，读取新目录后记录正式 Fig6 v2 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v2 正式 eval-only 结果审查 | `main.py`、`py_compile` | `运行结果/20260702_220605` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；`DFT-train-power`；Hadamard Salari 投影重构；PCA noisy waveform 训练样本 10000；diagnostic variants 开启；随机 DFT/Hadamard seed 数 5；自动导出 `tables/` | 总耗时约 75.7 min；mean RMSE 全 SNR 平均：Raw≈20.00、Clean≈5.10、Geometry≈0、DAE-CR16≈27.76、DFT-train-power≈36.79、Hadamard≈42.17、PCA≈45.99 m；低 SNR(-10..-4)均值：Raw≈57.82、DAE≈82.45、DFT-train-power≈103.77、Hadamard≈102.59、PCA≈107.80 m；高 SNR(10..20)均值：Raw≈6.54、DAE≈9.67、Hadamard≈9.84、DFT-train-power≈11.22、PCA≈18.01 m；DFT-bandlimited mean≈35.48 m，略优于 DFT-train-power 的 mean 但 median/trimmed 较差；Hadamard-random 高 SNR 均值约 82.96 m，sequency 均值约 64.27 m，offset-64 均值约 44.27 m | DAE-CR16 是本次合成 urban8 协议下综合最好的压缩方法，但 Raw 仍整体最强；`DFT-train-power` 降低典型误差但 top-k 离散频点引入离群，Hadamard 行选择极敏感，PCA 保谱不保 TDOA；该结果可作为 baseline 消融与方法诊断，不应声称严格复现 Chen Fig.6 | 已读取 `run.log`、`plot_data.pkl`、SVG 和 `tables/`，并审查当前代码；`py_compile` 通过 | 高 | 需复核 | 下一步建议审批 DFT 连续训练功率频带、随机组统计导出、Fig6 高 SNR zoom 和诊断耗时优化；若用于论文，需说明合成数据、oracle LOS、WLS 和传统 baseline 实现假设 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v3 代码验证与函数级 smoke | `py_compile`、inline Python smoke、`git diff --check` | 函数级小样本；未生成正式结果目录 | 默认 `USER_BASELINE_DFT_MODE="train_band"`；`DFT-train-band` 为训练集平均功率连续频带；`DFT-train-power` 为 top-k 离散频点补充；Salari Hadamard 主图为 Lemma 1 block-1，补充 `Hadamard-block-2`；Fig6 主图高 SNR inset；自动 export 默认 `export_fig6_figures=False` | `py_compile` 通过；inline smoke 成功构建 `DFT-train-band`、`DFT-train-power`、`Hadamard-block-2`，绘图检查到 Fig6 主图含 inset child axis，导出检查到 `fig6_traditional_baselines.csv` 与 `fig6_random_group_summary.csv` 生成且未生成重复 Fig6 SVG；smoke 输出 `SMOKE_OK DFT-train-band contiguous_power_band 1`；`git diff --check` 无空白错误，仅提示 Windows 行尾转换 | Fig6 v3 代码链路可编译并通过函数级验证，已进入待 PyCharm 完整 eval-only 运行状态；该记录不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、`git diff --check` 输出 | 高 | 否 | 下一步需完整运行 `main.py` 生成新结果目录，读取 `run.log`、`plot_data.pkl`、Fig6 SVG 和 `tables/` 后再记录正式 Fig6 v3 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v3 正式 eval-only 结果审查 | `main.py`、`py_compile` | `运行结果/20260703_110113` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；`DFT-train-band` 连续训练功率频带；Hadamard Salari block-1 投影重构；PCA noisy waveform 训练样本 10000；diagnostic variants 开启；随机 DFT/Hadamard seed 数 5；高 SNR inset 开启；`tables/` 不重复保存 Fig6 SVG | 总耗时约 83.2 min；mean RMSE 全 SNR 平均：Raw≈20.00、Clean≈5.10、Geometry≈0、DAE-CR16≈27.76、DFT-train-band≈37.61、DFT-train-power≈36.79、DFT-bandlimited≈35.48、Hadamard≈42.17、Hadamard-block-2≈44.27、Hadamard-sequency≈64.27、PCA≈45.99 m；低 SNR(-10..-4)均值：DAE≈82.45、DFT-train-band≈98.78、DFT-train-power≈103.77、Hadamard≈102.59、PCA≈107.80 m；高 SNR(10..20)均值：DAE≈9.66、Hadamard≈9.84、DFT-train-band≈12.85、DFT-train-power≈11.22、PCA≈18.00 m；`tables/fig6_random_group_summary.csv/md` 已生成 | DAE-CR16 仍是综合最优压缩方法；`DFT-train-band` 连续频带低 SNR 略优于 top-k，但中高 SNR 和整体不如 top-k/known-band，说明训练功率最大连续频带不是 TDOA 最优频带；Salari block-1 明显优于 block-2/sequency/random；当前结果适合作为 baseline sensitivity 分析，不应声称严格复现 Chen Fig.6 | 已读取 `run.log`、`plot_data.pkl`、SVG 和 `tables/`，并审查当前代码；`py_compile` 通过 | 高 | 需复核 | 下一步建议审批：确定 Fig6 主图口径，考虑把 `DFT-bandlimited` 或 `DFT-train-power` 作为主 DFT，`DFT-train-band` 放入补充；新增 eval-only 不复制模型权重选项；执行结果目录归档方案 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 baseline v4 代码验证与函数级 smoke | `py_compile`；PyTorch inline smoke；`git diff --check` | 函数级小样本；未生成正式结果目录 | 默认 `USER_BASELINE_DFT_MODE="fisher_power"`；`DFT-Fisher` 按训练集平均频谱功率 × 归一化频率² 选频，并用 5% 最大功率阈值屏蔽低功率噪声频点；主 Fig6 为 Raw/DAE-CR16/DFT-Fisher/Hadamard/PCA；eval-only 默认不复制模型权重 | `python -m py_compile main.py train.py signal_gen.py model.py evaluate.py replot.py baselines.py export_results.py` 通过；PyTorch smoke 输出 `smoke_ok DFT-Fisher 64 [...]`，确认 `DFT-Fisher` 选择 64 个 complex bins，`DFT-train-band/train-power/bandlimited/uniform/random`、Hadamard 变体和 PCA 均可构建并 forward 输出正确形状；Fig6 绘图对象生成成功；`git diff --check` 无空白错误，仅提示 Windows 行尾转换 | v4 代码进入待 PyCharm 完整 eval-only 运行状态；该记录只证明代码链路可运行，不包含正式 RMSE 指标 | 编译输出、inline smoke 输出、`git diff --check` 输出 | 高 | 否 | 下一步需完整运行 `main.py` 生成新结果目录，读取 `run.log`、`plot_data.pkl`、Fig6 SVG 和 `tables/` 后再记录正式 Fig6 v4 指标 |
| 2026-07-03 | `NewMethod-Codex` | DFT-Fisher 主图正式 eval-only 结果审查 | `main.py` | `运行结果/20260703_134722` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；`dft_mode=fisher_power`；Hadamard=`salari`；PCA noisy 10000 samples；diagnostic variants=True；fixed eval set；all-pair WLS | DFT-Fisher 全 SNR mean RMSE 平均约 `57.36 m`，低 SNR<=0 平均约 `95.62 m`，高 SNR>=10 平均约 `32.03 m`，20 dB 约 `31.53 m`。对比：Raw 约 `20.00 m`，DAE-CR16 约 `27.76 m`，DFT-bandlimited 约 `35.48 m`，DFT-train-power 约 `36.79 m`，Hadamard 约 `42.17 m`，PCA 约 `45.99 m`。DFT-Fisher bins 为 `[287..318]` 和 `[706..737]` 两段高频边缘；高 SNR 诊断约 `gcc_weighted_abs_tdoa_error_samples=1.689`、`within_1_sample_rate=0.397`、`sidelobe_ratio=0.978`、`false_peaks_gt_05=15.04` | DFT-Fisher 进入 reconstruction-based Fig6 主图后性能明显恶化；根因是 naive `power*f^2` 硬选高频边缘再做零填充 IDFT 伪重构，破坏波形可恢复性和 GCC 主峰结构。应从严格 Chen-style Fig6 主图移除，保留为 task-aware 消融或 direct-TDOA 轨道候选 | 已读取 `run.log`、`plot_data.pkl`、`tables/fig6_method_diagnostics.csv` | 高 | 需复核 | 如用于论文，需在新 Fig6/Fig7 分轨结果中重新导出表格并复核 SVG 可读性 |
| 2026-07-03 | `NewMethod-Codex` | Fig6 DFT direct-TDOA 口径修正代码验证 | `py_compile`；PyTorch inline smoke；`git diff --check` | 函数级小样本；未运行完整 eval-only | Fig6 主图的 `DFT` 改为 selected-bin cross-spectrum direct TDOA，来源优先为 `DFT-SCS-lite` partial-Fourier bins；零填充 IDFT 单路波形重构 DFT 仅作补充消融；Fig7 保留 `DFT-Fisher-Direct`、`DFT-train-power-Direct` 等任务导向变体 | 两套 `py_compile` 均通过；PyTorch 小样本 urban8 baseline/direct smoke 输出 `SMOKE_OK DFT-SCS-lite ['DFT', 'DFT-Fisher-Direct'] ...`；`git diff --check` 仅提示 Windows 行尾转换，无空白错误 | 新版 Fig6/Fig7 代码链路进入待 PyCharm 直接运行状态；本记录只验证代码结构和小样本流程，不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、静态检查输出 | 高 | 否 | 下一步需完整运行 `main.py`，读取新结果目录的 `run.log`、`plot_data.pkl`、Fig6/Fig7 SVG 和 `tables/` 后再记录正式指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6/Fig7 direct DFT 正式 eval-only 结果审查 | `main.py`；读取 `plot_data.pkl`/CSV；小样本 direct DFT 诊断脚本 | `运行结果/20260703_164540` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；`dft_mode=scs_lite`；Fig6 主线 `DFT` 为 `DFT-SCS-lite` selected-bin cross-spectrum direct-TDOA；diagnostic variants=True；all-pair WLS | Fig6 mean RMSE 平均：Raw≈19.996、DAE-CR16≈27.757、DFT-SCS-lite recon≈47.791、DFT direct≈103.632、DFT-train-power-Direct≈39.770、DFT-Fisher-Direct≈58.841 m；DFT direct 诊断：mean abs TDOA error≈122.567 samples、within1≈0.213、sidelobe ratio≈0.996；选频窗自身在 lag ±63 处旁瓣≈0.989，±126 处≈0.956；同一 clean 小样本中全频 direct 与 GCC mean error≈0.26 samples，DFT-SCS-lite direct≈50 samples，加入物理 lag limit≈48 后降至≈0.26 samples | 主线 DFT 变差不是频域互谱理论或符号错误，而是 `DFT-SCS-lite` 均匀梳状选频造成强周期歧义，且当前 direct estimator 全 lag 搜索未利用物理可行 TDOA 范围；Fig6/Fig7 supplement 曲线过密，需要 zoom 图和线型区分 | 已读取 `run.log`、`plot_data.pkl`、`fig6_traditional_baselines.csv`、`fig7_baselines.csv`、`fig6_method_diagnostics.csv`，并运行小样本诊断脚本 | 高 | 需复核 | 下一步需审批：统一物理 lag gate、调整 Fig6 DFT direct source/保留 uniform-comb 为诊断、增加 supplement/taskaware zoom SVG、改善 DFT 曲线样式 |
| 2026-07-03 | `NewMethod-Codex` | Fig6/Fig7 direct DFT lag-gate 与 zoom 代码验证 | `py_compile`；PyTorch inline smoke；临时目录导出 smoke | 函数级小样本；未生成正式结果目录 | `paper_repro_eval_only` 默认不训练；Direct DFT 支持 `lag_limit_samples`；Fig6 主线 `DFT` direct source 默认 `DFT-train-power`；urban8 自动物理 lag gate；Fig6/Fig7 low/high zoom SVG | 两套 `py_compile` 通过；`git diff --check` 仅提示 Windows 行尾转换；小样本 method comparison 输出 `SMOKE_OK ['Clean', 'DFT', 'DFT-Fisher-Direct', 'DFT-SCS-lite', 'Geometry', 'Raw'] ...`；临时目录导出输出 `EXPORT_SMOKE_OK 11` | 新代码链路可编译并通过流程验证，已进入待 PyCharm 完整 eval-only 运行状态；该记录不包含正式 RMSE 指标结论 | 编译输出、inline smoke 输出、导出 smoke 输出 | 高 | 否 | 需完整运行 `main.py` 后读取新结果目录，才能记录新的 Fig6/Fig7 指标 |
| 2026-07-03 | `NewMethod-Codex` | Fig6/Fig7 lag-gate direct DFT 正式 eval-only 结果审查 | `main.py`；读取 `run.log`/`plot_data.pkl`/CSV | `运行结果/20260703_191104` | `paper_repro_eval_only`；模型源 `运行结果/20260702_000925`；baseline CR=16；`dft_mode=scs_lite`；`dft_direct_source=DFT-train-power`；physical lag gate=48 samples；diagnostic variants=True；task-aware baselines=True | mean RMSE 平均值：Raw≈15.72、DAE-CR16≈21.38、DFT direct≈32.16、DFT-train-power recon≈32.22、DFT-bandlimited≈31.65、DFT-train-band≈35.73、Hadamard≈38.78、PCA≈34.83、DFT-Fisher recon≈52.95、DFT-Fisher-Direct≈53.88 m；`DFT` 与 `DFT-train-power-Direct` maxabsdiff=0；DFT-Fisher alias diagnostics `max_sidelobe_inside_physical_lag≈0.950`，GCC sidelobe ratio 约 0.978，within-1 rate 约 0.27 | lag gate 修复后主线 DFT 已从上一轮灾难性 103.63 m 降至约 32.16 m，但仍弱于 DAE-CR16；Fig7 存在重复曲线，zoom order 与完整图不一致，Fig6 supplement 过度拥挤；DFT-Fisher 失败来自高频边缘选频窗在物理 lag 内强旁瓣，不适合继续作为正式图默认曲线 | 已读取日志、pkl、fig6/fig7 表格和诊断表，并对照代码 | 高 | 需复核 | 等待用户审批后精简 Fig6/Fig7 方法集合、修复 zoom order、移除重复 direct 曲线；若继续增强 DFT，需实现 balanced/FIM-greedy 选频 |
| 2026-07-03 | `NewMethod-Codex` | Fig6/Fig7 精简视图重绘验证 | `replot.py`；`py_compile`；SVG 文本检查 | `运行结果/20260703_191104` | 不重新训练、不重新评估；从原 `plot_data.pkl` 重绘；Fig6 supplement 精简方法集合；Fig7 保留 `DFT-Fisher-Direct` 并移除重复 `DFT-train-power-Direct`；zoom order 继承完整图 order | 两套 `py_compile` 通过；smoke 输出 `SMOKE_OK`；`replot.py` 成功覆盖 Fig1/Fig2/Fig3/Fig6/Fig7 及 zoom SVG；SVG 检查确认 Fig7/zoom 中有 `Naive Fisher direct`，无 `DFT-train-power-Direct`，Fig6 supplement 无 random/sequency/Fisher waveform 默认曲线 | 原评估数据不变，只有 SVG 视图被替换为更清晰版本；该记录不能作为新 RMSE 结果，只说明图表重绘成功 | 编译输出、smoke 输出、replot 输出、SVG 文本检查 | 高 | 否 | 若需要 tables 与新视图完全一致，应后续补充导出精简表格或重新运行 eval-only 生成新 pkl/tables |
| 2026-07-03 | `NewMethod-Codex` | Balanced DFT-Fisher-Direct 单方法增量更新 | `update_dft_fisher_direct.py`；`py_compile`；`plot_data.pkl` 读取；SVG 文本检查 | `运行结果/20260703_191104` | 不重训 DAE，不重跑全部 baseline；只用 balanced Fisher/FIM + physical-lag sidelobe control 重新选择 DFT-Fisher bins，并只重算 `DFT-Fisher-Direct`；baseline CR=16；PCA/DFT 选频样本数 10000；fixed eval set；all-pair WLS；lag gate=48 samples | 小样本 alias smoke：naive Fisher 物理 lag 内最大旁瓣约 `0.907`，balanced Fisher 约 `0.483`。正式增量更新：selected bins 范围 `232..792`，`max_sidelobe_inside_physical_lag=0.456`，`max_sidelobe_outside_physical_lag=0.274`；`DFT-Fisher-Direct` mean RMSE 平均值 `53.8813 -> 40.6645 m`；低 SNR(≤0 dB)平均约 `91.79 m`，高 SNR(≥8 dB)平均约 `7.94 m`。Fig7 当前均值：Raw≈15.72、DAE-CR16≈21.38、DFT≈32.16、DFT-SCS-lite≈40.68、DFT-Fisher-Direct≈40.66、Hadamard≈38.78、PCA≈34.83 m | Balanced/FIM-greedy 选频显著降低了物理 lag 内旁瓣并改善 `DFT-Fisher-Direct`，但低 SNR 仍严重退化；该方法目前只能作为 task-aware DFT 候选/消融，不宜声称为成熟强基线 | 脚本运行输出、更新后 `plot_data.pkl`、SVG 文本检查 | 高 | 需复核 | 若用于论文，应重新在新时间戳目录完整 eval-only 跑一次，或明确说明这是对 `20260703_191104` 的单方法增量更新 |

## 待补充模板

| 日期 | 分支 | 实验名称 | 脚本 | 数据路径 | 关键参数 | 指标结果 | 结论 | 证据来源 | 可信度 | 是否可用于论文 | 复核需求 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 待补充 | 待确认 | 下一轮经审批的复现增强或创新实验 | `main.py` | `运行结果/YYYYMMDD_HHMMSS` | 待用户确认：可能包括多 seed 复核、DFT/Hadamard/PCA 基线、imperfect LOS/NLOS 消融或创新模式 | 待补充 | 待补充 | 待补充 | 待确认 | 待确认 | 需先审批方案，再运行并读取日志、pkl、SVG |
