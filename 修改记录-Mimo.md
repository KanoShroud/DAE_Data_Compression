# DAE Data Compression — 修改记录 (Mimo)

**基础文档**: `修改记录.md`（第1-4轮修改，截至 2026-05-26）
**本文档时间**: 2026-05-27 ~ 2026-05-29
**文档结构**: 按问题划分，每个问题记录完整的迭代过程

---

## 目录

1. [固定信道模式实现](#一固定信道模式实现)
2. [RRC 滚降系数修正](#二rrc-滚降系数修正)
3. [CR 排序反转问题](#三cr-排序反转问题)
4. [可视化问题](#四可视化问题)
5. [信道池重复创建](#五信道池重复创建)
6. [待解决问题](#六待解决问题)

---

## 一、固定信道模式实现

**问题描述**: 代码使用逐样本随机信道生成，导致 10,000 样本 = 10,000 种不同信道。论文使用 50 个固定信道快照 × 200 次噪声实现 = 10,000 样本，DAE 只需学习 50 种信道去噪模式。极端信道多样性导致 CR=4 的 3.2M 参数严重欠训练。

**优先级**: P0（最高）

### 迭代记录

| 轮次 | 日期 | 方案 | 结果 | 结论 |
|------|------|------|------|------|
| 第5轮 | 05-27 | 在 `SignalSimulator` 中新增 `channel_mode="fixed"` 参数，预生成 50 条信道的 FIR 池；`train_with_cv` 和 `main.py` 透传参数 | 固定信道模式实现成功，信道池生成 50 条可复现信道 | ✅ 已完成 |

### 最终实现

**修改文件**: `signal_gen.py`（+30 行）、`train.py`（+6 行）、`main.py`（+8 行）

**核心设计**:
- `SignalSimulator.__init__` 新增 `channel_mode`、`n_fixed_channels`、`channel_pool_seed` 参数
- `_build_channel_pool()` 预生成信道池：`main_delay ∈ [10, 50)`，2-4 个多径 tap，复增益 `uniform(0.1, 0.4) * exp(j·uniform(0, 2π))`
- `apply_channel_and_noise` 内按 `channel_mode` 分支：`"fixed"` 从池中随机选取，`"random"` 保持原逻辑
- 所有新参数有默认值，向后兼容

---

## 二、RRC 滚降系数修正

**问题描述**: 论文使用 RRC 滚降系数 β=0.2，代码中为 β=0.35。导致 RF 带宽标注错误（27 MHz vs 正确的 24 MHz）。

**优先级**: 中

### 迭代记录

| 轮次 | 日期 | 方案 | 结果 | 结论 |
|------|------|------|------|------|
| 第7轮 | 05-28 | `signal_gen.py` 中 `beta=0.35` → `beta=0.2`；更新注释和图标题带宽标注 | RRC 滤波器生成 17 taps（原 23 taps），能量归一化 ‖h‖₂=1.0 正确 | ✅ 已完成 |

### 修改详情

| 文件 | 修改内容 |
|------|----------|
| `signal_gen.py:56` | `beta=0.35` → `beta=0.2`，docstring 带宽 27→24 MHz |
| `evaluate.py` | 注释和图标题带宽 27→24 MHz |

---

## 三、CR 排序反转问题

**问题描述**: Monte Carlo 评估中 CR=4 的 TDOA RMSE 高于 CR=8 和 Original，与论文结论矛盾（论文预期低 CR < 高 CR）。

**优先级**: 高

### 迭代记录

| 轮次 | 日期 | 方案 | 结果 | 结论 |
|------|------|------|------|------|
| 第6轮 | 05-27 | 使用 GCC-PHAT 评估 | CR 排序完全反转，DAE 在 SNR=-5~0 反而比 Original 差 | ❌ GCC-PHAT 与 MSE 训练的 DAE 不兼容 |
| 第7轮 | 05-28 | 新增 `gcc_standard` 函数，`MonteCarloExperiment` 默认使用标准 GCC | SNR=5dB 时：PHAT RMSE=54.83，StdGCC RMSE=0.98（56 倍提升） | ✅ 标准 GCC 解决了 PHAT 不兼容问题 |
| 第8轮 | 05-28 | 添加 Median RMSE 面板 | Median RMSE 在所有 SNR 下给出正确 CR 排序；Mean 被 outlier 主导 | ✅ Median 是更鲁棒的指标 |
| 第10轮 | 05-29 | 样本数 20K→50K（改善低 SNR 泛化）；评估 SNR 范围恢复论文值 [-10, 20] dB | 运行中 | 待验证 |

### 根因分析

**第一层根因：GCC-PHAT 与 MSE 训练的 DAE 不兼容**

GCC-PHAT 将互功率谱归一化为单位幅度 `R / (|R| + ε)`，仅保留相位信息。DAE 用 MSE 训练，优化幅度重建。GCC-PHAT 丢弃 DAE 恢复的幅度信息（DAE 的优势），仅使用可能失真的相位信息。

**第二层根因：Mean RMSE 对 outlier 不鲁棒**

低 SNR 下，GCC 峰值搜索可能完全失败（找到错误的峰），产生数百样本的误差。这些 outlier 将 mean RMSE 拉高数十倍，掩盖了典型性能。Median RMSE 对 outlier 鲁棒，能反映典型 trial 的性能。

**第三层根因：训练 SNR 分布与评估范围不匹配**

训练数据使用 `np.random.uniform(-10, 20)` 生成 SNR，约 1/3 样本在 SNR > 10 dB（去噪容易）。但评估范围为 [-10, 10] dB。模型在训练时浪费容量学习高 SNR 去噪，低 SNR 性能不足。CR=4 参数最多（3.2M），受此分布偏移影响最大。

### 最终实现

**修改文件**: `evaluate.py`、`signal_gen.py`、`main.py`

1. 新增 `gcc_standard()` 函数：直接使用互功率谱做 IFFT，保留幅度和相位
2. `MonteCarloExperiment.__init__` 新增 `gcc_method` 参数（默认 `'standard'`）
3. `MonteCarloExperiment.run` 同时计算 mean 和 median RMSE
4. `plot_monte_carlo` 改为双面板：左图 Mean，右图 Median
5. 训练样本数增加至 50,000（改善低 SNR 泛化）

---

## 四、可视化问题

**问题描述**: 三张输出图片存在多种可视化问题，经历多轮迭代修复。

### 迭代记录

| 轮次 | 日期 | 问题 | 方案 | 结果 |
|------|------|------|------|------|
| 第6轮 | 05-27 | 互相关参考信号错误（X1_noisy vs X1_clean 同信道） | 改为 X1 vs X2 的 GCC 互相关 | ✅ 互相关峰在 TDOA 位置 |
| 第6轮 | 05-27 | 采样率注释写 80MHz，实际 40MHz | 修正注释 | ✅ |
| 第6轮 | 05-27 | 图标题带宽标注错误 | 修正为实际带宽 | ✅ |
| 第6轮 | 05-27 | 频域子标题 "Double-Sided" 易混淆 | 改为 "Complex Baseband" | ✅ |
| 第6轮 | 05-27 | 图片高度不足，标题重叠 | 调整 figsize 和 tight_layout | ✅ |
| 第6轮 | 05-27 | 缺少坐标轴标签和单位 | 添加完整标签 | ✅ |
| 第6轮 | 05-27 | 频域图缺少图例 | 添加 legend | ✅ |
| 第6轮 | 05-27 | 图片序号不一致 | 统一为 Figure 1/2/3 | ✅ |
| 第6轮 | 05-27 | 单行子图维度错误 (Bug) | 添加 `axes = axes[np.newaxis, :]` | ✅ |
| 第8轮 | 05-28 | 时域纵轴改为 dBFS | 以纯净信号峰值为 0 dB 参考 | ✅ 但后续用户反馈不适合 |
| 第8轮 | 05-28 | 频谱纵轴改为 dB | `10*log10(PSD)` | ✅ |
| 第8轮 | 05-28 | 互相关标签 "Noisy" 不明确 | 改为 "Raw" 和 "DAE" | ✅ |
| 第8轮 | 05-28 | 仅第一行有图例 | 所有行添加图例 | ✅ |
| 第8轮 | 05-28 | 标题未标明 CR | 新增 `cr` 参数 | ✅ |
| 第8轮 | 05-28 | 图片尺寸过大 | 缩减为 `(14, 2.8*n+0.5)` | ✅ 但用户仍觉得大 |
| 第8轮 | 05-28 | CV 训练曲线无图例 | 添加 Train/Val legend | ✅ |
| 第9轮 | 05-29 | dBFS 导致极小值不便观察 | 改回线性幅度 `\|s(t)\|` | ✅ |
| 第9轮 | 05-29 | 频谱图例遮挡有效区域 | 从右下角移至右上角 | ✅ |
| 第9轮 | 05-29 | 图片整体高度过大 | 高度缩减至 75%：`(14, 2.1*n+0.4)` | ✅ |
| 第9轮 | 05-29 | Monte Carlo SNR 步进 2dB 太粗 | 改为 1dB 步进 | ✅ |

### 当前状态

- **Figure 1 (CV 训练曲线)**: 3 个子图分别展示 CR=4/8/16 的 3-fold 训练/验证曲线，含图例
- **Figure 2 (SNR 对比图)**: 4 行（SNR=-10/0/5/10）× 3 列（时域/频谱/互相关），线性幅度，时间轴 μs，频谱 dB/Hz，所有行有图例
- **Figure 3 (Monte Carlo)**: 双面板（Mean + Median），SNR 范围 [-10, 10] dB，1dB 步进

---

## 五、信道池重复创建

**问题描述**: 运行 `main.py` 时，`[SignalSimulator] 固定信道池已生成` 消息出现两次。

**优先级**: 低

### 迭代记录

| 轮次 | 日期 | 方案 | 结果 | 结论 |
|------|------|------|------|------|
| 第8轮 | 05-28 | `train_with_cv` 新增可选 `sim` 参数；`main.py` 传入已创建的 `sim` 实例 | 消息只出现一次 | ✅ 已完成 |

### 修改详情

| 文件 | 修改内容 |
|------|----------|
| `train.py` | `train_with_cv` 新增 `sim=None` 参数，传入时复用，不传时自行创建 |
| `main.py:83` | 调用时传入 `sim=sim` |

---

## 六、待解决问题与历史分析

### 6.1 CR=8 在 -8dB 处的 RMSE spike

**状态**: ✅ 已解决

**现象**: 20K 样本训练时，Mean 和 Median RMSE 均在 SNR=-8dB 处出现异常升高。

**修复**: 样本数从 20K 增加到 30K 后 spike 消失。根因是训练样本不足导致 CR=8 在特定 SNR 下的去噪行为不稳定。30K 样本提供了 600 次/信道的噪声实现，统计波动被消除。

### 6.2 低 SNR 区间 CR 排序

**状态**: 修复中（第10轮）

**现象**: 在低 SNR 区间（-10 ~ -5 dB），CR=8 持续优于 CR=4，与论文预期不符。CR=4 和 CR=16 曲线出现交叉。

**根因分析**:
1. **训练 SNR 分布偏移**: 训练使用 [-10, 20] dB 均匀分布，约 1/3 样本在 SNR > 10 dB（去噪容易）。但评估范围为 [-10, 10] dB。模型在训练时浪费容量学习高 SNR 去噪。
2. **CR=4 过参数化**: 3.2M 参数在 50 个固定信道模式下，低 SNR 样本的去噪目标被噪声主导，大模型更容易记忆噪声模式而非学习通用去噪。
3. **CR=8 更平衡**: 1.6M 参数在相同数据下泛化更好，因为容量约束迫使模型学习更通用的去噪特征。

**修复方案**: 样本数增至 50K，改善低 SNR 泛化能力。训练和评估 SNR 范围均恢复论文值 [-10, 20] dB。

### 6.3 验证曲线高于训练曲线

**优先级**: 信息性（非 bug）
**状态**: 已分析，无需修复

**原因**: ComplexBatchNorm1d 的 running 统计量 + L2 正则化，属正常现象。

### 6.4 早停耐心值

**状态**: 当前 PATIENCE=10

**说明**: 用户将 PATIENCE 从 15 降至 10，目的是防止 CR=16 等大模型过拟合。当前值合理。

---

*本文档按问题维度组织，记录从 2026-05-27 至今的所有修改迭代。*
